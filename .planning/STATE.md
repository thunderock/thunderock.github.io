---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: Staff MLE Resume Optimization
status: executing
last_updated: "2026-08-24T18:17:25.828Z"
last_activity: 2026-08-24 -- Phase 06 planning complete
progress:
  total_phases: 6
  completed_phases: 5
  total_plans: 20
  completed_plans: 17
  percent: 83
---

# Project State

## Current Position

Phase: 6 — Palette & Site Sync
Plan: Not started
Status: Ready to execute
Last activity: 2026-08-24 -- Phase 06 planning complete

Progress: [██████████] 100%

## Project Reference

See: .planning/PROJECT.md (updated 2026-08-21)

**Core value:** Resume passes ATS keyword screens for Staff MLE / inference-framework roles — every claim interview-defensible.
**Current focus:** Phase 05 — Skills Rebuild

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table. Affecting current work:

- [Phase 04 / close + code review]: Phase 4 shipped the page-2 restructure — Projects → `rollout` + `graph_ml` (7 grad entries gone), Publication trimmed to citation + one fairness-aware line, Research → one line, Education institution-first with GPAs + cities dropped and `EDU_INSTITUTION` synced (D-07) — with **page 1 byte-frozen at `798c0fb`**. Teaching kept as 3 items (D-05 owner override); GH-01/02 deferred (D-08, no GitHub mutations), GH-03 confirmed. A standing page-2 content net (`scripts/verify-phase4-regressions.sh`, 38 assertions, wired as `REGRESS4` in `make verify`/`verify-regressions`) now guards it. The post-phase code-review gate found + fixed one BLOCKER (`cb6b590`): **multi-word (space-containing) literals in a `pdftotext` net MUST assert against `$SQUEEZED`, never the raw `$GATE` stream** — `pdftotext` wraps phrases across lines, so a raw-stream absence check FALSE-PASSES on a wrapped regression (proven with a `--gate-text` fixture: raw count 0, squeezed count 1, P4.32 correctly FAILs). Single-token needles (GPAs, `Poster`) stay on `$GATE`. ⚠ **STANDING RULE for Phase 5's Skills net:** its multi-word keywords (`CUDA graphs`, `Flash Attention`, `ONNX Runtime`, `PyTorch Lightning`) must assert against `$SQUEEZED`. — why: closes a false-green class the P2/P3 nets already avoid; recorded so the Phase-5 keyword net does not reintroduce it.

- [Post-Phase-3 / Page-1 Finalization + Gate Reconciliation]: The owner finalized the page-1 Work Experience block in commit `798c0fb` after an external review — precise internal figures generalized (fleet → "1000s×GPUs per model job across many model queues"; ingestion → "hyperscaled daily EMR ingestion"; "~700M" → "a Billion asset catalog"; the figures 27,135 / 9.7k-line / idempotent re-run / 24×A100-40GB / 9 model queues / 64 H100s / 202.6M-row / 24.8M / 40-node removed), **`H100` removed (A100 kept)**, Groupon subtitle gained "RECOMMENDATION SYSTEMS", PDF regenerated with source. Honesty flags on "1000s×GPUs per model job" and "a Billion asset catalog" were raised and **DISMISSED by the owner** — **page 1 is FINAL; never edit `docs/main.tex` lines 140–210 to satisfy a red gate.** The gates were then reconciled TO that document in `b6f8011` (gates follow the document, never the reverse): `docs/verify/manifest.txt` demoted `H100` KEYWORDS_REQUIRED → `KEYWORDS_TARGET` (`H100:5`, owner Phase 5); `scripts/verify-phase3-regressions.sh` retired the 10 removed-figure needles to `assert_absent` and demoted the two H100 keyword needles, with the here-doc loops expanded to explicit calls so **every pinned `P3.xx` id is preserved (`P3_SEQ` still 72)** — `P3.44`/`P3.65` are now the H100-demotion slots, `P3.62` still guards the surviving `35–38M`. All suites green (`verify-resume` 0 blockers, G6.6 now WARNs `H100` as a Phase-5 target; P3 net 72 PASS/0 FAIL; P2 net 27 PASS; self-test 10 G7/G8 PASS). ⚠ **PHASE-5 OBLIGATION (do not lose):** `$GATE`/`$SQUEEZED` are whole-document, so when the Skills rebuild (PG2-04) re-adds `H100` on page 2 it MUST, in the SAME commit, flip **`P3.44`** back to `assert_promoted 'H100'` and **`P3.65`** back to `assert_present 'H100' "$GATE"`, and re-promote `H100` KEYWORDS_TARGET → KEYWORDS_REQUIRED (`:5` suffix stripped) — otherwise the P3 net reddens mid-Phase-5. — why: the harness exists to protect the owner's chosen content; strengthen-never-weaken applies to assertion logic, not to superseded needle literals, so the net follows the finalized document.
- [Roadmap]: Gate → budget → content ordering is forced by measurement, not preference — page 1 has 0.15pt slack and `make build` is silent on a 3-page overflow (exit 0, zero warnings)
- [Roadmap]: Phase 1 is **expected to FAIL** on today's committed PDF (5 live defects) — that is the harness working, not a regression
- [Roadmap]: Summary/headline section stays Out of Scope (user chose fold-only) despite the research recommending it — do not re-add during planning
- [Spike 002]: Projects section = `rollout` + `graph_ml` (Variant A); `random_walk` privatized during the build
- [Phase 1 / A1]: LinkedIn vanity handle is **`ashutosh--tiwari`** (two ASCII hyphens) — user-confirmed at the Plan 01-01 Task 1 checkpoint against a live probe (HTTP 200; LinkedIn canonical `og:url` = `ashutosh--tiwari`; the single-hyphen URL returned 999). This resolves Assumption A1 and **supersedes the single-hyphen literal `ashutosh-tiwari` in ROADMAP.md Phase 2 criterion 3** (cited in planning as ROADMAP:47; the literal actually sits at ROADMAP.md line 50) — that spelling is a roadmapping transcription error and a later verification pass must NOT correct `docs/verify/manifest.txt` backwards to it. `docs/verify/manifest.txt` therefore freezes `ashutosh--tiwari` in `CONTACT_LITERALS` and the EN DASH form `ashutosh–tiwari` (U+2013) in `FORBIDDEN_LITERAL`. **`docs/main.tex:134` needs NO Phase 2 `\href` correction** — its link target and anchor text already read `ashutosh--tiwari` correctly; the only Phase 2 (EXP-06) defect is that the rendered text layer emits the en dash because LaTeX converts the source `--`.
- [Phase 1 / 01-02]: The harness is now **red on arrival by design** — `bash scripts/verify-resume.sh` exits **1** with exactly **5** BLOCKER FAILs (`G6.9` x3 masked contact literals, `G6.10` en-dash handle, `G6.12` Education city-before-institution ordering), each message ending in an explicit `Owner: Phase 2` clause. A 6th BLOCKER appearing means the harness is wrong — investigate the assertion before touching `docs/`. Phase 2 turns it green by fixing EXP-06 plus the `\resumeSubheading` reorder at `docs/main.tex:240-242`, with **no change to the harness**.
- [Phase 1 / 01-02]: `G6.12`'s window clause measures **institution -> date in non-empty lines** (per the plan spec) and **PASSES** today (span 2 vs `EDU_BIND_WINDOW=3`); only the city-first ordering clause fails. `01-01-SUMMARY`'s "5 lines wide" framing measured city -> date in raw lines — a different metric. Do not "fix" the window check on the strength of that older framing.
- [Phase 1 / 01-02]: `G6.13`'s role-adjacency window is a **local constant** (`ROLE_BIND_WINDOW=3`), deliberately not a manifest key: no requirement owns the role-header fix, so the commit that promotes `G6.13` to a BLOCKER is the commit that should add the key. It gates on **raw** line span (4 today, reproducing the research's "three non-adjacent blocks"), not non-empty span (which reads 2 and would print a misleading "bound together").
- [Phase 1 / 01-02]: `section_region`'s **terminal-section EOF fallback** is load-bearing, not defensive. `TECHNICAL SKILLS` is the last `SECTIONS` entry, so a helper returning an empty range there would make `G6.14` count 0 rendered lines — under `SKILLS_MAX_LINES=3` — and report a silent false OK in place of today's real 4.
- [Phase 01]: Phase 1 is DONE by its own gate: make verify-selftest exits 0 with four G7 and five G8 PASS lines, make verify exits non-zero (2 -- GNU Make 3.81 collapses every failed recipe to 2) naming exactly the three Phase-2-owned defects, bash scripts/verify-resume.sh exits 1, and a drifted source is refused at exit 2. Every assertion class has now been observed FAILING as well as passing. — An assertion never observed failing is not a gate. G7 proves the +5-line probe premise numerically (build exit 0, zero warnings) before proving G1.1/G2.1 catch the resulting 3-page PDF; five G8 controls prove the honesty, geometry, staleness, glyph and keyword classes each fire on a broken input.
- [Phase 01]: G7.4 and G8.6 assert the working-tree state as a BEFORE/AFTER delta plus explicit tracked-input checksums, not absolute git status --porcelain emptiness. Absolute emptiness conflates "the self-test changed nothing" with "the caller had no edits in progress" and would false-positive in Phase 2, which legitimately dirties docs/main.tex and the rebuilt PDF. Any leak out of a scratch directory still FAILS the assertion, and both clauses fail closed without git. — The property under test is the self-test blast radius, not the developer tree state. The plan-level gate asserts tree cleanliness separately.
- [Phase 01]: make verify-baseline re-measures ONLY the observation-only manifest keys (P1_TOP_PT, P1_BOTTOM_WS_PT). PAGES, CEILING_PT, PAGE_SIZE and PAGE2_OPENS_WITH are measured and REPORTED with a loud failure line on divergence but never written, and the honesty freeze is refused without ALLOW_FROZEN_UPDATE=1 (and even then only its derivable geometry hash is regenerated, after a printed unified diff). Raising a gated threshold is a reviewed edit, never a regeneration. — A generator that raises a BLOCKER threshold to match a document that moved away from it deletes the gate exactly the way re-freezing a changed employment date defeats VERIFY-03.
- [Phase 01]: Negative-control source copies MUST keep the basename main.tex: the freshness record is looked up by source filename, so a renamed copy finds no .fdb_latexmk record, escalates to the L2 rebuild arbiter, and a comment appended after the document end comes back FRESH -- silently disarming the staleness control. This is the 01-01 "L1 strictly stronger than L2" finding applied. — G8.3 depends on taking the strictly-stronger L1 byte-md5 path; the L2 semantic arbiter cannot see a byte-only change.
- [Phase 02]: [Phase 2 / 02-01]: The measured page-1 budget freed by Phase 2 Plan 01 is **50.4pt / 4.39 lines** (G3.2 p1 headroom +0.1pt -> +50.5pt; deepest content bottom 764.3pt -> 713.9pt). Career-break removal contributed 48.9pt (4.26 lines, matching CAREER_BREAK_PT exactly); the Groupon/NetSpeed fold contributed only 1.5pt (0.13 lines) because both descriptors wrap to two rendered lines at their locked D-05/D-06 length, so the fold recovers the itemize topsep/itemsep overhead and zero rendered lines per role. This measured figure supersedes ROADMAP Phase 2 criterion 5's ">=12 lines" AND corrects D-08's "10-11 lines" prediction downward. Do not raise or lower a manifest threshold to match it, and do not shorten locked content to improve it -- D-08 designates the two descriptors as Phase 3's first re-compression lever. — D-08 mandates asserting the measured post-change value rather than a predicted one. Both the ROADMAP figure and D-08's own estimate assumed the fold would save rendered lines; at measured one-line capacity (137-141 chars) against 167/164-char descriptors it saves none.
- [Phase 02]: [Phase 2 / 02-01]: TRAP T-2 (`PROBE_LINES`) from 02-PATTERNS.md §5.5 **does not trigger** and Plan 02 must NOT raise `PROBE_LINES`. Verbatim: `RESULT G7.2  PASS  the probe PDF is 3 page(s), above the manifest PAGES=2 budget, so the +5 probe is a genuine overflow`. `make verify-selftest` exits 0 with G7.1-G7.4 and G8.1-G8.6 all PASS. The trap was predicated on freeing 10-11 lines (115-127pt); the actual 50.4pt sits below the +5-line probe's ~57.5pt, so the probe still overflows page 1 and the self-test still proves something. `PROBE_LINES` stays 5. The probe injection anchor also still resolves (begin=127 break=211 insert=207) after both fold blocks kept their item lists. — The plan predicted G7.2 might go red and assigned the remedy to Plan 02 Task 3. Measurement shows the premise was wrong, so raising PROBE_LINES now would weaken the anti-rot guard for no reason.
- [Phase 02]: [Phase 2 / 02-01]: `\resumeItem{\textit{...}}` inside a minimal `\resumeItemListStart`/`End` pair is the descriptor carrier for folded roles; `\resumeSubSubheading` was rejected on measurement. Its `{@{}l@{\extracolsep{\fill}}r@{}}` cells cannot line-break, and both locked descriptors (167 and 164 chars) exceed the measured 137-141-char one-line capacity, so it would have produced an overfull \hbox and words printed past the page edge (G3.4 FAIL). `\resumeItem` renders through `\small{\justifying}` and wraps: 0 Overfull \hbox in the log, G3.4 PASS with 0 words outside the page box. Two rendered lines per descriptor is the sanctioned outcome and was kept in preference to the line; both wrap points land at word boundaries with no LaTeX hyphenation, so every locked literal survives extraction. `\resumeSubSubheading` still has zero usages in the document. — D-04 asks for one italic descriptor; the only shape with proven extraction AND line-breaking is \resumeItem. Keeping the two-line wrap preserves all four D-06 module names and the full D-05 literal set, which the forbidden ladder rungs would have dissolved.
- [Phase 02]: [Phase 2 / 02-01]: `EXP-01` is **claimed by both** `02-01-PLAN.md` and `02-02-PLAN.md`, so Plan 01 marked only `EXP-05` and `EXP-06` complete in REQUIREMENTS.md and deliberately left `EXP-01` open. Plan 01 satisfies EXP-01's literal text (career-break entry gone from source and text layer; `Aug. 2021 - May 2023` still extracts once from Education), but D-09's Education parse-order fix -- the still-red `G6.12` -- is Plan 02's, and closing EXP-01 while that BLOCKER stands would misreport the phase. Plan 02 closes EXP-01. — A requirement shared across two plans completes when the last plan lands, not the first. G6.12 is still FAIL, so the checkbox would have been premature.
- [Phase 02]: [Phase 2 / 02-02]: D-09's date-placement sub-clause was DEPARTED FROM on measurement: the Education degree dates live in `\resumeSubheading` arg #4 (row 2's right cell), NOT #2 (the header row's right cell) as D-09 specified. D-09's actual decision -- drop the redundant city cells -- shipped intact. Measurement A (date in #2, D-09 as written) extracted `Aug. 2021 - May 2023` at gate line 94 BEFORE `Indiana University, Bloomington` at 96: #2 is the cell measured to extract FIRST in Education rows, so the date merely replaced the city as the noise string a parser reads before the school -- the defect class survived. Measurement B (date moved to #4, #2 empty) extracts the institution at 94 as the region's first non-empty line, degree at 95, date at 97. Work Experience rows still extract #1 before #2; Education rows extract #2 before #1. Extraction order of a two-cell `tabular*` row is MEASURED, never predicted -- that asymmetry is exactly what G6.12 exists to catch. — The plan's Task 1 branch table anticipated this: it pre-authorized moving the date to #4 if #2 measured date-first, and forbade weakening the assertion or restoring a city cell. Both of D-09's date positions were tried; only #4 yields institution-first.
- [Phase 02]: [Phase 2 / 02-02]: G6.12's ordering clause is now ANCHOR-FREE and strictly stronger: the institution must BE the first non-empty extracted line inside the EDUCATION region (new helper `region_first_nonempty`, defined at :405 beside `region_nonempty_count`, before the --census short-circuit at :504). `EDU_CITY` was deleted from `docs/verify/manifest.txt` in the SAME commit (232abc2) as its only reader -- the seven references were all inside the G6.12 block, and a needle key left behind after its string leaves the document degrades into `grep -n -Fx -m1 -- ""`, which matches the region's first BLANK line. The rejected alternative (repoint the needle at the degree line) would have gone GREEN on the measured date-first document, silently un-asserting the defect. The `EDU_DATE`/`EDU_BIND_WINDOW` window clause is retained with `EDU_LO`/`EDU_HI`/`EDU_SPAN` byte-untouched. NEGATIVE CONTROL: the edited harness run against the pre-Phase-2 main.tex+PDF pair (f278020, `mktemp -d`, basename preserved, --skip-freshness) still emits `RESULT G6.12 FAIL` naming `Bloomington, IN` at line 100, with `RESULT G0.3 SKIP` confirming the override was the only waiver -- so the clause was fixed, not weakened, and is not a tautology. — A green gate reached by weakening a clause is worse than the red one you started with, so the plan required the replacement clause to be at least as strong and to be proven still-failing on the pre-change artifact.
- [Phase 02]: [Phase 2 / 02-02]: `PROBE_LINES` stays 5 -- TRAP T-2 confirmed NOT triggered, matching Plan 01's measurement. `RESULT G7.2 PASS the probe PDF is 3 page(s), above the manifest PAGES=2 budget, so the +5 probe is a genuine overflow`. Recorded margin proving the anti-rot guard is ARMED rather than merely quiet: probe height 5 x LINE_PT 11.5 = 57.5pt against measured page-1 slack 50.5pt (G3.2, +4.39 lines) -- the probe exceeds available slack by 7.0pt (0.61 lines). Raising `PROBE_LINES` here would enlarge a probe that already overflows and weaken the guard for no reason; the raise-only rule means it can never be lowered later either. `PAGES`, `CEILING_PT`, `LINE_PT` untouched; the whole-phase manifest diff is exactly the EDU_CITY removal plus its explanatory comment. — 02-PATTERNS 5.5 predicted the trap would fire on a 10-11 line (115-127pt) recovery. The measured recovery was 50.4pt, below the probe's 57.5pt, so the self-test still proves something and the remedy the plan pre-assigned to Task 3 was correctly not applied.
- [Phase 02]: [Phase 2 / 02-02]: The regenerated artifacts ride in the SOURCE commit (29999ff: main.tex + .pdf + .log + .fdb_latexmk together), so HEAD -- the doc-only `docs(02-02)` commit -- does not itself list `.fdb_latexmk`/`.pdf`. This continues Plan 01's Decision 5 and 02-PATTERNS 6: `.fdb_latexmk` carries the md5 `G0.3` reads, so a source-only commit would make a fresh checkout of THAT commit refuse at exit 2. The property Task 3 criterion 7 protects (T-02-12, stale greenlight) was verified substantively instead of by grepping HEAD: a genuine `git clone` of HEAD has a clean tree and its `bash scripts/verify-resume.sh` emits `RESULT G0.3 PASS PDF provably built from current main.tex (latexmk md5 eff2ec04067eb8fa60b775424a0a3a53)` and exits 0. `.aux`/`.fls`/`.out` are tracked but were byte-unchanged by the Education edit. `make clean` was never run. — Deferring artifacts to a trailing build(resume): commit would satisfy criterion 7's literal HEAD wording but leave 29999ff carrying a stale PDF -- strictly worse, and contradicting the read_first guidance the plan itself cites.
- [Phase 03]: [Phase 3 / 03-01]: `scripts/verify-phase3-regressions.sh` is Phase 3's acceptance oracle and is RED ON ARRIVAL BY DESIGN: 72 assertions, 26 PASS, **46 FAIL**, exit 1, **zero unowned FAILs**. Owner split is the standing contract: **32 -> 03-04**, **13 -> 03-05**, **1 -> 03-06**. `grep -E '^RESULT P3\.[0-9]+ +FAIL' | grep -vcE '/03-0[456] '` returning 0 is a gate. A 47th FAIL, or any FAIL whose label carries no owner tag, means the NET is wrong -- investigate the assertion before touching `docs/`. P3.29-P3.33 currently FAIL as **unmeasurable** (the governance region cannot be sliced until `Training-Data Governance` extracts), so 03-04 converts them to real framing assertions rather than merely flipping their status. Every needle measured EXACTLY as `03-01-PLAN.md <locked_literals>` states; no count differed. — Phase 1 established that a gate written after the content it checks tends to be shaped by that content. Writing the oracle first, red, with each FAIL naming its owner, makes the phase contract machine-readable instead of prose.
- [Phase 03]: [Phase 3 / 03-01]: The EXP-02a lead anchors (`fault-tolerant` / `fault-tolerance` / `distributed inference`) are an **ANY-OF set, not all-of**, matched case-insensitively against ONE extracted line -- the first non-blank line after the Adobe subtitle. Three sources agree on any-of: `03-04-PLAN.md` Task 1 ("MUST contain at least one of"), `03-RESEARCH.md`'s EXP-02a row ("must match one of"), `03-PATTERNS.md` 3.1 (a regex alternation). Do NOT "strengthen" this to all-of: `fault-tolerant` and `fault-tolerance` are morphological variants of one word, so all-of is unsatisfiable in English and 03-04's own gate would be unreachable. The line SCOPING is the real strength and must not be relaxed to a document-wide needle: lowercase `distributed inference` is x0 on the lead line but x1 in the gate stream, because the title-case topic heading `Distributed Inference Frameworks` sits three lines below -- a document-wide needle would be GREEN today, before the rewrite it gates. — The plan text "match the three anchors against it" admits an all-of reading that would have made the assertion a permanent FAIL its owner could not clear, and a document-wide needle would have been a tautology.
- [Phase 03]: [Phase 3 / 03-01]: A Class E gap is RECORDED, not forgotten: the branch-conditional `CUDA` absence needle for D-08's `triton-only-fallback` branch is deliberately NOT in the net at wave 1, because on `cudagraphs-confirmed` the bridge clause legitimately names CUDA-graph capture and the needle would assert the opposite of the truth. Owner **03-05 Task 3**, label `EXP-03c/03-05 fallback bridge names no CUDA graphs`, streams `$SQUEEZED` (per the `03-RESEARCH.md` `P3.12` spec) and `$TEX`, appended in the SAME commit that ships the fallback wording. Because it PASSES on arrival it also owes a crafted control (insert `CUDA` into a scratch text layer, `--gate-text`, require exit 1). On `cudagraphs-confirmed`, 03-05 must record in its SUMMARY that the needle was deliberately not added. The gap is written into the script at the end of Group 5. — D-08's fallback honesty constraint would otherwise be acceptance prose with no machine backing, and a later reader would have to rediscover why the phase asserts CUDA nowhere.
- [Phase 03]: [Phase 3 / 03-01]: Every P3 assertion class has been observed FAILING. Classes A/B and the D-05 `achiev` needle by the arrival run against the real committed artifact; retention, EXP-08 draft absence and the promotion conjunction by six crafted controls under `mktemp -d`. Two findings worth carrying: (1) the **source-side** EXP-08 needle catches a leak NO text-layer gate in this repo can see -- a draft behind a `%` comment never renders (proven: appending a commented AssumeRole draft to a basename-preserved scratch `main.tex` flips P3.67 alone); (2) a conjunction assertion cannot be controlled by an ID delta -- P3.42 is already FAIL at baseline, so the evidence is the CONJUNCT the FAIL names. All three conjuncts were observed naming themselves and a fourth configuration makes P3.42 PASS, so it is neither stuck-red nor stuck-green. Conjunct 3 is only reportable when conjunct 1 holds, so its control must pair `--gate-text` with `--manifest`. — An assertion never observed failing is not a gate (Phase 1 rule); an assertion that can never pass is equally useless, and a conjunction proven by exit code alone is one test wearing three names.
- [Phase 03]: [Phase 3 / 03-01]: `requirements-completed` for 03-01 is deliberately **EMPTY** and `requirements mark-complete` was NOT run, despite the plan's `requirements: [EXP-02, EXP-03, EXP-04, EXP-07, EXP-08]`. This plan builds those requirements' acceptance oracle; it does not satisfy them, and the oracle is red with 46 FAILs. EXP-02/03/04/07 close when 03-04/03-05/03-06 drive the census to zero; EXP-08 closes at 03-07/03-08, and **03-08 Task 3 owns the EXP-02 tick**. This follows the recorded Phase-2 precedent: a requirement shared across plans completes when the LAST plan lands, not the first. — Ticking five requirements while their own oracle reports 46 failures would misreport the phase -- exactly the error Phase 2 Plan 01 avoided by leaving EXP-01 open for Plan 02.
- [Phase 03]: [Phase 3 / 03-01]: TRAP T-1 from `03-PATTERNS.md` is FIXED in the P3 copy and the fix must not be reverted: the template's `assert_whole_line_once` documents 3 args but dereferences `$4`, so under `/bin/bash` 3.2.57 with `set -u` a 3-arg call whose assertion FAILS aborts the script -- no RESULT line, no `FAILURES` increment, every remaining assertion skipped -- while exiting 1, which reads in a log as "the document is wrong" rather than "the net stopped running". Now `${4:-...}` with the signature documented as 4-arg. Also fixed forward: the P3 header records that nothing in the file may contain the Phase-2 ID prefix literal, because the plan gates on a whole-file grep for it (the mechanical catch for T-2, a copy that renames the counter but forgets the `emit` printf or the failed-ID append). — T-1 is invisible while the document is green and only fires the day an assertion legitimately fails, which is precisely when the run must not die silently.

- [Phase 03]: [Phase 3 / 03-02]: The Phase-2 content-regression net `scripts/verify-phase2-regressions.sh` is now REACHABLE and is recorded here rather than only in the phase-local `02-VALIDATION.md` (audit IG-01/IG-02). It protects two invariants no assertion in `scripts/verify-resume.sh` covers: career-break absence (source and text layer, Work Experience running ADOBE FIREFLY -> SWIGGY with no employer between, master's dates still extracting from Education) and every locked **Phase-2** D-05/D-06 descriptor literal — the folded Groupon and NetSpeed descriptors. Read "Phase-2 D-05/D-06" literally: the identifier is overloaded, and **Phase-3's own D-05/D-06 are a different namespace** (the Ray swap and the figure spread, both live in this phase), so a bare "D-05/D-06" here would name the wrong content. The load-bearing literal in that set is the multi-word `C++ graph algorithms` anchor, which ROADMAP Phase 5 criterion 4 rests on: bare `C++` was already x2 from page 2, so the multi-word form is the only non-vacuous anchor and losing it has to be a BLOCKER now rather than a silent diff three phases later. New invocation surfaces: `make verify-regressions` runs the net standalone, and `@bash $(REGRESS)` is chained as the FIRST recipe line of `make verify`, above the harness (content-only and sub-second, and GNU Make 3.81 aborts a recipe at its first failing line, so a net chained second would never run whenever the harness is red). STANDING RULE, unchanged by this wiring: the gating invocation remains `bash scripts/verify-resume.sh && bash scripts/verify-phase2-regressions.sh` run directly, because GNU Make 3.81 collapses a recipe's exit 1 and exit 2 into its own exit 2 — `make verify` therefore cannot distinguish "the document is wrong" from "cannot verify" and is a convenience, not an oracle. Separately (audit IG-03 / WR-01), `G6.13`'s WARN is now owned by Phase 3 via the one-line `WARN_OWNERS` edit `G6.13:unassigned` -> `G6.13:3`, so all five role-block lines read `(owner: Phase 3)`. That claim is a WARN only: the gate is NOT promoted to a BLOCKER, and `ROLE_BIND_WINDOW` stays a deliberate local constant at `scripts/verify-resume.sh:1936` — per 01-02, the commit that promotes `G6.13` is the commit that adds the manifest key. — A protection nothing runs is not a protection, and IG-02's complaint was specifically that a planner reading the documented resume surfaces could not learn the net exists; naming the script, the anchor, the target and the gating rule on a canonical surface is what closes it.
- [Phase 03]: [Phase 3 / 03-02]: SITE HANDOFF TO PHASE 6 — `index.html:196` duplicates BOTH figures this phase retires. Verified still present at write time: `3–8K images/sec on 32 GPUs` (count 1) and `10–50× faster image loading` (count 1), inside the site's own parallel Adobe block at `index.html:194-200`. That file is published to GitHub Pages by `.github/workflows/jekyll.yml` on every push to `master`, so the strings are live and public. This is deliberately **NOT Phase-3 scope**: ROADMAP criterion 3 is PDF-scoped ("survives anywhere in the PDF"), and the site requirements are Phase 6. State the consequence plainly rather than leaving it implicit — until SITE-01 lands, the live public site contradicts the resume on exactly the two claims the honesty requirement removed, and the audit already records that no assertion anywhere checks resume-to-site agreement. The same obligation covers the site's second defect: `index.html:207-225`, the site's own career-break timeline entry (SITE-02), which until this record existed was written down ONLY at `.planning/REQUIREMENTS.md:49` — `grep -c 'index.html' .planning/STATE.md` returned 0 before this line, so this record is what first puts both site defects on the canonical surface, not a pointer to an entry that was already here. Phase 6 should treat `index.html:196` and `index.html:207-225` as one greppable site-sync obligation. — OQ-2 resolved as "Phase 3 records, Phase 6 fixes": the duplicate is the one retired-string location a grep of `docs/` would miss entirely, and recording it beats both silently ignoring it and pulling unscoped site edits into a PDF phase.
- [Phase 03]: [Phase 3 / 03-02]: `requirements-completed` for 03-02 is deliberately **EMPTY** and `requirements mark-complete` was NOT run, despite the plan's `requirements: [EXP-02, EXP-07]`. This plan wires the Phase-2 net into make, claims the G6.13 WARN and writes two records; it lands no Adobe content (EXP-02) and tightens no Swiggy/Flipkart bullet (EXP-07). EXP-02 is claimed by 03-01/03-02/03-03/03-04/03-08 and EXP-07 by 03-01/03-02/03-03/03-06/03-08, and **03-08 Task 3 owns the EXP-02 tick**. — Same rule 03-01 and Phase 2 Plan 01 followed: a requirement shared across plans completes when the LAST plan lands, not the first. Ticking a content requirement from a harness-wiring plan would misreport the phase.
- [Phase 03]: The criterion-1 disposition record is WRITTEN and COMMITTED (`03-DECISION-RECORD.md`, 586 lines; commits `8dd7d83`/`8711931`/`645deae`) with commit ordering as its proof: `git log --oneline -1 -- docs/main.tex` still reports the Phase-2 commit `29999ff`, so no resume content edit preceded it. Totals: **9 retain-and-revise** (5 topics A2/A6/A9/A10/A11 + 4 nested items A3/A5/A7/A8), **2 revise** (A1 lead per D-01, A4 figure swap per D-05), **1 add** (A12 governance per D-02), **ZERO retire**. Read the figure/entry distinction literally: the two RETIRED FIGURES (`10–50`, `3–8K images/sec on 32 GPUs`) live inside **A4**, whose ENTRY is revised — it keeps its bullet, shape and position and swaps an unprovenanced pair for an evidenced figure. Recording A4 as `retire` would have told a later reader a claim was dropped, which is the exact confusion criterion 1 exists to prevent. Named as surviving because `CODEBASE-EVIDENCE.md` does not restate them: `Falcon-40B`, `Llama 2 70B`, KEDA-on-`SQS queue depth`, `Source → Transform → Sink`. — Rebuilding the Adobe block from the evidence file alone makes everything that file does not restate look like it was never there (03-RESEARCH.md Pitfall 12, citing PITFALLS.md:689-696). A disposition per entry, committed before any new text, is the only mechanism that makes an implicit drop impossible to commit by accident.
- [Phase 03]: `assert_absent` in `scripts/verify-phase3-regressions.sh` counts with `gcount -Fc` — a fixed-string **CASE-SENSITIVE** match (read from the implementation, not assumed). So an EXP-08 needle only flips absent→present if the shipped bullet carries it byte-for-byte: `AssumeRole` (CamelCase, never `assume-role`), `6-week` (never `six-week`, never `6 week`, and never `6--week` which renders as an en dash), and `mentored` **lowercase and MID-SENTENCE**. That last one is a live trap: a mentoring bullet shipped as sentence-initial `Mentored applied-ML engineers…` would leave `assert_absent 'mentored'` reading count 0 forever, so 03-08's absent→present flip would report a **false absence on a bullet that is actually on the page** — a green gate proving nothing. A needle-casing table is recorded in `03-DECISION-RECORD.md` Section D and every draft is worded to satisfy it. Related, and unchanged: `STS` is NOT a needle because a fixed-string search matches inside `TESTS` (verified: `printf 'RUNNING TESTS\n' | LC_ALL=C grep -Fc 'STS'` returns 1); the wording may still say STS, the needle is `AssumeRole`. — The plan requires each draft to carry its locked needle so the post-checkpoint assertion can flip, but never states the flip is case-sensitive. Without the casing table a naturally-worded draft would silently disarm its own gate — precisely the failure class this phase's standing rule ("an assertion never observed failing is not a gate") exists to prevent.
- [Phase 03]: EXP-08 candidate (c) — the preemptible-fleet idempotent-autorecovery draft — has **NO Class D absence needle**, and the gap is recorded rather than left to be rediscovered. `03-01-PLAN.md` `<locked_literals>` Class D locks exactly three needles (`AssumeRole`, `6-week`, `mentored`) and none belongs to (c), so that candidate is not covered by an absent-by-default assertion at all. Consequence, stated: **if (c) is the approved draft, 03-08 must add its needle in the SAME commit that ships it**, or the bullet enters `docs/main.tex` with no machine record that it was ever gated — defeating D-14's mechanism for that one bullet. Suggested needle: `drop-before-create` (distinctive, whitespace-free after hyphenation, ×0 in `docs/main.tex` today, and `pdftotext` does not hyphenate it so it survives a wrap). This extends 03-01's Class E convention to a second gap: a gap that is written down is a decision; a gap that is not is an omission. — The plan names an absence needle for candidates (a) and (b) only. Discovering at approval time that the third candidate has no needle would mean either shipping it ungated or improvising a needle under checkpoint pressure.
- [Phase 03]: MEASURED rendered-line counts for the line-neutrality contract: **Adobe 18** (gate 11–28), **Swiggy 11** (gate 36–46), **Flipkart 9** (gate 54–62). `03-RESEARCH.md:487`'s "Swiggy 8 rendered lines" is **SUPERSEDED** — that row is internally inconsistent, since 36 through 46 inclusive is 11 lines, and the per-bullet breakdown sums to 11 (2+2+2+2+2+1). Flipkart's 9 and Adobe's 18 reproduce exactly. ⚠ **Absolute gate line numbers SHIFT when Adobe grows** — every rendered line the rebuild adds pushes Swiggy and Flipkart down by the same amount, so `36–46` and `54–62` are true TODAY ONLY. D-11's line-neutrality must therefore be re-derived from **region boundaries** (the employer subtitle anchor down to the next blank line), **never** from fixed line numbers: a differential check pinned to literals would report a false regression the moment the A12 governance topic lands. Corroborating check: `G3.2` p1 must move by EXACTLY the Adobe delta and nothing more. — The plan predicted this correction and the measurement confirms it. Recording the shift-on-growth property matters more than the counts themselves — a later plan that hard-codes 36-46 into a neutrality check would fail on a correct document.
- [Phase 03]: `requirements-completed` for 03-03 is deliberately **EMPTY** and `requirements mark-complete` was NOT run, despite the plan's `requirements: [EXP-02, EXP-04, EXP-07, EXP-08]`. This plan writes the phase's decision record: it lands no Adobe content (EXP-02), puts no figure into `docs/main.tex` (EXP-04), tightens no Swiggy/Flipkart bullet (EXP-07) and ships no EXP-08 bullet — and the P3 oracle still reports **46 owned FAILs** (byte-identical census across all three of this plan's commits, since it touched no script). All four requirements are shared: EXP-02 across 03-01/02/03/04/07/08, EXP-04 across 03-01/03/04/05/08, EXP-07 across 03-01/02/03/06/08, EXP-08 across 03-01 and 03-03 through 03-08 — with **03-08 the last claiming plan for all four**, and 03-08 Task 3 owning the EXP-02 tick. Third consecutive plan in this phase to follow the rule. — Same precedent 03-01, 03-02 and Phase 2 Plan 01 followed: a requirement shared across plans completes when the LAST plan lands, not the first. Ticking four content requirements from a plan whose own oracle reports 46 failures would misreport the phase.
- [Phase 03]: BUDGET LEDGER handed to waves 2–4, all measured, none predicted. Baseline `G3.2` p1 **+50.5pt (+4.39 lines)** at `LINE_PT=11.5`; unit cost **11.4–11.5pt** per rendered line; ceiling **+4 rendered lines** (+4 stays 2 pages at +4.7pt; **+5 is a 3-page PDF with `G1.1` FAIL + `G2.1` FAIL**). Spend table: A12 governance **+2** (topic-only — the `\resumeTopic`-plus-nested-item shape measures **4 rendered lines, the entire budget**, and is FORBIDDEN), A4 Ray swap **+1**, D-04 mechanisms + D-06 figures + D-07 bridge at a **0pt target** written into the ≈484 free tail characters, Swiggy/Flipkart **0** per D-11 → expected end state **+3, `G3.2` p1 ≈ +16.1pt**, holding **one rendered line (11.5pt) as the EXP-08 approval fund**. Contingency ladder: ≥+16.1pt reserve intact · +4.7pt…+16.1pt reserve consumed and the EXP-08 checkpoint context must say so · below +4.7pt or 3 pages, back the edit out. TWO HARD RAILS: (1) never a fifth line; (2) **`G3.2` p1 must never RISE above +50.5pt** — net-freeing a rendered line was reproduced taking slack to 61.4pt and making the +5 probe render 2 pages, i.e. `G7.2` FAIL, and `bash scripts/verify-resume.sh` stays GREEN through that failure because `G7` runs only under `--selftest`. So spend before you free and no wave may end net-freed. `PROBE_LINES` is raise-only and is NOT raised this phase; `DIGIT_BULLET_FLOOR` is never lowered. Also recorded: `G1.1` is the fit gate, NOT `G3.1`/`G3.2` (at +5 lines `G3.1`, `G2.3` and `G2.4` all stayed green on a 3-page document); `G5.4` is warn-tier and cannot fail, so assert the **value in its message** (baseline **8**, criterion 4 needs ≥8) and remember it cannot see a digit that wrapped onto a continuation line (live proof: Swiggy's `4Bn rows, 10K QPS` on gate line 43 is absent from the match set). The sanctioned Groupon/NetSpeed reword-shorter lever requires all three preconditions AND lands in the SAME commit as whatever it funds, so the net delta is zero. — Phase 2's core lesson is that budget arithmetic is measured, never predicted. Putting the thresholds, the ceiling, the two rails and the ladder on the canonical surface lets 03-04/05/06 spend without re-deriving the phase, and makes "back the edit out" a numeric decision rather than a judgement call.
- [Phase 03]: [Phase 3 / 03-04]: MEASURED end state of the Adobe rebuild, all read from the harness, none predicted. `G3.2` p1 walked +50.5pt (+4.39 lines) -> +50.5pt after Task 1 -> **+28.1pt (+2.44 lines)** after Task 2 -> +28.1pt after Task 3, i.e. the ONLY rendered-line spend in the whole plan is the +2 the ledger allocated to the A12 governance topic. The Adobe rendered region grew 18 -> 20 gate lines (11-30). The lead rewrite, all seven D-04 mechanisms, all eight 03-04-owned D-06 figures and the manifest promotion each cost a measured **0pt**, written into already-wrapped bullets' last rendered lines. `G5.4` rose 8 -> 9 -> **10** (Adobe digit-bearing gate lines 15, 16, 21, 22 plus new 25 and 27; both new figures were deliberately placed on their bullets' FIRST rendered lines because the regex only matches bullet-glyph-prefixed lines -- the A2 fleet figures wrapped onto continuation line 14 and are correctly invisible). `G6.5` 13 -> **15** PASS after promoting `A100` and `H100`. **EXP-08 reserve is INTACT** (+28.1pt is above the ledger's >= +16.1pt rung): 03-05's D-05 swap has its +1 line (~11.5pt) and roughly one further line remains as the approval fund, so the sanctioned Groupon/NetSpeed reword-shorter lever was NOT pulled and no same-commit obligation is inherited. `G3.2` p1 never ROSE above the +50.5pt baseline at any point, so rail 2 held and `make verify-selftest` still reports `G7.2 PASS the probe PDF is 3 page(s)`. P3 census 46 FAIL -> **14 FAIL**, with all **32** `/03-04`-owned assertions cleared and the remainder owned only by `/03-05` (13) and `/03-06` (1). — Phase 2's core lesson is that budget arithmetic is measured, never predicted, and the next plan spends against these exact figures. Recording the walk rather than only the end state also proves rail 2 (never net-free a rendered line) held at every intermediate commit, not merely at the end.
- [Phase 03]: [Phase 3 / 03-04]: TWO placement departures from `03-DECISION-RECORD.md` Section A, both forced by measured capacity and both leaving criterion 3 untouched. (1) The three genie/asml GPU **fleet** figures `24×A100-40GB`, `9 model queues` and `64 H100s` landed in **A2 `Distributed Inference Frameworks`** (`main.tex:152`), not in A10 `Enrichment Service` as the owning-topic column lists. A10 renders 2 lines with a measured ~268-character capacity, and the table assigns it FIVE figures plus the four D-04 control-plane mechanisms -- measured ~345 characters, so there is no arrangement that fits at Task 3's 0-additional-line target, and the contingency ladder forbids spending a line while room remains elsewhere. A2 is the topic whose subsystem actually runs those GPU jobs (the offline/batch inference modules, `CODEBASE-EVIDENCE.md:62` and `:98`), so every figure still sits in an owning topic -- D-06's binding rule is spread-by-owning-topic, never an aggregate figure bullet -- and the evidence citations are byte-unchanged. The two **migration** figures `202.6M-row` and `24.8M` stayed in A10 with `idempotent re-run`, `concurrency-safe S3 checkpoint`, `lease/TTL heartbeat` and `circuit breaker`. (2) `40-node` landed in the LEAD bullet's lifecycle clause rather than A10, using the pre-authorization in Task 1's own action text; it sits on continuation line 12 and so contributes nothing to `G5.4`. Also recorded because it removes a clause: A10's retained claim was compressed from "Orchestration and abstraction layer over the inference framework and other ML Platform offerings; runs fire-and-forget pipelines on datasets of billions of images and videos" to "Orchestration over the inference framework; ...", replacing the vague scale phrase with the two evidenced migration figures. No locked literal and no keyword carrier was involved, D-03's disposition for A10 is retain-and-revise ("revised tighter"), and "billions of images and videos" still survives verbatim at A6 (`main.tex:158`). — The traceability table's owning-topic column piles five figures and four mechanisms onto one 2-rendered-line topic, which is physically unsatisfiable -- so a later reader comparing the record to the document would otherwise read the difference as an implicit drop, which is exactly the confusion criterion 1 exists to prevent. Writing down which figure moved, where, and why keeps the disposition record auditable.
- [Phase 03]: [Phase 3 / 03-04]: The P3 net's FAIL census must be counted with the ANCHORED form `^RESULT P3\.[0-9]+ +FAIL`, never the bare word `FAIL` -- and `03-04-PLAN.md` Task 3's own `<automated>` clause gets this wrong in a way that makes it UNSATISFIABLE on any document. The net emits two trailing footer lines that contain the word FAIL and carry no owner tag by construction: `!! FAIL: 14 of 72 Phase-3 regression assertion(s) failed: P3.41 ...` and `!! Every RESULT ... FAIL line above names its owning plan and its remedy.` So `grep -E 'FAIL' | grep -vcE '/03-0[56] '` reads **2** on a fully-correct document and can never reach 0, and the whole `<automated>` command exits 1 while every acceptance criterion is in fact satisfied (verified: anchored unowned-FAIL count 0, `/03-04`-owned FAIL count 0, 58 anchored PASS). Gated on the anchored form instead. This is the SAME trap the plan already documents three times for the P2 net's PASS side ("a green run emits 28 lines containing the word PASS -- the `>> PASS:` summary is the 28th, so always count the anchored form"); the rule was simply never applied to FAIL. **03-05 and 03-08 reuse this clause shape and will hit it** -- use `^RESULT P3\.[0-9]+ +FAIL` there too. Separately for 03-05: the promotion witness `LC_ALL=C grep -Fc 'A100|H100' docs/verify/manifest.txt` == 1 is only valid until `torch.compile` and `Triton` are appended after `H100`, so 03-05 must re-derive its own adjacency witness rather than reusing this one. — A verification clause that cannot pass is indistinguishable, in a log, from a document that is wrong -- the same failure class Phase 1's "an assertion never observed failing is not a gate" rule guards from the other direction. Recording it here stops the next two plans from re-diagnosing a green document as red.
- [Phase 03]: [Phase 3 / 03-04]: `requirements-completed` for 03-04 is deliberately **EMPTY** and `requirements mark-complete` was NOT run, despite the plan's `requirements: [EXP-02, EXP-04]`. Fourth consecutive plan in this phase to follow the rule. EXP-02 is claimed by 03-01/02/03/04/08 and EXP-04 by 03-01/03/04/05/08, with **03-08 the last claiming plan for both and 03-08 Task 3 owning the EXP-02 tick**. EXP-04 is also substantively incomplete, not merely shared: its own `REQUIREMENTS.md:21` text names the `1.09M`-row / `8×A100` Ray benchmark, and `P3.46`-`P3.55` (the retirement pair, the `achiev` attribution needle and the three D-05 replacement figures) are still RED under 03-05, so the two unverified page-1 numbers `10–50` and `3–8K images/sec on 32 GPUs` are still live in the committed PDF at gate line 16. EXP-02's own sub-clauses (a/b/c) are all green now, but the requirement is not closed until the last claiming plan lands. Also unchanged and still owed by later plans: the Class E branch-conditional `CUDA` needle (owner 03-05 Task 3) and EXP-08 candidate (c)'s missing Class D needle (owner 03-08). — Same precedent 03-01, 03-02, 03-03 and Phase 2 Plan 01 followed: a requirement shared across plans completes when the LAST plan lands, not the first. Ticking EXP-04 while its own oracle still reports the unverified figures present would misreport the phase.
- [Phase 03]: [Phase 3 / 03-04]: The free-tail ledger's per-shape capacity of "≈120–124 characters" is CONSERVATIVE and should be re-derived from the committed artifact before budgeting, not used as the working number. Measured on the committed PDF this plan produced: itemize-2 lines (lead + topics) reach **135–140 characters** and itemize-3 lines (nested items) reach **128–130** -- including a LAST line at 135 (governance, gate 28) and 133 (A2, gate 14), so a wrapped bullet's final line is just as wide as its first. Practical 2-rendered-line content capacity is therefore ~265–270 characters for a topic and ~254 for a nested item, roughly twice the ledger's implied figure. Method that made all four revisions land at their target line count on the FIRST build with zero retries: extract the gate stream, take `len()` in characters (never bytes -- the U+2013 bullet prefix is 3 bytes, `×` is 2, `’` is 3, which is what inflates a naive `awk '{print length}'` measurement), length-check every candidate string against the measured width, and only then edit. The ledger's own ⚠ measurement note warns about the byte/character confusion; this entry supplies the corrected widths. — Budgeting against ~120 would have made this plan's content look unfittable and pushed it toward spending rendered lines it did not need -- consuming the EXP-08 reserve for nothing. The ledger's pt figures are exact and stay authoritative; only its character estimate needed correcting.
- [Phase 3 / D-08]: **`cudagraphs-confirmed`** — the production compile config WAS read, and it does not disable CUDA graphs. Evidence supplied by the user at the 03-05 Task 1 checkpoint: the genie `1.0.0+957ce74` wheel, sha256-verified (`c22efd9a…68dd0`), whose `glimmer/modules/dp/content_type_classifier/module.py:76-77` carries `torch.compile(mode="max-autotune")` on BOTH the CLIP encoder and the classifier head — matching `CODEBASE-EVIDENCE.md:54` byte-for-byte — with a wheel-wide search returning **ZERO** hits for every disabling form: `cudagraph`, `cuda_graph`, `triton.cudagraphs`, `torch._inductor.config` and the `max-autotune-no-cudagraphs` mode string, and no YAML override anywhere in the wheel. This settles the one surface `CODEBASE-EVIDENCE.md:54` could not: that citation fixed the `mode=` string only, while the Inductor toggle was tagged `[ASSUMED]` with no source. CONSEQUENCE for 03-05 Task 3: the bridge clause ships the FULL compiler bridge — Triton-generated kernels, compiler-driven fusion AND CUDA-graph capture — resting on the official-docs semantics of the mode actually in production (`.planning/research/STACK.md:460-495`), phrased as what the mode buys per documented default rather than as a hand-built optimization; `STACK.md:483`'s silent-fallback caveat (input mutation, dynamic shapes, graph breaks) stays the interview answer, not a reason to weaken the clause. This **SUPERSEDES the `[Phase 5]` CUDA-graphs blocker line in `### Blockers/Concerns`** — the read it was waiting on is now discharged against a checksummed artifact, so that concern is closed rather than carried. Two consequences deliberately NOT taken on this branch: (1) the Class E `EXP-03c/03-05` `CUDA` absence needle recorded at `[Phase 3 / 03-01]` is **NOT** appended to `scripts/verify-phase3-regressions.sh`, because on this branch the clause legitimately names CUDA-graph capture and the needle would assert the opposite of the truth — the recorded gap therefore closes as a decision, not as an omission; (2) the deferred `CUDA graphs` exact-match keyword stays an unpromoted `KEYWORDS_TARGET` entry owned by **Phase 5's PG2-04**, and D-07's hyphenated singular `CUDA-graph capture` does not land the fixed string anyway (verified: a line containing only `CUDA-graph capture` does not match `CUDA graphs`). — why: D-08 made the CUDA-graph half of the bridge conditional on one read no agent could perform from this repo; it is now discharged with a checksummed wheel rather than an assumption, so the strongest honest clause ships with its evidence recorded next to it.
- [Phase 03]: [Phase 3 / 03-05]: MEASURED end state of the figure retirement and the production compile path. `G3.2` p1 walked +28.1pt (+2.44 lines) -> **+17.1pt (+1.49 lines)** after Task 2 -> +17.1pt after Task 3: the ONLY rendered-line spend is the +1 the ledger allocated to the D-05 Ray swap (measured -11.0pt), and the whole D-07 bridge clause cost a measured **0pt**. Adobe grew 20 -> **21** rendered gate lines (11-31), so absolute gate line numbers below Adobe shifted down by one AGAIN -- re-derive Swiggy/Flipkart regions from region boundaries, never from literals. `G5.4` held at **10**; `G6.5` 15 -> **17** PASS after promoting `torch.compile` and `Triton`; `G6.4` census UNCHANGED at U+201C x1 / U+201D x1, proving the plain ASCII quote in `torch.compile(mode="max-autotune")` extracted as U+0022 with zero curly quotes added, and U+2013 fell 40 -> 38 exactly as the retirement predicted. P3 census 14 FAIL -> 4 -> **1 FAIL / 71 PASS**, the sole survivor being `P3.66`, `/03-06`'s own `manual deploys` needle -- all 13 `/03-05`-owned assertions cleared. **EXP-08 reserve SURVIVED** (+17.1pt is above the ladder's >= +16.1pt top rung, by 1.0pt), so the sanctioned Groupon/NetSpeed reword-shorter lever was NOT pulled and no same-commit obligation is inherited. Rail 2 held at every intermediate commit -- p1 never ROSE above +50.5pt -- and `make verify-selftest` still reports `G7.2 PASS the probe PDF is 3 page(s)` with ten G7/G8 PASS and zero anchored SKIP. Also measured, and correcting `[Phase 3 / 03-04]`: **itemize-3 line-1 capacity is 135 characters, not 128-130** (gate line 16 rendered at 135), so two-line itemize-3 content capacity is ~266 -- exactly what the shipped A4 bullet uses. Budgeting against 130 would have predicted a third rendered line for the honest bridge and consumed the EXP-08 reserve for nothing. — Phase 2's rule is that budget arithmetic is measured, never predicted, and 03-06 spends against these exact figures. The corrected itemize-3 width is what made the full honest bridge (Triton codegen, fusion, CUDA-graph capture) fit at zero line cost.
- [Phase 03]: [Phase 3 / 03-05]: The D-08 branch detector had to be SCOPED TO THE MARKER LINE, and the same correction is owed by any later plan that reads a decision marker's payload. `03-05-PLAN.md` asserts as measured fact that neither D-08 branch identifier appears anywhere in `.planning/STATE.md`, and specifies both Task 1's uniqueness clauses and Task 3's branch detector as document-scoped greps on that basis. **That measurement is stale**: the `[Phase 3 / 03-01]` Class E gap record at `.planning/STATE.md:63` -- committed during wave 1, AFTER 03-05 was written -- carries BOTH identifiers as history. Two verified consequences: Task 1's `<automated>` block became unsatisfiable (document-scoped counts read 2 and 1, so the `-le 1` and `sum = 1` clauses fail on a fully correct file, exit 1), and far worse, Task 3's document-scoped detector returned the FALLBACK branch regardless of the decision -- it would have shipped the wrong bridge wording, appended the Class E `CUDA` absence needle that asserts the opposite of the truth on the branch actually chosen, then failed its own `grep -Fci CUDA docs/main.tex` = 0 gate. Fix: pipe through the marker first -- `LC_ALL=C grep -F '<the D-08 marker literal>' .planning/STATE.md | LC_ALL=C grep -q <identifier>` -- which reads 1 and 0 and selects correctly (the marker literal is deliberately NOT reproduced verbatim here: this file must contain exactly ONE occurrence of it, or the marker-line scoping this entry prescribes would itself match two lines). **`.planning/STATE.md:63` was deliberately NOT edited**: its content is correct and rewriting a prior plan's record to make a later grep convenient would destroy the audit trail. STANDING RULE for 03-06/03-07/03-08 and Phase 5: a decision-marker payload is read by scoping to its marker line, never by a document-wide grep, because this file accumulates prose that legitimately quotes both sides of a past branch. This is the same class as the anchored-vs-unanchored FAIL census recorded at `[Phase 3 / 03-04]` -- and 03-05's Task 3 `<automated>` reproduced that one too (the unanchored form read 2 on a correct document, from the two owner-tag-free `!!` footer lines), so both were gated on the corrected forms. — A verification clause that cannot pass is indistinguishable in a log from a document that is wrong, and a branch detector that always answers the same way silently routes a correct decision to the wrong honesty gate -- exactly the failure T-03-26 exists to prevent. Recording the scoping rule stops the remaining plans from re-diagnosing it.
- [Phase 03]: [Phase 3 / 03-05]: The Class E `EXP-03c/03-05` `CUDA` absence needle was DELIBERATELY NOT appended, and the recorded gap therefore closes as a DECISION rather than as an omission. D-08 resolved to the branch on which the bridge clause legitimately names CUDA-graph capture, so the needle would have asserted the opposite of the truth and been permanently red on a correct document. It stays exactly where 03-01 left it: a commented RECORDED GAP block at the end of Group 5 in `scripts/verify-phase3-regressions.sh:876-900`. Consequences, all verified: `grep -Fc 'EXP-03c/03-05'` returns 1 from that COMMENT only, there is NO live `assert_absent 'CUDA'` anywhere in the file, the net is byte-unchanged by this plan (`shellcheck -f gcc` clean, `/bin/bash` 3.2.57 run identical at 71 PASS / 1 FAIL), and `LC_ALL=C grep -Fci CUDA docs/main.tex` is **1** -- which is the CORRECT value on this branch, not a leak. The crafted `--gate-text` control the plan owed on the other branch was therefore NOT run and is NOT owed: the assertion it would have proven failable does not exist. Related and unchanged: `CUDA graphs` stays an unpromoted `KEYWORDS_TARGET` entry at x0 (`grep -c 'CUDA graphs' docs/verify/manifest.txt` = 1) owned by Phase 5's PG2-04, because D-07's hyphenated singular `CUDA-graph capture` does not land the fixed string. Separately, `requirements-completed` for 03-05 is deliberately **EMPTY** and `requirements mark-complete` was NOT run despite the plan's `requirements: [EXP-03, EXP-04]`. EXP-03 is claimed by 03-01/03-05/03-08 and EXP-04 by 03-01/03-03/03-04/03-05/03-08, with **03-08 the last claiming plan for both**. Both requirements' TEXT is now substantively satisfied -- EXP-03's bridge clause ships here, and EXP-04's six figure families are complete across 03-04 and this plan -- but the phase oracle is still red on the `/03-06` needle. Sixth consecutive plan in this phase to follow the rule. — An assertion that can never pass is as useless as one never observed failing, so a branch-conditional needle whose branch was not taken must be closed with its reason rather than added for symmetry. And a requirement shared across plans completes when the LAST plan lands, not the first -- the precedent 03-01 through 03-04 and Phase 2 Plan 01 all followed.
- [Phase 03]: [Phase 3 / 03-06]: MEASURED end state of the line-neutral Swiggy/Flipkart reword, and the number 03-07's checkpoint must quote. `G3.2` p1 held at **+17.1pt (+1.49 lines)** at BOTH task commits -- byte-identical to the figure `03-05-SUMMARY.md` recorded -- so the plan spent and freed exactly **zero** rendered lines. Regions re-derived from BOUNDARIES (the frozen subtitle matched as a whole line, then the walk to the next blank line), never from gate-line literals: **Swiggy 11 -> 11** (gate 39-49), **Flipkart 9 -> 9** (gate 57-65), Adobe untouched at 21 (gate 11-31). Neutrality was held PER BULLET, not merely per region (Swiggy 2+2+2+2+2+1, Flipkart 2+1+2+2+2), so no intra-region compensation could mask a spend. `G5.4` rose **10 -> 11** (match set 15, 16, 22, 23, 26, 28, **39, 45, 49**, 64, 73 = Adobe 6 / Swiggy 3 / Flipkart 1 / Groupon 1 / NetSpeed 0): the gain is gate 45, the Feature Store bullet's FIRST rendered line, because replacing the unmeasured qualifier `at production scale` with the bullet's own `4Bn rows and 10K QPS` pulled `4Bn rows` off the continuation line where Pitfall 5 makes it invisible. `DIGIT_BULLET_FLOOR` was NOT raised to match -- the Phase-1 rule against moving a threshold toward a document applies in both directions. P3 census **1 FAIL -> 0**: `bash scripts/verify-phase3-regressions.sh` exits **0** with **72 anchored PASS**, the red-on-arrival census of 46 fully discharged (32 by 03-04, 13 by 03-05, the final `manual deploys` needle here). `G6.5` 17 PASS / 0 FAIL with `fault` x3 (sole carrier still Swiggy's `fault isolation across clusters`) and page-1 `Spark` x3; `G6.4` census unchanged incl. U+2014 x1. **EXP-08 reserve SURVIVED**: +17.1pt is above the ladder's >= +16.1pt top rung by 1.0pt, the Groupon/NetSpeed reword-shorter lever was NOT pulled, so NO same-commit obligation is inherited -- 03-07's context must present the RESERVE-INTACT situation (~one rendered line of approval fund; one approval lands p1 at roughly +5.6pt and is fundable, TWO are not). Rail 2 held at every commit (p1 never ROSE above +50.5pt) and `make verify-selftest` still reports `G7.2 PASS the probe PDF is 3 page(s)` with ten G7/G8 PASS and zero anchored SKIP. Capacity re-measured on this artifact and correcting forward again: itemize-2 line-1 reaches **137** characters and itemize-3 line-1 **136** (vs 03-05's 135), so the working sizing band is content **>~140 and <~270** for a 2-line target and **<~130** for a 1-line target -- all eleven bullets landed at their target line count on the FIRST build with zero retries. — Phase 2's rule is that budget arithmetic is measured, never predicted, and 03-07 spends against these exact figures at a checkpoint that cannot auto-advance. Recording the per-bullet neutrality and the intermediate p1 reading proves rail 2 held at every commit rather than only at the end -- a net-freed rendered line reddens G7.2 only under --selftest, so bash scripts/verify-resume.sh would stay green straight through that failure.
- [Phase 03]: [Phase 3 / 03-06]: The plan's absolute **SOURCE** line numbers were stale by +1 for BOTH blocks, and the frozen-cell assertion pinned to them would have checked the WRONG lines. `03-06-PLAN.md` cites Swiggy bullets at `:172`/`:174`/`:175`/`:177`/`:178`/`:179` and freezes `:169`/`:170`; the live file has them at `:173`/`:175`/`:176`/`:178`/`:179`/`:180` with the frozen cells at **`:170`** (title + dates) and **`:171`** (employer + subtitle). Flipkart: plan `:186`/`:187`/`:188`/`:190`/`:192` and freezes `:183`/`:184`; live `:187`/`:188`/`:189`/`:191`/`:193` with frozen cells at **`:184`** and **`:185`**. Cause: 03-05's D-05 Ray swap added one SOURCE line to the Adobe block. This is the source-side twin of the gate-line shift `03-DECISION-RECORD.md` Section C warns about -- the plan warns about RENDERED lines, then hard-codes SOURCE lines. Left uncorrected, `:169`/`:170` byte-identity would have compared `\resumeSubheading` and the title+dates row and **never inspected the employer/subtitle line at all** -- which is precisely the anchor `scripts/verify-phase3-regressions.sh` Group 2 resolves `GENERATIVE AI, COMPUTER VISION, LARGE LANGUAGE MODELS` against, and whose Swiggy/Flipkart twins are this plan's region boundaries. Fix, strictly stronger than the plan's own assertion: (1) prove the whole source diff touches ONLY the intended bullet lines via `git diff -U0 docs/main.tex` hunk headers (measured `@@ -173 +173 @@`, `@@ -175,2 +175,2 @@`, `@@ -178,3 +178,3 @@` and `@@ -187,3 +187,3 @@`, `@@ -191 +191 @@`, `@@ -193 +193 @@`), and (2) assert byte-identity against the previous commit on the CONTENT-resolved frozen lines (Swiggy `:170`/`:171`, Flipkart `:183`/`:184`/`:185` -- all five BYTE-IDENTICAL). STANDING RULE for 03-07/03-08 and Phase 5: resolve a `docs/main.tex` line reference by CONTENT, never by the number a plan wrote down, because every wave that adds a source line invalidates every downstream literal. `docs/verify/baseline-frozen.txt`, `docs/verify/manifest.txt` and `scripts/*.sh` are all zero-diff for this plan -- the net was driven green by content alone. — A frozen-surface assertion pinned to a stale line number is worse than none: it reports PASS while checking a line nobody asked about, so the surface it was written to protect goes unguarded silently. The whole-diff-scope check has no such failure mode because it enumerates what DID change rather than what did not.
- [Phase 03]: [Phase 3 / 03-06]: `03-06-PLAN.md` Task 1's `<automated>` clause is UNSATISFIABLE at Task-1 time, for the third variant of the same trap this phase keeps hitting. Its tail reads `grep -cE 'PASS.*EXP-07' "$SC/p06a.txt" | grep -qvx 0`, meant as a vacuity guard proving the EXP-07 slice was actually inspected. But `scripts/verify-phase3-regressions.sh` carries **exactly ONE** EXP-07-labelled RESULT assertion -- `P3.66`, the `manual deploys` needle that **Task 2** lands. Measured at Task-1 time: EXP-07 lines total **2** (the one RESULT line plus the `>> Groups run:` footer, which merely mentions the string), EXP-07 **PASS** lines **0**, EXP-07 FAIL lines **1**. So the count reads 0, `grep -qvx 0` exits 1, and the whole command exits non-zero while every acceptance criterion is in fact satisfied. Fix: keep the criterion's load-bearing half verbatim (filter the slice to FAIL lines FIRST, then require zero non-`manual deploys` survivors -- measured 0) and replace the vacuity guard with two stronger checks that do not depend on an EXP-07 label existing: the anchored `^RESULT P3\.[0-9]+ +FAIL` census equals 1 AND that line is the `manual deploys` one; plus each of the six named Swiggy retention needles looked up BY ITS LITERAL and asserted non-FAIL (`18M+` P3.10/P3.18, `4Bn rows` P3.11, `10K QPS` P3.12, `fault isolation across clusters` P3.7, `backpressure-aware scheduling` P3.8, `sustained low P99` P3.9 -- all PASS). Naming the needles is what the guard was reaching for and is strictly stronger than trusting a label. The net was NOT edited. THIRD SIGHTING of this family in one phase, so treat it as the phase's signature defect: (1) `[Phase 3 / 03-04]` -- the unanchored FAIL census matching the two `!!` footer lines; (2) `[Phase 3 / 03-05]` -- the document-wide decision-marker grep matching a prior plan's history record; (3) this one -- a label-count guard whose label the plan's own task has not landed yet. All three share one shape: a verification clause written against a PREDICTED output shape rather than a measured one. 03-07/03-08 must RUN each `<automated>` clause and read its output before trusting a non-zero exit as a document defect. — A verification clause that cannot pass is indistinguishable, in a log, from a document that is wrong -- the mirror of Phase 1's rule that an assertion never observed failing is not a gate. Recording the third instance as a family, with its shared root cause, is what turns three separate rediscoveries into one rule the remaining plans can apply up front.
- [Phase 03]: [Phase 3 / 03-06]: The Flipkart employer-vs-subtitle extraction inversion **has SELF-HEALED**, and all five role blocks now extract employer BEFORE subtitle -- measured, deliberately NOT gated, and not to be assumed. Measured on the committed artifact: ADOBE FIREFLY employer@9 / subtitle@10; SWIGGY 37/38; FLIPKART (a Walmart company) 55/56; GROUPON 71/72; NETSPEED SYSTEMS (Acquired by Intel) 80/81 -- every one employer-first. `03-06-PLAN.md` predicted exactly this ("a `pdftotext` row-order artifact measured as self-healing once Adobe grows by a rendered line") and instructed that the order be RECORDED but not fixed and not gated; nothing in this plan touched a `\resumeSubheading`, so the change is an artifact of Adobe reaching 21 rendered lines, not of an edit. A later plan must NOT treat employer-first as guaranteed: it is the row order of a two-cell `tabular*` row, and the Phase-2 `[Phase 2 / 02-02]` finding is explicit that Work-Experience rows extracted arg #1 before #2 while EDUCATION rows extracted #2 before #1 -- that asymmetry is exactly what `G6.12` exists to catch and is MEASURED, never predicted. Consequence for anyone tempted to lean on it: the `scripts/verify-phase3-regressions.sh` Group 2 lead-bullet slice resolves its region from the SUBTITLE line as a whole-line anchor, never from the employer line, so it is immune either way -- and it should stay that way. Separately, one incidental improvement worth not re-diagnosing: `millions every day` now reads **x1 in the RAW gate stream too** (it fits wholly on gate 57 after the reword) where it previously read x0 raw / x1 squeezed. The P3 assertion stays squeezed-ONLY and stays green, because squeezing can never destroy a match for a phrase already on one line; do NOT "strengthen" it to a raw assertion, since the next reflow could legitimately wrap it at "every / day" again. — The plan asked for the order to be recorded rather than fixed precisely so a future reader would not read the change as a regression caused by this edit. Writing down that it is ungated, plus why the Group 2 anchor does not depend on it, closes the tempting follow-up of gating a pdftotext row-order artifact that Phase 2 already proved is asymmetric between section types.
- [Phase 03]: [Phase 3 / 03-06]: `requirements-completed` for 03-06 is deliberately **EMPTY** and `requirements mark-complete` was NOT run, despite the plan's `requirements: [EXP-07]`. **Seventh consecutive plan in this phase to follow the rule.** EXP-07 is claimed by 03-01, 03-02, 03-03, **03-06** and **03-08**, with 03-08 the LAST claiming plan. What is different this time, and is recorded so nobody reads it as an oversight: EXP-07's own `REQUIREMENTS.md:24` text -- "Swiggy/Flipkart bullets tightened to staff-signal formulas (adoption-count + failure-class-closure slots)" -- is now **substantively satisfied**, and unlike every prior handover in this phase the oracle is no longer red on anything (`bash scripts/verify-phase3-regressions.sh` exits 0, 72 anchored PASS). The tick still belongs to 03-08 for two reasons: the standing precedent that a requirement shared across plans completes when the LAST plan lands, and the substantive one -- EXP-08's conditional wave can still add a page-1 bullet, and every EXP-07 claim rests on the line-neutrality contract that a new rendered line would renegotiate. Also unchanged and still owed: EXP-08 candidate (c) has NO Class D absence needle, so if it is the approved draft 03-08 must add its needle (suggested `drop-before-create`) in the SAME commit that ships it; all three existing Class D needles (`AssumeRole`, `6-week`, `mentored`) remain absent-by-default and PASS at P3.67-P3.72; and the needle-casing table in `03-DECISION-RECORD.md` Section D governs any approved wording -- a sentence-initial `Mentored` would leave `assert_absent 'mentored'` reading 0 forever, so the absent->present flip would report a false absence on a bullet that is actually on the page. — Same precedent 03-01 through 03-05 and Phase 2 Plan 01 followed. Stating that the requirement TEXT is now satisfied while the tick is still deferred is what prevents the next reader from either ticking it early or concluding the work is incomplete -- the distinction is ownership of the last claiming plan, not outstanding work.
- [Phase 3 / EXP-08]: **PER-BULLET SIGN-OFF TAKEN** at the 03-07 Task 2 blocking checkpoint, against a rebuild proven green FIRST as D-15 requires (harness exit 0 with zero FAIL and zero SKIP · P2 net exit 0 with **27** anchored `^RESULT P2\.[0-9]+ +PASS` · P3 net exit 0 with **72** anchored PASS · self-test exit 0 with ten G7/G8 PASS and zero anchored SKIP; `make verify` exit 0 recorded as ROADMAP-criterion-5 corroboration only and explicitly NOT the oracle, since GNU Make 3.81 collapses a recipe's exit 1 and exit 2 into its own 2). Verdicts, one per draft ID, recorded as DECISIONS and never as omissions: **(a) STS assume-role storm fix (needle `AssumeRole`) — REJECTED** · **(b) 6-week silent outage root-cause + smoke-test CI (needle `6-week`) — REJECTED** · **(c) preemptible-fleet idempotent autorecovery (⚠ no locked needle) — REJECTED** · **mentoring / leadership line (needle `mentored`, lowercase mid-sentence) — APPROVED**, the as-drafted wording approved in principle with the user explicitly accepting that the people-relationship claim rests on their own word rather than on mined evidence (the Section D honesty note — commit history cannot record who learned what from whom — was presented and read before the answer). ⚠ **The three rejections are affirmative decisions, not gaps**: D-14 makes absence the default and rejection a fully green outcome, so `assert_absent` stays byte-unchanged for `AssumeRole` (P3.67/P3.68) and `6-week` (P3.69/P3.70), and **03-08 must NOT ship (a), (b) or (c) in any wording or any length** — a later reader must not read their absence as an oversight. The recorded (c) Class D needle obligation is therefore **DISCHARGED BY REJECTION**: `drop-before-create` is NOT added to `scripts/verify-phase3-regressions.sh` and stays ×0 in both source and text layer, because the bullet it would have gated is not shipping — nobody should hunt for a missing needle later. **APPROVED WORDING, VERBATIM** (extracted byte-exact from `03-DECISION-RECORD.md` Section D so 03-08 ships exactly this claim and nothing reconstructed from memory; pure ASCII, the apostrophe is U+0027): "Grew the platform's engineering bench: mentored applied-ML and platform engineers onto the training and inference frameworks, authoring the architecture guide, runbook and multi-node launch templates teams onboard with, and reviewing their module integrations through to production." Owning topic on approval is **A6 `\resumeTopic{Foundation Model Training Framework}`** as a nested `\resumeItem`. **staff-signal: yes** — the user read page 1 of the rendered PDF end to end and judged the rebuilt Adobe block to read as staff signal rather than job description, so ROADMAP criterion 2's qualitative half is satisfied on a POSITIVE read: **03-08 Task 1 Branch C is NOT triggered** (no line-neutral lead-bullet revision is owed and no change was named, because none was wanted), and **03-08 Task 3 may tick EXP-02** provided the EXP-02a any-of lead-anchor assertion is still PASS at that point. ⚠ **BUDGET FINDING — the approval does NOT fit as drafted, and the one sanctioned lever cannot fund it either.** Measured headroom at decision time: `G3.2` p1 **+17.1pt (+1.49 lines)** at `LINE_PT=11.5`, i.e. `floor(17.1 / 11.5)` = **1** rendered line of approval fund, with the reserve **INTACT** (above the contingency ladder's ≥ +16.1pt top rung by 1.0pt), so **no same-commit reword obligation was inherited** from waves 2–4. But the approved draft measures **284 characters** (282 prose + the 2-character `– ` bullet prefix) against the measured itemize-3 line-1 capacity of **135**, so `284 / 135` = **2.10 → 3 derived rendered lines**, costing `3 × 11.5` = **34.5pt** against 17.1pt available → **−17.4pt, i.e. a 3-page PDF with `G1.1` FAIL**; even a 2-line landing is `17.1 − 23.0` = **−5.9pt** and over budget. The sanctioned Groupon/NetSpeed reword-shorter lever recovers **≈0 rendered lines** and therefore cannot fund it — measured on this committed artifact both descriptors already render as **2 rendered lines**, Groupon at **167** characters (`docs/main.tex:200` resolved by CONTENT not by a stored number, rendered gate 73–74, line 1 = 129 chars) and NetSpeed at **164** (`docs/main.tex:207`, rendered gate 82–83, line 1 = 136 chars), and a rung that shortens them to **154 / 150** characters still exceeds the measured ≈135–136 line-1 capacity, so both still wrap to two lines and the net recovery is **zero**. Recovering one rendered line would mean dropping a descriptor to ≤135 characters (−32 on Groupon, −19%), which is exactly the aggressive cut the two forbidden shortened forms (`Virtual Channel and Polarity-based Arbitration`, `Cyclops service platform`) exist to forbid, and it would put the load-bearing multi-word `C++ graph algorithms` anchor that ROADMAP Phase 5 criterion 4 rests on at risk. **CONSEQUENCE FOR 03-08 Task 1 Branch B, recorded as the authority it spends against:** land the approved mentoring claim **COMPRESSED to ONE rendered line** — target **≤135 characters measured on prose-plus-prefix (≈133 prose characters)** — which lands p1 at `17.1 − 11.5` = **+5.6pt**, above the +4.7pt back-out rung, with `G1.1` still reporting 2 pages. The compression **must** keep the lowercase mid-sentence `mentored` needle byte-exact per the Section D casing table (a sentence-initial `Mentored` leaves `assert_absent 'mentored'` reading count 0 forever, so the absent→present flip would report a **false absence on a bullet that is actually on the page** — a green gate proving nothing), **must** keep D-13's no-invented-numerals rule (no headcount: "mentored N engineers" is precisely the invented figure D-13 forbids, and the claim is qualitative on purpose), **must not** breach the +4.7pt back-out rung or `G1.1`, and **must** flip P3.71/P3.72 from `assert_absent` to `assert_present` in the **SAME commit** that ships the bullet. If no faithful one-rendered-line rendering of the approved claim is achievable inside those constraints, **03-08 backs the edit out and records the approval as APPROVED-BUT-UNAPPLIABLE-WITHIN-BUDGET** rather than breaking the page — per the ladder, never tighten a frozen surface and never lower a threshold to make content fit. All costs above are estimates from the committed artifact and are **RE-MEASURED at 03-08** by reading `G1.1` first and then `G3.2` p1, per Phase 2's standing rule that the real cost is read from the build and never trusted from an estimate. — Only the person who will be interviewed can accept a provisional magnitude or an unmined people claim, so the gate is the sole authorization for the draft→`docs/main.tex` crossing; recording all four verdicts plus the positive staff-signal read is what lets 03-08 add exactly one bullet, in exactly one approved claim, and tick EXP-02 on a measured read instead of an assumed one. Recording the budget arithmetic in the same breath is what stops 03-08 from discovering at build time that an approved bullet is three rendered lines wide with room for one, and then improvising a fix under pressure.
- [Phase 03]: [Phase 3 / 03-08]: The APPROVED mentoring claim SHIPPED, **COMPRESSED to one rendered line**, and the compression is recorded as a departure from the byte-exact approved wording rather than presented as it. The phase's per-bullet sign-off record -- the EXP-08 verdict marker earlier in this same Decisions section -- approved a 284-character draft (282 prose + the 2-character bullet prefix) and, in the same breath, measured that it renders as **3** lines against a **1**-line fund, so its own CONSEQUENCE clause instructed 03-08 to land the claim at <=135 characters prose-plus-prefix or back it out. SHIPPED WORDING, verbatim, as a nested `\resumeItem` in the approved owning topic A6 `\resumeTopic{Foundation Model Training Framework}`: "Grew the engineering bench: mentored engineers onto the framework, authored its onboarding guides, and reviewed their integrations." -- **131 prose characters, 133 with the prefix**, pure ASCII. All three limbs of the approved claim family survive: engineers **mentored onto the framework**, its **onboarding guides authored**, their **integrations reviewed**. The compression is entirely by DROPPING DETAIL, never by inventing: dropped were "platform's", the enumeration "architecture guide, runbook and multi-node launch templates teams onboard with" (compressed to "its onboarding guides"), the qualifier "applied-ML and platform" on engineers, "training and inference" on frameworks, and "module ... through to production" on integrations. Every dropped fragment narrows the claim; not one word claims more than the approved text did. Both attribution rails hold: **zero numerals** (D-13 forbids an invented headcount and the claim is qualitative on purpose) and `achiev` stays x0. CANDIDATE SELECTION WAS MEASURED IN A SCRATCH BUILD, not guessed, precisely so no intermediate build of the real artifact could overflow: four candidates were compiled under `mktemp -d` and their rendered widths read. The finding worth carrying is that **itemize-3 line-1 capacity is ~133 characters for this character mix, not a flat 135** -- a 134-character candidate ("...onto the frameworks, authored the onboarding guides...") WRAPPED to two rendered lines, leaving a 13-character orphan line, while the 133-character one shipped fits exactly. Character count is a proxy for typeset width, never the width itself, so the last two characters of any "135" budget are not spendable on sight. MEASURED LANDING, read from the build in the mandated order (`G1.1` first, then `G3.2`): `G1.1` **PASS 2 pages**; `G2.1` **PASS** page 2 still opens at PUBLICATIONS; `G3.2` p1 **+17.1pt (+1.49 lines) -> +5.9pt (+0.51 lines)**, i.e. a real cost of **11.2pt** for the one line, so the landing sits **1.2pt above the +4.7pt back-out rung** rather than the +5.6pt predicted at 11.5pt/line -- that prediction is SUPERSEDED by measurement, in the same way Phase 2 recorded its own superseded figures. Adobe grew **21 -> 22** rendered gate lines (8-29), so absolute gate line numbers below Adobe shift down by one AGAIN: re-derive Swiggy/Flipkart regions from boundaries, never from stored numbers. `G5.4` held at **11**; `G6.4` PASS with U+2013 rising 38 -> **39** (the new bullet's own prefix glyph, already declared in `NONASCII_ALLOW`); `G6.5` 17 PASS. Rail 2 held -- p1 never ROSE above the +50.5pt baseline -- and `G7.2` still reports the probe PDF at 3 pages. THE SANCTIONED GROUPON/NETSPEED LEVER WAS NOT PULLED and no reword rides in the commit: it was needed only if the addition had to be funded, and compressing the claim to one line made the +17.1pt fund sufficient on its own. So both forbidden shortened forms stay forbidden, both descriptor literal sets are byte-untouched, the `C++ graph algorithms` anchor ROADMAP Phase 5 criterion 4 rests on is intact, and the P2 net still reads **27** anchored `^RESULT P2\.[0-9]+ +PASS`. BOTH SIGN-OFF OUTCOMES ARE NOW MACHINE-ASSERTED, which is the whole point of flipping per draft rather than wholesale: `P3.71`/`P3.72` flipped `assert_absent` -> `assert_present` on `mentored` in the SAME commit that ships the bullet, while `P3.67`-`P3.70` keep their `assert_absent` pairs for the REJECTED `AssumeRole` and `6-week` -- deleting those would erase the only proof that nothing unapproved shipped. The flipped pair was placed AFTER the draft loop on purpose so it retains the IDs 71/72 that every prior record cites; renumbering would have broken those citations. Both new assertions were proven FAILABLE by crafted controls rather than assumed: stripping the claim from a `--gate-text` fixture reddens `P3.72`, and a basename-preserved scratch `--tex` copy with the bullet removed reddens `P3.71` (exit 1 both times). Branch C was NOT triggered -- the criterion-2 qualitative read is POSITIVE, taken by scoping to the verdict marker line per the standing rule -- so no lead-bullet revision is owed, Branch C's revision-authorization marker (the literal string Task 3 demands before it may tick EXP-02 against a NEGATIVE read; defined in `03-08-PLAN.md` and deliberately NOT reproduced here) is correctly ABSENT from this file, and EXP-02 ticks at Task 3 on the positive read with the EXP-02a lead anchor still PASS. THREE literals were deliberately kept OUT of this bullet after a first draft included them, and that is the FIFTH sighting of this phase's signature defect class -- a machine-read gate colliding with prose that merely quotes it. Measured on the draft: reproducing Branch C's marker took its file count 0 -> 1, which would have told Task 3's gate a revision had landed when none did; writing the criterion-2 token's colon form took that token 1 -> 2, colliding with the marker-line-scoped read; and naming the EXP-08 verdict marker verbatim took ITS count 1 -> 2, which fails Task 3's own uniqueness assertion outright. STANDING RULE for Phases 4-6: never reproduce a machine-read marker inside prose the same grep will scan -- describe it and cite where it is defined. -- An approval handed forward with a measured finding that it cannot be applied as approved leaves exactly two honest outcomes: compress the claim faithfully, or back it out and say so. Recording the shipped string next to the approved one, with every dropped fragment named, is what lets a later reader confirm the compression narrowed the claim rather than reshaped it -- the alternative is a document whose wording no record accounts for.
- [Phase 03]: [Phase 3 / 03-08]: MEASURED PHASE-3 END-STATE BUDGET, every figure read from the final build rather than carried from a prediction. `G3.2` p1 closed at **+5.9pt (+0.51 lines)** and p2 at **+53.5pt (+4.65 lines)** at `LINE_PT=11.5`; `G3.1` deepest content bottom 758.5pt against the 764.4pt ceiling. Phase-3 walk of p1, so the whole spend is auditable rather than only its total: **+50.5pt (arrival, inherited from Phase 2) -> +28.1pt (03-04, the A12 governance topic's +2) -> +17.1pt (03-05, the D-05 Ray swap's +1) -> +17.1pt (03-06, line-neutral) -> +5.9pt (03-08, the approved mentoring claim's +1)**. The Adobe rendered region grew **18 -> 22 gate lines (8-29), i.e. +4 rendered lines across the phase**, which is EXACTLY the ledger's +4 ceiling and not one line more; Swiggy held at **11** (gate 34-44) and Flipkart at **9** (gate 49-57), so D-11 line-neutrality held for both all phase. Two prediction corrections, both superseded by measurement: the ledger predicted the +4 ceiling would land p1 at **+4.7pt** and the sign-off record predicted the final line would land it at **+5.6pt**; it measured **+5.9pt**, because the real per-rendered-line cost on this document is **11.2pt**, slightly under the 11.5pt `LINE_PT` the arithmetic assumed. The direction matters: the phase closed **1.2pt above** the +4.7pt back-out rung rather than exactly on it. DERIVED `G7.2` MARGIN at the end state = probe height `5 x 11.5` = **57.5pt** minus final p1 slack **5.9pt** = **+51.6pt of margin**, up from Phase 2's recorded **7.0pt**. Spending page-1 slack GREW that margin -- less slack means the +5-line probe overflows harder -- so the anti-rot guard is more firmly armed at phase close than at phase start, and `G7.2` still reports `the probe PDF is 3 page(s)`. **`PROBE_LINES` was deliberately NOT raised** and stays **5**: it is raise-only, so a raise can never be undone, and the end state leaves **5.9pt** of slack, nowhere near the 57.5pt that would make the probe stop proving anything. `DIGIT_BULLET_FLOOR` stays **7** and was likewise not raised toward the measured 11 -- the Phase-1 rule against moving a threshold toward a document applies in both directions. `docs/verify/manifest.txt` is byte-unchanged by this plan and `docs/verify/baseline-frozen.txt` was untouched all phase. Rail 1 (never a fifth rendered line) and rail 2 (p1 must never RISE above +50.5pt) both held at every intermediate commit. -- Phase 4 spends against these exact figures, and its page-2 budget is the independent +53.5pt one. Recording the four-step walk rather than only the closing number is what proves no wave net-freed a line, which is the failure mode rail 2 exists to catch and which no single end-state figure could show.
- [Phase 03]: [Phase 3 / 03-08]: WR-01 MEASURED AND DELIBERATELY NOT FIXED -- and the measurement REVERSED 03-06's reading, which is the strongest possible argument for leaving it ungated. Employer-versus-subtitle extraction order over all five Work-Experience blocks on the FINAL PDF (raw `pdftotext` line numbers): **ADOBE FIREFLY employer@9 / subtitle@10 -- employer-first** · **SWIGGY 38/39 -- employer-first** · **FLIPKART (a Walmart company) employer@57 / subtitle@56 -- SUBTITLE-FIRST** · **GROUPON 72/73 -- employer-first** · **NETSPEED SYSTEMS (Acquired by Intel) 81/82 -- employer-first**. So **four of five are employer-first and Flipkart has RE-INVERTED**. `[Phase 3 / 03-06]` measured all five employer-first and recorded the Flipkart inversion as SELF-HEALED once Adobe reached 21 rendered lines; this plan's single added rendered line took Adobe to 22 and un-healed it. Nothing in this plan touched a `\resumeSubheading`. That settles the ordering's nature beyond argument: it is a **`pdftotext` row-position artifact of a two-cell `tabular*` row, not a property of the document**, and it flips both ways on a purely cosmetic y-shift. **It was measured, not fixed**, and **no employer-first BLOCKER assertion was added**. The Phase-4 assertion candidacy stands but now carries counter-evidence that must be read before acting on it: an employer-first BLOCKER added at any point in Phase 3 would be **RED RIGHT NOW on a fully correct document**, which is exactly the gate-on-nothing outcome the candidacy was flagged to avoid. If Phase 4 still wants coverage, the assertable property is that the two header rows are ADJACENT, never which of them extracts first. A CONCRETE HAZARD THIS PRODUCED, worth carrying because it bit during this plan's own measurement: a region derivation anchored on the SUBTITLE line over-counts Flipkart by one rendered line, because the employer line now extracts after the subtitle and falls inside the derived region -- it read 10 instead of 9 until the boundary was re-derived from `max(employer, subtitle) + 1`. Derive a role region from BOTH header rows, never from the subtitle alone. The `scripts/verify-phase3-regressions.sh` Group 2 lead-bullet slice is unaffected and should stay that way: it anchors on the ADOBE subtitle, and Adobe is employer-first, but its immunity is positional luck rather than design. `G6.13` remains a **WARN owned by Phase 3** and fires on all five blocks (Flipkart at line span 5 against the window of 3, the other four at 4) -- it was claimed, not cleared, and `ROLE_BIND_WINDOW` stays a **local constant** rather than a manifest key, because no requirement owns the role-header fix and the commit that promotes `G6.13` to a BLOCKER is the commit that should add the key. -- OQ-4 asked for document-and-measure rather than fix, and the measurement justified that choice twice over: the artifact moved twice inside one phase, in both directions, driven only by how much text sat above it.
- [Phase 03]: [Phase 3 / 03-08]: UPSTREAM ARTIFACT CORRECTIONS this phase discovered, recorded so measurement supersedes the document rather than the reverse. (1) **The Swiggy rendered-line count in `03-RESEARCH.md` is WRONG in two places and the measurement supersedes both**: `:487` states "gate 36-46, 8 rendered lines" and `:666` restates "Swiggy, **8** today" as the EXP-07c line-neutrality oracle, but 36 through 46 inclusive is **11** lines, the per-bullet breakdown sums to 11 (2+2+2+2+2+1), and the region measures **11** at both 03-03 and this end state. A differential neutrality check that had trusted the 8 would have reported a **false regression of +3 lines on an untouched block**. Flipkart's 9 in the same rows is correct. Related and equally load-bearing: those gate spans are true only at the moment they were written -- Adobe grew four times this phase, shifting every line below it, so a neutrality check must re-derive regions from boundaries (and from BOTH header rows, per the WR-01 record above), never from stored line numbers. (2) **`G6.6`'s target-keyword WARN fires UNCONDITIONALLY, with no branch on the measured count** -- read from the implementation, not assumed: the loop computes `N` and then calls `warn G6.6 ... "target keyword '$KW' x$N"` on every iteration, so a keyword that has been ADDED to the document but NOT promoted into `KEYWORDS_REQUIRED` prints the same advisory it printed at x0 and never escalates. Consequence, stated plainly: **`G6.5`/`G6.6` are the promotion CONVENTION, not a promotion GATE**, and the only machine backing for same-commit promotion anywhere in this repo is the new P3 net's promotion triple -- `P3.42`-`P3.45`, each a three-conjunct assertion (present in the text layer AND a whole pipe-field of `KEYWORDS_REQUIRED` AND no `KEYWORDS_TARGET` field left carrying its `name:` prefix), all four PASS at the end state for `torch.compile`, `A100`, `H100` and `Triton`. Phase 5 adds keywords and inherits this: a promotion it lands outside that triple's coverage has no gate behind it. -- Phase 2's rule is that a predicted figure written as measured is how a wrong number survives into the next phase. Both corrections are of exactly that shape: an arithmetic slip promoted into an oracle, and a convention mistaken for an assertion.
- [Phase 03]: [Phase 3 / 03-08]: PHASE-5-OWNED KEYWORD LANDED EARLY, and the ownership label is recorded as routing rather than permission. `Triton` was promoted from `KEYWORDS_TARGET` into `KEYWORDS_REQUIRED` by 03-05 and reads **x1** at `G6.5` PASS at the end state, with `P3.45` asserting the full promotion triple. This was unconditional rather than branch-dependent: **both** D-08 branches name Triton-generated kernels, so 03-05 promoted it either way. The point worth carrying is that a `KEYWORDS_TARGET` entry's `:phase` suffix is a **WARN-ROUTING LABEL** -- it decides which phase a `G6.6` advisory is addressed to -- and is **not a permission gate**: nothing prevents an earlier phase from landing and promoting a later phase's target, and nothing should, because the promotion rule is same-commit-as-the-content and the content landed here. Phase 5's PG2-04 obligation is therefore **ADDITIVE, not duplicated**: its Skills-tier form of Triton is a second surface for the same keyword, and Phase 5 must not read this early promotion as PG2-04 already discharged. **`CUDA graphs` was deliberately LEFT UNPROMOTED on both D-08 branches** and still reads `G6.6` **x0** with owner Phase 3 at the end state, because D-07's shipped wording is the hyphenated singular `CUDA-graph capture`, which the fixed-string plural target never matches. Promoting it would require either changing shipped honest wording to satisfy a needle or relaxing the needle -- both forbidden. It stays a Phase-5 PG2-04 target, and the WARN naming Phase 3 as owner is the record that the decision was taken here rather than overlooked. Finally, on the Class E gap: D-08 resolved to the branch on which the bridge clause legitimately names CUDA-graph capture, so the `EXP-03c/03-05` `CUDA` ABSENCE assertion was **deliberately NOT added** to `scripts/verify-phase3-regressions.sh` and does **not** stand in the net -- it survives only as the commented RECORDED GAP block at the end of Group 5, `grep -Fc 'EXP-03c/03-05'` returns 1 from that comment alone, there is no live `assert_absent 'CUDA'` anywhere in the file, and `LC_ALL=C grep -Fci CUDA docs/main.tex` reads **1**, which is the CORRECT value on this branch rather than a leak. Adding it would have asserted the opposite of the truth and been permanently red on a correct document. -- A phase boundary in a keyword table is a routing hint, not a lock; saying so explicitly stops Phase 5 from either re-promoting a keyword that is already required or treating its own Skills obligation as already met.
- [Phase 03]: Single-file LaTeX source is a standing constraint: the entire resume lives in docs/main.tex with no local \input/\include splits, so the source compiles anywhere with a standard TeX distribution (distro-shipped inputs like glyphtounicode are fine). Verified today: main.tex is the only .tex in the repo. Applies to all future phases (4-6): never split sections into separate files.
- [Phase 4]: 04-01: D-05 owner override — Teaching left byte-unchanged (3 TA items), intentional deviation from ROADMAP Phase-4 criterion 4
- [Phase 4]: 04-01: spike Variant A graph_ml block needed one escaped underscore (\_) in the \href display text to compile; text layer unchanged (Rule 1 auto-fix)
- [Phase 4]: 04-01: EDU_INSTITUTION manifest needle synced to 'Indiana University' in the same commit as the D-06 source edit (D-07); G6.12 logic untouched
- [Phase 4]: GH-01 (privatize random_walk) DEFERRED by owner (D-08) — unlinked/empty, no résumé click-through; ready command preserved, not ticked
- [Phase 4]: GH-02 (fix rollout README stale status) DEFERRED by owner (D-08) — accepted trade-off: featured rollout link reads stale status; ready fix preserved, not ticked
- [Phase 4]: GH-03 SATISFIED + ticked — rollout wording owner-confirmed (D-01), shipped in 04-01; no checkpoint (confirmation pre-existed)
- [Phase 4]: Page-2 content net verify-phase4-regressions.sh (38 P4.x, green on arrival, each class --gate-text failable) wired into make; D-05 Teaching-3-TA override machine-recorded via assert_count
- [Phase ?]: [Phase 5 / 05-01]: Skills rebuilt into 3 keyword tiers (Languages & Inference / Training & Distributed / Concepts), 3 rendered lines (G6.14 ok, first build, no rebalance). 4 new exact forms landed (ONNX Runtime, CUDA graphs, Iceberg, H100); manifest promotions ONNX/CUDA graphs/Iceberg/H100 (KEYWORDS_TARGET now empty) with P3.44 flipped to assert_promoted 'H100' and P3.65 to assert_present 'H100' $GATE in place (ids preserved), same commit. New scripts/verify-phase5-regressions.sh (25 assertions, option A owns ONNX/CUDA graphs/Iceberg assert_promoted; each class --gate-text/--manifest failable) wired as REGRESS5. Page 1 byte-frozen; C++ anchors intact; prohibited terms zero. requirements-completed EMPTY — PG2-04/05/06 tick at 05-02 (last-plan precedent).

- [Phase 5 / 05-02]: **PER-ITEM STRETCH-SKILL SIGN-OFF TAKEN** at the 05-02 Task 1 blocking checkpoint (criterion 5; D-07), against the green document Plan 05-01 shipped — proven green FIRST as the green-first model requires (harness exit 0 · P2/P3/P4/P5 nets exit 0 with the P3 net at 72 anchored PASS · self-test exit 0). One verdict per stretch item, recorded as DECISIONS and never as omissions, mirroring the shape of the Phase-3 EXP-08 per-bullet sign-off record earlier in this section (its bracketed marker is deliberately NOT reproduced here — that record is uniqueness-guarded and must stay a single occurrence). **ALL TEN CONFIRMED — zero rejections, zero revisions**, so this took the all-confirmed branch: NO document change, NO backout, `docs/main.tex` / `docs/verify/manifest.txt` / the P3+P5 nets all byte-untouched. Verdicts with the confirmed interview-defense tier each carries:
  - **H100 — CONFIRMED at tier OPERATED**, evidence the 64-H100 parallel enrichment wave.
  - **CUDA graphs — CONFIRMED at tier OPERATED**, evidence torch.compile max-autotune driving CUDA-graph capture (settled on the D-08 cudagraphs-confirmed branch against a checksummed wheel).
  - **ONNX Runtime — CONFIRMED at tier OPERATED**, evidence the ONNX Runtime CUDAExecutionProvider fingerprint embedding generator.
  - **PyTorch Lightning — CONFIRMED at tier OPERATED**, evidence the Ray Data + PyTorch Lightning benchmark.
  - **Iceberg — CONFIRMED at tier EVALUATED/CONVERSANT** — the one operated-LOOKING tools-tier placement resting on an evaluated-only defense (D-06 flag). The evaluated-tier framing under the tools-tier placement was explicitly raised at the checkpoint and accepted: the owner is comfortable discussing table format / schema evolution / time-travel conceptually and has NOT run it in production, so the claim is honest as conversant vocabulary, not as operated.
  - **tensor parallelism — CONFIRMED at tier EVALUATED/CONVERSANT** (vocabulary, no commit evidence).
  - **pipeline parallelism — CONFIRMED at tier EVALUATED/CONVERSANT** (vocabulary, no commit evidence).
  - **Mixture of Experts (MoE) parallelism — CONFIRMED at tier EVALUATED/CONVERSANT** (vocabulary, no commit evidence).
  - **KV-cache management — CONFIRMED at tier EVALUATED/CONVERSANT** (vocabulary, no commit evidence).
  - **continuous batching — CONFIRMED at tier EVALUATED/CONVERSANT** (vocabulary, no commit evidence).

  The gate was presented for real and could not auto-advance; a single blanket "looks good" was NOT accepted — each item drew an individual verdict against the rendered PDF. Because every item confirmed, the reject/revise backout branch (remove the keyword from Skills, reverse its manifest promotion, re-flip P3.44/P3.65 for H100) was NOT exercised and nothing was un-promoted. With the verdicts on record and the suite re-confirmed green (unchanged), PG2-04/PG2-05/PG2-06 were ticked complete (checkboxes + status table) and `requirements mark-complete` run — the phase-closing plan owning the tick per the last-plan precedent 03-01 through 03-08 and Phase 2 Plan 01 all followed. — Criterion 5 makes each stretch claim interview-defensible on the owner's own word, the only authority for a stretch claim crossing into the shipped résumé; recording one verdict per item with its tier is the audit trail that the claim was individually accepted, not blanket-approved.

### Pending Todos

None yet.

### Blockers/Concerns

- [Requirements]: `REQUIREMENTS.md` stated "21 total" v1 requirements but enumerates **23** IDs (3 VERIFY + 8 EXP + 6 PG2 + 3 GH + 1 VIS + 2 SITE). Corrected to 23 during roadmap creation; all 23 are mapped.
- [Phase 3]: EXP-08 carries a hard user sign-off checkpoint on provisional cost/mentoring figures — cannot auto-advance past it
- [Phase 5]: ~~`torch.compile` production config still needs one read to confirm CUDA graphs are not disabled (`max-autotune-no-cudagraphs` / `triton.cudagraphs=False` would invalidate the EXP-03 compiler bridge)~~ — **RESOLVED / SUPERSEDED** by the D-08 decision record in `### Decisions` (search that section for `D-08`): the read was performed against a sha256-verified genie wheel, neither disabling form is present, and the full compiler bridge shipped in 03-05.
- [Phase 5]: ⚠ **H100 needle flip owed.** The page-1 finalization removed `H100`; the gate reconciliation (`b6f8011`) demoted it to `KEYWORDS_TARGET` (`H100:5`) and set `P3.44`/`P3.65` to `assert_absent 'H100'`. When PG2-04 re-adds `H100` in Skills, the SAME commit MUST flip both needles back (`assert_promoted` / `assert_present`) and re-promote `H100` → `KEYWORDS_REQUIRED`. `$GATE`/`$SQUEEZED` are whole-document, so skipping the flip reddens the P3 net mid-Phase-5. Full record in `### Decisions` under `[Post-Phase-3 / Page-1 Finalization + Gate Reconciliation]`.
- [Phase 6]: **SITE-01 scope EXPANDED (2026-08-23, owner directive "keep website in sync as well").** `index.html` (756 lines) mirrors the resume broadly and is stale on more than the two known spots: `index.html:196` (retired page-1 figures `3–8K images/sec` / `10–50×`), `index.html:215`+ (career-break entry, SITE-02), AND the Projects / Education / Publication / Experience sections that Phases 3–4 rewrote (Projects must become `rollout` + `graph_ml`; grad-school projects, GPAs, cities gone). Full résumé→site sync is now SITE-01 (see REQUIREMENTS.md). Deliberately kept in **Phase 6** (not pulled into Phase 4) so it syncs once against the fully-settled resume — Phases 5 (Skills) and 6 (palette) still change it; the roadmap ordering rule is "syncing earlier means syncing twice." Site is published by `.github/workflows/jekyll.yml` on push to `master` (no push happens until the owner pushes). Full record in `### Decisions` under `[Phase 3 / 03-02]`.

## Deferred Items

Items acknowledged and carried forward:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Credibility | CRED-01 upstream vLLM/SGLang PR | v2 backlog | Requirements |
| Credibility | CRED-02 live ATS scan (Jobscan/Lever/Greenhouse) | v2 backlog | Requirements |
| Credibility | CRED-03 LinkedIn profile rewrite | v2 backlog | Requirements |
| Portfolio | PORT-01 full site content refresh | v2 backlog | Requirements |

## Session Continuity

Last session: 2026-08-24T16:43:43.277Z
Stopped at: Phase 6 context gathered
Resume file: .planning/phases/06-palette-site-sync/06-CONTEXT.md

## Performance Metrics

| Phase | Plan | Duration | Notes |
|-------|------|----------|-------|
| 01 | 01 | 11 min | 3 tasks, 4 files |
| 01 | 02 | 22 min | 4 tasks, 1 file |
| 01 | 03 | 33 min | 3 tasks, 2 files |
| Phase 02 P01 | 6 min | 3 tasks | 5 files |
| Phase 02 P02 | 11 min | 3 tasks | 6 files |
| Phase 03 P01 | 18 min | 3 tasks | 1 files |
| Phase 03 P02 | 8min | 2 tasks | 3 files |
| Phase 03 P03 | 12 min | 3 tasks | 1 files |
| Phase 03 P04 | 34 min | 3 tasks | 5 files |
| Phase 03 P05 | 9 min | 3 tasks | 6 files |
| Phase 03 P06 | 13 min | 2 tasks | 4 files |
| Phase 03 P07 | 48 min | 2 tasks | 2 files |
| Phase 03 P08 | 17 min | 3 tasks | 9 files |
| Phase 04 P01 | 6min | 3 tasks | 8 files |
| Phase 04 P02 | 5min | 2 tasks | 5 files |
**Per-Plan Metrics:**

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| Phase 05 P01 | 14 min | 2 tasks | 7 files |
| Phase 05 P02 | 6 min | 2 tasks | 3 files |
