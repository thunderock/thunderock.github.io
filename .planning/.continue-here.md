---
context: default
phase: 3-complete -> 4-next
task: gate-reconciliation, then Phase 4
total_tasks: 4
status: paused
last_updated: 2026-08-23T10:20:32.984Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: PAGE-1 EXPERIENCE IS FINAL — the Work Experience block in `docs/main.tex` (commit `798c0fb`) was finalized by the owner after an external review; the verification gates are currently RED against it BY DESIGN. The failure mode: a resuming agent "fixes" main.tex to turn the gates green, destroying the owner's finalized wording. Structural mitigation: the reconciliation task edits ONLY `docs/verify/manifest.txt` and `scripts/verify-phase3-regressions.sh`; any diff touching `docs/main.tex` lines 140–210 is wrong and must be reverted.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Gates-fix-the-document | Harness/P3-net FAILs name removed figures with "Restore the literal verbatim" remedies; following them verbatim would revert the owner's finalized page-1 wording | blocking | Reconciliation commit's `git diff --name-only` must contain only `docs/verify/manifest.txt` + `scripts/verify-phase3-regressions.sh` (+ regenerated artifacts if a build runs); main.tex byte-identical to `798c0fb` |
| Unanchored PASS/FAIL greps | Counting bare `PASS`/`FAIL` matches footer/summary lines (P2 green run = 28 not 27; P3 footers add 2) — bit five times during phase 3 | advisory | Always `grep -cE '^RESULT P2\..*PASS'` (=27) and `^RESULT P3\.[0-9]+ +FAIL` |
| Tool-name leakage | Internal workflow/planning tool names in commits or shipped files | advisory | Plain engineering prose; plan-number scopes like `feat(04-01):` are fine; audit with `git log --format='%s %b' | grep -icE '<toolnames>'` before push |
| make verify as exit-code oracle | GNU Make 3.81 collapses exit 1/2 | advisory | Gate on `bash scripts/verify-resume.sh` directly; `make verify` is corroboration only |
| make clean before commit | Deletes the `.fdb_latexmk` md5 record `G0.3` needs | advisory | Never `make clean`; artifacts ride IN the source commit |

<current_state>
Milestone v1.0 (Staff MLE Resume Optimization), 3/6 phases complete and fully closed (VERIFICATION passed 37/37 + REVIEW 0-blockers + SECURITY 63/63 + VALIDATION compliant). After phase 3 closed, the owner ran an external review cycle and FINALIZED page 1 in commit `798c0fb`: precise internal fleet/dataset figures generalized ("hyperscaled daily EMR ingestion", "1000s×GPUs per model job", figures 202.6M/24.8M/27,135/9.7k/40-node/24×A100/9-queues/64-H100s removed, "~700M" → "a Billion asset catalog"), Groupon subtitle gained "RECOMMENDATION SYSTEMS", PDF regenerated and committed with source. Honesty flags on the two overstating phrases were raised and dismissed by the owner — page 1 is closed. Consequence: harness has exactly 1 BLOCKER (G6.5 `H100` — a required keyword no longer in the text layer) and the phase-3 content net has 12 FAILs (needles for the removed figures + the H100 promotion pair). P2 net, self-test, structure gates all green. Page 2 has never been touched by any phase.
</current_state>

<completed_work>

Completed:
- Phases 1–3: verification harness (G0–G8) · page-1 budget + text-layer defects · Adobe rebuild with staff-signal bullets, evidence-traced figures, torch.compile bridge (cudagraphs-confirmed via sha256-verified production wheel), EXP-08 sign-off (3 drafts rejected, compressed mentoring line approved+shipped)
- Standing regression nets: `scripts/verify-phase2-regressions.sh` (27 assertions) + `scripts/verify-phase3-regressions.sh` (72) wired into `make verify-regressions` and chained in `make verify`
- Owner's page-1 finalization committed: `798c0fb` (source + regenerated artifacts together — G0.3 holds on a fresh clone)
</completed_work>

<remaining_work>

1. **Gate reconciliation to `798c0fb` (do this FIRST, ~30 min):**
   - `docs/verify/manifest.txt`: demote `H100` from `KEYWORDS_REQUIRED` back to `KEYWORDS_TARGET` with an owner-decision comment (owner removed the only H100 occurrence; do not re-add to text). Reviewed commit — this is a sanctioned demotion recording an owner supersession, not gate-weakening.
   - `scripts/verify-phase3-regressions.sh`: update/delete the 12 red needles — figure-retention needles for `27,135`, `9.7k-line`, `idempotent re-run`, `24×A100-40GB`, `9 model queues`, `64 H100s`, `202.6M-row`, `24.8M`, `~700M`, `40-node` (superseded by owner finalization — delete each with a dated supersession comment, or re-point at surviving finalized literals like `1.09M rows`, `8$\times$A100`, `35--38M images/month`, `hyperscaled`), plus the H100 promotion pair P3.44/P3.65 (follows the manifest demotion).
   - Re-run everything green: `bash scripts/verify-resume.sh` (exit 0) · `bash scripts/verify-phase2-regressions.sh` (27 anchored PASS) · `bash scripts/verify-phase3-regressions.sh` (exit 0) · `make verify-selftest` (10 PASS). PDF may need `make build` after any tex-adjacent change — artifacts ride in the same commit.
2. **Phase 4 — Page-2 Restructure & GitHub Presence** (`/gsd-discuss-phase 4` → plan → execute). Owner-discussed changes to include:
   - **Projects (PG2-01):** exactly `rollout` + `graph_ml` using the spike Variant A LaTeX drafts (`.opencode/skills/spike-findings-thunderock-github-io/references/projects-section.md`); all seven 2017–2022 grad-school entries deleted
   - **Education (PG2-03):** drop both GPAs (3.87/4.0, 8.32/10.0), compress; institution must stay the FIRST non-empty extracted line in the region (G6.12 anchor-free clause — its EDU_OWNER already names Phase 4 / PG2-03); keep the `Aug. 2021 – May 2023` range
   - Publication trimmed to citation + one contribution line (PG2-02); Research + Teaching one line each
   - **GitHub presence:** GH-01 privatize `random_walk` · GH-02 fix rollout README stale "pre-implementation" status line · GH-03 checkpoint: owner confirms rollout wording
   - Page-2 budget: +53.5pt independent slack; adopt the two phase-3 advisories (Makefile net-chaining diagnostics WR-01; G6.13 owner label WR-02)
3. **Phase 5 — Skills Rebuild** (owner-discussed): ≤3 rendered lines; exact-match forms (vLLM, torch.compile, Kubernetes, FSDP, Ray, ONNX Runtime, A100, H100, CUDA graphs, Triton, Rust, C++, PyTorch Lightning, Flash Attention, KEDA, Spark, Iceberg); explicitly-labeled concept-vocabulary group (tensor/pipeline/MoE parallelism, KV-cache management, continuous batching); `speculative decoding` and `Go` ×0; C++ anchored via NetSpeed NoC + graph_ml kernels; per-item approval checkpoint
4. **Phase 6 — Palette & Site Sync**: incl. the recorded handoffs — `index.html:196` still carries both retired figures ("10–50×", "3–8K images/sec"), `index.html:207-225` career-break entry removal (SITE-02)
</remaining_work>

<decisions_made>

- Page-1 Experience block is FINAL (`798c0fb`) — owner decision after external review; generalized figures preferred over precise internal numbers; honesty flags on "1000s×GPUs per model job" and "a Billion asset catalog" raised and dismissed by owner
- Gates reconcile TO the document, never the reverse — assertion logic stays strong; superseded needle literals follow the owner's content
- Single-file LaTeX source: everything in `docs/main.tex`, no local `\input`/`\include` (distro `\input{glyphtounicode}` at :15 stays) — must compile anywhere
- Artifacts (.pdf/.log/.fdb_latexmk/.aux/.fls/.out) ride IN the source commit; never `make clean`
- No internal workflow/planning tool names in commits or shipped files
- D-08 recorded `cudagraphs-confirmed`; EXP-08 verdicts behind `[Phase 3 / EXP-08]` in STATE.md
</decisions_made>

<blockers>
- Harness G6.5 `H100` BLOCKER + P3 net 12 FAILs: expected state, resolved by remaining_work item 1 (gate reconciliation) — NOT by editing main.tex
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable twin of this file
2. `.planning/STATE.md` — Phase-3 decisions incl. D-08/EXP-08 markers, single-file constraint, WR-01 role-order artifact
3. `.planning/ROADMAP.md` — Phase 4/5/6 goals + success criteria (page-2 work is Phase 4+5)
4. `.planning/v1.0-MILESTONE-AUDIT.md` — milestone posture + advisory carry-forwards
5. `.opencode/skills/spike-findings-thunderock-github-io/references/projects-section.md` — ready LaTeX drafts for the Projects swap
6. `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-VERIFICATION.md` — what "green" looked like before the finalization edit

## Critical Anti-Patterns (do NOT repeat these)
- GATES-FIX-THE-DOCUMENT: FAIL remedies say "restore the literal" → structural mitigation: reconciliation diff touches only manifest + P3 net, main.tex byte-identical to `798c0fb`
- UNANCHORED GREPS: bare PASS/FAIL counts hit footer lines → always the anchored `^RESULT …` forms

## Infrastructure State
- No running services. Build = local latexmk; gates = bash + poppler. Repo: `git@github.com:thunderock/thunderock.github.io.git` (personal-account key required — use the `git@personal:` host alias on multi-account boxes), branch `master`, HEAD `798c0fb`.
- Remote-pod convention: push to the working branch after every atomic commit (ephemeral pod durability).
- Fresh-clone sanity: `bash scripts/verify-resume.sh` should show ONLY the known G6.5 H100 FAIL until reconciliation lands; `G0.3 PASS` proves the committed PDF matches the committed source.

<context>
The owner iterated on page 1 with an outside reviewer and locked it — the remaining milestone value is entirely on page 2 (Projects/Education/Publication/Research/Teaching via Phase 4, Skills via Phase 5) plus the site sync (Phase 6). The immediate chore is mechanical: make the gates describe the finalized document again, then the normal phase chain continues. All planning artifacts, evidence (CODEBASE-EVIDENCE.md), spike drafts, and the two regression nets travel with the repo — nothing lives outside it except ~/code/genie evidence (already discharged and recorded).
</context>

<next_action>
Start with: clone the repo, `git log -1` to confirm HEAD `798c0fb`, run `bash scripts/verify-resume.sh` to reproduce the expected single G6.5 FAIL, then execute remaining_work item 1 (gate reconciliation — manifest H100 demotion + 12 P3 needle supersessions, all suites green, one reviewed commit). Then `/gsd-discuss-phase 4`.
</next_action>
