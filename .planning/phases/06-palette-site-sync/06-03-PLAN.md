---
phase: 06-palette-site-sync
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - scripts/verify-site-regressions.sh
  - Makefile
autonomous: false
requirements: [SITE-01, SITE-02]
estimate:
  tokens: 50000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "scripts/verify-site-regressions.sh asserts the durable site-sync invariants over the RAW index.html source: the two retired figures absent, career-break absent, rollout + graph_ml present, GPAs (3.87 / 8.32) absent, Bloomington absent, the pruned sections (Competitive D.S. Experience / Certifications) absent, torch.compile present, and the grad-project titles absent (D-11)."
    - "The net is green on the synced index.html (exit 0) and every assertion class is proven failable by a crafted negative control over a scratch HTML copy (an --html waiver flag, mirroring the résumé nets' --gate-text)."
    - "The net is wired into the Makefile as REGRESS6 — chained into `make verify` alongside REGRESS2-REGRESS5 and runnable standalone via `make verify-regressions` (D-11)."
    - "A final full gate passes: bash scripts/verify-resume.sh exit 0, bash scripts/verify-site-regressions.sh exit 0, the P2/P3/P4/P5 nets exit 0, and `make verify` runs green as corroboration (criterion 5)."
    - "The plain-text paste test passes at a blocking human checkpoint: the PDF pasted as plain text reads in correct order with the ashutosh--tiwari handle and the portfolio/GitHub/LinkedIn URLs intact (criterion 5)."
  artifacts:
    - "scripts/verify-site-regressions.sh — the S6.x site-sync regression net (green on arrival, each class failable, --html waiver for negative controls)"
    - "Makefile — REGRESS6 variable + the net chained into `make verify` and `make verify-regressions`"
  key_links:
    - "index.html (synced by 06-02) -> the S6.x assertions -> `make verify` REGRESS6 chain -> a silent public regression is caught before push"
    - "the net greps RAW HTML byte-exact under LC_ALL=C -> no pdftotext reflow, so the résumé nets' $SQUEEZED multi-word rule does NOT apply here (D-11)"
  prohibitions:
    - "The Makefile wiring must NOT prefix the net invocation with `-` or `|| true` and must NOT pass any --html waiver flag from a build target — a silenced or waived net reads as coverage while asserting nothing (the exact defect the milestone audit named for the Phase-2 net). (flagged-unverified; no wired check descriptor)"
    - "A site-sync assertion must NOT be relaxed to reach green — if index.html legitimately still carries a banned string, the fix is in index.html (Plan 06-02), never a weakened needle. (flagged-unverified; no wired check descriptor)"
  flagged_assumptions:
    - "EDGE SITE-01/encoding (unresolved): the net greps the RAW index.html source (bytes / UTF-8), NOT a pdftotext layer, so the retired-figure needles are matched BYTE-EXACT under LC_ALL=C — `3–8K images/sec` carries an en dash (U+2013) and `10–50×` a multiplication sign (U+00D7); HTML source is not reflowed, so the $SQUEEZED multi-word handling of the résumé nets does not carry over. Resolved into the net's fixed-string LC_ALL=C matching."
---

<objective>
Give the site sync a standing, machine-enforced gate and close the phase. Build scripts/verify-site-regressions.sh (D-11) — a lightweight grep net over the RAW index.html source asserting the durable sync invariants, green on the 06-02-synced file, each class proven failable — wire it into the Makefile as REGRESS6 (alongside REGRESS2-REGRESS5, standalone via `make verify-regressions`), then run the final full gate and the plain-text paste test (criterion 5).

Purpose: SITE-01 + SITE-02 — a silent public regression (a retired figure or career-break entry creeping back) is caught before the owner pushes; and the milestone artifact set is verified shippable.
Output: the site-sync net, its Makefile wiring, the final green gate, and the recorded paste-test sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-palette-site-sync/06-CONTEXT.md

# The model for the new net (standalone target + make chain + each class failable)
@scripts/verify-phase5-regressions.sh   # emit S6.n / gcount / exit 0-1-2 pattern; NOTE: site net greps raw HTML, no $SQUEEZED
@scripts/verify-phase4-regressions.sh
@Makefile                                # REGRESS/REGRESS3/4/5 wiring in `make verify` + `make verify-regressions`

# The artifact under assertion
@index.html                              # synced by Plan 06-02
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Site-sync net — ONE class end-to-end (retired figures absent), proven failable (D-11)</name>
  <files>scripts/verify-site-regressions.sh</files>
  <read_first>
    - scripts/verify-phase5-regressions.sh — the structure to copy: bash-3.2 emit()/gcount()/die2(), exit vocabulary (0 all pass / 1 a regression / 2 cannot verify), LC_ALL=C fixed-string counts, a waiver flag for negative controls over a scratch fixture, negative-control documentation in the header
    - index.html — the synced file (retired figures should already be gone after 06-02); the byte-exact needles `3–8K images/sec` (en dash U+2013) and `10–50×` (× U+00D7)
    - .planning/phases/06-palette-site-sync/06-CONTEXT.md D-11 (the invariant list; the raw-HTML/no-$SQUEEZED note)
  </read_first>
  <action>
    Create scripts/verify-site-regressions.sh modeled on verify-phase5-regressions.sh but targeting the RAW index.html source (no pdftotext, no $SQUEEZED — HTML is not reflowed). It reads a default target of index.html and accepts `--html FILE` to point at a scratch fixture for negative controls ONLY (a run carrying --html prints a "does NOT certify the committed index.html" disclaimer and no build target may pass it). Emit one `RESULT S6.<n> <PASS|FAIL> <message>` line per assertion; exit 0 (all pass) / 1 (a regression) / 2 (cannot verify: missing file). Implement the FIRST assertion class end-to-end: assert_absent of the two retired figures — `3–8K images/sec` and `10–50×` — matched byte-exact under LC_ALL=C grep -F. Document + demonstrate the negative control in the header the way the P5 net does: copy index.html to a scratch file, re-insert `3–8K images/sec on 32 GPUs (10–50× faster image loading)`, run with --html on the fixture, and confirm the retired-figure S6 assertions FAIL (exit 1). Prove the net is green (exit 0) on the real synced index.html. bash-3.2-only (no associative arrays, no mapfile, no `local`, no `[[ ]]`); every grep under LC_ALL=C; read-only (never writes into the working tree).
  </action>
  <verify>
    <automated>bash scripts/verify-site-regressions.sh && cp index.html "${TMPDIR:-/tmp}/site-fx.html" && printf '<li>achieving 3–8K images/sec on 32 GPUs (10–50× faster image loading)</li>\n' >> "${TMPDIR:-/tmp}/site-fx.html" && ! bash scripts/verify-site-regressions.sh --html "${TMPDIR:-/tmp}/site-fx.html"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-site-regressions.sh runs under /bin/bash 3.2 and exits 0 on the synced index.html, emitting `RESULT S6.<n> PASS` lines
    - the retired-figure class matches byte-exact: `3–8K images/sec` and `10–50×` under LC_ALL=C grep -F (encoding edge resolved)
    - the negative control FAILs: a scratch copy with the figure re-inserted, run via `--html`, exits 1 and names the S6 retired-figure assertion
    - `--html` prints a "does NOT certify the committed index.html" disclaimer; the script never writes into the working tree
    - exit vocabulary is 0 (all pass) / 1 (a regression) / 2 (cannot verify) — a missing index.html is exit 2, never a false pass
  </acceptance_criteria>
  <reversibility rating="reversible">A new standalone script; no effect on tracked content.</reversibility>
  <done>The site net exists, is green on the synced site, and its retired-figure class is proven failable end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the assertion classes + wire REGRESS6 into the Makefile (D-11)</name>
  <files>scripts/verify-site-regressions.sh, Makefile</files>
  <read_first>
    - scripts/verify-site-regressions.sh (Task 1 output — extend with the remaining classes)
    - index.html — the synced needles (rollout/graph_ml links present; Career Break / Competitive D.S. Experience / Certifications / 3.87 / 8.32 / Bloomington / grad-project titles absent; torch.compile present)
    - Makefile — the `verify:` recipe (REGRESS..REGRESS5 chained above VERIFY) and the `verify-regressions:` recipe; the comment block forbidding `-`/`|| true` prefixes and waiver flags in recipes
  </read_first>
  <action>
    Extend scripts/verify-site-regressions.sh with the remaining D-11 assertion classes, each with a documented + demonstrated negative control (a scratch --html fixture, proven failable): (a) career-break absent — `Career Break` count 0; (b) rollout + graph_ml present — `github.com/thunderock/rollout` and `github.com/thunderock/graph_ml` each >= 1; (c) GPAs absent — `3.87` and `8.32` count 0; (d) Bloomington absent — count 0; (e) pruned sections absent — `Competitive D.S. Experience` and `Certifications` count 0; (f) torch.compile present — >= 1 (proves the Adobe rewrite landed); (g) grad-project titles absent — e.g. `BiasNet` and `DeepFoodie` count 0. Every needle byte-exact under LC_ALL=C grep -F. Then wire it into the Makefile exactly like the other nets: add `REGRESS6 := scripts/verify-site-regressions.sh`, append `@bash $(REGRESS6)` to the `verify:` recipe (after `@bash $(REGRESS5)`) and to the `verify-regressions:` recipe, and extend the help/comment text to name the site net (P2.x + P3.x + P4.x + P5.x + S6.x). Never prefix the invocation with `-` or `|| true`, and never pass `--html` from a make target (both forbidden per the Makefile comment block and this plan's prohibitions).
  </action>
  <verify>
    <automated>bash scripts/verify-site-regressions.sh && LC_ALL=C grep -q 'REGRESS6' Makefile && make verify-regressions >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-site-regressions.sh` exits 0 with all S6 classes PASS on the synced index.html
    - each of the 8 classes has a documented negative control in the header and is proven failable (fixture -> exit 1)
    - Makefile carries `REGRESS6 := scripts/verify-site-regressions.sh`; `@bash $(REGRESS6)` is in BOTH `verify:` and `verify-regressions:`; no `-`/`|| true` prefix; no `--html` in any recipe
    - `make verify-regressions` runs REGRESS2..REGRESS6 and the site net participates
  </acceptance_criteria>
  <reversibility rating="reversible">Additive net + Makefile wiring; reversible via git.</reversibility>
  <done>All D-11 site invariants are asserted, each class failable, and the net is chained into `make verify` + `make verify-regressions` as REGRESS6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final full gate + plain-text paste test (criterion 5)</name>
  <what-built>
    The milestone artifact set is complete: the palette is applied (Plan 06-01), the site is synced (Plan 06-02), and the site-sync net is wired into make (Tasks 1-2). This checkpoint runs the final full gate and the one human verification the harness cannot automate — the plain-text paste test.
  </what-built>
  <how-to-verify>
    1. Run the full gate directly (the oracle; `make verify` collapses exit 1/2 so it is corroboration only):
       `bash scripts/verify-resume.sh` -> exit 0;
       `bash scripts/verify-phase2-regressions.sh && bash scripts/verify-phase3-regressions.sh && bash scripts/verify-phase4-regressions.sh && bash scripts/verify-phase5-regressions.sh && bash scripts/verify-site-regressions.sh` -> exit 0;
       `make verify` -> runs green (corroboration).
    2. Plain-text paste test (criterion 5): `pdftotext docs/AshutoshTiwari.pdf -` (or paste the PDF text into an actual web form field). Confirm by eye: the content reads top-to-bottom in the correct order (name/contact -> Work Experience Adobe->Swiggy->Flipkart->Groupon->Netspeed -> Publications -> Education -> Research -> Teaching -> Selected Projects rollout+graph_ml -> Technical Skills); the LinkedIn handle extracts as the literal `ashutosh--tiwari` (two hyphens, no en dash); and the portfolio (`thunderock.github.io`), GitHub (`github.com/thunderock`) and LinkedIn URLs are present as literal text.
    3. Open index.html in a browser (or eyeball the diff) to confirm the public site reads as the same story as the résumé (Adobe voice, rollout+graph_ml Projects, no career-break, no GPAs/cities).
  </how-to-verify>
  <read_first>
    - docs/AshutoshTiwari.pdf (the shipped résumé, palette applied)
    - index.html (the synced site)
    - .planning/ROADMAP.md Phase 6 criterion 5
  </read_first>
  <acceptance_criteria>
    - all six scripts exit 0 (verify-resume + P2/P3/P4/P5 + site net); `make verify` runs green
    - the user confirms the paste test: read order intact, `ashutosh--tiwari` handle intact, portfolio/GitHub/LinkedIn URLs present as literal text
    - on approval, SITE-01 and SITE-02 are ticked complete (REQUIREMENTS.md checkboxes + traceability + ROADMAP), per the last-plan precedent; the commit stays local (no push — the sync goes public only on the owner's own push)
  </acceptance_criteria>
  <resume-signal>Type "approved" (paste test passed) or describe what reads wrong</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub Pages (`index.html`) | a silent public regression (a retired figure / career-break entry creeping back into index.html) would ship on the next push with no gate |
| local commit -> owner's push | the sync goes public only on the owner's push; no push happens in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | site content drift | high | mitigate | scripts/verify-site-regressions.sh asserts the durable invariants over raw HTML, chained into `make verify` as REGRESS6; each class proven failable so it cannot silently rot |
| T-06-08 | Repudiation | gate that asserts nothing | medium | mitigate | No `-`/`|| true` prefix and no `--html` waiver in any make target (a waived run certifies nothing) — enforced as a plan prohibition and checked in Task 2 acceptance |
| T-06-09 | Information disclosure | shipped PDF text layer | medium | mitigate | Plain-text paste test confirms the ATS layer (read order, handle, literal URLs) before the artifact set is declared shippable |

No npm/pip/cargo installs in this plan — package-legitimacy gate N/A.
</threat_model>

<artifacts_produced>
- **scripts/verify-site-regressions.sh** — the S6.x site-sync regression net over raw index.html. Assertion classes + representative needles:
  - S6 retired figures absent: `3–8K images/sec`, `10–50×`
  - S6 career-break absent: `Career Break`
  - S6 rollout + graph_ml present: `github.com/thunderock/rollout`, `github.com/thunderock/graph_ml`
  - S6 GPAs absent: `3.87`, `8.32`
  - S6 Bloomington absent: `Bloomington`
  - S6 pruned sections absent: `Competitive D.S. Experience`, `Certifications`
  - S6 Adobe rewrite landed: `torch.compile` present
  - S6 grad-project titles absent: `BiasNet`, `DeepFoodie`
  - waiver flag `--html FILE` (negative controls only; disclaims certification); exit 0/1/2.
- **Makefile** — `REGRESS6 := scripts/verify-site-regressions.sh`; `@bash $(REGRESS6)` chained into `verify:` and `verify-regressions:`.
- Final green gate + recorded plain-text paste-test sign-off.
</artifacts_produced>

<verification>
- `bash scripts/verify-site-regressions.sh` exit 0 (all S6 PASS) on the synced index.html; exit 1 on each crafted `--html` fixture.
- `make verify-regressions` runs REGRESS2..REGRESS6; `make verify` runs green.
- `bash scripts/verify-resume.sh` exit 0; P2/P3/P4/P5 nets exit 0.
- The paste test is confirmed by the user (read order, `ashutosh--tiwari`, literal URLs).
</verification>

<success_criteria>
- SITE-01/SITE-02: a standing site-sync net (green, each class failable) guards the invariants and is wired into `make verify`.
- Criterion 5: a final `make verify` passes and the PDF pasted as plain text reads in correct order with the handle and URLs intact.
- The milestone artifact set is verified shippable; SITE-01/SITE-02 ticked; no push performed.
</success_criteria>

<output>
Create `.planning/phases/06-palette-site-sync/06-03-SUMMARY.md` when done. Record: the S6 assertion IDs + their negative controls, the Makefile REGRESS6 wiring, the final gate results (six scripts exit 0, `make verify` green), the recorded paste-test verdict, and the SITE-01/SITE-02 tick. Note the commit is local (no push).
</output>
