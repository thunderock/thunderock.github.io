---
phase: 06-palette-site-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements: [SITE-01, SITE-02]
estimate:
  tokens: 52000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "index.html's Adobe block leads on fault-tolerance / distributed-inference, names torch.compile(mode=\"max-autotune\") and the Rust governance/lineage subsystem, and keeps Falcon-40B / Llama 2 70B — at the generalized public register with no hard fleet/throughput numbers (D-08/D-09)."
    - "The two retired page-1 figures (3–8K images/sec on 32 GPUs, 10–50× faster image loading) are gone from index.html (SITE-01, the :196 obligation)."
    - "Education mirrors the résumé: both GPAs (3.87/4.0, 8.32/10.0) dropped, 'Indiana University, Bloomington' -> 'Indiana University', the B-Tech range aligned to 'Aug 2011 – May 2015', and the hero/about 'Bloomington' swept (D-06)."
    - "The Projects section is exactly rollout + graph_ml, mirroring the résumé's \\section{Selected Projects} wording, with all 8 grad-school Project cards gone (SITE-01, D-05)."
    - "The two off-message grad-era sections — 'Competitive D.S. Experience' and 'Certifications' — are removed entirely (D-05)."
    - "The site's own career-break timeline entry ('Career Break — Master's Degree') is removed, so résumé and site agree (SITE-02, D-10)."
    - "Research keeps its 3 entries unchanged (on-message, backs the kept Publication) (D-07); Teaching keeps its 3 TA items (already matches the résumé)."
    - "The three 'Senior Machine Learning Engineer' occurrences read consistently and the sweep introduces no title/figure/claim the rebuilt résumé contradicts."
  artifacts:
    - "index.html — Adobe block rewritten; Education trimmed; hero/about swept; Projects replaced with rollout + graph_ml; Competitive D.S. Experience + Certifications sections removed; career-break entry removed"
  key_links:
    - "docs/main.tex \\section{Selected Projects} (rollout + graph_ml, owner-confirmed GH-03 wording) -> the two replacement Project cards in index.html"
    - "docs/main.tex Adobe block + CODEBASE-EVIDENCE.md honest-framing rules -> the rewritten site Adobe bullets"
    - "removing the career-break <li> + trimming Education + sweeping hero/about -> Bloomington count in index.html falls to 0 (cross-check all three edits landed)"
  prohibitions:
    - "The site Adobe-block rewrite must NOT re-introduce either retired figure and must NOT invent a new unverified figure, and must NOT present speculative decoding / Go / TensorRT-LLM / MoE-TP-PP as accomplishments on the public web — same honesty boundary as the résumé (CODEBASE-EVIDENCE.md, D-08/D-09). (flagged-unverified; no wired check descriptor)"
    - "This plan must NOT touch docs/main.tex or any résumé artifact — the site sync is index.html-only; the résumé is already finalized and page 1 is byte-frozen at 798c0fb. (flagged-unverified; no wired check descriptor)"
  flagged_assumptions:
    - "EDGE SITE-01/adjacency (unresolved): each prune/replace is scoped to its own container (a full div.row for a section, a full li.timeline-item for an entry); removing one section must NOT merge with or delete content from an adjacent section. Resolved: edits are container-boundary-scoped and the sections above/below each removal are left byte-intact."
    - "EDGE SITE-01/empty (unresolved): the Projects section renders exactly TWO cards (rollout, graph_ml) — not zero, not a stray empty card; no section is left with an empty timeline/grid shell after pruning. Resolved into acceptance."
    - "EDGE SITE-01/ordering (unresolved): mirrored sections keep their stable page order, and the two Project cards render rollout THEN graph_ml (matching the résumé's Selected Projects order). Resolved into acceptance."
    - "EDGE SITE-02/adjacency (unresolved): removing the career-break li must not collide with the adjacent Adobe (above) or Swiggy (below) timeline items — both stay byte-intact. Resolved into acceptance."
    - "EDGE SITE-02/empty (unresolved): after the career-break li is removed, no empty timeline-item / dangling marker is left in the Experience list. Resolved into acceptance."
    - "EDGE SITE-02/ordering (unresolved): the Experience timeline order (Adobe -> Swiggy -> Flipkart -> Groupon -> Netspeed) is preserved after the removal. Resolved into acceptance."
    - "ASSUMPTION (Education degree naming): the résumé Education has no city and reads 'National Institute of Technology'; the site currently reads 'National Institute of Technology, Patna'. This plan drops the 'Patna' city to mirror the résumé's city-free Education, but leaves the site's 'B-Tech (Computer Science & Engineering)' degree wording (not a factual contradiction of the résumé's 'Bachelor of Science in Computer Science'). Flagged for the executor to confirm-by-content against docs/main.tex; default is drop-city, keep-degree-wording."
---

<objective>
Resync index.html so the public portfolio tells the same story as the rebuilt résumé: rewrite the Adobe block to the fault-tolerance / distributed-inference / torch.compile / Rust-governance voice at the generalized public register (D-08/D-09), remove the two retired page-1 figures at :196, mirror Education (D-06), replace the grad-school Projects with rollout + graph_ml (D-05), prune the off-message Competitive D.S. Experience and Certifications sections (D-05), remove the site's own career-break entry (D-10/SITE-02), and sweep the hero/about title + city — all content-only edits inside existing containers (no layout/CSS change; stays clear of PORT-01). Research keeps its 3 entries (D-07); Teaching keeps its 3 items.

Purpose: SITE-01 + SITE-02 — the live public site stops contradicting the résumé on titles, figures, and claims.
Output: a fully-synced index.html (the resync harness/gate and final acceptance are Plan 06-03; this plan lands the content).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-palette-site-sync/06-CONTEXT.md

# Source of truth the site mirrors (resolve every reference by CONTENT)
@docs/main.tex                          # Adobe block, \section{Selected Projects} (rollout+graph_ml), \section{Education}, \section{Publications}
@.planning/research/CODEBASE-EVIDENCE.md # honesty boundary for the Adobe rewrite (no retired/invented figures; evidenced themes only)

# The sync target
@index.html                             # 756 lines; section map in 06-CONTEXT.md canonical_refs (resolve by content)
</context>

<tasks>

<!-- planner-discipline-allow: 3–8K images/sec -->
<!-- planner-discipline-allow: 10–50× -->
<!-- planner-discipline-allow: 3.87 -->
<!-- planner-discipline-allow: 8.32 -->

<task type="tracer">
  <name>Task 1: Rewrite the Adobe block to the rebuilt-résumé voice + strip the two retired figures (D-08/D-09)</name>
  <files>index.html</files>
  <read_first>
    - index.html — the Adobe timeline-item (the `<span id="section-heading">Adobe Firefly</span>` block and its `<ul>` of bullets; retired figures are byte-exact `3–8K images/sec on 32 GPUs` and `10–50×` inside one `<li>`; resolve by content)
    - docs/main.tex — the shipped Adobe block (fault-tolerant lead; Distributed Inference Frameworks with vLLM+Ray, Falcon-40B, Llama 2 70B; Ray Data + PyTorch Lightning + torch.compile(mode="max-autotune") compiler bridge; Source->Transform->Sink plugins + KEDA on SQS; Foundation Model Training Framework FSDP/FSDP2 + Flash Attention; Build & Deployment Rust control plane + MCP; Enrichment checkpoint cache + lease/TTL + circuit breaker; Training-Data Governance Rust lineage; Data Quality on Cerberus)
    - .planning/research/CODEBASE-EVIDENCE.md — the binding honest-framing rules (NO retired figures, NO speculative decoding / Go / TensorRT-LLM / MoE-TP-PP as accomplishments; evidenced themes are fault-tolerance / distributed-inference / torch.compile / Rust-governance)
    - .planning/STATE.md [Phase 3 / 03-02] SITE HANDOFF (the :196 duplicate figures obligation)
  </read_first>
  <action>
    Rewrite the Adobe block's bullets in place (the `<li>` list inside the Adobe timeline-item; keep the surrounding container, timeline markup and the col-md structure untouched). Reshape them to mirror the résumé's Adobe thesis: lead on fault-tolerant distributed inference and training infrastructure that survives node loss / partial failure; name the distributed inference frameworks (vLLM + Ray for LLMs such as Falcon-40B and Llama 2 70B — KEEP these two, they survived the résumé rebuild); state offline inference on Ray Data + PyTorch Lightning WITH torch.compile(mode="max-autotune") described as a compiler bridge (compiler-generated Triton kernels, fusion, CUDA-graph capture — as a compiler-mediated claim, never "wrote Triton kernels"); the plugin Source->Transform->Sink pipelines KEDA-autoscaled on SQS; the PyTorch foundation-model training framework (FSDP/FSDP2, Flash Attention 2/3, context/distributed attention for DiT); the Build & Deployment system (Rust control plane, MCP interface); and explicitly frame the Rust training-data governance/lineage subsystem (Content Gate) plus the Cerberus data-quality framework. DELETE the sentence carrying the two retired figures — `3–8K images/sec on 32 GPUs` and `10–50×` must be byte-gone. Public register (D-09): NO hard fleet/throughput numbers (no "32 GPUs", no "N images/sec", no fleet counts); keep the generalized "billions of images and videos" framing and the keyword-level stack nouns (vLLM, Ray, FSDP/FSDP2, Flash Attention, torch.compile, KEDA, Cerberus, Rust). Nothing on the public web is more exposed than the PDF. Do NOT touch docs/main.tex. Also sweep the Adobe `<p><b>` title so it reads consistently with the other two "Senior Machine Learning Engineer" occurrences (Task 2 finishes the sweep).
  </action>
  <verify>
    <automated>[ "$(LC_ALL=C grep -c -F '3–8K images/sec' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F '10–50×' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F 'torch.compile' index.html)" -ge 1 ] && [ "$(LC_ALL=C grep -c -F 'Falcon-40B' index.html)" -ge 1 ] && [ "$(LC_ALL=C grep -c -F 'Rust' index.html)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C grep -c -F '3–8K images/sec' index.html` == 0 and `LC_ALL=C grep -c -F '10–50×' index.html` == 0 (byte-exact; en-dash U+2013 and × U+00D7)
    - `torch.compile` appears at least once in index.html; `Falcon-40B` and `Llama 2 70B` are still present (kept per D-08)
    - the Adobe bullets name the Rust governance/lineage subsystem and carry NO hard fleet/throughput number (no "32 GPUs", no "images/sec", no fleet counts) — generalized public register (D-09)
    - no prohibited term (speculative decoding, Go as a language, TensorRT-LLM, MoE/tensor/pipeline parallelism as an accomplishment) is introduced
    - docs/main.tex is byte-unchanged (`git diff --quiet docs/main.tex`); only the Adobe `<li>`/`<p>` content changed, container markup intact
  </acceptance_criteria>
  <reversibility rating="reversible">Content edit inside an existing container; reversible via git; pruned/rewritten text remains in history.</reversibility>
  <done>The site Adobe block reads in the rebuilt-résumé voice, the two retired figures are byte-gone, torch.compile and the Rust governance subsystem are named, and no hard number or invented figure was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Education + sweep hero/about (D-06)</name>
  <files>index.html</files>
  <read_first>
    - index.html — the Education section (Indiana University, Bloomington + `GPA - 3.87/4.0`; National Institute of Technology, Patna + `CGPA - 8.32/10.0`; B-Tech dates `Jul 2011 - Jun 2015`; resolve by content) and the hero/about `#intro` / `#about` block (the `#about` paragraph names "Indiana University, Bloomington")
    - docs/main.tex \section{Education} (Indiana University, no city, no GPA; National Institute of Technology, no city; degree dates Aug. 2021 -- May 2023 and Aug. 2011 -- May 2015)
  </read_first>
  <action>
    Mirror Education to the résumé (D-06), resolving each by content: DELETE the `<p><b>GPA - 3.87/4.0</b></p>` and `<p><b>CGPA - 8.32/10.0</b></p>` lines; change `Indiana University, Bloomington` -> `Indiana University` in the Education heading; drop the `, Patna` city from `National Institute of Technology, Patna` -> `National Institute of Technology` (mirror the résumé's city-free Education — keep the site's `B-Tech (Computer Science & Engineering)` degree wording, which is not a contradiction); align the B-Tech date range `Jul 2011 - Jun 2015` -> `Aug 2011 – May 2015` (en dash, matching the site's Experience date style and the résumé range). Then sweep the hero/about: in the `#about` paragraph change `Indiana University, Bloomington` -> `Indiana University`; and make the three `Senior Machine Learning Engineer` occurrences (intro, about, Adobe title) read consistently. After this task no `3.87`, `8.32`, or `Bloomington` remains anywhere in index.html (the career-break li carrying the third Bloomington is removed in Task 3 — so Bloomington reaches 0 only after Task 3; Education + about drop two of the three here).
  </action>
  <verify>
    <automated>[ "$(LC_ALL=C grep -c -F '3.87' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F '8.32' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F 'Aug 2011 – May 2015' index.html)" -ge 1 ] && [ "$(LC_ALL=C grep -c -F 'Senior Machine Learning Engineer' index.html)" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C grep -c -F '3.87' index.html` == 0 and `LC_ALL=C grep -c -F '8.32' index.html` == 0 (both GPAs gone)
    - the Education heading reads `Indiana University` (no `, Bloomington`) and `National Institute of Technology` (no `, Patna`)
    - the B-Tech range reads `Aug 2011 – May 2015` (byte-exact, en dash U+2013)
    - the `#about` paragraph no longer contains `Bloomington`; the three `Senior Machine Learning Engineer` occurrences read consistently
    - docs/main.tex byte-unchanged; the Master's date row (Aug 2021 - May 2023) is preserved in Education
  </acceptance_criteria>
  <done>Education mirrors the résumé (no GPAs, no cities, B-Tech range aligned) and the hero/about title + city are swept.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Projects with rollout + graph_ml, prune grad-era sections, remove career-break (D-05/D-10)</name>
  <files>index.html</files>
  <read_first>
    - index.html — the Projects grid (sidebar-heading `Projects`, the 8 `col-md-4` cards: BiasNet, Bias Manifolds, BlindNet, DeepFoodie, Flipkart Hackday, Groupon Geekon, Continuous Dominant Set, Snake and Ladders); the Competitive D.S. Experience section (sidebar-heading `Competitive D.S. Experience`); the Certifications section (sidebar-heading `Certifications`); the career-break timeline-item (`Career Break — Master's Degree`); resolve every one by content, not by stored line number
    - docs/main.tex \section{Selected Projects} — the rollout card (Rust, Distributed Training & Inference, RL for LLMs, vLLM, PyO3, Postgres; 18 crates, 197 test files, CI; PPO/GRPO/DPO/SFT/RM; actor/learner split with work-stealing; CRIU snapshots; PyO3 plugins; github.com/thunderock/rollout) and the graph_ml card (Python, PyTorch Geometric, C++/Cython; sklearn-style Graph-ML library with C++/Cython random-walk kernels; CI-tested; github.com/thunderock/graph_ml)
  </read_first>
  <action>
    Three container-scoped deletions/replacements, each resolved by content:
    (1) REPLACE the Projects grid's 8 grad-school `col-md-4` cards with exactly TWO cards mirroring the résumé's Selected Projects (D-05): a `rollout` card and a `graph_ml` card, in that order (rollout then graph_ml), each linking to its repo (github.com/thunderock/rollout, github.com/thunderock/graph_ml) and carrying the owner-confirmed GH-03 capability wording from docs/main.tex, adapted to the site's card markup (keep the `col-md-4`/`section-line`/`project-icon` shell). All 8 grad-project titles (BiasNet, Bias Manifolds, BlindNet, DeepFoodie, Flipkart Hackday, Groupon Geekon, Continuous Dominant Set, Snake and Ladders) must be gone.
    (2) DELETE the entire Competitive D.S. Experience section (its whole `div.row`, sidebar-heading `Competitive D.S. Experience`) — the 6 AnalyticsVidhya/MachineHack/Humana hackathon cards.
    (3) DELETE the entire Certifications section (its whole `div.row`, sidebar-heading `Certifications`) — Udacity / IISc PG Diploma / School of AI / Udemy.
    (4) DELETE the career-break timeline-item (`Career Break — Master's Degree`, SITE-02/D-10) — its whole `<li class="timeline-item">`, leaving the Experience list running Adobe -> Swiggy with no dangling empty item.
    Leave Research (3 entries, D-07) and Teaching (3 items) byte-unchanged. Container boundaries only — no CSS/layout change, adjacent sections stay byte-intact.
  </action>
  <verify>
    <automated>[ "$(LC_ALL=C grep -c -F 'github.com/thunderock/rollout' index.html)" -ge 1 ] && [ "$(LC_ALL=C grep -c -F 'github.com/thunderock/graph_ml' index.html)" -ge 1 ] && [ "$(LC_ALL=C grep -c -F 'Competitive D.S. Experience' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F 'Certifications' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F 'Career Break' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F 'Bloomington' index.html)" = "0" ] && [ "$(LC_ALL=C grep -c -F 'BiasNet' index.html)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `github.com/thunderock/rollout` and `github.com/thunderock/graph_ml` both present; the two Project cards render rollout THEN graph_ml (ordering edge)
    - all 8 grad-project titles gone: `BiasNet`, `Bias Manifolds`, `BlindNet`, `DeepFoodie`, `Flipkart Hackday`, `Groupon Geekon`, `Continuous Dominant Set`, `Snake and Ladders` each count 0
    - `Competitive D.S. Experience` count 0 and `Certifications` count 0 (both sections removed whole)
    - `Career Break` count 0; the Experience list runs Adobe -> Swiggy -> Flipkart -> Groupon -> Netspeed with no dangling/empty timeline-item (ordering + empty edges)
    - `Bloomington` count 0 across the whole file (career-break removal drops the last of the three occurrences — cross-checks Tasks 1-3 landed)
    - Research still has its 3 entries and Teaching its 3 items (byte-unchanged); adjacent sections around each removal are byte-intact
    - docs/main.tex byte-unchanged (`git diff --quiet docs/main.tex`)
  </acceptance_criteria>
  <reversibility rating="reversible">Pruned/replaced sections remain in git history; restorable if v2 wants them (D-05).</reversibility>
  <done>Projects is exactly rollout + graph_ml, the two off-message grad-era sections are gone, the career-break entry is removed, and Bloomington reaches 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub Pages (`index.html`, published by .github/workflows/jekyll.yml on push to master) | index.html is live public HTML; a stale/dishonest claim or a leaked internal figure is publicly readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information disclosure | site Adobe block (public register) | high | mitigate | D-09 — no hard fleet/throughput numbers on the public web; the two retired figures byte-removed; only generalized framing + keyword-level stack nouns kept |
| T-06-05 | Spoofing (qualifications) | rewritten Adobe bullets / Projects | high | mitigate | Bound to CODEBASE-EVIDENCE.md honest-framing rules; no retired/invented figure; no speculative decoding / Go / TensorRT-LLM / MoE-TP-PP as accomplishment; Plan 06-03's net asserts the retired figures absent |
| T-06-06 | Tampering | résumé freeze | medium | mitigate | This plan is index.html-only; docs/main.tex byte-unchanged (`git diff --quiet docs/main.tex`); page 1 freeze untouched |

No npm/pip/cargo installs in this plan — package-legitimacy gate N/A. (The commit stays local; the sync only goes public on the owner's push — no push happens in this phase.)
</threat_model>

<artifacts_produced>
- **index.html** — the fully-synced public portfolio:
  - Adobe block rewritten to the fault-tolerance / distributed-inference / torch.compile / Rust-governance voice (D-08/D-09); two retired figures byte-removed
  - Education mirrored (GPAs `3.87`/`8.32` gone, cities `Bloomington`/`Patna` gone, B-Tech range `Aug 2011 – May 2015`) + hero/about swept
  - Projects replaced with exactly `rollout` + `graph_ml` (8 grad cards gone)
  - `Competitive D.S. Experience` + `Certifications` sections removed whole
  - career-break timeline entry (`Career Break — Master's Degree`) removed
- No new files; no docs/main.tex touch. The site-sync regression net + Makefile wiring + final acceptance are Plan 06-03.
</artifacts_produced>

<verification>
- Retired figures: `LC_ALL=C grep -c -F '3–8K images/sec' index.html` == 0; `LC_ALL=C grep -c -F '10–50×' index.html` == 0.
- Sync present: `torch.compile`, `github.com/thunderock/rollout`, `github.com/thunderock/graph_ml`, `Aug 2011 – May 2015`, `Falcon-40B` all present.
- Pruned/removed: `3.87`, `8.32`, `Bloomington`, `Competitive D.S. Experience`, `Certifications`, `Career Break`, `BiasNet` all count 0.
- `Senior Machine Learning Engineer` count == 3 (consistent).
- `git diff --quiet docs/main.tex` (résumé untouched).
</verification>

<success_criteria>
- SITE-01: index.html matches the finalized résumé across hero/about, Experience, Projects (rollout + graph_ml), Education, and Publication — no title/figure/claim the rebuilt résumé contradicts, including the retired :196 figures gone.
- SITE-02: the site's career-break timeline entry is gone.
- Research keeps its 3 entries (D-07); Teaching keeps its 3 items; the résumé is untouched.
</success_criteria>

<output>
Create `.planning/phases/06-palette-site-sync/06-02-SUMMARY.md` when done. Record: the exact byte-removals (retired figures, GPAs, cities, career-break, pruned sections), the rollout/graph_ml replacement wording, the hero/about sweep, the final grep counts (all the =0 and >=1 checks), and confirmation docs/main.tex is byte-unchanged. SITE-01/SITE-02 are NOT ticked here — they tick at 06-03 (last-plan precedent), which also asserts these invariants in the site-sync net.
</output>
