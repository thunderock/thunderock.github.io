---
phase: 06-palette-site-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-palette-samples.sh
  - docs/main.tex
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: false
requirements: [VIS-01]
estimate:
  tokens: 55000
  raw_tokens: 35000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "The four palette columns (teal incumbent, deep navy, oxblood, graphite mono per D-01) each render the FINAL frozen résumé content as a 2-page PDF plus a grayscale conversion (D-02 single-hue family)."
    - "Each column's text accent (accentdark) measures >= 7:1 vs white, its rule accent (accent) measures >= 3:1 vs white, its grayscale text accent measures >= 4.5:1 and grayscale rule accent >= 3:1 — the fixed WCAG bar from PITFALLS.md Pitfall 20 / ARCHITECTURE.md §4.3 (D-03)."
    - "The user picks one palette from the rendered samples at a blocking checkpoint (teal staying is a legitimate zero-challenger outcome) (D-04)."
    - "The chosen palette is applied by editing exactly the two hex literals (accent, accentdark) and the dead accentlight definition is deleted in the same change; zero unused color definitions remain (D-03)."
    - "After the apply, bash scripts/verify-resume.sh exits 0 and every résumé regression net (P2/P3/P4/P5) stays green — the palette change is color-only with zero content/layout/gate impact."
    - "Page 1 stays byte-frozen at 798c0fb: the palette edit touches only the preamble color definitions (docs/main.tex color-def lines), never the page-1 Experience content."
  artifacts:
    - "scripts/build-palette-samples.sh — the sed+latexmk variant harness that renders + grayscales + WCAG-measures all four columns"
    - "build/palette/*.pdf — the four color sample PDFs and their grayscale conversions (untracked decision artifacts)"
    - "docs/main.tex — two hex literals set to the chosen palette, accentlight definition deleted"
    - "docs/AshutoshTiwari.pdf (+ .log/.fdb_latexmk/.aux/.fls/.out) — regenerated in the same commit as the source edit"
  key_links:
    - "sed substitution of the two color-def literals -> latexmk build -> the 13 downstream color use sites (2 accent rules, 11 accentdark text) update automatically with no macro/body edit (ARCHITECTURE.md §4.1)"
    - "chosen hex -> WCAG relative-luminance + BT.601 grayscale contrast computation -> the numeric acceptance bar"
  prohibitions:
    - "A palette must NOT ship if it drops any TEXT accent below 7:1, any RULE accent below 3:1, or fails either grayscale floor (4.5:1 text / 3:1 rule) — the harness has no color gate, so this is a values/quality must-NOT the plan enforces before apply. (flagged-unverified; no wired check descriptor)"
    - "The palette edit must NOT alter any page-1 Experience content or any docs/main.tex line beyond the accent / accentdark / accentlight color definitions — page 1 is byte-frozen at 798c0fb. (flagged-unverified; no wired check descriptor)"
  flagged_assumptions:
    - "EDGE VIS-01/adjacency (unresolved): the WCAG bar is inclusive — a value landing exactly at 7:1 / 3:1 / 4.5:1 PASSES (>= comparison, not >). Resolved into the acceptance criteria as >=."
    - "EDGE VIS-01/empty (unresolved): a proposal that fails any threshold is excluded from the pick; if no challenger beats the bar in a way the user prefers, 'teal stays' is the valid zero-winner outcome (D-01 burden-of-proof-on-challenger). Resolved: teal is rendered as a fourth column and is a selectable pick."
    - "EDGE VIS-01/encoding (unresolved): LaTeX hex literals are ASCII; contrast is computed from the hex via WCAG sRGB relative-luminance and BT.601 luma (a deterministic formula), never sampled from a rendered raster. Resolved: measurement reads the hex string, not pixels."
    - "EDGE VIS-01/ordering (unresolved): the four sample columns are rendered and presented in a fixed, stable order (teal, navy, oxblood, graphite) so the checkpoint choices are unambiguous. Resolved into the harness's fixed iteration order."
---

<objective>
Choose the résumé's accent palette against the now-frozen content and apply it. Render the three researched challengers (deep navy, oxblood, graphite mono) side-by-side with today's teal incumbent — each a single-hue family (D-02) rendering the FINAL résumé, each with a grayscale conversion — measure every column against the fixed WCAG bar (D-03), let the user pick at a blocking checkpoint (D-04, teal-stays is legitimate per D-01), then apply the pick via the two hex literals and delete the dead accentlight definition (D-03).

Purpose: VIS-01 — the palette is decided against the artifact the user will actually ship, with contrast/grayscale/dead-definition all measured rather than eyeballed.
Output: a reproducible sample harness, four color + four grayscale sample PDFs, the applied palette in docs/main.tex, and a rebuilt PDF that keeps scripts/verify-resume.sh green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-palette-site-sync/06-CONTEXT.md

# Palette research — already computed, do NOT re-derive
@.planning/research/PITFALLS.md      # Pitfall 20: the numeric WCAG acceptance table + current teal values
@.planning/research/ARCHITECTURE.md   # §1.5 propagation model + §4 palette integration (candidate hexes, sed+latexmk harness, grayscale)

# Source of truth (resolve every reference by CONTENT, never a stored line number)
@docs/main.tex                        # color defs (accent / accentlight / accentdark) + the frozen résumé content the samples render
@Makefile                             # make build hardcodes JOBNAME -> cannot emit variants; the sample harness is separate
</context>

<tasks>

<!-- planner-discipline-allow: accentlight -->

<task type="tracer">
  <name>Task 1: End-to-end palette-sample harness, proven on ONE hue (navy)</name>
  <files>scripts/build-palette-samples.sh</files>
  <read_first>
    - docs/main.tex — the three \definecolor lines (accent = #17685C, accentlight = #E4EFEC, accentdark = #0E463E; resolve by content) and the frozen résumé body the sample renders
    - .planning/research/ARCHITECTURE.md §4.3 (the candidate hex table + expected contrast values) and §4.5 (the sed+latexmk build mechanic)
    - .planning/research/PITFALLS.md Pitfall 20 (the numeric acceptance table + current teal values)
    - Makefile (LATEXMK_FLAGS, JOBNAME; the harness must NOT reuse make build because it hardcodes JOBNAME)
  </read_first>
  <action>
    Create scripts/build-palette-samples.sh: a bash-3.2-compatible, read-only-of-the-repo harness (writes only under a build/palette/ output dir and a scratch dir; never edits docs/main.tex). For a single named hue it must: (1) copy docs/main.tex to a scratch file; (2) sed-substitute the two color-def literals — accent's hex and accentdark's hex — matching the definecolor lines by content, leaving all other lines byte-identical; (3) latexmk-build the scratch file with a per-name jobname (e.g. palette-navy) using the same pdf/nonstopmode/halt-on-error flags make build uses; (4) assert the built PDF is exactly 2 pages via pdfinfo; (5) produce a grayscale conversion of the color PDF, best-effort via ghostscript (-sColorConversionStrategy=Gray) when gs is on PATH, and note when it is skipped; (6) compute and print the four contrast numbers for that hue via an inline python3 pass — accentdark-vs-white and accent-vs-white using the WCAG sRGB relative-luminance formula, and their BT.601-luma grayscale equivalents (0.299R+0.587G+0.114B) vs white — and PASS/FAIL each against the fixed bar (text >= 7:1, rule >= 3:1, grayscale text >= 4.5:1, grayscale rule >= 3:1, inclusive). Prove the whole pipeline on the navy hue (accent #1F4E79, accentdark #14345B): its measured values must reproduce ARCHITECTURE.md §4.3 (accent 8.66:1, accentdark 12.56:1) and PASS every threshold, and the navy PDF must be 2 pages. Do NOT inline the résumé content anywhere — the harness reads docs/main.tex as the single source (single-file LaTeX; never make clean). Output PDFs go to build/palette/ (untracked).
  </action>
  <verify>
    <automated>bash scripts/build-palette-samples.sh navy && test -s build/palette/palette-navy.pdf && [ "$(pdfinfo build/palette/palette-navy.pdf | awk '/^Pages:/{print $2}')" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-palette-samples.sh exists and runs under /bin/bash 3.2 with no unbound-variable or syntax error
    - `bash scripts/build-palette-samples.sh navy` prints a measurement block reporting accentdark 12.56:1 (>= 7:1 PASS), accent 8.66:1 (>= 3:1 PASS), and both grayscale floors PASS
    - build/palette/palette-navy.pdf exists and is exactly 2 pages (pdfinfo)
    - docs/main.tex is byte-unchanged after the run (the harness never edits the tracked source; confirm with `git diff --quiet docs/main.tex`)
    - the grayscale conversion is produced when ghostscript is present, or an explicit "grayscale render skipped: gs not on PATH" note is printed (the numeric grayscale contrast is computed regardless)
  </acceptance_criteria>
  <reversibility rating="reversible">A new standalone script; no effect on tracked artifacts.</reversibility>
  <done>The harness renders, page-checks, grayscales and WCAG-measures the navy column end-to-end, and navy passes the bar.</done>
</task>

<task type="auto">
  <name>Task 2: Render + measure all four columns (D-01 candidate set)</name>
  <files>scripts/build-palette-samples.sh</files>
  <read_first>
    - scripts/build-palette-samples.sh (Task 1 output — extend its per-hue path to iterate the full set)
    - .planning/research/ARCHITECTURE.md §4.3/§4.4 (all four hexes + expected contrast per column)
  </read_first>
  <action>
    Extend the harness so a no-argument (or `all`) invocation renders the four columns in this fixed, stable order (D-02, resolves the VIS-01/ordering edge): teal (accent #17685C, accentdark #0E463E — the incumbent baseline), navy (#1F4E79/#14345B), oxblood (#7B2D3B/#571F29), graphite (#3E4A59/#22303C). For each: build the color PDF, page-count-check (== 2), produce its grayscale conversion, and print the four contrast numbers with PASS/FAIL against the fixed bar. Emit a final summary table, one row per column, and a machine line the checkpoint can read (e.g. `>> PALETTE <name> text=<r>:1 rule=<r>:1 grayText=<r>:1 grayRule=<r>:1 pages=2 VERDICT=PASS`). Every one of the four is expected to PASS the bar (all three challengers are higher-contrast than teal, and teal already clears every bar per D-01) — a column that FAILs any threshold is a harness bug (the values are pre-computed in ARCHITECTURE.md §4.3) and must be investigated, not shipped. All eight PDFs (four color + four grayscale) land in build/palette/ for the user to open side-by-side at the checkpoint.
  </action>
  <verify>
    <automated>bash scripts/build-palette-samples.sh all && ls build/palette/palette-teal.pdf build/palette/palette-navy.pdf build/palette/palette-oxblood.pdf build/palette/palette-graphite.pdf && [ "$(bash scripts/build-palette-samples.sh all | grep -c 'VERDICT=PASS')" -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - all four color sample PDFs exist in build/palette/ and each is exactly 2 pages
    - the summary reports every column PASS on text (>= 7:1), rule (>= 3:1), grayscale text (>= 4.5:1), grayscale rule (>= 3:1); measured values match ARCHITECTURE.md §4.3 within rounding (teal 6.62/10.69, navy 8.66/12.56, oxblood 9.21/12.86, graphite 9.02/13.50)
    - the four columns render in the fixed order teal, navy, oxblood, graphite (resolves the ordering edge)
    - a grayscale conversion exists for each column when gs is present (else the skip is noted); the numeric grayscale check is present for all four regardless
    - docs/main.tex remains byte-unchanged (`git diff --quiet docs/main.tex`)
  </acceptance_criteria>
  <done>Four color + four grayscale samples of the final content exist, and all four columns pass the fixed WCAG bar.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: User picks the palette from the rendered samples (VIS-01 / D-04)</name>
  <decision>Which accent palette ships on the résumé: teal (incumbent), deep navy, oxblood, or graphite mono?</decision>
  <context>
    All four columns render the final frozen résumé content and every one clears the fixed WCAG bar (text >= 7:1, rule >= 3:1, grayscale text >= 4.5:1, grayscale rule >= 3:1). The three challengers are all higher-contrast than today's teal, which itself already clears every bar — so the burden of proof is on the challenger and "teal stays" is an explicitly legitimate outcome (D-01). Open the eight PDFs in build/palette/ (four color + four grayscale) side-by-side and judge on both the color and the grayscale (print) rendering. This selection is the one thing the harness cannot decide; it is reversible (any pick is a 2-hex-literal swap) but requires the user against real samples (D-04). This checkpoint is blocking and cannot auto-advance.
  </context>
  <options>
    <option id="teal">
      <name>Teal (incumbent #17685C / #0E463E)</name>
      <pros>No change; already clears every bar; the document's established restrained look.</pros>
      <cons>Lowest contrast of the four (still well above the bar).</cons>
    </option>
    <option id="navy">
      <name>Deep navy (#1F4E79 / #14345B)</name>
      <pros>The best-precedented senior-technical register; institutional, not decorative; higher contrast than teal.</pros>
      <cons>Least distinctive — many candidates use blue.</cons>
    </option>
    <option id="oxblood">
      <name>Oxblood (#7B2D3B / #571F29)</name>
      <pros>Distinctive-but-professional; high contrast; mature.</pros>
      <cons>No corpus precedent; some readers may read burgundy as legal/academic.</cons>
    </option>
    <option id="graphite">
      <name>Graphite mono (#3E4A59 / #22303C)</name>
      <pros>Highest contrast (13.50:1 text); maximum print safety; closest to pure-black while keeping the two-tone system.</pros>
      <cons>Quietest — loses the accent identity.</cons>
    </option>
  </options>
  <read_first>
    - build/palette/*.pdf (the rendered samples)
    - .planning/research/ARCHITECTURE.md §4.4 (per-column rationale + risk)
  </read_first>
  <acceptance_criteria>
    - the user's pick is recorded verbatim (one of: teal, navy, oxblood, graphite) and carried into Task 4
    - if the user picks teal, that is recorded as an affirmative decision (not a skip): the accentlight deletion still lands in Task 4
  </acceptance_criteria>
  <resume-signal>Select: teal, navy, oxblood, or graphite</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Apply the chosen palette + delete accentlight (D-03), keep the harness green</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk, docs/AshutoshTiwari.aux, docs/AshutoshTiwari.fls, docs/AshutoshTiwari.out</files>
  <read_first>
    - docs/main.tex — the three \definecolor lines (resolve by content), and confirm page-1 Experience content is not in the edit window
    - .planning/STATE.md standing rules (artifacts ride in the source commit; never make clean; gate on bash scripts/verify-resume.sh exit 0; page 1 byte-frozen at 798c0fb)
    - the recorded pick from Task 3
  </read_first>
  <action>
    Apply the Task-3 pick to docs/main.tex, resolving the color-def lines by CONTENT: set accent's HTML hex and accentdark's HTML hex to the chosen column's values (for teal, both are byte-unchanged), and DELETE the accentlight definition line entirely (D-03 — unused color definitions must be 0; accentlight has 0 use sites per ARCHITECTURE.md §4.1). Touch nothing else in docs/main.tex — not a single page-1 Experience line, not a macro, not a body line (the 13 downstream use sites re-color automatically). Then rebuild with latexmk (never make clean), so the regenerated docs/AshutoshTiwari.pdf and its .log/.fdb_latexmk/.aux/.fls/.out ride in the SAME commit as the source edit (the .fdb_latexmk carries the md5 G0.3 reads). Confirm the palette change is color-only: bash scripts/verify-resume.sh exits 0, and the four résumé nets (P2/P3/P4/P5) stay green.
  </action>
  <verify>
    <automated>make build >/dev/null 2>&1; [ "$(LC_ALL=C grep -c 'accentlight' docs/main.tex)" = "0" ] && [ "$(LC_ALL=C grep -c 'definecolor{accent}' docs/main.tex)" -ge 1 ] && [ "$(LC_ALL=C grep -c 'definecolor{accentdark}' docs/main.tex)" -ge 1 ] && bash scripts/verify-resume.sh >/dev/null 2>&1 && bash scripts/verify-phase2-regressions.sh >/dev/null 2>&1 && bash scripts/verify-phase3-regressions.sh >/dev/null 2>&1 && bash scripts/verify-phase4-regressions.sh >/dev/null 2>&1 && bash scripts/verify-phase5-regressions.sh >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - docs/main.tex: accentlight count is 0; accent and accentdark definitions both still present; the two hex literals equal the chosen column's values (teal = unchanged)
    - the git diff of docs/main.tex touches ONLY the three color-def lines (two edited literals + the deleted accentlight line) — `git diff -U0 docs/main.tex` shows no hunk outside the color block, and no page-1 Experience line is in the diff
    - bash scripts/verify-resume.sh exits 0 (the DONE signal); P2/P3/P4/P5 nets all exit 0
    - the regenerated PDF and aux files are staged in the same commit as docs/main.tex (a fresh checkout of the commit verifies green: G0.3 PASS)
    - page 1 is unchanged from 798c0fb (the freeze holds; the edit is preamble-only)
  </acceptance_criteria>
  <reversibility rating="reversible">Any palette is a two-hex-literal swap; fully reversible via git.</reversibility>
  <done>The chosen palette is live, accentlight is gone, zero unused color definitions remain, and the harness + all four résumé nets are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public résumé PDF (GitHub Pages `docs/AshutoshTiwari.pdf`) | the built PDF is downloadable from the public site; a low-contrast/print-unsafe palette degrades a public, accessibility-relevant artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information disclosure (accessibility) | applied accent palette | medium | mitigate | Every column measured against the fixed WCAG bar (text >= 7:1, rule >= 3:1, grayscale text >= 4.5:1, rule >= 3:1) before the pick; a below-bar palette is a plan-level prohibition and never applied |
| T-06-02 | Tampering | docs/main.tex page-1 freeze | high | mitigate | The apply edits only the three color-def lines; `git diff -U0 docs/main.tex` proves no page-1 Experience line moved (798c0fb freeze) |
| T-06-03 | Tampering | build provenance (G0.3) | medium | mitigate | Regenerated PDF + .fdb_latexmk ride in the source commit; never make clean; bash scripts/verify-resume.sh G0.3 PASS on a fresh checkout |

No npm/pip/cargo installs in this plan (latexmk / poppler / python3 already present) — the package-legitimacy gate is N/A this phase.
</threat_model>

<artifacts_produced>
- **scripts/build-palette-samples.sh** — sed+latexmk variant harness; renders any of {teal,navy,oxblood,graphite}, page-checks (== 2), grayscale-converts (best-effort gs), and computes WCAG + BT.601 contrast with PASS/FAIL against the fixed bar. Machine line: `>> PALETTE <name> text=<r>:1 rule=<r>:1 grayText=<r>:1 grayRule=<r>:1 pages=2 VERDICT=PASS`.
- **build/palette/palette-{teal,navy,oxblood,graphite}.pdf** (+ grayscale variants) — untracked decision artifacts for the VIS-01 checkpoint.
- **New color literal values** (whichever column is chosen) in docs/main.tex: teal `#17685C`/`#0E463E`, navy `#1F4E79`/`#14345B`, oxblood `#7B2D3B`/`#571F29`, or graphite `#3E4A59`/`#22303C`.
- **Deleted definition:** the accentlight `\definecolor` line (unused-color-def count driven to 0).
- **Regenerated** docs/AshutoshTiwari.pdf (+ .log/.fdb_latexmk/.aux/.fls/.out) in the apply commit.
</artifacts_produced>

<verification>
- `bash scripts/build-palette-samples.sh all` reports 4 columns, each 2 pages, each VERDICT=PASS against the fixed bar.
- After apply: `LC_ALL=C grep -c 'accentlight' docs/main.tex` == 0; accent + accentdark definitions present.
- `bash scripts/verify-resume.sh` exits 0; P2/P3/P4/P5 nets exit 0.
- `git diff -U0 docs/main.tex` shows only the color-def block changed (no page-1 Experience hunk).
</verification>

<success_criteria>
- VIS-01: 2-3 challenger palettes + grayscale conversions render as side-by-side samples of the final content, each measured against the WCAG bar; the user picks (teal-stays legitimate); the pick is applied via the two hex literals; no unused color definition (accentlight) remains.
- The résumé harness and all four content nets stay green (palette is color-only).
- Page 1 stays byte-frozen at 798c0fb.
</success_criteria>

<output>
Create `.planning/phases/06-palette-site-sync/06-01-SUMMARY.md` when done. Record: the harness path + machine lines for all four columns, the user's recorded pick (teal/navy/oxblood/graphite), the applied hex literals, confirmation accentlight is gone, the measured verify-resume/G0.3 result, and confirmation page 1 is unchanged. VIS-01 ticks here (its only plan).
</output>
