---
phase: 04-page-2-restructure-github-presence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/main.tex
  - docs/verify/manifest.txt
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: true
requirements: [PG2-01, PG2-02, PG2-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low          # 0 calibration samples (estimate-calibration: sample_count 0)

must_haves:
  truths:
    - "D-06/D-07: Education emits institution-first with no GPAs and no IU city; G6.12 PASS with manifest EDU_INSTITUTION updated to 'Indiana University' in the SAME commit; both date ranges retained (master's 'Aug. 2021 – May 2023' still extracts, bachelor's 'Aug. 2011 – May 2015' present)."
    - "D-03: Publications is the first-author citation + venue links + Poster link + ONE fairness-aware-graph-ML contribution line; the abstract body paragraph is gone from source and text layer."
    - "D-04: Research/Independent Study renders as ONE \\resumeItem covering all three threads (IUNI debiasing study, paid RA 'User Intent as a Network', NLP-Lab TieML / fine-tuned-LLM work), key hyperlinks kept, per-item semester stamps dropped."
    - "D-05 (owner override): the Teaching section is byte-unchanged — three TA items retained; NOT compressed. Recorded as an intentional deviation from ROADMAP Phase-4 criterion 4."
    - "D-01/D-02: Selected Projects contains EXACTLY 'rollout' then 'graph_ml' (spike Variant A LaTeX verbatim); all seven 2017–2022 grad-school entries gone; residual2vec_ NOT re-featured; no unverified perf/throughput numbers added; the graph_ml 'C++/Cython' phrasing is present (Phase-5 PG2-06 anchor)."
    - "D-09: PDF is exactly 2 pages, page 2 still opens at PUBLICATIONS, and all four suites end green."
  artifacts:
    - docs/main.tex               # page-2 body restructured; page 1 byte-frozen
    - docs/verify/manifest.txt    # EDU_INSTITUTION updated (D-07)
    - docs/AshutoshTiwari.pdf     # rebuilt from source, rides in the source commit
  key_links:
    - "manifest EDU_INSTITUTION  <->  G6.12 institution-first clause (must be synced in the SAME commit as the D-06 Education source edit — D-07)"
    - "rollout/graph_ml \\href anchor text  <->  literal click-through URLs in the pdftotext layer (github.com/thunderock/rollout, github.com/thunderock/graph_ml)"
    - "page-1 freeze (docs/main.tex lines ~140–210 == commit 798c0fb)  <->  verify-phase2/phase3 nets staying green (they guard page 1, which this plan does not touch)"
---

<objective>
Restructure page 2 of the LaTeX résumé (`docs/main.tex`, everything behind the `\newpage`) so it leads with current, click-through-safe artifacts instead of seven grad-school entries. Four content edits — Education tighten (D-06) + manifest sync (D-07), Publication trim (D-03), Research compress (D-04), and the Projects 7→2 swap (D-01/D-02) — plus the owner-override decision to leave Teaching AS-IS (D-05). Page 1 (Experience, ~lines 140–210) is FINAL and byte-frozen at commit `798c0fb`; every edit here is at/after the `\newpage`/`\section{Publications}` boundary or in `docs/verify/manifest.txt` (D-09).

Purpose: this plan satisfies PG2-01 (Projects), PG2-02 (Publication) and PG2-03 (Education + Research + Teaching-unchanged). It leads with a tracer — the one edit that touches a gate surface (Education ↔ manifest ↔ G6.12) — proven end-to-end green before the lower-risk pure-source edits expand out from it.

Output: a rebuilt 2-page PDF (page 2 opening at PUBLICATIONS) whose page 2 carries the trimmed Publications, tightened Education, one-line Research, unchanged Teaching, and the two featured projects — with all four verification suites green and page 1 byte-identical to `798c0fb`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-2-restructure-github-presence/04-CONTEXT.md

# Ready-to-drop-in Projects LaTeX + verified facts + "what to avoid" (D-01/D-02):
@.opencode/skills/spike-findings-thunderock-github-io/references/projects-section.md
</context>

<constraints>
STANDING CONSTRAINTS (from STATE.md Decisions + 04-CONTEXT.md — NON-NEGOTIABLE):

1. **PAGE 1 IS FROZEN.** `docs/main.tex` lines ~140–210 (Work Experience) are byte-identical to commit `798c0fb`. Every edit in this plan is at/after the `\newpage`/`\section{Publications}` boundary, or in `docs/verify/manifest.txt`. GATE: `git diff 798c0fb -- docs/main.tex` must show changed hunks ONLY at line numbers ≥ the `\newpage` line. A hunk touching a page-1 Experience line is a WRONG edit — back it out.
2. **Resolve every `docs/main.tex` line reference by CONTENT (section/anchor strings), never by a stored line number.** Prior waves shifted page-2 lines repeatedly; the numbers in `<read_first>` are today-only orientation, not addresses.
3. **Single-file LaTeX** — no `\input`/`\include`. Regenerated artifacts (`.pdf`/`.log`/`.fdb_latexmk`/`.aux`/`.fls`/`.out`) ride IN the same commit as the source edit. **NEVER run `make clean`** (it deletes the `.fdb_latexmk` md5 G0.3 reads → a fresh checkout would refuse at exit 2).
4. **The oracle is `bash scripts/verify-resume.sh` (exit 0) run directly.** `make verify` collapses exit 1/2 (GNU Make 3.81) and is corroboration only. Every task ends with ALL FOUR suites green:
   - `bash scripts/verify-resume.sh` → exit 0, 0 BLOCKER FAILs
   - `bash scripts/verify-phase2-regressions.sh` → exit 0, 27 anchored `^RESULT P2\.[0-9]+ +PASS`
   - `bash scripts/verify-phase3-regressions.sh` → exit 0, 72 anchored `^RESULT P3\.[0-9]+ +PASS`
   - `make verify-selftest` → exit 0, ten G7/G8 PASS, zero anchored SKIP
5. **Do NOT touch the P2/P3 nets, `scripts/verify-resume.sh`, or `docs/verify/baseline-frozen.txt`** — they guard the frozen page 1. The only gate-surface edit permitted this plan is the single `EDU_INSTITUTION` value in `docs/verify/manifest.txt` (D-07). Do NOT touch G6.12's assertion logic.
6. **Do NOT edit the `\newcommand{\resumeProjectHeading}` macro definition** in the preamble — `\resumeProjectHeading` occurs 8× doc-wide (7 project uses + 1 definition); the Projects edit removes uses only.
</constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Education tighten (D-06) + manifest EDU_INSTITUTION sync (D-07) — the gate-coupled end-to-end slice</name>
  <files>docs/main.tex, docs/verify/manifest.txt, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk, docs/AshutoshTiwari.aux, docs/AshutoshTiwari.fls, docs/AshutoshTiwari.out</files>
  <read_first>
    - docs/main.tex — the `\section{Education}` body (the two `\resumeSubheading` blocks; today ~lines 234–239, resolve by the `\section{Education}` anchor). Arg #1 = institution (must extract first for G6.12), arg #3 = degree cell (carries a trailing GPA fraction today), arg #4 = date range.
    - docs/verify/manifest.txt — the `EDU_INSTITUTION`, `EDU_DATE`, `EDU_HEADING`, `EDU_BIND_WINDOW` keys.
    - scripts/verify-resume.sh — the G6.12 block (reads the EDU_* manifest keys; the institution must BE the first non-empty extracted line in the EDU region). Read to understand the assertion; do NOT edit it.
  </read_first>
  <reversibility rating="reversible">Content + one manifest value; fully git-revertible. Per D-07 this is a needle-literal update FOLLOWING the document, not gate-weakening.</reversibility>
  <action>
Make the Education section institution-first, GPA-free, city-free on Indiana University, dates retained (D-06), and sync the manifest in the SAME commit (D-07).

In the `\section{Education}` body of docs/main.tex, in the FIRST `\resumeSubheading`:
  - In the institution cell (arg #1), remove the city qualifier so the cell reads exactly `Indiana University`.
  - In the degree cell (arg #3), remove the trailing GPA fraction so the cell ends at `Computational Data Science` (nothing after it).
  - Leave the date cell (arg #4) exactly as written — the master's range must survive verbatim (EXP-01/G6.12 extract it).
In the SECOND `\resumeSubheading` (National Institute of Technology):
  - Leave arg #1 exactly as written (no city today per D-06).
  - In the degree cell (arg #3), remove the trailing GPA fraction so it ends at `Computer Science`.
  - Leave the date cell (arg #4) exactly as written.

In docs/verify/manifest.txt: change the `EDU_INSTITUTION` value to `Indiana University` (drop the city qualifier). Leave `EDU_DATE` exactly as written (it carries the U+2013 EN DASH form the PDF renders from the source `--`). Leave `EDU_BIND_WINDOW` and `EDU_HEADING` untouched. This is a needle-literal update following the document (D-07) — do NOT change any G6.12 logic in scripts/verify-resume.sh.

Rebuild with `make build` (NEVER `make clean`) and stage the source, the manifest, and the regenerated `.pdf`/`.log`/`.fdb_latexmk`/`.aux`/`.fls`/`.out` together. Then run the full four-suite gate. This is the tracer: it exercises source → gate-surface → build → all-four-suites end to end; do not proceed to Task 2 until it is green.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh; echo "resume exit=$?"</automated>
    <automated>bash scripts/verify-phase2-regressions.sh | grep -c '^RESULT P2\.[0-9]\+ \+PASS'   # expect 27</automated>
    <automated>bash scripts/verify-phase3-regressions.sh | grep -c '^RESULT P3\.[0-9]\+ \+PASS'   # expect 72</automated>
    <automated>make verify-selftest; echo "selftest exit=$?"   # expect exit 0, ten G7/G8 PASS, 0 anchored SKIP</automated>
    <automated>pdftotext docs/AshutoshTiwari.pdf - | tr '\f' '\n' | grep -c -F '3.87/4.0'    # expect 0 (GPA dropped)</automated>
    <automated>pdftotext docs/AshutoshTiwari.pdf - | tr '\f' '\n' | grep -c -F '8.32/10.0'   # expect 0 (GPA dropped)</automated>
    <automated>git diff 798c0fb -- docs/main.tex | grep -E '^@@' </automated>   <!-- inspect: every hunk must start at a line >= the \newpage line; none in the page-1 Experience block -->
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-resume.sh` exits 0 with 0 BLOCKER FAILs and G6.12 PASS (message: institution line is the first non-empty line in the EDU region; date within window).
    - The manifest `EDU_INSTITUTION` value equals `Indiana University` (city qualifier gone); `EDU_DATE` byte-unchanged; the source edit and the manifest edit are in ONE commit.
    - Text layer: `3.87/4.0` count 0 and `8.32/10.0` count 0 (both GPAs dropped); `Indiana University, Bloomington` count 0 (city dropped); `Indiana University` present; `Aug. 2021 – May 2023` still extracts exactly once (master's dates retained); `Aug. 2011 – May 2015` present (bachelor's dates retained).
    - P2 net 27 anchored PASS, P3 net 72 anchored PASS, `make verify-selftest` exit 0 with ten G7/G8 PASS and zero anchored SKIP.
    - `git diff 798c0fb -- docs/main.tex` shows hunks only at/after the `\newpage`/`\section{Publications}` boundary — no page-1 Experience line changed.
    - `make clean` was never run; the committed `.fdb_latexmk` still resolves G0.3 (fresh-checkout FRESH).
  </acceptance_criteria>
  <done>The Education section is institution-first, GPA-free, city-free on IU, dates retained; the manifest is synced in the same commit; the PDF rebuilt to 2 pages; and all four suites are green end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Publication trim (D-03) + Research one-line compress (D-04)</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk, docs/AshutoshTiwari.aux, docs/AshutoshTiwari.fls, docs/AshutoshTiwari.out</files>
  <read_first>
    - docs/main.tex — the `\section{Publications}` block (the `\begin{thebibliography}` / `\bibitem` / Poster `\href` / `\end{thebibliography}`, plus the plain body paragraph after the Poster line that is the abstract) and the `\section{Research Experience / Independent Study}` block (its `\resumeItemListStart` … three `\resumeItem` … `\resumeItemListEnd`). Resolve both by their `\section{...}` anchors.
  </read_first>
  <reversibility rating="reversible">Prose compressions on page 2; fully git-revertible. Final one-line wording is Claude's discretion within the D-03/D-04 framings.</reversibility>
  <action>
Two independent page-2 prose trims, both of which shrink the page (no fit risk — D-09).

PUBLICATION (D-03): Keep the venue line ("Accepted at NetSci 2023 … and IC2S2 2023 … as first author"), the `\bibitem` citation, the venue `\href` links, and the Poster `\href` link exactly as written. DELETE the abstract — the plain body paragraph that sits immediately after the Poster line and before `\end{thebibliography}` (the multi-sentence paragraph describing the framework and evaluations). In its place put ONE contribution line framed as fairness-aware graph ML (NOT the literal paper title), reusing only claims already present in the cut abstract (no new numbers, no perf claims). Land exactly this line:

  `A non-parametric contrastive framework for fairness-aware graph representation learning: it debiases GNN embeddings with respect to sensitive node attributes and structural homophily for more organic recommendations.`

Leave the two already-commented lines (the `% \textbullet …` ICWSM line and the `% \resumeItem …` IC2S2 line) commented and untouched.

RESEARCH (D-04): Replace the three `\resumeItem` entries inside the `\section{Research Experience / Independent Study}` list with ONE `\resumeItem` covering all three threads, keeping the IUNI, NLP Lab and TieML hyperlinks and dropping every per-item semester stamp (the `\hfill Summer/Fall …` clauses). Land exactly this list body:

      \resumeItem {Debiasing independent study at \href{https://iuni.iu.edu/}{\underline{IUNI}} (Profs. YY Ahn \& S Kojaku) on fair recommendation from biased graphs; paid RA on \textbf{``User Intent as a Network''} (Kelly Business School); \href{https://nlp-lab.org/}{\underline{NLP Lab}}@IUB with Prof. D Cavar on \href{https://github.com/dcavar/tieml}{\underline{TieML}} and event-timeline modelling with fine-tuned LLMs.}

Keep the surrounding `\resumeItemListStart` / `\resumeItemListEnd` and the `\section{...}` heading. Do not introduce any glyph outside the manifest NONASCII_ALLOW set, the prohibited word `Go`, or the prohibited phrase `speculative decoding`. Rebuild with `make build` and stage source + artifacts together. Run the full four-suite gate.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh; echo "resume exit=$?"</automated>
    <automated>bash scripts/verify-phase2-regressions.sh | grep -c '^RESULT P2\.[0-9]\+ \+PASS'   # expect 27</automated>
    <automated>bash scripts/verify-phase3-regressions.sh | grep -c '^RESULT P3\.[0-9]\+ \+PASS'   # expect 72</automated>
    <automated>make verify-selftest; echo "selftest exit=$?"</automated>
    <automated>A=$(pdftotext docs/AshutoshTiwari.pdf - | tr -s ' \t\f\n' ' '); echo "$A" | grep -c -F 'In this work, we propose'    # expect 0 (abstract cut)</automated>
    <automated>A=$(pdftotext docs/AshutoshTiwari.pdf - | tr -s ' \t\f\n' ' '); echo "$A" | grep -c -F 'fairness-aware graph representation learning'  # expect >=1</automated>
    <automated>A=$(pdftotext docs/AshutoshTiwari.pdf - | tr -s ' \t\f\n' ' '); for n in 'first author' 'NetSci 2023' 'IC2S2 2023' 'Poster' 'IUNI' 'User Intent as a Network' 'TieML'; do printf '%s: ' "$n"; echo "$A" | grep -c -F "$n"; done  # each >=1</automated>
  </verify>
  <acceptance_criteria>
    - Publications: the abstract paragraph is gone from source and text layer (squeezed text layer count of `In this work, we propose` == 0); the first-author citation, both venue links, and the Poster link are intact (`first author`, `NetSci 2023`, `IC2S2 2023`, `Poster` each ≥1 in the text layer); the fairness-aware contribution line is present (`fairness-aware graph representation learning` ≥1 in the squeezed text layer).
    - Research: exactly ONE `\resumeItem` in the Research list; all three thread anchors present in the text layer (`IUNI`, `User Intent as a Network`, `TieML` each ≥1); per-item semester stamps dropped.
    - No new glyph outside NONASCII_ALLOW; G6.4 stays PASS/WARN; prohibited `Go` and `speculative decoding` absent (harness G6 stays green).
    - All four suites green; PDF still 2 pages; page 2 still opens at PUBLICATIONS.
    - `git diff 798c0fb -- docs/main.tex` still page-2-only.
  </acceptance_criteria>
  <done>Publications is citation + venue/Poster links + one fairness-aware contribution line (abstract gone); Research is one line covering all three threads; all four suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Selected Projects 7→2 swap — rollout + graph_ml, spike Variant A verbatim (D-01/D-02)</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk, docs/AshutoshTiwari.aux, docs/AshutoshTiwari.fls, docs/AshutoshTiwari.out</files>
  <read_first>
    - docs/main.tex — the `\section{Selected Projects}` body, everything between `\resumeSubHeadingListStart` and `\resumeSubHeadingListEnd` (today the seven grad-school `\resumeProjectHeading` entries; resolve by the `\section{Selected Projects}` anchor). Note the `\newcommand{\resumeProjectHeading}` DEFINITION lives in the preamble and must NOT be touched.
    - .opencode/skills/spike-findings-thunderock-github-io/references/projects-section.md — the ready-to-drop-in `rollout` + `graph_ml` LaTeX under "How to Build It", the verified facts backing each bullet, and the "What to Avoid" list (do NOT re-feature residual2vec_; add no unverified perf numbers).
  </read_first>
  <reversibility rating="reversible">Page-2 content swap; fully git-revertible. rollout wording was owner-confirmed to survive "walk me through it" (GH-03, resume side).</reversibility>
  <action>
Replace ALL current `\resumeProjectHeading` entries in the Selected Projects section (the seven 2017–2022 grad-school projects — see `<read_first>`) with EXACTLY two entries, in this order, taken VERBATIM from the spike `projects-section.md` "How to Build It" block (D-01). Keep the surrounding `\resumeSubHeadingListStart` / `\resumeSubHeadingListEnd` wrappers and the `\section{Selected Projects}` heading; do NOT touch the `\newcommand{\resumeProjectHeading}` definition in the preamble. Land exactly:

\resumeProjectHeading
    {\textbf{rollout} $|$ \emph{Rust, Distributed Training \& Inference, RL for LLMs, vLLM, PyO3, Postgres}}{\href{https://github.com/thunderock/rollout}{\underline{github.com/thunderock/rollout}}}
    \resumeItemListStart
      \resumeItem{Multi-node reinforcement-learning framework for LLMs in Rust (18 crates, 197 test files, CI): PPO/GRPO/DPO/SFT/RM algorithms, vLLM-backed batch + online inference, actor/learner split with work-stealing, training-state and CRIU process snapshots, PyO3 in-process plugins.}
    \resumeItemListEnd
\resumeProjectHeading
    {\textbf{graph\_ml} $|$ \emph{Python, PyTorch Geometric, C++/Cython}}{\href{https://github.com/thunderock/graph_ml}{\underline{github.com/thunderock/graph_ml}}}
    \resumeItemListStart
      \resumeItem{sklearn-style Graph-ML library on PyTorch/PyTorch Geometric with optimized C++/Cython random-walk kernels; CI-tested.}
    \resumeItemListEnd

Per D-02: only these two public repos are featured, claims come from the verified tree facts in the spike (not README self-descriptions), residual2vec_ is NOT re-featured (it stays linked only from Publications), and NO unverified perf/throughput numbers are added — shipping the spike block verbatim satisfies all three. Do NOT delete or alter the preamble macro. Rebuild with `make build` and stage source + artifacts together. Run the full four-suite gate.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh; echo "resume exit=$?"</automated>
    <automated>bash scripts/verify-phase2-regressions.sh | grep -c '^RESULT P2\.[0-9]\+ \+PASS'   # expect 27 (bare C++ still >=1: NetSpeed p1 + Skills p2 + new graph_ml C++/Cython p2)</automated>
    <automated>bash scripts/verify-phase3-regressions.sh | grep -c '^RESULT P3\.[0-9]\+ \+PASS'   # expect 72</automated>
    <automated>make verify-selftest; echo "selftest exit=$?"</automated>
    <automated>G=$(pdftotext docs/AshutoshTiwari.pdf - | tr '\f' '\n'); S=$(printf '%s' "$G" | tr -s ' \t\n' ' '); for n in 'rollout' 'graph_ml' 'github.com/thunderock/rollout' 'github.com/thunderock/graph_ml'; do printf 'present %s: ' "$n"; printf '%s' "$G" | grep -c -F "$n"; done; printf 'present C++/Cython: '; printf '%s' "$S" | grep -c -F 'C++/Cython'</automated>
    <automated>G=$(pdftotext docs/AshutoshTiwari.pdf - | tr '\f' '\n'); for n in 'BiasNet' 'DeepFoodie' 'BlindNet' 'Continuous Dominant Set' 'Humana' 'AnalyticsVidhya' 'Bias Manifolds'; do printf 'absent %s: ' "$n"; printf '%s' "$G" | grep -c -F "$n"; done  # each expect 0</automated>
    <automated>grep -c -F '\resumeProjectHeading' docs/main.tex   # expect 3 (1 preamble definition + rollout + graph_ml)</automated>
  </verify>
  <acceptance_criteria>
    - Text layer contains `rollout`, `graph_ml`, `github.com/thunderock/rollout`, `github.com/thunderock/graph_ml` (each ≥1) and `C++/Cython` (squeezed ≥1, the Phase-5 PG2-06 anchor).
    - Text layer contains 0 occurrences of each retired name: `BiasNet`, `DeepFoodie`, `BlindNet`, `Continuous Dominant Set`, `Humana`, `AnalyticsVidhya`, `Bias Manifolds`.
    - Source doc-wide `\resumeProjectHeading` count == 3 (preamble definition + the two featured entries) — i.e. exactly two project uses; the preamble macro definition is untouched.
    - The two entries are byte-verbatim the spike Variant A block; no perf/throughput number added; residual2vec_ not featured as a project.
    - All four suites green; PDF still 2 pages; page 2 still opens at PUBLICATIONS; `git diff 798c0fb -- docs/main.tex` still page-2-only.
  </acceptance_criteria>
  <done>Selected Projects is exactly rollout + graph_ml (verbatim), all seven grad entries gone, click-through URLs literal in the text layer, and all four suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source edit → frozen page 1 | The one boundary that matters: a page-2 edit must never mutate the byte-frozen page-1 Experience block (commit 798c0fb) or the honesty freeze. |
| source content → shipped claim | New/kept résumé text must be interview-defensible (verified tree facts only) — no invented perf numbers, no README self-descriptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | docs/main.tex page-1 lines ~140–210 | high | mitigate | Every task's acceptance criteria run `git diff 798c0fb -- docs/main.tex` and require all hunks at/after the `\newpage` boundary; a page-1 hunk = back out. |
| T-04-02 | Tampering | gate surfaces (G6.12 logic, P2/P3 nets, baseline-frozen.txt) | high | mitigate | Only ONE manifest VALUE (`EDU_INSTITUTION`) may change (D-07); assertion logic is off-limits. All four suites must stay green — a gate weakened to fit content is a WRONG edit. |
| T-04-03 | Repudiation | Projects/Publication claims | medium | mitigate | rollout/graph_ml bullets are spike-verified tree facts shipped verbatim (D-02); the Publication contribution line reuses only claims from the cut abstract — no new numbers. |
| T-04-SC | Tampering | package installs (npm/pip/cargo) | n/a | accept | This phase installs NO packages and adds no dependency; the build uses the existing TeX/poppler toolchain. No package-legitimacy gate applies. |
</threat_model>

<verification>
Whole-plan gate (run directly — `make verify` is corroboration only):
1. `bash scripts/verify-resume.sh` → exit 0, 0 BLOCKER FAILs, G6.12 PASS, G1.1 PASS (2 pages), G2.1 PASS (page 2 opens at PUBLICATIONS).
2. `bash scripts/verify-phase2-regressions.sh` → exit 0, 27 anchored `^RESULT P2\.[0-9]+ +PASS`.
3. `bash scripts/verify-phase3-regressions.sh` → exit 0, 72 anchored `^RESULT P3\.[0-9]+ +PASS`.
4. `make verify-selftest` → exit 0, ten G7/G8 PASS, zero anchored SKIP.
5. `git diff 798c0fb -- docs/main.tex` → hunks only at/after the `\newpage` boundary (page 1 byte-frozen).
6. A fresh checkout of HEAD verifies FRESH (G0.3) — `make clean` never run; artifacts ride in each source commit.
</verification>

<success_criteria>
- PG2-01: Selected Projects = exactly rollout + graph_ml (spike Variant A verbatim); all seven grad-school entries gone; residual2vec_ not re-featured; C++/Cython phrasing present.
- PG2-02: Publications = first-author citation + venue links + Poster link + one fairness-aware-graph-ML contribution line; abstract paragraph gone.
- PG2-03: Education institution-first, no GPAs, no IU city, both date ranges retained, manifest EDU_INSTITUTION synced (D-07), G6.12 green; Research one line (all three threads); Teaching byte-unchanged (D-05 owner override, recorded as an intentional deviation from ROADMAP criterion 4).
- All four suites green; page 1 byte-frozen; PDF 2 pages opening at PUBLICATIONS.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
- `docs/main.tex` — page-2 body restructured (Publications trimmed, Education tightened, Research one-lined, Projects swapped to rollout+graph_ml); page 1 byte-frozen.
- `docs/verify/manifest.txt` — `EDU_INSTITUTION` value changed to `Indiana University` (D-07). No new manifest KEY; no assertion-logic change.
- `docs/AshutoshTiwari.pdf` (+ `.log`/`.fdb_latexmk`/`.aux`/`.fls`/`.out`) — regenerated, riding in the source commits.
- No new script/gate/manifest-key. (The page-2 content regression net `scripts/verify-phase4-regressions.sh` is produced by Plan 04-02.)
</artifacts_produced>

<output>
Create `.planning/phases/04-page-2-restructure-github-presence/04-01-SUMMARY.md` when done. Record: the measured page-2 headroom walk (G3.2 p2 before/after), G6.12 PASS message, the four-suite green evidence with anchored counts, the `git diff 798c0fb` page-2-only proof, and the D-05 Teaching-unchanged decision as an INTENTIONAL deviation from ROADMAP Phase-4 criterion 4 (not an oversight). Leave PG2-01/02/03 mark-complete to this plan (it is the last/only claiming plan for them) OR per project convention record them satisfied here and note the Plan 02 net guards them.
</output>
