---
phase: 04-page-2-restructure-github-presence
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/verify-phase4-regressions.sh
  - Makefile
autonomous: true
requirements: [GH-01, GH-02, GH-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low          # 0 calibration samples (estimate-calibration: sample_count 0)

must_haves:
  truths:
    - "A standing page-2 content regression net (scripts/verify-phase4-regressions.sh) exists, guarding PG2-01/02/03 the way the P2/P3 nets guard page 1 — green on arrival against the Plan-01 PDF, exit 0, and each assertion class proven FAILABLE by a crafted --gate-text control."
    - "D-05 (owner override) is machine-guarded: the net asserts the Teaching section still carries its three TA items (NOT compressed) — the override is recorded, not merely prose."
    - "D-08: GH-01 (privatize random_walk) and GH-02 (fix rollout README) are recorded as explicit OWNER DEFERRALS with their ready commands and the accepted trade-off — NO GitHub mutation is performed this phase; GH-03 is recorded as SATISFIED on the résumé side (rollout wording owner-confirmed to survive 'walk me through it')."
    - "D-09: after the net lands and is wired into make, all four existing suites plus the new P4 net are green; the PDF is unchanged from Plan 01 (2 pages, page 2 opens at PUBLICATIONS); page 1 stays byte-frozen at 798c0fb."
  artifacts:
    - scripts/verify-phase4-regressions.sh   # NEW page-2 content net (P4.x assertions)
    - Makefile                               # REGRESS4 wired into verify / verify-regressions
  key_links:
    - "verify-phase4-regressions.sh  <->  the finalized page-2 text layer + source (asserts PG2-01/02/03 landed and stay landed; Phase 5 touches page 2 for Skills, so this guard has real future value)"
    - "GH-01/GH-02 deferral records  <->  04-CONTEXT.md Deferred Ideas (ready commands preserved for a later reactivation)"
---

<objective>
Add a standing page-2 content regression net and close the GitHub-presence requirements as owner decisions. Today nothing re-runnable guards page 2 — the P2/P3 nets guard the frozen page 1 only — yet Phase 5 will edit page 2 (Skills rebuild at `\section{Technical Skills}`). This plan creates `scripts/verify-phase4-regressions.sh` (P4.x), the page-2 twin of the P2/P3 nets, wired into `make`, following their exact idioms; and it records GH-01/GH-02 as owner deferrals (D-08) and GH-03 as satisfied on the résumé side.

Purpose: this plan satisfies GH-03 (documented confirmation) and records GH-01/GH-02 as explicit deferrals; it does NOT mutate GitHub. The P4 net re-asserts and future-guards the PG2-01/02/03 content that Plan 04-01 landed (it does not re-claim those requirements — 04-01 is their satisfier).

Output: a new content net (green on arrival, each assertion class proven failable), a Makefile wired to run it alongside P2/P3, and a recorded GitHub-presence decision set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-2-restructure-github-presence/04-CONTEXT.md

# Idioms to copy VERBATIM in the new net (emit/assert helpers, gate.txt + squeezed.txt
# streams, --gate-text control, exit vocab 0/1/2, bash 3.2.57 style, LC_ALL=C):
@scripts/verify-phase2-regressions.sh

# The Plan-01 SUMMARY (finalized page-2 measurements the net asserts against):
@.planning/phases/04-page-2-restructure-github-presence/04-01-SUMMARY.md
</context>

<constraints>
1. **This plan does NOT edit `docs/main.tex` and does NOT rebuild the PDF.** The net reads whatever PDF is on disk (the one Plan 04-01 committed). Page 1 stays byte-frozen at `798c0fb`; the net never writes under `docs/`.
2. **NO GitHub mutations.** GH-01/GH-02 are DEFERRED by owner decision (D-08). Do NOT run `gh api -X PATCH`, do NOT edit the rollout README. Record the deferrals with their ready commands (from 04-CONTEXT.md Deferred Ideas) so a later reactivation is turnkey.
3. **Copy the P2 net's idioms exactly** — same `emit`/`assert_present`/`assert_present_i`/`assert_absent` helpers, the mktemp `gate.txt` (form-feed→newline) + `squeezed.txt` (whitespace-squeezed) streams, the `--gate-text FILE` negative-control flag, the three-way exit vocabulary (0 all-pass / 1 regressed / 2 cannot-verify), bash-3.2.57 style (no assoc arrays, no `mapfile`, no `local`, `_`-prefixed temporaries), and `LC_ALL=C` on every grep. Multi-word literals match the SQUEEZED stream; whitespace-free tokens match the RAW gate stream.
4. **Standing rule: an assertion never observed failing is not a gate.** The net is green on arrival (content already landed in Plan 01), so each assertion CLASS (present / absent / count) MUST be proven failable by a crafted `--gate-text` fixture that makes it FAIL (exit 1), recorded in the SUMMARY — exactly the pattern the P2/P3 nets and the Phase-3 controls use.
5. **The gating invocation stays `bash scripts/verify-resume.sh && bash scripts/verify-phase2-regressions.sh && bash scripts/verify-phase3-regressions.sh && bash scripts/verify-phase4-regressions.sh` run directly** — `make verify` is corroboration only (GNU Make 3.81 collapses exit 1/2).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify-phase4-regressions.sh (page-2 content net) and wire it into make</name>
  <files>scripts/verify-phase4-regressions.sh, Makefile</files>
  <read_first>
    - scripts/verify-phase2-regressions.sh — copy its structure verbatim: header/usage, arg parse (`--pdf`/`--tex`/`--gate-text`/`-h`), `emit`/`die2`/`gcount`/`display_of`/`assert_present`/`assert_present_i`/`assert_absent` helpers, the mktemp scratch + gate.txt/squeezed.txt streams, the `>> PASS:` / `!! FAIL:` footer, and the `P2.` → rename to `P4.` sequence prefix.
    - Makefile — the `REGRESS`/`REGRESS3` variable block, the `verify` and `verify-regressions` targets (each `@bash $(REGRESS)` then `@bash $(REGRESS3)`), the `.PHONY` line, and the help comment for `verify-regressions`.
    - .planning/phases/04-page-2-restructure-github-presence/04-01-SUMMARY.md — the finalized page-2 literals/measurements the assertions pin to.
  </read_first>
  <action>
Create `scripts/verify-phase4-regressions.sh` as the page-2 content twin of the P2/P3 nets. Reuse the P2 net's helpers and two-stream setup verbatim (rename the sequence counter and RESULT prefix to `P4.`; add a small `assert_count <label> <needle> <file> <N>` helper — fixed-string count must equal exactly N, using `gcount -Fc`, reporting FAIL with the observed count on mismatch and the -1 sentinel as unmeasurable). It asserts CONTENT only (freshness is the harness's G0.3) and reads whatever artifact is on disk. Assertions, grouped and emitted in this stable order (needles are today's finalized page-2 strings; multi-word → squeezed stream, whitespace-free → raw gate stream):

PG2-01 (Projects):
  - present `rollout` (gate); present `graph_ml` (gate); present `github.com/thunderock/rollout` (gate); present `github.com/thunderock/graph_ml` (gate); present `C++/Cython` (squeezed — the Phase-5 PG2-06 anchor).
  - absent (gate) each retired name: `BiasNet`, `DeepFoodie`, `BlindNet`, `Continuous Dominant Set`, `Humana`, `AnalyticsVidhya`, `Bias Manifolds`.
  - absent (source `$TEX`) a representative subset to catch a commented-out resurrection: `BiasNet`, `Continuous Dominant Set`, `AnalyticsVidhya`.
  - source guard: `\resumeProjectHeading` occurs exactly 3× in `$TEX` (1 preamble definition + the two featured uses) → `assert_count` == 3.

PG2-02 (Publication):
  - present (gate): `first author`, `NetSci 2023`, `IC2S2 2023`, `Poster`.
  - present (squeezed): `fairness-aware graph representation learning` (the contribution line).
  - absent (squeezed AND source): the abstract's distinctive opening clause `In this work, we propose` (abstract cut).

PG2-03 (Research):
  - present: `IUNI` (gate); `User Intent as a Network` (squeezed); `TieML` (gate); `NLP Lab` (squeezed).

PG2-03 (Education):
  - absent (gate AND source): `3.87/4.0`, `8.32/10.0` (GPAs dropped).
  - absent (gate): `Indiana University, Bloomington` (city dropped); present (gate): `Indiana University`.
  - present (squeezed): `Aug. 2021` and `Aug. 2011` (both date ranges retained).

PG2-03 / D-05 (Teaching — owner override, UNCHANGED): this is the machine record that Teaching was deliberately NOT compressed —
  - `assert_count 'Teaching Assistant' "$GATE" 3` (exactly three TA items retained);
  - present (gate): `INFO-I 606` and `CSCI-B 555` (the distinctive course codes).

Then wire it into the Makefile: add `REGRESS4 := scripts/verify-phase4-regressions.sh`; add `@bash $(REGRESS4)` as the last net line of BOTH the `verify` and `verify-regressions` targets (after `@bash $(REGRESS3)`); update the `verify-regressions` help comment and the make-help line to mention the P4.x page-2 net; update the gating-invocation comment block to read `bash $(VERIFY) && bash $(REGRESS) && bash $(REGRESS3) && bash $(REGRESS4)`. `chmod +x` the new script. Do NOT edit the P2/P3 nets or `scripts/verify-resume.sh`.

Failability proof (standing rule): after the net is green on arrival, run at least one crafted `--gate-text` fixture per assertion class and confirm exit 1 — e.g. a fixture that reinserts a retired name (an `absent` flips FAIL), one that drops `rollout` (a `present` flips FAIL), one that reinserts the abstract opening clause (the abstract-absent flips FAIL), and one with a 4th `Teaching Assistant` line (the `assert_count` flips FAIL). These run over crafted scratch fixtures only; record each in the SUMMARY.
  </action>
  <verify>
    <automated>test -x scripts/verify-phase4-regressions.sh && bash -n scripts/verify-phase4-regressions.sh && echo "syntax ok"</automated>
    <automated>bash scripts/verify-phase4-regressions.sh; echo "p4 exit=$?"   # expect exit 0, green on arrival</automated>
    <automated>bash scripts/verify-phase4-regressions.sh | grep -c '^RESULT P4\.[0-9]\+ \+PASS'   # all assertions PASS, 0 FAIL</automated>
    <automated>bash scripts/verify-phase4-regressions.sh | grep -c '^RESULT P4\.[0-9]\+ \+FAIL'    # expect 0</automated>
    <automated>command -v shellcheck >/dev/null 2>&1 && shellcheck -f gcc scripts/verify-phase4-regressions.sh || echo "shellcheck not installed (optional)"</automated>
    <automated>printf 'BiasNet\nrollout\ngraph_ml\n' > /tmp/p4ctl.txt; bash scripts/verify-phase4-regressions.sh --gate-text /tmp/p4ctl.txt; echo "control exit=$? (expect 1: a retired name present flips an absent assertion)"</automated>
    <automated>make verify >/dev/null 2>&1; echo "make verify exit=$? (corroboration only)"; bash scripts/verify-resume.sh >/dev/null 2>&1; echo "resume exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase4-regressions.sh` is executable, passes `bash -n`, and (if shellcheck is installed) is `shellcheck -f gcc` clean.
    - A bare run exits 0 with ALL P4.x assertions PASS and 0 FAIL (green on arrival against the Plan-01 PDF).
    - The net copies the P2 idioms: `--gate-text` supported and prints the "does NOT certify the committed artifact" note; exit vocab 0/1/2; multi-word needles matched on the squeezed stream, whitespace-free tokens on the raw gate stream.
    - Each assertion CLASS (present / absent / count) is proven failable by a crafted `--gate-text` fixture returning exit 1 — recorded in the SUMMARY.
    - Makefile: `REGRESS4` defined and `@bash $(REGRESS4)` chained into both `verify` and `verify-regressions`; help + gating-invocation comment updated; `.PHONY` unchanged targets still valid.
    - The P2 net (27 PASS), P3 net (72 PASS), `scripts/verify-resume.sh` (exit 0) and `make verify-selftest` (ten G7/G8 PASS) are ALL still green and byte-unchanged — this plan touched no page-1 guard.
    - `git diff 798c0fb -- docs/main.tex` is empty (this plan does not touch docs/main.tex); the PDF is byte-identical to Plan 01's.
  </acceptance_criteria>
  <done>The page-2 content net exists, is green on arrival, is proven failable per class, is wired into make alongside P2/P3, and all pre-existing suites stay green with page 1 frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Record GH-01/GH-02 deferrals + GH-03 confirmation (D-08); final phase verification</name>
  <files>scripts/verify-phase4-regressions.sh</files>
  <read_first>
    - .planning/phases/04-page-2-restructure-github-presence/04-CONTEXT.md — the `<deferred>` block (GH-01 ready command, GH-02 ready fix) and D-08 (resume-only, no GitHub mutations; GH-03 satisfied on the résumé side).
    - .planning/REQUIREMENTS.md — the GH-01/GH-02/GH-03 texts and the traceability table.
    - .planning/ROADMAP.md — Phase-4 criteria 2 & 3 (deferred per D-08) and the Checkpoints table row for GH-03 (already satisfied — the rollout wording was owner-confirmed during discuss-phase, so NO checkpoint is presented here).
  </read_first>
  <action>
This is a documentation + final-verification task — NO code, NO GitHub mutation, NO rebuild.

Record the GitHub-presence decisions as explicit outcomes (not gaps), in the 04-02-SUMMARY.md and (per project convention) as STATE.md Decisions entries:
  - GH-01 — privatize `random_walk`: DEFERRED by owner (D-08). Rationale: `random_walk` is public but ~empty, not linked from the résumé (only rollout + graph_ml are), so leaving it public has no click-through impact; privatization was account-hygiene only. Ready command preserved verbatim from 04-CONTEXT.md Deferred Ideas for a later reactivation. NOT ticked — genuinely deferred.
  - GH-02 — fix `rollout` README stale status: DEFERRED by owner (D-08). Accepted, documented trade-off: the featured `github.com/thunderock/rollout` link keeps reading its stale "spec / pre-implementation" status. Ready fix preserved verbatim from 04-CONTEXT.md Deferred Ideas. NOT ticked — genuinely deferred.
  - GH-03 — rollout capability wording confirmed: SATISFIED on the résumé side. The rollout bullet wording was owner-confirmed to survive "walk me through it" during discuss-phase (04-CONTEXT.md D-01); it shipped verbatim in Plan 04-01 Task 3. Recorded as a documented confirmation (no checkpoint presented, since the confirmation already exists). Ticked.

Then run the whole-phase verification (all five suites) and confirm the phase is green end to end and page 1 is byte-frozen. Update REQUIREMENTS.md/ROADMAP traceability: mark GH-03 complete; leave GH-01/GH-02 as Deferred with a one-line D-08 note (do not silently tick them). Confirm PG2-01/02/03 are marked satisfied (by Plan 04-01, their claiming plan) and that the P4 net guards them.
  </action>
  <verify>
    <automated>bash scripts/verify-resume.sh >/dev/null 2>&1; echo "resume exit=$?"</automated>
    <automated>bash scripts/verify-phase2-regressions.sh | grep -c '^RESULT P2\.[0-9]\+ \+PASS'   # expect 27</automated>
    <automated>bash scripts/verify-phase3-regressions.sh | grep -c '^RESULT P3\.[0-9]\+ \+PASS'   # expect 72</automated>
    <automated>bash scripts/verify-phase4-regressions.sh | grep -c '^RESULT P4\.[0-9]\+ \+FAIL'    # expect 0</automated>
    <automated>make verify-selftest >/dev/null 2>&1; echo "selftest exit=$?"</automated>
    <automated>git diff 798c0fb -- docs/main.tex | wc -l    # non-empty (page-2 edits from Plan 01) but 'git diff 798c0fb -- docs/main.tex | grep -E "^@@"' hunks must all be at/after the \newpage boundary</automated>
    <automated>gh() { echo "GUARD: no gh calls this phase"; return 0; }; echo "GH mutations deferred (D-08) — none performed"</automated>
  </verify>
  <acceptance_criteria>
    - GH-01 and GH-02 are recorded as explicit owner DEFERRALS (D-08) with their ready commands/fixes preserved; NO GitHub API call or README edit was made this phase.
    - GH-03 is recorded as SATISFIED on the résumé side and ticked; GH-01/GH-02 remain Deferred (not ticked) with a D-08 note in REQUIREMENTS.md/ROADMAP.
    - All five suites green: `scripts/verify-resume.sh` exit 0; P2 27 PASS; P3 72 PASS; P4 0 FAIL; `make verify-selftest` exit 0 (ten G7/G8 PASS, 0 anchored SKIP).
    - `git diff 798c0fb -- docs/main.tex` hunks all at/after the `\newpage` boundary — page 1 byte-frozen.
    - The Deferred Ideas trade-off (recruiter clicking rollout reads a stale README status) is stated plainly in the SUMMARY as an accepted outcome.
  </acceptance_criteria>
  <done>GH-01/GH-02 recorded as owner deferrals with ready commands, GH-03 recorded satisfied + ticked, and the whole phase is green with page 1 frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new net → existing gates | The new P4 net must not weaken or perturb the page-1 guards (P2/P3 nets, verify-resume.sh, baseline-frozen.txt). |
| phase scope → GitHub account | GH-01/GH-02 are explicitly out of scope this phase; no credential is used and no repo is mutated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | scripts/verify-phase4-regressions.sh (green-on-arrival net) | medium | mitigate | Each assertion class proven failable by a crafted `--gate-text` fixture (exit 1) recorded in the SUMMARY — an assertion never observed failing is not a gate. |
| T-04-05 | Repudiation | GH-01/GH-02 deferral | low | accept | Deferrals recorded with ready commands + accepted trade-off (D-08); a later reader sees decisions, not gaps. Accepted: the featured rollout link reads a stale README status until GH-02 is reactivated. |
| T-04-06 | Elevation of Privilege | GitHub token / gh CLI | low | mitigate | No `gh`/token use this phase; the deferral records the per-command-token form for later, never an account switch. |
| T-04-SC | Tampering | package installs (npm/pip/cargo) | n/a | accept | No package installs this phase; no dependency added. No package-legitimacy gate applies. |
</threat_model>

<verification>
Whole-phase gate (run directly):
1. `bash scripts/verify-resume.sh` → exit 0, 0 BLOCKER FAILs.
2. `bash scripts/verify-phase2-regressions.sh` → 27 anchored PASS.
3. `bash scripts/verify-phase3-regressions.sh` → 72 anchored PASS.
4. `bash scripts/verify-phase4-regressions.sh` → exit 0, 0 FAIL, each class proven failable.
5. `make verify-selftest` → exit 0, ten G7/G8 PASS, 0 anchored SKIP.
6. `git diff 798c0fb -- docs/main.tex` hunks all at/after `\newpage` — page 1 byte-frozen.
7. No GitHub mutation performed (D-08).
</verification>

<success_criteria>
- GH-03 satisfied (rollout wording owner-confirmed, shipped verbatim in Plan 01) and ticked; GH-01/GH-02 recorded as explicit owner deferrals (D-08) with ready commands, not ticked.
- A standing page-2 content net exists, is green on arrival, is proven failable per class, and is wired into make alongside the P2/P3 nets.
- All five suites green; page 1 byte-frozen; PDF unchanged from Plan 01.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- `scripts/verify-phase4-regressions.sh` — NEW standing page-2 content regression net (`P4.x` assertions), guarding PG2-01/02/03 including the D-05 Teaching-unchanged override; `--gate-text` control support; exit vocab 0/1/2.
- `Makefile` — new `REGRESS4` variable; `@bash $(REGRESS4)` chained into `verify` and `verify-regressions`; help + gating-invocation comment updated to include the P4.x net.
- No new manifest key, no docs/ artifact change. (This plan does not rebuild the PDF.)
</artifacts_produced>

<output>
Create `.planning/phases/04-page-2-restructure-github-presence/04-02-SUMMARY.md` when done. Record: the P4 net assertion inventory + anchored PASS count, the per-class `--gate-text` failability controls (each exit 1), the make wiring, the five-suite green evidence, and the GH-01/GH-02 deferral + GH-03 confirmation as decisions (with the accepted stale-README trade-off stated plainly). Mark GH-03 complete in REQUIREMENTS.md; leave GH-01/GH-02 Deferred with a D-08 note.
</output>
