---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - docs/main.tex
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: true
requirements: [EXP-07]

must_haves:
  truths:
    - "D-09: Swiggy carries an adoption-count claim and an explicit failure-class closure, both built only from claim material already in those lines"
    - "D-10: Flipkart's ML Workflow Automation carries the closure story naming the error class it closed"
    - "D-11: Both edits are rendered-line neutral — the Swiggy and Flipkart regions have exactly the same rendered line counts before and after, so no line is freed and the G7.2 probe margin is untouched"
    - "The digit-bearing page-1 bullet count is still 8 or higher, and every required keyword still passes"
  artifacts:
    - path: "docs/main.tex"
      provides: "staff-signal Swiggy and Flipkart bullets, line-neutral"
      contains: "manual deploys"
  key_links:
    - from: "docs/main.tex"
      to: "docs/AshutoshTiwari.pdf"
      via: "make build; rendered region line counts re-derived from region boundaries"
      pattern: "AshutoshTiwari"
---

<objective>
Tighten the Swiggy and Flipkart bullets to staff-signal formulas (D-09/D-10) with **zero** net rendered-line change (D-11).

Purpose: ROADMAP criterion 4 requires each of those blocks to carry an adoption-count or failure-class-closure claim while the digit-bearing bullet count never drops below baseline. D-11 makes these edits line-neutral because only the Adobe block spends budget this phase — freeing a line here would raise page-1 slack and stop the +5 probe from overflowing, reddening `G7.2` at `--selftest`.

Output: both blocks rewritten in place, and the last remaining P3 FAIL driven to zero.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-05-SUMMARY.md
</context>

<measurement_loop>
Same as plans 03-04 and 03-05: `make build`, then `bash scripts/verify-resume.sh` (the ORACLE — never `make verify`), then `bash scripts/verify-phase2-regressions.sh`, then `bash scripts/verify-phase3-regressions.sh`. Read `G1.1` first, then `G2.1`, then `G3.2` p1, then the `G5.4` value, then `G6.5`, then `G6.4`. Never `make clean`.

**Line-neutrality is measured as RENDERED gate lines, not characters.** Character counts may move freely inside a line. Measured baselines before the Adobe rebuild: Swiggy region 11 rendered lines, Flipkart region 9. Note that RESEARCH's "Swiggy 8" figure is a counting error superseded by measurement, and that ABSOLUTE gate line numbers have shifted because the Adobe block grew — so derive each region from its boundaries (the employer subtitle line down to the next blank line) BEFORE editing and compare the same derivation after. Do not use fixed line numbers.

Because this plan targets a zero net delta, `G3.2` p1 must come out EQUAL to the figure `03-05-SUMMARY.md` recorded. A rise means a line was freed, which reddens `G7.2`.
</measurement_loop>

<tasks>

<task type="auto">
  <name>Task 1: Swiggy — adoption count on the first line, explicit failure-class closure, line-neutral</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - docs/main.tex :168-181 (the whole Swiggy block; :172 Online Inference Platform, :174 routing item, :175 Akka item, :177 Feature Store, :178 Forecasting, :179 DAQ)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md Section C (the per-bullet disposition, the no-new-claims constraint, and the measured rendered-line counts)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md D-09 and D-11
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Keyword Mechanics fragile-keyword map (`fault` ×1 lives in the exact bullet D-09 rewords; `Spark` has one page-1 carrier in the Feature Store bullet) and §Pitfall 5
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-01-PLAN.md `<locked_literals>` Class C (the six Swiggy retention needles)
  </read_first>
  <action>
Reword the Swiggy bullets in place for staff-signal density. Measure the region's rendered line count first, then edit, then confirm the identical count.

D-09 fixes what each bullet carries. `:172` Online Inference Platform keeps the adoption count `18M+` **on its first rendered line** — that placement is why `G5.4` can see it, since the regex matches only lines beginning with a bullet glyph and a digit on a continuation line is invisible to it. `:177` Feature Store keeps `4Bn rows` and `10K QPS` (note both sit on a continuation line and are therefore already invisible to `G5.4` — that is the live example of Pitfall 5 and is acceptable, because `:172` and `:179` carry the region's countable digits). The routing pair `:174`/`:175` carries the explicit failure-class closure: `fault isolation across clusters`, `backpressure-aware scheduling`, `sustained low P99`.

HARD CONSTRAINT — no new claims requiring new evidence. `CODEBASE-EVIDENCE.md` covers Adobe repos only, so there is no evidence source for a new Swiggy claim. Every edit here is a rewording of material already present in these six lines: platform adoption at 18M+ monthly transacting users, region-and-load-domain cluster partitioning, actor-framework resilience, fault isolation, backpressure-aware scheduling, sustained low P99 under high QPS, the feature-store scale figures, the forecasting founding-member framing, and DAQ's 15M rows daily. Do not invent a percentage, an incident count, a headcount, or a named outage.

PRESERVE: `fault` has exactly ONE carrier in the entire document and it is `fault isolation across clusters` on `:175` — dropping or rephrasing that string turns the BLOCKER keyword `fault` red. `Spark` has exactly one page-1 carrier, in `:177`'s multichannel-ingestion clause. Keep `18M+`, `4Bn rows`, `10K QPS`, `backpressure-aware scheduling` and `sustained low P99` as literal strings.

Do not touch `:169` or `:170` — title, dates, employer and subtitle are frozen.

Then `make build` and run the full measurement loop. The Swiggy region must render the same number of gate lines as before, and `G3.2` p1 must be UNCHANGED from the figure `03-05-SUMMARY.md` recorded. Commit source and artifacts together.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h06a.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h06a.txt && grep -q "required keyword present (case-sensitive): 'fault'" /tmp/h06a.txt && grep -q "required keyword present (case-sensitive): 'Spark'" /tmp/h06a.txt && grep -Eq 'RESULT G5\.4 .*: (8|9|1[0-9])' /tmp/h06a.txt && bash scripts/verify-phase2-regressions.sh >/dev/null && SC="$(mktemp -d)" && bash scripts/verify-phase3-regressions.sh | tee "$SC/p06a.txt" | grep -E 'EXP-07' | grep FAIL | grep -vc 'manual deploys' | grep -qx 0 && grep -cE 'PASS.*EXP-07' "$SC/p06a.txt" | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - the Swiggy region's rendered gate-line count is identical before and after, derived from region boundaries rather than fixed line numbers, and both counts are recorded in the task evidence
    - `G3.2` p1 headroom is byte-identical to the figure recorded in `03-05-SUMMARY.md` — no line freed, none spent
    - every Swiggy retention needle is PASS in `bash scripts/verify-phase3-regressions.sh`: `18M+`, `4Bn rows`, `10K QPS`, `fault isolation across clusters`, `backpressure-aware scheduling`, `sustained low P99`
    - the EXP-07 slice contains NO FAIL line other than the `manual deploys` needle (Task 2's job) — asserted by filtering the slice to FAIL lines FIRST and then requiring zero non-`manual deploys` survivors, plus a non-empty EXP-07 PASS slice. Never `grep -vc 'manual deploys'` over the unfiltered slice: that only proves some EXP-07 line is not the manual-deploys one and never inspects PASS/FAIL at all, so every retention needle could be red while this task reported green
    - `bash scripts/verify-resume.sh` exits 0 with `G6.5 PASS` for `'fault'` and for `'Spark'`
    - `18M+` appears on the Online Inference Platform bullet's FIRST rendered line
    - the `G5.4` message reads 8 or higher
    - `G1.1` 2 pages; `G2.1` PASS; `G6.4` PASS
    - `docs/main.tex:169` and `:170` are byte-identical to the previous commit
    - no new numeric claim appears that is absent from the six pre-edit Swiggy lines
    - `bash scripts/verify-phase2-regressions.sh` prints 27 anchored `^RESULT P2\..*PASS` lines (a green run emits 28 lines containing the word PASS — the `>> PASS:` summary is the 28th, so always count the anchored form)
    - the commit contains `docs/main.tex` with `.pdf`, `.log` and `.fdb_latexmk`
  </acceptance_criteria>
  <done>Swiggy carries the adoption count and an explicit failure-class closure at exactly the same rendered line count. Commit: `feat(03-06): sharpen the swiggy platform bullets to adoption and failure-closure formulas`.</done>
</task>

<task type="auto">
  <name>Task 2: Flipkart — the ML workflow closure story naming the error class closed, line-neutral</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - docs/main.tex :182-193 (the whole Flipkart block; :186 Search Intent Models, :187 Tail Query Classifier, :188 ML Workflow Automation, :190 Airflow framework item, :192 Large-Scale Data Pipelines)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md Section C (the Flipkart rows, the `manual deploys` entailment justification, and the measured 9-line count)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md D-10 and D-11
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-01-PLAN.md `<locked_literals>` (the `manual deploys` needle owned by this plan, plus the `first workflow at Flipkart`, `validations` and `millions every day` retention needles)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §2.4 (the Flipkart shapes; `---` extracts as U+2014 and is allowed)
  </read_first>
  <action>
Reword the Flipkart bullets in place. Measure the region's rendered line count first, then edit, then confirm the identical count.

D-10 puts the closure story on ML Workflow Automation (`:188`) and its nested Airflow item (`:190`): the first automated train-to-deploy workflow at Flipkart, plus the error class it closed — manual-deploy drift and missing data-and-model validations. The locked needle the P3 net asserts is `manual deploys`. That phrasing is licensed by the claim already in the line: a workflow that is the first to automate training and auto-deployment entails the deploys were manual before it. Do not extend past that entailment — no incident count, no failure rate, no duration, no named outage.

`:186` Search Intent Models keeps its adoption framing `millions every day`. Note that string extracts only from the whitespace-squeezed stream because it wraps at "every / day", so the P3 assertion for it is squeezed-only; a raw whole-line search returns 0 on a perfectly correct document.

PRESERVE as literal strings: `first workflow at Flipkart`, `validations`, `millions every day`, and the `4Bn+` figure on `:192`. Keep `train$\rightarrow$deploy` rendering as an allowed arrow, and keep `---` where it renders as U+2014.

Do not touch `:183` or `:184` — title, dates, employer and subtitle are frozen. Note the Flipkart employer line historically extracted AFTER its subtitle (a `pdftotext` row-order artifact measured as self-healing once Adobe grows by a rendered line); record the observed order but do not attempt to fix it here, and do not treat a change in it as caused by this edit.

Then `make build` and run the full measurement loop. The Flipkart region must render the same number of gate lines as before and `G3.2` p1 must be unchanged. This is the last content plan, so the P3 net must now exit **0**. Commit source and artifacts together.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h06b.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h06b.txt && grep -q 'RESULT G2.1   PASS' /tmp/h06b.txt && grep -Eq 'RESULT G5\.4 .*: (8|9|1[0-9])' /tmp/h06b.txt && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh && make verify-selftest >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-phase3-regressions.sh` exits **0** — every P3 assertion passes, the red-on-arrival census is fully discharged
    - the Flipkart region's rendered gate-line count is identical before and after, derived from region boundaries, both counts recorded
    - `G3.2` p1 headroom is identical to the figure recorded after Task 1 — nothing freed, nothing spent
    - the text layer contains `manual deploys`, `first workflow at Flipkart` and `validations`; the squeezed stream contains `millions every day`
    - no incident count, failure rate, duration or named outage was added
    - `bash scripts/verify-resume.sh` exits 0; `G1.1` 2 pages; `G2.1` PASS; `G6.4` PASS; every `G6.5` line PASS; `G5.4` reads 8 or higher
    - `docs/main.tex:183` and `:184` are byte-identical to the previous commit
    - `bash scripts/verify-phase2-regressions.sh` prints 27 anchored `^RESULT P2\..*PASS` lines (a green run emits 28 lines containing the word PASS — the `>> PASS:` summary is the 28th, so always count the anchored form)
    - `make verify-selftest` exits 0 with ten G7/G8 PASS and no SKIP — the wave does not end net-freed
    - the observed employer-vs-subtitle extraction order for all five role blocks is recorded in the task evidence
    - the commit contains `docs/main.tex` with `.pdf`, `.log` and `.fdb_latexmk`
  </acceptance_criteria>
  <done>Flipkart carries the closure story with the error class named, at the same rendered line count. The full P3 net is green and the document is ready for the EXP-08 checkpoint. Commit: `feat(03-06): carry the flipkart workflow closure story and the error class it closed`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/main.tex` → text layer | rewording can silently drop a single-carrier BLOCKER keyword or a countable digit |
| rendered line count → `G7.2` probe margin | freeing a rendered line raises page-1 slack until the +5 probe stops overflowing, disarming the anti-rot guard |
| claim material → new claim | Swiggy and Flipkart have no evidence file, so any addition beyond a rewording is unfalsifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-34 | Repudiation | `fault` (×1) and `Spark` (page-1 ×1) dropped by a reword | mitigate | both `G6.5` lines asserted PASS by name; the P3 retention group covers `fault isolation across clusters` |
| T-03-35 | Tampering | line freed instead of held | mitigate | region rendered-line counts derived from boundaries and asserted equal; `G3.2` p1 asserted unchanged; `make verify-selftest` proves `G7.2` still armed |
| T-03-36 | Repudiation | new unfalsifiable claim added | mitigate | no-new-claims constraint asserted as "no numeric claim absent from the pre-edit lines"; the `manual deploys` needle is justified by entailment in the decision record |
| T-03-37 | Repudiation | metric silently eaten by compression | mitigate | `G5.4` VALUE asserted at 8 or higher (not merely the absence of a FAIL, since it is warn-tier); `18M+` required on a first rendered line |
| T-03-38 | Tampering | frozen title/date/employer cells | mitigate | `:169`/`:170` and `:183`/`:184` asserted byte-identical against the previous commit; `baseline-frozen.txt` untouched |
| T-03-39 | Repudiation | employer-order artifact mistaken for a regression | mitigate | the order is measured and RECORDED for all five blocks, explicitly not claimed as fixed and not gated |
| T-03-SC | Tampering | package installs | mitigate | none — zero packages installed this phase |
</threat_model>

<verification>
Full measurement loop after each task, `G1.1` first. After Task 2 additionally `make verify-selftest` (exit 0, ten G7/G8 PASS, no SKIP) and `bash scripts/verify-phase3-regressions.sh` exit 0.

Record `make verify` exit 0 as ROADMAP-criterion-5 corroboration only; the oracle stays `bash scripts/verify-resume.sh`. Never `make clean`.
</verification>

<success_criteria>
- Swiggy carries `18M+` on a first rendered line plus the fault-isolation / backpressure / low-P99 closure; Flipkart carries `manual deploys` and `first workflow at Flipkart`
- Both regions are rendered-line neutral, derived from region boundaries; `G3.2` p1 unchanged across both tasks
- No new claim beyond a rewording of existing material; no invented metric
- `bash scripts/verify-phase3-regressions.sh` exits 0 — the whole P3 census discharged
- `bash scripts/verify-resume.sh` exit 0 with `G1.1` 2 pages, `G6.4` PASS, all `G6.5` PASS, `G5.4` ≥8
- `make verify-selftest` exit 0; frozen cells and `baseline-frozen.txt` untouched; artifacts committed with source
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-06-SUMMARY.md` when done. Record: the before/after rendered-line counts for both regions and how they were derived, the `G3.2` p1 figure (unchanged), the `G5.4` value, confirmation that the P3 net exits 0, the five-block employer-vs-subtitle extraction order, and whether the EXP-08 line reserve survived — plan 03-07's checkpoint context needs that number.
</output>
