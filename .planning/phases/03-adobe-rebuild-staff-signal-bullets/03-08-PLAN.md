---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 08
type: execute
wave: 6
depends_on: ["03-07"]
files_modified:
  - docs/main.tex
  - scripts/verify-phase3-regressions.sh
  - Makefile
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: true
requirements: [EXP-02, EXP-03, EXP-04, EXP-07, EXP-08]

must_haves:
  truths:
    - "Exactly the approved EXP-08 bullets are present and every rejected one is absent — both outcomes proven by an assertion, not by inspection"
    - "The new Phase-3 regression net is reachable from make, so this phase does not recreate the very defect it was told to close"
    - "The five-block employer-vs-subtitle extraction order is measured and recorded without being claimed as fixed"
    - "The whole suite is green at the end state and every Phase-3 requirement checkbox reflects reality"
  artifacts:
    - path: "scripts/verify-phase3-regressions.sh"
      provides: "EXP-08 assertions flipped to match the recorded verdicts"
      contains: "EXP-08"
    - path: "Makefile"
      provides: "the Phase-3 net invoked alongside the Phase-2 net"
      contains: "REGRESS3"
    - path: ".planning/STATE.md"
      provides: "end-state records — WR-01 measurement, budget end state, phase decisions"
      contains: "[Phase 3]"
  key_links:
    - from: "Makefile"
      to: "scripts/verify-phase3-regressions.sh"
      via: "REGRESS3 invoked by verify-regressions and chained into verify"
      pattern: "REGRESS3"
    - from: ".planning/STATE.md"
      to: "docs/main.tex"
      via: "the [Phase 3 / EXP-08] verdicts are the only authorization for the bullets present"
      pattern: "\\[Phase 3 / EXP-08\\]"
---

<objective>
Apply exactly the EXP-08 bullets the user approved (or record that none were), flip the P3 net's EXP-08 assertions to match the verdicts, make the new net reachable from `make`, and close the phase with a measured end state.

Purpose: ROADMAP criterion 5 requires `make verify` to pass with the rebuilt section in place and the cost/mentoring bullets either shipped with explicit recorded sign-off or absent — never present unapproved. The net this phase created must also be wired in, or Phase 3 recreates IG-01 for its own regression net.

Output: the phase's end state, verified, recorded, and reachable.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply exactly the approved bullets and flip the EXP-08 assertions to match the verdicts</name>
  <files>docs/main.tex, scripts/verify-phase3-regressions.sh, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - .planning/STATE.md (the `[Phase 3 / EXP-08]` marker — the ONLY authorization for what may be added, and the source of any revised wording)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-SUMMARY.md (the per-draft verdicts, the headroom at decision time, whether the sanctioned reword is required)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (the draft texts, their owning topics, and the contingency ladder)
    - scripts/verify-phase3-regressions.sh (the EXP-08 assertion group being flipped)
    - docs/main.tex :146-210 (the target blocks; :199 and :206 are the Groupon and NetSpeed descriptors if the sanctioned reword is required)
    - scripts/verify-phase2-regressions.sh :445-456 and :484-509 (the exact literals the reword must preserve and the two shortened forms it must not produce)
  </read_first>
  <action>
Read the `[Phase 3 / EXP-08]` marker first and treat it as the sole authorization. Ship exactly what it approved, in exactly the wording it records — for a revised draft use the recorded replacement text verbatim, never a reconstruction. Add nothing it did not approve.

BRANCH A — every draft rejected. Change no line of `docs/main.tex`. The EXP-08 assertions in the P3 net already assert absence and already PASS, so no script edit is needed either. This is a fully green outcome under D-14; record it as a decision rather than as an omission. Skip to the verification below and confirm nothing drifted.

BRANCH B — one or more approved. For each approved bullet: add it to the Adobe topic or block the decision record named, as a `\resumeItem` or `\resumeTopic` matching the shape already used there. Then flip that draft's assertions in `scripts/verify-phase3-regressions.sh` from `assert_absent` to `assert_present` — on `$SQUEEZED` for the multi-word needle, keeping the label's requirement-and-owner prefix and updating the owner tag to `/03-08`. Leave every REJECTED draft's `assert_absent` pair exactly as it is: those assertions are what prove the phase shipped only what was approved, and deleting them would erase the guarantee. Both outcomes must remain asserted, which is why the flip is per draft and not wholesale.

Budget: each approved bullet costs 1-2 rendered lines. Rebuild and read `G1.1` and `G3.2` after EVERY single bullet, not once at the end. If headroom will not cover the addition, apply the sanctioned Groupon/NetSpeed reword-shorter in the SAME commit as the addition so the net rendered-line delta is zero — that keeps page-1 slack from rising and leaves the `G7.2` probe margin untouched. The reword must preserve every literal the Phase-2 net asserts on those two descriptor lines, and must not produce either of the two forbidden shortened forms. Run the Phase-2 net immediately after any such reword; 27 PASS is the only acceptable result.

If a figure in an approved bullet needs to raise the digit-bearing count, put it on the bullet's FIRST rendered line — `G5.4`'s regex matches only lines beginning with a bullet glyph, so a digit on a continuation line is invisible to it.

Hard rails unchanged: never a fifth rendered line beyond the pre-phase baseline; `G3.2` p1 must never rise above +50.5pt; `PROBE_LINES` and `DIGIT_BULLET_FLOOR` are not edited; `docs/verify/baseline-frozen.txt` is untouched. Commit source, any script edit, and the regenerated artifacts together.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h08a.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h08a.txt && grep -Eq 'RESULT G5\.4 .*: (8|9|1[0-9])' /tmp/h08a.txt && grep -q 'RESULT G6.4   PASS' /tmp/h08a.txt && bash scripts/verify-phase2-regressions.sh | grep -c 'PASS' | grep -qx 27 && shellcheck -f gcc scripts/verify-phase3-regressions.sh && /bin/bash scripts/verify-phase3-regressions.sh</automated>
  </verify>
  <acceptance_criteria>
    - `/bin/bash scripts/verify-phase3-regressions.sh` exits 0 — the EXP-08 group asserts presence for every approved draft and absence for every rejected one
    - for every rejected draft, its needle returns 0 in both `docs/main.tex` and the extracted text layer
    - for every approved draft, its needle is present in the text layer and its wording matches the STATE.md record verbatim
    - no bullet is present that the `[Phase 3 / EXP-08]` marker did not approve
    - `bash scripts/verify-resume.sh` exits 0; `G1.1` 2 pages; `G2.1` PASS; `G6.4` PASS; every `G6.5` line PASS; `G5.4` reads 8 or higher
    - `G3.2` p1 recorded; it never rose above +50.5pt and the document never reached 3 pages at any intermediate build
    - `bash scripts/verify-phase2-regressions.sh` prints 27 PASS — including after any Groupon/NetSpeed reword
    - if a reword was applied, it and the addition are in ONE commit and the net rendered-line delta is zero
    - `git status --porcelain docs/verify/baseline-frozen.txt` is empty; `PROBE_LINES` and `DIGIT_BULLET_FLOOR` are byte-unchanged
    - `shellcheck -f gcc scripts/verify-phase3-regressions.sh` prints zero findings
  </acceptance_criteria>
  <done>The document carries exactly the approved bullets and nothing else, with both outcomes machine-asserted. Commit: `feat(03-08): ship the approved candidate bullets and assert the rejected ones absent` (Branch A: `test(03-08): confirm the candidate bullets remain absent per the recorded verdicts`).</done>
</task>

<task type="auto">
  <name>Task 2: Make the Phase-3 net reachable from make</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (the state plan 03-02 left it in — the `REGRESS` variable, the `verify-regressions` target, the `.PHONY` list, the header target index, and the chained `verify` recipe)
    - .planning/v1.0-MILESTONE-AUDIT.md :132-140 (IG-01's exact complaint: a regression net with zero invokers)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Harness Wiring (the recipe shape, the no-prerequisite rationale, and the rejected alternatives)
    - scripts/verify-phase3-regressions.sh (its contract: reads whatever artifact is on disk, never rebuilds, exits 2 when it cannot verify)
  </read_first>
  <action>
Plan 03-02 closed IG-01 for the Phase-2 net. Phase 3 then created a second net — and leaving it uninvoked would recreate the identical defect the audit told this phase to close, for the file this phase authored. Wire it now, at the end, when it is green: chaining a red net into the default target mid-phase would have made `make verify` red for the whole phase.

Add `REGRESS3 := scripts/verify-phase3-regressions.sh` beside the existing `REGRESS` variable. Add `@bash $(REGRESS3)` to the `verify-regressions` recipe, after the Phase-2 line, so one target runs both standing nets. Add the same line to the chained `verify` recipe, again after the Phase-2 line and before `@bash $(VERIFY)` — the content nets stay first because they are sub-second and content-only, so a content regression is reported before the slower page and geometry harness, and Make 3.81 aborts a recipe at its first failing line so a red harness cannot hide them.

Update the `verify-regressions` doc comment to say it runs both nets (P2.x and P3.x) and refresh its line in the header target index. No new `.PHONY` entry is needed — the target already exists.

Prohibitions unchanged: no `$(PDF)` prerequisite on `verify-regressions`, no `--gate-text` and no `--skip-freshness` in any recipe, no `-` prefix and no `|| true`. Do not widen the `make help` format string.
  </action>
  <verify>
    <automated>make verify-regressions >/dev/null && make -n verify-regressions | grep -c 'verify-phase[23]-regressions.sh' | grep -qx 2 && make -n verify | grep -c 'verify-phase[23]-regressions.sh' | grep -qx 2 && test "$(grep -c -- '--gate-text\|--skip-freshness' Makefile)" = 0 && make verify >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-regressions` exits 0 and prints both the 27 `P2.` PASS lines and the full `P3.` PASS set
    - `make -n verify-regressions` shows both net invocations; `make -n verify` shows both nets BEFORE `bash scripts/verify-resume.sh`
    - `grep -c -- '--gate-text\|--skip-freshness' Makefile` returns 0
    - `grep -c 'REGRESS3' Makefile` returns 2 or more (variable plus at least two recipe uses)
    - the header target index and the target's doc comment both state that it runs the P2.x and P3.x nets
    - `make verify` exits 0 — ROADMAP criterion 5's corroboration, recorded as corroboration and not as the oracle
    - `make verify-selftest` exits 0 with ten G7/G8 PASS and no SKIP
    - `git diff --stat Makefile` shows no change outside the variable block, the verification section and the header index
  </acceptance_criteria>
  <done>Both standing nets are reachable from `make`; Phase 3 did not recreate IG-01 for its own net. Commit: `build(03-08): run both standing content nets from the regression target`.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the end state, record the phase decisions, and update the requirement checkboxes</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (append to `### Decisions`; confirm the `[Phase 3 / D-08]`, `[Phase 3 / EXP-08]` and `index.html:196` records from earlier plans are all present)
    - .planning/ROADMAP.md (the Phase 3 section — its five success criteria and its `**Plans:**` line and plan list)
    - .planning/REQUIREMENTS.md (the EXP-02/03/04/07/08 checkboxes and the Traceability table rows)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Code Examples E-4 (the five-block employer-vs-subtitle measurement) and §Open Questions OQ-4
    - .planning/v1.0-MILESTONE-AUDIT.md :158-164 (WR-01 / IG-03 / IG-04 wording, and the WARN tier owned forward)
  </read_first>
  <action>
MEASURE the end state before writing anything. Run the full suite one final time — `make build`, `bash scripts/verify-resume.sh`, `bash scripts/verify-phase2-regressions.sh`, `bash scripts/verify-phase3-regressions.sh`, `make verify-selftest`, plus `make verify` as corroboration. Then run the five-block employer-versus-subtitle extraction-order measurement over the final PDF, covering all five Work-Experience blocks.

RECORD in `.planning/STATE.md` `### Decisions`, as `[Phase 3]`-marked bullets in the established statement-plus-rationale style:

1. The measured end-state budget: the final `G3.2` p1 headroom in pt and lines, the total rendered lines the Adobe block gained, and the derived `G7.2` margin computed as 57.5pt minus the final p1 slack. State that spending grew the margin and that `PROBE_LINES` was deliberately not raised — it is raise-only and the end state does not leave more than 57.5pt of slack. Correct any prediction this phase superseded by measurement, in the same way Phase 2 recorded its own superseded figures.
2. The WR-01 measurement, per OQ-4's document-and-measure recommendation: the employer-versus-subtitle extraction order for all five blocks as measured at the end state. Say plainly that the ordering is a `pdftotext` row-position artifact rather than a property, that it was measured and not fixed, and that adding an employer-first BLOCKER assertion is a Phase-4 candidate because a future cosmetic y-shift could otherwise redden the gate on nothing. Note `G6.13` remains a WARN owned by Phase 3 and that `ROLE_BIND_WINDOW` stays a local constant.
3. Any correction this phase discovered in an upstream artifact. At minimum: the Swiggy rendered-line count measured for D-11's line-neutrality differs from the figure in `03-RESEARCH.md`, and the measurement supersedes the document. Also record the `G6.6` finding worth carrying forward — the target-keyword WARN fires unconditionally with no branch on the count, so promotion has no machine backing outside the new P3 net's promotion triple.
4. If `Triton` was promoted, record that Phase 3 landed a Phase-5-owned `KEYWORDS_TARGET` entry early, that ownership is a WARN-routing label rather than a permission, and that PG2-04's Skills-tier form remains additive rather than a duplicate obligation. Also record that `CUDA graphs` was deliberately left unpromoted on both D-08 branches.

UPDATE `.planning/ROADMAP.md`: tick the Phase 3 entry in the phase list with its completion date, set the Phase 3 `**Plans:**` line to the real count, and list all eight plan files with their one-line objectives and ticked boxes. Do not alter the five success criteria; they are the record of what was promised.

UPDATE `.planning/REQUIREMENTS.md`: tick EXP-02, EXP-03, EXP-04, EXP-07 and EXP-08, and set their Traceability rows to Complete. If any requirement is only partially satisfied, do NOT tick it — record what is outstanding in STATE.md instead. Do not remove the retired figure strings from `.planning/` prose anywhere: ROADMAP, REQUIREMENTS, PROJECT.md and the research corpus retain them by design as the record of what was retired and why.

Confirm the earlier records still exist and are singular: `[Phase 3 / D-08]`, `[Phase 3 / EXP-08]`, the `index.html:196` Phase-6 handoff, and the IG-02 documentation bullet.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh >/dev/null && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh >/dev/null && make verify-selftest >/dev/null && make verify >/dev/null && LC_ALL=C grep -Fc '[Phase 3 / D-08]' .planning/STATE.md | grep -qx 1 && LC_ALL=C grep -Fc '[Phase 3 / EXP-08]' .planning/STATE.md | grep -qx 1 && LC_ALL=C grep -Fc 'index.html:196' .planning/STATE.md | grep -qvx 0 && test "$(grep -c '^- \[ \] \*\*EXP-0[23478]\*\*' .planning/REQUIREMENTS.md)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - all five suites green at the end state: harness exit 0, P2 net 27 PASS, P3 net exit 0, self-test exit 0 with ten G7/G8 PASS and no SKIP, `make verify` exit 0
    - `.planning/STATE.md` carries a `[Phase 3]` bullet with the final `G3.2` p1 figure, the Adobe rendered-line delta, and the derived `G7.2` margin, stating `PROBE_LINES` was not raised
    - a `[Phase 3]` bullet records the five-block employer-versus-subtitle order as measured, explicitly not claimed as fixed, and names the Phase-4 assertion candidate
    - a `[Phase 3]` bullet records the superseded Swiggy line-count figure and the `G6.6` unconditional-WARN finding
    - if `Triton` was promoted, a bullet records the early landing of a Phase-5-owned target and that `CUDA graphs` stays unpromoted
    - `LC_ALL=C grep -Fc '[Phase 3 / D-08]' .planning/STATE.md` and the same for `[Phase 3 / EXP-08]` each return 1
    - `.planning/ROADMAP.md` Phase 3 is ticked with a date, its `**Plans:**` count is 8, and all eight plan files are listed and ticked
    - `.planning/REQUIREMENTS.md` shows no unticked EXP-02/03/04/07/08 box, and their Traceability rows read Complete
    - the retired figure strings still appear in `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` — the planning record was not "cleaned"
    - `git status --porcelain docs/verify/baseline-frozen.txt` is empty
  </acceptance_criteria>
  <done>The end state is measured, the phase's decisions and corrections are recorded on canonical surfaces, and every requirement checkbox reflects the verified reality. Commit: `docs(03-08): record the phase-3 end state and close its requirements`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded verdict → `docs/main.tex` | the `[Phase 3 / EXP-08]` marker is the only authorization for a bullet's presence |
| `Makefile` → both standing nets | an unwired net is an unenforced invariant — the exact defect IG-01 named |
| measurement → recorded claim | a predicted figure written as measured is how a superseded number survives into the next phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-45 | Tampering | unapproved bullet ships | mitigate | only the STATE.md verdicts authorize an addition; rejected drafts keep their `assert_absent` pairs, so absence stays machine-asserted rather than merely observed |
| T-03-46 | Repudiation | approved wording drifts from what was signed off | mitigate | revised wording is taken verbatim from the STATE.md record; the P3 presence assertion pins the needle |
| T-03-47 | Tampering | absence assertions deleted rather than flipped | mitigate | the flip is explicitly per draft; every rejected draft's pair must remain, asserted by the P3 net still exiting 0 with those lines present |
| T-03-48 | Tampering | approval overflows page 1 or frees a line | mitigate | rebuild and read `G1.1` plus `G3.2` after EVERY bullet; the sanctioned reword rides in the same commit so the net delta is zero; `G3.2` p1 never rises above +50.5pt |
| T-03-49 | Tampering | protected descriptor literals lost in the reword | mitigate | the Phase-2 net is run immediately after any reword and 27 PASS is the only acceptable result; both forbidden shortened forms stay forbidden |
| T-03-50 | Repudiation | Phase 3 recreates IG-01 for its own net | mitigate | Task 2 wires `REGRESS3` into both the standalone target and the chained `verify` recipe, asserted by `make -n` |
| T-03-51 | Repudiation | requirement ticked without satisfaction | mitigate | checkboxes ticked only after all five suites are green; a partial requirement is explicitly left unticked with the gap recorded |
| T-03-52 | Repudiation | cosmetic extraction artifact claimed as fixed | mitigate | WR-01 is measured and recorded as an artifact, not a property; no BLOCKER assertion added; the Phase-4 candidacy is named |
| T-03-SC | Tampering | package installs | mitigate | none — zero packages installed this phase |
</threat_model>

<verification>
End-state suite, all five: `bash scripts/verify-resume.sh` exit 0 (the oracle), `bash scripts/verify-phase2-regressions.sh` 27 PASS, `bash scripts/verify-phase3-regressions.sh` exit 0, `make verify-selftest` exit 0 with ten G7/G8 PASS and no SKIP, and `make verify` exit 0 as ROADMAP-criterion-5 corroboration only — Make 3.81 collapses exit 1 and 2 into its own 2, so it is never the oracle. Never `make clean`.

Read `G1.1` first after every build. Record `G3.2` p1 in pt and lines at the end state; Phase 4 inherits it.
</verification>

<success_criteria>
- Exactly the approved EXP-08 bullets present, every rejected one asserted absent, `scripts/verify-phase3-regressions.sh` exit 0
- Both standing nets reachable from `make verify-regressions` and chained into `make verify`; no waiver flag in any recipe
- End-state budget, the `G7.2` margin, the WR-01 five-block measurement, the superseded Swiggy count and the `G6.6` finding all recorded on `.planning/STATE.md`
- `[Phase 3 / D-08]` and `[Phase 3 / EXP-08]` each present exactly once; the `index.html:196` Phase-6 handoff intact
- ROADMAP Phase 3 ticked with 8 plans listed; EXP-02/03/04/07/08 ticked and Traceability rows Complete
- All five suites green; `baseline-frozen.txt`, `PROBE_LINES` and `DIGIT_BULLET_FLOOR` untouched all phase
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-08-SUMMARY.md` when done. Its Next Phase Readiness section must carry, for Phase 4: the final `G3.2` p1 headroom in pt and lines for BOTH pages, the derived `G7.2` margin with the raise-only note, the five-block employer-order measurement with the Phase-4 assertion candidacy, the `index.html:196` obligation, and the standing-net invocation now available as `make verify-regressions`.
</output>
