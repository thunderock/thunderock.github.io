---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 05
type: execute
wave: 3
depends_on: ["03-04"]
files_modified:
  - .planning/STATE.md
  - docs/main.tex
  - docs/verify/manifest.txt
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: false
requirements: [EXP-03, EXP-04]

must_haves:
  truths:
    - "The D-08 config read is discharged one way or the other, and which branch was taken is recorded behind a greppable marker"
    - "Neither `10–50` nor `3–8K images/sec on 32 GPUs` survives anywhere in the PDF or the source"
    - "The replacement figures trace to the Ray Data benchmark and are attributed as his module benchmarked on that engine, not as his engine"
    - "`torch.compile` appears in the text layer with the dot, with compiler-bridge phrasing matching the branch the D-08 read selected"
    - "`torch.compile` and `Triton` are promoted to KEYWORDS_REQUIRED in the same commit that lands them"
  artifacts:
    - path: "docs/main.tex"
      provides: "D-05 Ray-benchmark replacement and the D-07 torch.compile production claim with bridge clause"
      contains: "torch.compile"
    - path: "docs/verify/manifest.txt"
      provides: "torch.compile and Triton promoted; CUDA graphs deliberately left as an unpromoted target"
      contains: "torch.compile"
    - path: ".planning/STATE.md"
      provides: "the [Phase 3 / D-08] outcome record"
      contains: "[Phase 3 / D-08]"
  key_links:
    - from: ".planning/STATE.md"
      to: "docs/main.tex"
      via: "the recorded D-08 branch selects which bridge wording ships"
      pattern: "\\[Phase 3 / D-08\\]"
    - from: "docs/verify/manifest.txt"
      to: "docs/main.tex"
      via: "promotion paired with the text that lands the keyword, in one commit"
      pattern: "KEYWORDS_REQUIRED"
---

<objective>
Retire both unverified page-1 figures and replace them with the Ray Data benchmark (D-05), then land the `torch.compile(mode="max-autotune")` production claim with the honest compiler-bridge clause the D-08 config read licenses (D-07/D-08), promoting `torch.compile` and `Triton` in the same commit.

Purpose: ROADMAP criterion 3 requires that neither retired figure survives anywhere in the PDF, and criterion 2's first clause requires `torch.compile` in the text layer with the dot. The D-08 read cannot be performed from this repo — the genie checkout is not on this machine — so it is a blocking human-verify with a pre-authorized fallback that cannot stall the phase.

Output: the offline-inference item rebuilt at a measured +1 rendered line, the bridge clause written into free tail room, and two more keywords promoted.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-04-SUMMARY.md
@.planning/research/CODEBASE-EVIDENCE.md
</context>

<measurement_loop>
Identical to plan 03-04. `make build`, then `bash scripts/verify-resume.sh` (the ORACLE — never `make verify`), then `bash scripts/verify-phase2-regressions.sh`, then `bash scripts/verify-phase3-regressions.sh`. Read `G1.1` FIRST, then `G2.1`, then record `G3.2` p1 in pt and lines, then the `G5.4` value, then `G6.5`, then `G6.6`, then `G6.4`. Never `make clean`.

Entering headroom is whatever `03-04-SUMMARY.md` recorded. This plan spends +1 rendered line on the D-05 swap and targets 0 for the bridge clause. Hard rails unchanged: never a fifth line, `G3.2` p1 must never rise above +50.5pt, `PROBE_LINES` and `DIGIT_BULLET_FLOOR` are not edited.
</measurement_loop>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the production compile config before the CUDA-graph clause ships</name>
  <decision>Does the glimmer content-type-classifier compile config disable CUDA graphs — i.e. does it use `max-autotune-no-cudagraphs` or set `triton.cudagraphs=False`?</decision>
  <context>
D-07 ships `torch.compile(mode="max-autotune")` as a production claim plus a compiler-bridge clause naming what `max-autotune` honestly buys. The official PyTorch documentation for `torch.compile` states verbatim that `max-autotune` "leverages Triton or template based matrix multiplications … and Triton based convolutions on GPU" and that it "enables CUDA graphs by default on GPU", with `max-autotune-no-cudagraphs` as the opt-out variant (recorded in `.planning/research/STACK.md:466-470`, verified 2026-08-21).

So the bridge clause is compiler-mediated and needs no new evidence — EXCEPT that two separate configuration surfaces could revoke the CUDA-graph half. `CODEBASE-EVIDENCE.md:54` records the mode string as `max-autotune`, which by citation rules out the `max-autotune-no-cudagraphs` variant (it is a different string). It says nothing about `torch._inductor.config.triton.cudagraphs`, which is an Inductor toggle rather than the `mode=` argument. `STACK.md:483` also flags that CUDA graphs can silently fall back on input mutation, dynamic shapes, or graph breaks.

**The agent cannot perform this read.** Verified on this machine: `~/code` contains no `genie` checkout; `~/code/ai-platform/aidoc/genie` and `.../aidoc/glimmer` are documentation trees with no `modules/dp/content_type_classifier` and no `module.py`; a repo-wide grep of `~/code/ai-platform` for `max-autotune`, `max_autotune`, `cudagraph` or `torch.compile` returns nothing. The user has the checkout; the agent does not.

Blast radius: one clause of one bullet. **Both branches land `torch.compile(mode="max-autotune")`, the `torch.compile` keyword, ROADMAP criterion 2's first clause, and EXP-03's core.** The fallback drops only the CUDA-graph capture wording. The phase therefore cannot be blocked by an unavailable read — if the checkout is gone or the answer is unclear, answer `triton-only-fallback` and the phase proceeds.
  </context>
  <options>
    <option id="cudagraphs-confirmed">
      <name>Mode is `max-autotune` with no CUDA-graph disabling — ship the full bridge</name>
      <pros>Ships the strongest honest clause: Triton-generated kernels plus CUDA-graph capture, both licensed by the official docs' documented semantics of the mode actually used in production.</pros>
      <cons>Rests on the read being accurate; a later silent CUDA-graph fallback (input mutation, dynamic shapes, graph breaks) would make the capture clause optimistic, though still doc-accurate for the mode.</cons>
    </option>
    <option id="triton-only-fallback">
      <name>Either disabling form is present, or the read is unavailable — ship Triton-only</name>
      <pros>Pre-authorized by D-08. Keeps the production claim, the compiler-driven fusion clause and `Triton`, all with zero exposure on the CUDA-graph question. Cannot stall the phase.</pros>
      <cons>Leaves one Tier-1 concept unclaimed on page 1. `CUDA graphs` stays a `KEYWORDS_TARGET` WARN at ×0 — which is the correct state either way, since its exact-match form is Phase 5's.</cons>
    </option>
  </options>
  <read_first>
    - .planning/research/CODEBASE-EVIDENCE.md :53-58 (the content-type-classifier entry citing `glimmer/modules/dp/content_type_classifier/module.py:76-77`)
    - .planning/research/STACK.md :460-495 (the verbatim official-docs quotations and the silent-fallback caveat)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §D-08 (what the recorded citation does and does not settle) and §Assumptions Log A1/A2
    - .planning/STATE.md `### Blockers/Concerns` (the `[Phase 5]` line this decision supersedes)
  </read_first>
  <how-to-verify>
1. In your genie checkout, run: `sed -n '70,85p' glimmer/modules/dp/content_type_classifier/module.py`
2. Then run: `grep -rn 'cudagraph\|max-autotune\|max_autotune\|_inductor' glimmer/modules/dp/content_type_classifier/`
3. Paste both outputs. If neither `max-autotune-no-cudagraphs` nor a `triton.cudagraphs` set to False appears, answer `cudagraphs-confirmed`.
4. If either appears, or the checkout is unavailable, or the output is ambiguous, answer `triton-only-fallback` — that branch is pre-authorized and costs the phase nothing.
  </how-to-verify>
  <resume-signal>Select: cudagraphs-confirmed, or triton-only-fallback</resume-signal>
  <files>.planning/STATE.md</files>
  <action>
Present the decision above and wait. Do not guess and do not infer the answer from `CODEBASE-EVIDENCE.md` — that citation settles the `mode=` string only, and the `triton.cudagraphs` surface is tagged `[ASSUMED]` with no supporting source anywhere.

Once the user answers, record the outcome in `.planning/STATE.md` under `## Accumulated Context` → `### Decisions` as a single bullet beginning with the literal marker `[Phase 3 / D-08]` and containing: the branch identifier verbatim (`cudagraphs-confirmed` or `triton-only-fallback`), what was read or why it could not be, and the consequence for the bridge wording Task 3 ships. In the same bullet, state that this supersedes the `[Phase 5]` CUDA-graphs blocker line in `### Blockers/Concerns` — and if the branch is `cudagraphs-confirmed`, note that the deferred `CUDA graphs` exact-match keyword remains Phase 5's PG2-04 work and is deliberately NOT promoted here.

Change nothing else in STATE.md. Task 3 reads this marker as the authority for which clause it writes.
  </action>
  <verify>
    <automated>LC_ALL=C grep -Fc '[Phase 3 / D-08]' .planning/STATE.md | grep -qx 1 && LC_ALL=C grep -F '[Phase 3 / D-08]' .planning/STATE.md | LC_ALL=C grep -qE 'cudagraphs-confirmed|triton-only-fallback' && LC_ALL=C grep -F '[Phase 3 / D-08]' .planning/STATE.md | LC_ALL=C grep -q 'Phase 5'</automated>
    <human-check>The user has run both commands against their own genie checkout, or has explicitly stated the checkout is unavailable and chosen the fallback.</human-check>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C grep -Fc '[Phase 3 / D-08]' .planning/STATE.md` returns 1
    - that line contains exactly one of the branch identifiers `cudagraphs-confirmed` or `triton-only-fallback`
    - that line records what was read, or why the read could not be performed
    - that line states it supersedes the `[Phase 5]` CUDA-graphs blocker entry
    - `grep -c 'CUDA graphs' docs/verify/manifest.txt` still returns 1 — `CUDA graphs` remains an unpromoted target on BOTH branches
    - `git diff --stat .planning/STATE.md` shows additions only, inside the Decisions list
    - no file under `docs/` was modified by this task
  </acceptance_criteria>
  <done>The bridge branch is decided and greppable. Task 3 has an unambiguous authority for its wording, and the STATE.md Phase-5 blocker is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Retire both unverified figures and land the Ray Data benchmark</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - docs/main.tex :152-157 (the Distributed Inference Frameworks group; `:155` is the offline-inference item being rewritten — the exact D-05 target)
    - .planning/research/CODEBASE-EVIDENCE.md :58 (the benchmark: his AIDE module on the Ray Data engine, doc by Joel C., 1×p4d.24xlarge with 8×A100, 16 fractional-GPU actors at 0.5 GPU each, 1,087,682 rows at 109 rows/s steady) and :7-13 (the framing rules)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (the A4 row, the traceability rows for `1.09M` / `8×A100` / `16 fractional-GPU actors`, the retirement row and its EN-DASH assertion rule)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §2.3 (the source-idiom table: `$\times$` renders as U+00D7 and is allowed; the retired range's rendered form is the EN DASH)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-04-SUMMARY.md (the entering `G3.2` p1 headroom this task spends against)
  </read_first>
  <action>
Rewrite `docs/main.tex:155` — today "Built offline inference on Ray Data + PyTorch Lightning, achieving 10--50$\times$ faster image loading and 3--8K images/sec on 32 GPUs." Both figures are unprovenanced and both must disappear from the source and therefore from the text layer.

The replacement carries the Ray Data benchmark with the locked literals `1.09M`, `8$\times$A100` (rendering as `8×A100`), and `16 fractional-GPU actors`. ATTRIBUTION IS BINDING: the Ray Data engine and the benchmark document are Joel C.'s work while the AIDE module is his, so the phrasing is that **his module was benchmarked on** the Ray Data engine. Never "achieved", never any wording that reads as if he built or tuned the engine. This must survive an interview probe.

Preserve on this item the keywords it carries today — `inference`, `Ray`, `PyTorch` — and keep `Ray Data + PyTorch Lightning` as the named stack.

Cost is measured at exactly +1 rendered line: the item is a single 135-character line today at capacity, and the replacement makes it two. That +1 is budgeted. Do not try to keep it on one line by dropping a locked literal.

Do NOT write the retired strings anywhere else in the document, and do not "soften" them into a nearby range. The P3 net asserts absence in four places: the rendered EN DASH form `10–50` in both the raw and squeezed text streams, `3–8K images/sec on 32 GPUs` in the squeezed stream, and the SOURCE forms `10--50` and `3--8K` in `docs/main.tex`. The source-side assertions exist because an ASCII-form assertion against the text layer is vacuous — the source writes `10--50` while the layer holds `10–50`.

Then `make build` and run the full measurement loop. Expect `G3.2` p1 to fall by about 11.5pt and `G5.4` to hold at 9 or higher (the replacement keeps digits on the item's first rendered line). `G6.4`'s census should show U+2013 dropping by two and U+00D7 rising. Commit source and artifacts together.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h05b.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h05b.txt && grep -q 'RESULT G6.4   PASS' /tmp/h05b.txt && test "$(grep -c 'RESULT G6.5   PASS' /tmp/h05b.txt)" = 15 && grep -Eq 'RESULT G5\.4 .*: (8|9|1[0-9])' /tmp/h05b.txt && test "$(LC_ALL=C grep -Ec '10--50|3--8K' docs/main.tex)" = 0 && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh > /tmp/p05b.txt; grep -E 'EXP-04' /tmp/p05b.txt | grep -c FAIL | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - every EXP-04 assertion in `bash scripts/verify-phase3-regressions.sh` is PASS — both retirement assertions and every replacement-figure assertion
    - `LC_ALL=C grep -Ec '10--50|3--8K' docs/main.tex` returns 0
    - `pdftotext docs/AshutoshTiwari.pdf -` piped through a fixed-string search for `10–50` (EN DASH) and for `3–8K images/sec on 32 GPUs` returns 0 for both
    - the text layer contains `1.09M`, `8×A100` and `16 fractional-GPU actors`
    - the wording attributes the benchmark to his module running ON the Ray Data engine, and contains no verb claiming he achieved or built the engine
    - `bash scripts/verify-resume.sh` exits 0; `G1.1` 2 pages; `G2.1` PASS; `G6.4` PASS; 15 `G6.5` PASS; `G5.4` reads 8 or higher
    - `G3.2` p1 recorded, at or above `+4.7pt`, and never risen above +50.5pt
    - the offline-inference item renders as exactly 2 gate lines
    - `bash scripts/verify-phase2-regressions.sh` prints 27 PASS
    - the commit contains `docs/main.tex` with `.pdf`, `.log` and `.fdb_latexmk`
  </acceptance_criteria>
  <done>Both unprovenanced figures are gone from source and text layer; the Ray Data benchmark ships with safe attribution at the budgeted +1 line. Commit: `feat(03-05): replace the unprovenanced speedup figures with the ray data benchmark`.</done>
</task>

<task type="auto">
  <name>Task 3: Land the torch.compile production claim with the branch-selected bridge, and promote it</name>
  <files>docs/main.tex, docs/verify/manifest.txt, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - .planning/STATE.md (the `[Phase 3 / D-08]` marker Task 1 wrote — this is the authority for which clause ships)
    - docs/main.tex :152-165 (the Adobe topics; the bridge clause goes in the inference-owning topic, written into free tail room)
    - .planning/research/CODEBASE-EVIDENCE.md :53-54 (the content-type classifier: CLIP ViT-B/32 plus head, `torch.compile(mode="max-autotune")` introduced on encoder and classifier, prod job with 11 daily feature-store partitions per run) and :10 (the closest-defensible list)
    - .planning/research/STACK.md :460-495 (the verbatim official-docs semantics the bridge clause rests on)
    - docs/verify/manifest.txt :51-54 (the promotion rule; Trap A — the `:phase` suffix must be stripped)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Keyword Mechanics (Trap A reproduced, Trap B's unconditional WARN, and the OQ-3 Triton governance decision)
  </read_first>
  <action>
Land D-07's production claim in the Adobe block: the literal `torch.compile(mode="max-autotune")` introduced on the CLIP encoder and classifier head in production. Write the literal with plain ASCII double quotes — verified to render and extract as U+0022 with zero curly quotes added to the glyph census, and the token is verified never to break across a wrap, so no `\texttt{}` or `\char34` wrapper is needed. `%`, `$`, `#` and `&` still need their normal escapes if the wording introduces any.

The BRIDGE CLAUSE depends entirely on the `[Phase 3 / D-08]` branch recorded in `.planning/STATE.md`. Read that marker first; do not choose for yourself.
- On `cudagraphs-confirmed`: name Triton-generated kernels, compiler-driven fusion, and CUDA-graph capture, phrased as what the mode buys per documented semantics — not as a hand-built optimization.
- On `triton-only-fallback`: name Triton-generated kernels and compiler-driven fusion, and say nothing about CUDA graphs anywhere in the clause.

On both branches, the claim is compiler-mediated. Do not write TensorRT, TRT-LLM, speculative decoding, quantization, or MoE / tensor / pipeline parallelism as accomplishments — `CODEBASE-EVIDENCE.md:9` has no commit evidence for any of them, and `G6.7` gates the first two families' literal forms while the rest are honesty rules held by construction.

Place the clause in the topic that owns inference work, writing into free tail room. Target 0 additional rendered lines. If the clause forces a new line, consult the contingency ladder in `03-DECISION-RECORD.md`: a fourth line is permitted only while `G1.1` still reports 2 pages and `G3.2` p1 stays at or above +4.7pt, and it must be recorded as consuming the EXP-08 reserve.

MANIFEST PROMOTION, in the SAME commit as the text: append `torch.compile` to `KEYWORDS_REQUIRED` at `:53` and remove `torch.compile:3` from `KEYWORDS_TARGET` at `:54`, suffix STRIPPED — a verbatim move makes `G6.5` grep for the literal string `torch.compile:3` and FAIL. If the wording lands `Triton`, promote it the same way and remove `Triton:5`: the manifest rule is that the commit which adds a keyword promotes it, and ownership is a WARN-routing label rather than a permission, so Phase 3 landing a Phase-5-owned target early is a decision to record in the SUMMARY, not a violation. Phase 5's PG2-04 Skills-tier form stays additive.

Do NOT promote `CUDA graphs` on either branch. Its exact-match form is Phase 5's per CONTEXT's deferred list, and D-07's hyphenated singular phrasing does not land the fixed string `CUDA graphs` anyway — verified: a line containing only `CUDA-graph capture` does not match.

The promotion matters because nothing else enforces it: `G6.6` calls `warn` unconditionally with no branch on the count, so a commit that adds a target keyword and forgets to promote it leaves the harness at exit 0 with a WARN nobody must act on. The P3 net's promotion triple is the enforcement.

Then `make build` and run the full measurement loop. Commit source, manifest and artifacts together.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h05c.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h05c.txt && grep -q "RESULT G6.5   PASS  required keyword present (case-sensitive): 'torch.compile'" /tmp/h05c.txt && grep -q 'RESULT G6.4   PASS' /tmp/h05c.txt && test "$(LC_ALL=C grep -c 'torch.compile:3' docs/verify/manifest.txt)" = 0 && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh > /tmp/p05c.txt; grep -E 'FAIL' /tmp/p05c.txt | grep -vcE '/03-06 ' | grep -qx 0 && make verify-selftest >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-phase3-regressions.sh` has ZERO remaining FAILs owned by `/03-05`; the only remaining FAIL is the `/03-06` Flipkart closure needle
    - the EXP-03 `torch.compile(mode="max-autotune")` assertion and the `assert_promoted` clauses for `torch.compile` and (if landed) `Triton` are PASS
    - `bash scripts/verify-resume.sh` prints a `G6.5 PASS` line for `'torch.compile'`, and for `'Triton'` if it was landed
    - `LC_ALL=C grep -c 'torch.compile:3' docs/verify/manifest.txt` returns 0, and `grep -c 'CUDA graphs' docs/verify/manifest.txt` returns 1 — still an unpromoted target
    - the shipped bridge wording matches the branch recorded behind `[Phase 3 / D-08]`; on `triton-only-fallback` the clause contains no occurrence of `CUDA`
    - `LC_ALL=C grep -Ec 'TensorRT|TRT-LLM|speculative decoding|quantization' docs/main.tex` returns 0
    - `G6.4` PASS with zero U+201C or U+201D added to the census — the quotes rendered as ASCII
    - `G1.1` 2 pages; `G2.1` PASS; `G5.4` reads 8 or higher; `G3.2` p1 recorded, at or above +4.7pt, never risen above +50.5pt
    - `bash scripts/verify-phase2-regressions.sh` prints 27 PASS
    - `make verify-selftest` exits 0 with ten G7/G8 PASS and no SKIP — the wave does not end net-freed
    - the commit contains `docs/main.tex`, `docs/verify/manifest.txt` and the regenerated artifacts together
  </acceptance_criteria>
  <done>The production `torch.compile` claim ships with an honest, branch-matched bridge clause, and every keyword it landed is promoted in the same commit. Commit: `feat(03-05): claim the production compile path with an honest compiler bridge`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external checkout (genie) → resume claim | the D-08 read is the only evidence path for the CUDA-graph half, and it cannot be performed inside this repo |
| `docs/main.tex` → text layer | the retirement must hold in the RENDERED form; an ASCII-form assertion is vacuous |
| `docs/verify/manifest.txt` → `scripts/verify-resume.sh` | a suffix-intact promotion turns a green gate red; a missed promotion leaves an ATS keyword unguarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-26 | Repudiation | CUDA-graph clause without the config read | mitigate | blocking `checkpoint:human-verify` with a pre-authorized Triton-only fallback; the branch is recorded behind a greppable `[Phase 3 / D-08]` marker and Task 3 must match it |
| T-03-27 | Repudiation | attribution inflation on the Ray benchmark | mitigate | wording asserted to attribute his MODULE benchmarked ON the engine; "achieved" and engine-ownership verbs forbidden and checked in review |
| T-03-28 | Tampering | vacuous retirement assertion | mitigate | absence asserted in four places — rendered EN DASH in both text streams plus both ASCII source forms in `docs/main.tex` |
| T-03-29 | Tampering | keyword promoted with `:phase` suffix intact | mitigate | `grep -c 'torch.compile:3'` returns 0 plus the P3 `assert_promoted` three-conjunct clause |
| T-03-30 | Repudiation | keyword landed but never promoted | mitigate | `G6.6` cannot catch this (unconditional warn), so the P3 promotion triple is the enforcement and is asserted PASS |
| T-03-31 | Repudiation | unevidenced accelerator claim enters the text | mitigate | `LC_ALL=C grep -Ec 'TensorRT|TRT-LLM|speculative decoding|quantization' docs/main.tex` returns 0; `G6.7` covers the first two families |
| T-03-32 | Tampering | curly quotes void the keyword match | mitigate | plain ASCII quotes verified to extract as U+0022; `G6.4` census asserted to add no U+201C/U+201D |
| T-03-33 | Spoofing | stale artifact certified fresh | mitigate | artifacts ride in the source commit; `make clean` never run |
| T-03-SC | Tampering | package installs | mitigate | none — zero packages installed this phase |
</threat_model>

<verification>
Task 1 is blocking and cannot be auto-approved: `.planning/STATE.md` `### Blockers/Concerns` records that this phase carries a hard user sign-off, and an attended gate must be presented for real even where an auto-advance setting exists. Present it and wait.

After Tasks 2 and 3: the full measurement loop, `G1.1` first. After Task 3 additionally `make verify-selftest` (exit 0, ten G7/G8 PASS, no SKIP). Gate on `bash scripts/verify-resume.sh`, never `make verify`. Never `make clean`.
</verification>

<success_criteria>
- `[Phase 3 / D-08]` exists in `.planning/STATE.md` naming exactly one branch and superseding the `[Phase 5]` blocker line
- Neither retired figure survives in the source or the text layer; all four absence assertions PASS
- `1.09M`, `8×A100` and `16 fractional-GPU actors` present with attribution-safe phrasing
- `torch.compile(mode="max-autotune")` in the text layer with the dot; bridge clause matches the recorded branch
- `torch.compile` (and `Triton` if landed) promoted with suffixes stripped; `CUDA graphs` still an unpromoted target
- `G1.1` 2 pages, `G2.1` PASS, `G6.4` PASS, `G6.5` all PASS, `G5.4` ≥8, `G3.2` p1 recorded ≥ +4.7pt and never risen above +50.5pt
- Only `/03-06`-owned P3 FAILs remain; `make verify-selftest` exit 0
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-05-SUMMARY.md` when done. Record: the D-08 branch and what was read (or why not), the exact shipped bridge wording, the retirement proof (all four absence counts at 0), the promotion diff, the `G3.2` p1 figure, and whether the EXP-08 reserve survived.
</output>
