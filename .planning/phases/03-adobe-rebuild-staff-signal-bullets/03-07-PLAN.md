---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 07
type: execute
wave: 5
depends_on: ["03-06"]
files_modified:
  - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md
  - .planning/STATE.md
autonomous: false
requirements: [EXP-08]

must_haves:
  truths:
    - "The rebuild is proven green before the user is asked anything — D-15's precondition is measured, not assumed"
    - "The user sees each draft alongside the rendered block it would join and the real line cost of approving it"
    - "A per-bullet verdict exists for every draft, recorded behind a greppable marker"
    - "ROADMAP criterion 2's qualitative half is captured as a greppable `staff-signal: yes|no` verdict with, on a negative read, the specific change the user wants — so 03-08 has a remediation path and does not tick EXP-02 against an unfavourable read"
    - "Rejection of every draft is a green outcome and is recorded as a decision rather than an omission"
  artifacts:
    - path: ".planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md"
      provides: "rendered Adobe block, measured headroom, and each draft with its evidence citation and line cost"
      contains: "UNVERIFIED"
    - path: ".planning/STATE.md"
      provides: "the [Phase 3 / EXP-08] per-bullet verdict record plus the criterion-2 staff-signal verdict"
      contains: "staff-signal:"
  key_links:
    - from: ".planning/STATE.md"
      to: "docs/main.tex"
      via: "the recorded verdicts authorize plan 03-08 to add exactly the approved bullets and nothing else"
      pattern: "\\[Phase 3 / EXP-08\\]"
---

<objective>
Present the EXP-08 cost/efficiency and mentoring drafts for per-bullet sign-off, after the Adobe rebuild has been proven green, and record the verdicts.

Purpose: D-14 makes these bullets absent-by-default — they enter `docs/main.tex` only after explicit recorded per-bullet approval, and the phase can close green with all of them rejected. D-15 requires the checkpoint be blocking and come **after** the rebuild verifies green. `.planning/STATE.md` `### Blockers/Concerns` records this as a hard gate that cannot be auto-advanced past.

Output: an evidence packet the user can actually judge against, and a `[Phase 3 / EXP-08]` verdict record that authorizes plan 03-08.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-06-SUMMARY.md
@.planning/research/CODEBASE-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the rebuild green and assemble the evidence packet the decision needs</name>
  <files>.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-06-SUMMARY.md (the end-state `G3.2` p1 figure and whether the line reserve survived)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (the drafts, their needles, their owning topics, and the contingency ladder)
    - docs/main.tex :146-195 (the rebuilt blocks as shipped)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md D-12 through D-15
  </read_first>
  <action>
D-15's precondition is that the Adobe rebuild is VERIFIED green before the user is asked. Measure it, do not assume it: run `make build`, then `bash scripts/verify-resume.sh`, then `bash scripts/verify-phase2-regressions.sh`, then `bash scripts/verify-phase3-regressions.sh`, then `make verify-selftest`. All four must be green — harness exit 0, P2 net 27 anchored `^RESULT P2\..*PASS` lines (count the anchored form; a green run emits 28 lines containing the word PASS because of the `>> PASS:` summary), P3 net exit 0, self-test exit 0 with ten G7/G8 PASS and no SKIP. Also record `make verify` exit 0 as ROADMAP criterion 5 corroboration, while remembering the oracle is the direct `bash` invocation because Make 3.81 collapses exit 1 and 2 into its own 2. If anything is red, STOP and fix it before the checkpoint — presenting drafts against a red document would waste the user's only sign-off.

Then write a short evidence file containing exactly what the decision needs and nothing else:

1. The rendered Adobe block as extracted text (the page-1 gate lines for the Adobe region), so the user reads the drafts against the block they would actually join rather than against LaTeX source.
2. The measured page-1 headroom: the `G3.2` p1 figure in pt and in lines, and the derived cost statement — how many rendered lines are available for an approval right now, computed as `floor(headroom_pt / 11.5)`.
3. Per draft: the `⚠ UNVERIFIED` text verbatim from the decision record, the `CODEBASE-EVIDENCE.md` line its magnitude leans on, the Adobe topic or block that would carry it, and its estimated rendered cost of 1 or 2 lines.
4. The consequence table, stated plainly: if headroom allows the approval, the cost is just those lines; if it does not, the approval requires the sanctioned Groupon/NetSpeed reword-shorter in the SAME commit as the addition so the net delta stays zero and the `G7.2` margin is untouched. Name every literal that reword must preserve, since the P2 net asserts them and two shortened forms are explicitly forbidden.
5. The digit-bearing count and the note that an approved bullet carrying a figure on its first rendered line raises `G5.4`, while one carrying it on a continuation line does not.

Do not editorialize and do not recommend. The user's judgment on provisional figures is the whole point of the gate. Do not touch `docs/main.tex` in this task.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh >/dev/null && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh >/dev/null && make verify-selftest >/dev/null && test -f .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md && grep -Fc 'UNVERIFIED' .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md | grep -qvx 0 && grep -Ec 'G3\.2|headroom' .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-resume.sh` exits 0; `bash scripts/verify-phase2-regressions.sh` prints 27 anchored `^RESULT P2\..*PASS` lines (a green run emits 28 lines containing the word PASS — the `>> PASS:` summary is the 28th, so always count the anchored form); `bash scripts/verify-phase3-regressions.sh` exits 0; `make verify-selftest` exits 0 with ten G7/G8 PASS and no SKIP
    - `make verify` exit 0 is recorded as corroboration, with a note that it is not the oracle
    - the evidence file contains the extracted rendered Adobe region, not LaTeX source
    - it states the `G3.2` p1 headroom in pt and lines and the derived number of rendered lines available for an approval
    - every draft appears with its `⚠ UNVERIFIED` text, its `CODEBASE-EVIDENCE.md` citation, its owning topic and its estimated line cost
    - the consequence table names the same-commit reword rule and lists the literals that reword must preserve
    - the file contains no recommendation for or against any draft
    - `git status --porcelain docs/` is empty — this task changed no content
  </acceptance_criteria>
  <done>The rebuild is proven green and the user has a self-contained packet: the rendered block, the real headroom, each draft with its evidence, and the true cost of approving one. Commit: `docs(03-07): assemble the sign-off evidence packet for the candidate bullets`.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Per-bullet sign-off on the cost/efficiency and leadership bullets</name>
  <decision>For each drafted bullet — the cost/efficiency candidates and the mentoring/leadership line — does it ship on the resume, or is it rejected?</decision>
  <context>
EXP-08 requires cost-savings and mentoring bullets to be drafted with provisional figures, explicitly flagged UNVERIFIED, and shipped **only** after your explicit sign-off. The agent cannot make this call: every figure in these drafts is an evidence-implied magnitude rather than a measured number, and only you can say whether each one survives a hiring-manager probe.

State of the document right now: the Adobe rebuild is verified green — the harness exits 0, the Phase-2 regression net prints its full 27 anchored `RESULT P2.<n> PASS` lines, the new Phase-3 net exits 0, and the self-test passes with the overflow probe still armed. The evidence packet at `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md` carries the rendered Adobe block, the measured page-1 headroom, and each draft with the `CODEBASE-EVIDENCE.md` line its magnitude leans on.

What each answer costs. Approving a bullet adds 1-2 rendered lines to page 1. The packet states the measured headroom and how many lines are actually available. If headroom is short, the approval requires rewording the Groupon or NetSpeed descriptor shorter in the same commit so the net line delta is zero — that lever is sanctioned but it is a real edit to protected content, and every locked literal in those two lines must survive it. Rejecting a bullet costs nothing: **D-14 makes absence the default and rejection a fully green outcome**, not a failure. Rejecting all of them closes the phase green.

Per D-13 no draft contains an invented dollar amount, headcount or percentage. If a magnitude still reads as too strong, you can also answer `revise` and say what it should read instead — that is cheaper than a rejection if the underlying claim is sound but the wording overreaches.

One thing worth reading while you are here, and it is not optional this time: criterion 2's qualitative half — whether the rebuilt Adobe block reads as staff signal rather than job description — is not machine-checkable. The mechanical part is asserted (the block provably opens on fault-tolerance / distributed-inference language), but the judgment is yours, and the packet's rendered block is the place to make it. **Answer it explicitly as `staff-signal: yes` or `staff-signal: no — <what still reads as job description, and what it should say instead>`.** A negative read is not a dead end: plan 03-08 Task 1 carries a bounded Branch C that applies exactly the change you name to the lead bullet (line-neutral, re-verified), and if you leave it negative with no actionable change, 03-08 leaves the EXP-02 requirement box UNTICKED and records the gap rather than quietly claiming the criterion was met. Saying nothing is the one answer that produces a false record.

The bridge clause is also worth one glance: `.planning/STATE.md`'s `[Phase 3 / D-08]` bullet records which branch was taken, and the Adobe inference topic's compiler wording should match it — Triton-generated kernels and compiler-driven fusion on both branches, CUDA-graph capture ONLY on `cudagraphs-confirmed`.
  </context>
  <options>
    <option id="approve-all">
      <name>Approve every draft</name>
      <pros>Maximum coverage of the cost/efficiency and leadership signals recruiters look for at staff level.</pros>
      <cons>Highest line cost; likely forces the Groupon/NetSpeed reword-shorter. Every approved magnitude becomes something you must defend in an interview.</cons>
    </option>
    <option id="approve-subset">
      <name>Approve a named subset — say which bullets by their draft IDs</name>
      <pros>Ships only the claims you are comfortable defending; usually fits the measured headroom without touching protected content.</pros>
      <cons>Requires you to name the IDs precisely so the record is unambiguous.</cons>
    </option>
    <option id="revise">
      <name>Approve in principle, revise the wording — say what each figure should read instead</name>
      <pros>Keeps a sound underlying claim while removing an overreaching magnitude. Cheaper than rejection.</pros>
      <cons>Needs one more short round: the revised wording is re-recorded before it ships.</cons>
    </option>
    <option id="reject-all">
      <name>Reject every draft — ship the rebuild without them</name>
      <pros>Zero interview exposure on unverified figures. Explicitly a green outcome under D-14; the phase closes normally and page-1 headroom stays as reserve for later phases.</pros>
      <cons>Page 1 carries no explicit cost-savings or mentoring signal this milestone.</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-EVIDENCE.md (the whole packet — the rendered Adobe block, the measured headroom, and each draft with its evidence citation)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (the draft section, and the contingency ladder if a reword is needed)
    - .planning/research/CODEBASE-EVIDENCE.md §ff-data-engineering and §asml-img-data-dags (the commit evidence the cost drafts lean on)
    - docs/AshutoshTiwari.pdf (open it — the drafts join page 1, and how full it already looks is part of the decision)
  </read_first>
  <how-to-verify>
1. Open `docs/AshutoshTiwari.pdf` and read page 1 end to end. Judge whether the rebuilt Adobe block reads as staff signal rather than job description, and answer it as `staff-signal: yes` or `staff-signal: no — <what still reads as job description, and what it should say instead>`. On a negative read, name the change concretely enough that it can be applied to the lead bullet without another round: 03-08 Task 1's Branch C applies exactly what you name, line-neutral.
2. Read `03-07-EVIDENCE.md` and note the measured headroom line and how many rendered lines are available.
3. Read the Adobe inference topic's compiler bridge clause against the `[Phase 3 / D-08]` branch recorded in `.planning/STATE.md`: Triton-generated kernels and compiler-driven fusion are expected on both branches; CUDA-graph capture is expected ONLY on `cudagraphs-confirmed`. On `triton-only-fallback` the P3 net already asserts no `CUDA` anywhere in the document, so this is a second pair of eyes on wording rather than the only check.
4. For each draft, read its text and its evidence citation, then ask yourself the interview question: if someone says "walk me through that number", can you?
5. Answer per bullet by draft ID — approve, reject, or revise with the replacement wording — and include the `staff-signal:` line.
  </how-to-verify>
  <resume-signal>Select: approve-all, reject-all, approve-subset with the draft IDs, or revise with the replacement wording per draft — and in every case include a `staff-signal: yes` or `staff-signal: no — <what should change>` line for ROADMAP criterion 2</resume-signal>
  <files>.planning/STATE.md</files>
  <action>
Present the decision above and wait. Do not guess a verdict, do not approve by default, and do not proceed to plan 03-08 without an answer — `.planning/STATE.md` `### Blockers/Concerns` records this as a hard gate, and an attended gate must be presented for real even where an auto-advance setting exists. Silence is not approval; under D-14 the default is absence, and shipping an unapproved claim is the exact failure EXP-08 exists to prevent.

Once the user answers, record the outcome in `.planning/STATE.md` under `## Accumulated Context` → `### Decisions` as a single bullet beginning with the literal marker `[Phase 3 / EXP-08]` and containing: one explicit verdict per draft ID (approved, rejected, or revised-with-wording), the measured page-1 headroom at decision time, whether an approval requires the sanctioned Groupon/NetSpeed reword, and — as a literal `staff-signal: yes` or `staff-signal: no — <what should change>` token — the user's ROADMAP-criterion-2 qualitative read. If any draft is revised, record the replacement wording verbatim so plan 03-08 ships exactly the approved text and nothing reconstructed from memory.

The `staff-signal:` token is load-bearing, not commentary: it is the ONLY input 03-08 has for criterion 2's unmechanizable half. On `no`, 03-08 Task 1 Branch C applies the named change to the lead bullet line-neutrally, and 03-08 Task 3 ticks EXP-02 only after that revision lands and the EXP-02a assertion is still PASS; on `no` with nothing actionable named, 03-08 Task 3 leaves the EXP-02 box UNTICKED and records the gap. Record the token verbatim as the user phrased it — do not soften a negative read into a positive one, and do not omit it because every draft was rejected. If the user answered the draft question but not this one, ask for the `staff-signal:` line before writing the record; it is one line and it is the difference between a measured tick and an assumed one.

Record rejections as decisions, never as omissions — a draft that simply disappears from the record is indistinguishable from one that was forgotten. Change nothing else in STATE.md, and do not edit `docs/main.tex` in this task: plan 03-08 applies whatever was approved, under measurement.
  </action>
  <verify>
    <automated>LC_ALL=C grep -Fc '[Phase 3 / EXP-08]' .planning/STATE.md | grep -qx 1 && LC_ALL=C grep -F '[Phase 3 / EXP-08]' .planning/STATE.md | LC_ALL=C grep -qE 'approved|rejected|revised' && LC_ALL=C grep -Fc 'staff-signal:' .planning/STATE.md | grep -qvx 0 && LC_ALL=C grep -F 'staff-signal:' .planning/STATE.md | LC_ALL=C grep -qE 'staff-signal: (yes|no)' && git status --porcelain docs/ | grep -vc '^$' | grep -qx 0</automated>
    <human-check>The user has read page 1 of the rendered PDF and given a verdict for every draft by ID, plus an explicit `staff-signal:` read for ROADMAP criterion 2.</human-check>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C grep -Fc '[Phase 3 / EXP-08]' .planning/STATE.md` returns 1
    - that record carries one explicit verdict per draft ID, with no draft unaccounted for
    - it records the measured page-1 headroom at decision time and whether an approval requires the sanctioned reword
    - `LC_ALL=C grep -Fc 'staff-signal:' .planning/STATE.md` returns **1 or more** and the token reads either `staff-signal: yes` or `staff-signal: no — <what should change>`, quoted as the user phrased it. This plan writes it exactly once; the assertion is ≥1 rather than ==1 because plan 03-08 may legitimately quote the token verbatim again in its Branch C revision record or its `EXP-02 qualitative gap` bullet, which would make a strict ==1 gate fail on a re-run of this check after 03-08 lands. This token is 03-08's only authority for criterion 2's qualitative half, and on `no` it must name something actionable or 03-08 leaves EXP-02 unticked
    - any revised wording is recorded verbatim
    - rejections are recorded as decisions, phrased as such, not merely absent
    - `git status --porcelain docs/` is empty — no content changed at the checkpoint
    - `git diff --stat .planning/STATE.md` shows additions only, inside the Decisions list
    - the SUMMARY repeats the per-draft verdicts and the `staff-signal:` token under a heading naming EXP-08
  </acceptance_criteria>
  <done>Every draft has an explicit recorded verdict, and criterion 2's qualitative half is recorded as a greppable `staff-signal:` token with an actionable change on a negative read. Plan 03-08 is authorized to add exactly the approved bullets, in exactly the approved wording, and nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft document → `docs/main.tex` | the crossing requires a recorded per-bullet verdict; this checkpoint is the only authorization |
| agent judgment → honesty claim | provisional magnitudes are outside the agent's competence to accept; only the person who will be interviewed can |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-40 | Tampering | draft ships without approval | mitigate | blocking `checkpoint:decision`; the plan states silence is not approval and absence is the default; the P3 Class D absence assertions remain PASS until 03-08 acts on a recorded verdict |
| T-03-41 | Repudiation | verdict not recorded, or a rejection lost | mitigate | one greppable `[Phase 3 / EXP-08]` marker carrying a verdict per draft ID; rejections recorded as decisions so they cannot read as oversights |
| T-03-42 | Repudiation | decision taken against a red document | mitigate | Task 1 proves all four suites green BEFORE the ask; a red result stops the plan instead of consuming the user's sign-off |
| T-03-43 | Tampering | approval silently overflows page 1 | mitigate | the packet states measured headroom and derived available lines; the same-commit reword rule is presented as part of the cost, so the decision is informed |
| T-03-44 | Repudiation | revised wording reconstructed from memory | mitigate | any revision is recorded verbatim in STATE.md and plan 03-08 ships exactly that text |
| T-03-44b | Repudiation | criterion 2's qualitative half assumed satisfied | mitigate | the read is captured as a literal greppable `staff-signal: yes\|no — <change>` token asserted present exactly once; on `no` 03-08 Task 1 Branch C applies the named change, and 03-08 Task 3 refuses to tick EXP-02 without a positive read or a landed revision |
| T-03-SC | Tampering | package installs | mitigate | none — zero packages installed this phase |
</threat_model>

<verification>
Task 1 gates the checkpoint: all four suites green, plus `make verify` exit 0 recorded as corroboration. Task 2 is blocking and must be presented for real — it cannot be auto-approved.

Neither task edits `docs/main.tex`; `git status --porcelain docs/` must be empty after both.
</verification>

<success_criteria>
- All four suites verified green before the user is asked anything
- `03-07-EVIDENCE.md` carries the rendered Adobe block, the measured headroom with derived available lines, and every draft with its evidence citation, owning topic and line cost — with no recommendation
- `[Phase 3 / EXP-08]` in `.planning/STATE.md` carries one explicit verdict per draft, the headroom at decision time, and any revised wording verbatim
- A greppable `staff-signal: yes|no — <change>` token records ROADMAP criterion 2's qualitative read — written exactly once by this plan, and asserted as ≥1 so that 03-08 quoting it verbatim later does not break the check — so 03-08 has both a remediation path and a tick authority
- Rejection is recorded as a decision; no content file changed in this plan
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-07-SUMMARY.md` when done. Record the per-draft verdicts verbatim, the measured headroom at decision time, whether the sanctioned reword is required, and the `staff-signal:` token exactly as recorded in STATE.md (including, on a negative read, the change the user named) — plan 03-08 needs all four, and the last one drives both its Branch C and its EXP-02 checkbox.
</output>
