---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase3-regressions.sh
autonomous: true
requirements: [EXP-02, EXP-03, EXP-04, EXP-07, EXP-08]

must_haves:
  truths:
    - "A standing P3 net fails when any Phase-3 locked literal is missing, when a retired figure reappears, when a landed keyword was not promoted, or when an unapproved EXP-08 draft literal enters the document"
    - "The net is RED ON ARRIVAL and every FAIL names the plan that owns the fix, so the census is auditable rather than noise"
    - "Every assertion class has been observed FAILING — new-content, retirement and the D-05 attribution needle by the wave-1 run itself, retention/absence/promotion by crafted fixtures"
    - "The D-08 fallback honesty rule has a recorded machine owner: the branch-conditional `CUDA` needle is specified here as Class E and appended by 03-05 in the commit that ships the fallback wording"
    - "The net runs under /bin/bash 3.2.57, is shellcheck-clean, never writes inside docs/ and never rebuilds"
  artifacts:
    - path: "scripts/verify-phase3-regressions.sh"
      provides: "P3.x standing content-regression net for Phase 3's locked literals"
      contains: "P3_SEQ"
      mode: "755"
  key_links:
    - from: "scripts/verify-phase3-regressions.sh"
      to: "docs/verify/manifest.txt"
      via: "--manifest fourth input stream read by the promotion-pairing assertion"
      pattern: "MANIFEST"
    - from: "scripts/verify-phase3-regressions.sh"
      to: "docs/AshutoshTiwari.pdf"
      via: "pdftotext into dual gate.txt + squeezed.txt streams"
      pattern: "squeezed"
---

<objective>
Build `scripts/verify-phase3-regressions.sh` — the standing P3.x net that is Phase 3's acceptance oracle. The harness gates page fit, glyphs, honesty and keyword *presence*, but nothing today asserts a single one of Phase 3's own locked literals, the lead-bullet voice, the keyword-promotion pairing, or the EXP-08 absent-by-default rule.

Purpose: every later plan in this phase is verified against this file. It is written FIRST and is deliberately **red on arrival** — Phase 1's proven design (`STATE.md` `[Phase 1 / 01-02]`: red by design, each FAIL naming its owner). That red state is simultaneously the anti-tautology control for the new-content and retirement classes.

Output: one new executable script, three commits, a recorded FAIL census later plans drive to zero.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md
@.planning/research/CODEBASE-EVIDENCE.md
</context>

<locked_literals>
Measured live against the committed artifact 2026-08-22 (`gate.txt` = form-feed-normalized pdftotext; `squeezed.txt` = whitespace-squeezed). The stated count is TODAY's and fixes whether each assertion starts green or red. Do not re-derive; do not substitute synonyms.

**Class C — retention guards (×1 today, must stay green forever).** Multi-word → `$SQUEEZED`; short whitespace-free tokens → `$GATE` too.
`Falcon-40B` · `Llama 2 70B` · `SQS queue depth` · `Flash Attention` · `FSDP` · `Rust` · `fault isolation across clusters` · `backpressure-aware scheduling` · `sustained low P99` · `18M+` · `4Bn rows` · `10K QPS` · `first workflow at Flipkart` · `validations`
`millions every day` — ×0 in `$GATE`, ×1 in `$SQUEEZED` (it wraps at "every / day"). **Squeezed stream ONLY.** This needle is the dual-stream rule proving itself.

**Class B — retirement (×1 today → FAIL now, green after 03-05).**
`10–50` (U+2013) in `$GATE` and in `$SQUEEZED` · `3–8K images/sec on 32 GPUs` in `$SQUEEZED` · source forms `10--50` and `3--8K` in `$TEX`. The ASCII form in the text layer is **vacuous** — source writes `10--50`, the layer holds `10–50`.
`achiev` — the D-05 attribution-safety needle, owner 03-05, ×1 today (`main.tex:155` "achieving"), asserted absent in `$TEX` and in `$SQUEEZED`. **Matched CASE-INSENSITIVELY** — this is the net's only case-insensitive absence needle, because `achieving` / `achieved` / `Achievement` are one honesty violation, not three. D-05's binding rule is *his module benchmarked on* the Ray Data engine, never that he achieved anything with it; without this needle the rule is subjective acceptance prose with no machine backing. ×1 today in both streams, so Task 2's own run proves it failable — no crafted control needed.

**Class A — new content (×0 today → FAIL now, green when its owner lands).**
Owner 03-04: lead-line anchor set `fault-tolerant` / `fault-tolerance` / `distributed inference` (case-insensitive) · `Training-Data Governance` · `27,135` · `claim/receipt` · `immutable S3 evidence layouts` · `9.7k-line` · `lineage` · `exponential backoff` · `full jitter` · `concurrency-safe S3 checkpoint` · `lease/TTL heartbeat` · `DLQ` · `circuit breaker` · `idempotent re-run` · `24×A100-40GB` · `9 model queues` · `64 H100s` · `202.6M-row` · `24.8M` · `~700M` · `35–38M` · `40-node`
Owner 03-05: `torch.compile(mode="max-autotune")` · `1.09M` · `8×A100` · `16 fractional-GPU actors` · `Triton` · promotion triples for `torch.compile`, `A100`, `H100`, `Triton`
Owner 03-06: `manual deploys`

**Class D — EXP-08 absent-by-default (×0 today → PASS now, flips only on recorded approval).**
`AssumeRole` · `6-week` · `mentored`, each asserted absent in **both** `$TEX` and `$GATE`. The `$TEX` half catches a `%`-commented draft, invisible to every existing gate.
⚠ `STS` is REJECTED as a needle: a fixed-string search matches inside `TESTS`. Use `AssumeRole`.

**Class E — branch-conditional, NOT present at wave 1 (owner 03-05, appended by 03-05 Task 3).**
`CUDA` — ×0 today, asserted absent in `$SQUEEZED` (the `03-RESEARCH.md` Validation-Architecture spec for EXP-03 c, `P3.12`) and in `$TEX`, **only on the `triton-only-fallback` branch** of D-08. This is the machine backing for D-08's fallback honesty constraint: on that branch the bridge clause must say nothing about CUDA graphs anywhere, and nothing else in the phase enforces it. It cannot be written at wave 1 because the branch is unknown until 03-05 Task 1's checkpoint answers — on `cudagraphs-confirmed` the clause legitimately names CUDA-graph capture and this needle would be wrong. 03-05 Task 3 appends it in the SAME commit that ships the fallback wording, exactly the way 03-08 Task 1 lands its per-draft `assert_absent`→`assert_present` flip. Label form `EXP-03c/03-05 fallback bridge names no CUDA graphs`. Failability: `CUDA` is ×0 in `docs/main.tex` today, so the assertion is well-formed rather than vacuous; because it passes on arrival it needs a crafted control, and that control is 03-05 Task 3's obligation (same shape as Task 3 Control 2 below — insert `CUDA` into a scratch text layer, require exit 1), not this plan's.
</locked_literals>

<tasks>

<task type="auto">
  <name>Task 1: Copy the P2 net to a P3 sibling — scaffold, helpers, fourth input stream, retention group</name>
  <files>scripts/verify-phase3-regressions.sh</files>
  <read_first>
    - scripts/verify-phase2-regressions.sh (whole file, 525 lines — the template; read before writing anything)
    - scripts/verify-resume.sh :385-442 (`region_nonempty_count` — comment :385, function opens :386 — and `region_first_nonempty` — comment :395, function opens :405: the anchor-free region primitives Task 2 composes)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §3, §5.1, §5.2, §5.3, §5.4
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Code Examples E-5, §Pitfall 10
    - docs/verify/manifest.txt (the new fourth input stream; KEYWORDS_REQUIRED :53, KEYWORDS_TARGET :54)
  </read_first>
  <action>
Copy `scripts/verify-phase2-regressions.sh` to `scripts/verify-phase3-regressions.sh`, `chmod 755`, then apply exactly these changes. Do not restructure — the P2 ordering encodes six measured constraints.

RENAME for the P3 namespace (trap T-2, three spots, all mandatory): counter `P2_SEQ` → `P3_SEQ`; the `emit` printf prefix → `P3.$P3_SEQ`; the `FAILED_IDS` append → `P3.$P3_SEQ`. Also the two summary printfs reading "Phase-2 regression assertion(s)", the `usage()` text, and the whole header block. The distinct prefix is a correctness property: the P2 header states it is what keeps the two streams concatenable.

FIX trap T-1 (silent death): `assert_whole_line_once` documents 3 args but its FAIL branch dereferences `$4`. Under `/bin/bash` 3.2.57 with `set -u` a 3-arg call whose assertion fails aborts the script with an unbound-variable error, emits no RESULT line, never increments `FAILURES`, and skips every remaining assertion — while exiting 1, which reads as "the document is wrong". Change the FAIL branch to a `${4:-...}` default expansion naming this plan's locked-literal table, and correct the doc comment to `<label> <needle> <file> <remedy>`. Leave `assert_absent` alone (genuinely 4-arg).

ADD the fourth input stream `MANIFEST` (trap T-3, four coordinated additions): (a) default `MANIFEST="docs/verify/manifest.txt"` beside the `FROZEN` default; (b) a `--manifest PATH` case arm with the same arity guard as `--frozen`; (c) a precondition beside the existing two, failing via `die2` with the wording "verification manifest missing or empty"; (d) a `"${MANIFEST:-}"` arm in `display_of` rendering "the verification manifest". Document `--manifest` in `usage()` and add it to the `>> Inputs:` banner. Without it the promotion assertions have nothing to read, and the flag is also what makes them negative-controllable (03-PATTERNS §5.6).

ADD helper `assert_promoted <label> <literal>` in the P2 house style (`gcount`-based where possible, three-branch verdict, `LC_ALL=C`, `-1` sentinel for unmeasurable, `emit` only from the current shell). All three conjuncts of RESEARCH E-5 must hold: (1) the literal is in `$GATE`; (2) it is a WHOLE pipe-field of `KEYWORDS_REQUIRED` — translate pipes to newlines then match whole-line fixed, never substring, because substring would let `A100` pass on an `A100-40GB` field; (3) no `KEYWORDS_TARGET` field begins with the literal followed by a colon. The FAIL message names which conjunct failed and states the remedy: promote in the SAME commit that adds the keyword, `:phase` suffix stripped — a suffix-intact promotion makes `G6.5` grep for `torch.compile:3` and FAIL.

ADD helper `region_first_nonempty <start> <end>` from `scripts/verify-resume.sh:405-414`, plus the NT-02 guard a second caller needs: an early `case` on the concatenated arguments returning 0 for empty or non-numeric input so it cannot reach the arithmetic comparison. Keep `gate_line_of`.

WRITE the header block stating: what the net asserts; why it is a separate file (`02-02-PLAN.md` criterion 11 pinned the harness at 83 RESULT lines / 39 IDs and the G7/G8 controls count them); the `RESULT P3.<n>` contract; the 0/1/2 exit vocabulary; the dual-stream rule with the `millions every day` example; the bash-3.2 constraint.

THEN add Group 1 — Class C retention guards only. Every label carries requirement and owner, form `EXP-02c/03-01 retained true claim`. Group 1 all passes today so this task exits 0. Feed batch loops from a here-doc, never a pipe — `emit` mutates `FAILURES` and a subshell loses the increment.

Whole-file: bash 3.2.57 (no arrays, no mapfile, no local, no double-bracket tests, no case-converting expansions); `set -uo pipefail` with NO errexit; `LC_ALL=C` on every grep; scratch from `mktemp -d` only with the existing inline single-quoted trap; never write inside `docs/`; never rebuild.
  </action>
  <verify>
    <automated>shellcheck -f gcc scripts/verify-phase3-regressions.sh && /bin/bash scripts/verify-phase3-regressions.sh && bash scripts/verify-resume.sh >/dev/null && bash scripts/verify-phase2-regressions.sh >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck -f gcc scripts/verify-phase3-regressions.sh` prints zero findings
    - `/bin/bash scripts/verify-phase3-regressions.sh` exits 0 and every RESULT line matches `^RESULT P3\.[0-9]+ +PASS`
    - `grep -c 'P2\.' scripts/verify-phase3-regressions.sh` returns 0
    - `grep -Fc '${4:-' scripts/verify-phase3-regressions.sh` returns 1 or more (T-1 fixed)
    - `bash scripts/verify-phase3-regressions.sh --manifest /dev/null` exits 2, prints "verification manifest missing or empty" on stderr, emits zero RESULT lines
    - `bash scripts/verify-phase3-regressions.sh --manifest` with no value exits 2 with the arity-guard message
    - `bash scripts/verify-phase3-regressions.sh --help` exits 0 and names `--manifest`
    - the `millions every day` RESULT line reads "the whitespace-squeezed text layer", not the raw layer
    - `ls -l scripts/verify-phase3-regressions.sh` shows `-rwxr-xr-x`
    - `git status --porcelain docs/` is empty
    - `bash scripts/verify-resume.sh` exits 0; `bash scripts/verify-phase2-regressions.sh` prints 27 anchored `^RESULT P2\..*PASS` lines (a green run emits 28 lines containing the word PASS — the `>> PASS:` summary is the 28th, so always count the anchored form)
  </acceptance_criteria>
  <done>The P3 net exists with the full helper set, the manifest input stream, both new helpers, and the retention group green. Commit: `test(03-01): add phase-3 content regression net with retention guards`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the six new-content assertion groups — red on arrival, every FAIL naming its owner</name>
  <files>scripts/verify-phase3-regressions.sh</files>
  <read_first>
    - scripts/verify-phase3-regressions.sh (as Task 1 left it)
    - scripts/verify-phase2-regressions.sh :374-398 (three-branch discipline: unmeasurable is a FAIL reading "the verdict is UNKNOWN, which is never a pass"), :441-456 and :481-509 (here-doc batch idiom; dual source+text-layer assert_absent pairs)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §2.1, §3.1, §3.2
    - .planning/research/CODEBASE-EVIDENCE.md :7-13 (binding honest-framing rules)
    - docs/main.tex :146-195 (current text — confirms every Class A needle absent, every Class C needle present)
  </read_first>
  <action>
Add six groups after Group 1. Every label carries requirement AND owning plan, form `EXP-04a/03-05 unverified speedup retired`. Take every needle verbatim from `<locked_literals>`.

GROUP 2 — EXP-02a lead-bullet voice, owner 03-04. Compose per 03-PATTERNS §3.1: resolve the Adobe subtitle `GENERATIVE AI, COMPUTER VISION, LARGE LANGUAGE MODELS` as a whole gate line via `gate_line_of`, then `region_first_nonempty` from one past it to four past it gives the lead line (measured: no blank line separates subtitle from lead bullet). Write that one line to a scratch file under `$WORK` and match the three anchors against it CASE-INSENSITIVELY. Record in a comment why: lowercase `distributed inference` is absent today while title-case `Distributed Inference Frameworks` is present, so the clause is scoped to the extracted lead line — the anchor must be earned by the opening line, not by a topic heading. If the subtitle does not resolve, FAIL with unmeasurable wording pointing at the harness's G5.x. This is ROADMAP criterion 2's second clause; no existing gate covers it.

GROUP 3 — EXP-02b governance topic, owner 03-04. `assert_present` on `$SQUEEZED`: `Training-Data Governance`, `27,135`, `claim/receipt`, `immutable S3 evidence layouts`, `9.7k-line`, `lineage`. Add a `Rust`-count clause requiring 2 or more in `$GATE` — `Rust` is ×1 today at `main.tex:163` only, and D-02's group adds the second, so the clause proves the group landed AND guards a single-point-of-failure BLOCKER keyword. Add the honest-framing clause: slice the governance topic's rendered region out of `$GATE` (from the line containing `Training-Data Governance` through the last line before the next line beginning with the U+2013 bullet prefix, or the end of the Adobe region) and assert that slice contains none of `throughput`, `latency`, `speedup`, `faster`, `images/sec`. That mechanizes `CODEBASE-EVIDENCE.md:12`: the Content Gate work is fault-tolerant distributed systems in Rust, never inference optimization. Unresolvable topic line → FAIL as unmeasurable.

GROUP 4 — D-04 fault-tolerance mechanisms, owner 03-04. `assert_present` on `$SQUEEZED`: `exponential backoff`, `full jitter`, `concurrency-safe S3 checkpoint`, `lease/TTL heartbeat`, `DLQ`, `circuit breaker`, `idempotent re-run`. D-04 requires mechanisms named inline where they occurred; these needles make "named" checkable.

GROUP 5 — EXP-03 torch.compile + promotion pairing, owner 03-05. `assert_present` the literal `torch.compile(mode="max-autotune")` on `$SQUEEZED` with plain ASCII double quotes (verified to render and extract as U+0022; the token is verified never to break across a wrap). Then `assert_promoted` for `torch.compile`, `A100`, `H100`, `Triton` — all four unconditional, because both D-08 branches land `Triton` (see 03-05 Task 3) and 03-04 lands both accelerator keywords. These exist because `G6.6` calls `warn` unconditionally with no branch on the count — a commit that adds a target keyword and forgets to promote it leaves the harness at exit 0, so the promotion rule has zero machine backing until this group exists.

Write a comment at the END of Group 5 recording the Class E gap deliberately left here: the `CUDA` absence needle for D-08's `triton-only-fallback` branch is NOT added at wave 1, because the branch is undecided and the needle would be wrong on the other branch. Name its owner (03-05 Task 3), its label (`EXP-03c/03-05 fallback bridge names no CUDA graphs`), and the streams it will use (`$SQUEEZED` per the RESEARCH `P3.12` spec, plus `$TEX`). The comment is what makes the gap a recorded decision rather than an omission a later reader has to rediscover.

GROUP 6 — EXP-04 retirement and replacement; owner 03-05 for the swap and the attribution needle, 03-04 for the spread figures. Retirement, four `assert_absent` each with a remedy: `10–50` against `$GATE`; `10–50` against `$SQUEEZED`; `3–8K images/sec on 32 GPUs` against `$SQUEEZED`; source forms `10--50` and `3--8K` against `$TEX`. Then the D-05 attribution-safety pair, label form `EXP-04c/03-05 attribution-safe wording`: `achiev` asserted absent against `$TEX` and against `$SQUEEZED`, matched CASE-INSENSITIVELY (a fixed `-i` count — the only case-insensitive absence needle in the net; record why in a comment, and that the remedy is D-05's *his module benchmarked on the Ray Data engine* phrasing). Replacements, `assert_present` on `$SQUEEZED`: `1.09M`, `8×A100`, `16 fractional-GPU actors`, `24×A100-40GB`, `9 model queues`, `64 H100s`, `202.6M-row`, `24.8M`, `~700M`, `35–38M`, `40-node`. Additionally `assert_present` the short tokens `A100` and `H100` against `$GATE` so the squeeze can never be what passes them.

GROUP 7 — EXP-07 Flipkart closure (owner 03-06) plus EXP-08 absent-by-default (owner 03-07/03-08). `assert_present` `manual deploys` on `$SQUEEZED`. Then for each of `AssumeRole`, `6-week`, `mentored`, `assert_absent` against `$TEX` and against `$GATE`, remedy naming D-14's absent-by-default rule and the recorded-approval requirement. Add a comment recording why `STS` is not a needle (matches inside `TESTS`).

Print a `>> Groups run:` line naming all seven groups before the summary, matching the P2 shape. The script now exits 1 — the designed state.
  </action>
  <verify>
    <automated>SC="$(mktemp -d)" && shellcheck -f gcc scripts/verify-phase3-regressions.sh; /bin/bash scripts/verify-phase3-regressions.sh > "$SC/census.txt"; test $? -eq 1 && grep -E '^RESULT P3\.[0-9]+ +FAIL' "$SC/census.txt" | grep -vcE '/03-0[456] ' | grep -qx 0 && grep -cE '^RESULT P3\.[0-9]+ +FAIL.*EXP-04c' "$SC/census.txt" | grep -qvx 0 && grep -cE '^RESULT P3\.[0-9]+ +FAIL' "$SC/census.txt" | grep -qvx 0 && bash scripts/verify-resume.sh >/dev/null && bash scripts/verify-phase2-regressions.sh >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck -f gcc` prints zero findings
    - `/bin/bash scripts/verify-phase3-regressions.sh` exits 1 and its final line names the failing P3 IDs
    - every FAIL label contains one of `/03-04`, `/03-05`, `/03-06` — no FAIL is unowned
    - every Group 1 retention assertion and every Group 7 EXP-08 absence assertion is PASS
    - the `10–50` and `3–8K images/sec on 32 GPUs` retirement assertions are FAIL — not vacuous, since those strings are present today
    - the `10--50` and `3--8K` source-side assertions are FAIL against `docs/main.tex`
    - BOTH `EXP-04c/03-05 attribution-safe wording` assertions (`achiev` against `$TEX` and against `$SQUEEZED`) are FAIL — `achiev` is ×1 today at `main.tex:155`, so this class is proven failable by this run
    - Group 5 carries the recorded-gap comment naming the Class E `CUDA` needle, its owner (03-05 Task 3) and its streams — the gap is a decision, not an omission
    - `bash scripts/verify-resume.sh` exits 0; `bash scripts/verify-phase2-regressions.sh` prints 27 anchored `^RESULT P2\..*PASS` lines (count the anchored form: a green run emits 28 lines containing the word PASS, because the `>> PASS:` summary is the 28th)
    - `git status --porcelain docs/` is empty
    - the FAIL count and the full ID→owner list are captured in the task evidence
  </acceptance_criteria>
  <done>All seven groups exist; the net exits 1 with a fully-owned FAIL census that 03-04, 03-05 and 03-06 drive to zero. Commit: `test(03-01): assert phase-3 locked literals, promotion pairing and draft absence`.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the retention, absence and promotion classes can fail — crafted negative controls</name>
  <files>scripts/verify-phase3-regressions.sh</files>
  <read_first>
    - scripts/verify-phase3-regressions.sh (as Task 2 left it)
    - scripts/verify-phase2-regressions.sh :88-92 and :330-332 (the --gate-text fixture flag and its stdout disclaimer — the precedent for a "certifies nothing" input override)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §5.6 (negative-control shape; why a promotion assertion needs a scratch manifest instead)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Code Examples E-7
    - .planning/STATE.md (the Phase-1 rule this discharges: an assertion never observed failing is not a gate)
  </read_first>
  <action>
Task 2's run already proved Class A and Class B failable against the real committed artifact. Three things remain unproven: the retention guards and the EXP-08 absence assertions pass today, and the promotion conjunction reads a file `--pdf`/`--tex` cannot vary. Prove all three in scratch space only.

CONTROL 1 — retention guards. Under `mktemp -d`, extract the real text layer and delete a retention literal from it, then run with `--gate-text` pointing at the fixture. Require exit 1 naming that assertion's ID, and require stdout to carry the "does NOT certify the committed artifact" disclaimer. Use two separate fixtures so both needle classes are proven failable: `Flash Attention` for the squeezed multi-word class, `FSDP` for the raw short-token class. Note `--gate-text` feeds the raw stream from which both derived streams are built, so one deletion propagates to both.

CONTROL 2 — EXP-08 absence, both halves. Text-layer half: a fixture that INSERTS `AssumeRole`, `6-week` and `mentored`; require exit 1 naming the three absence assertions. Source half (the one that matters most — it is the leak no existing gate can see): copy `docs/main.tex` into scratch keeping the basename `main.tex`, append a `%`-commented line containing `AssumeRole`, run with `--tex` pointing at it, and require FAIL.

CONTROL 3 — promotion pairing is a conjunction, not one test wearing three names. Scratch manifest A: hand-add `torch.compile` to `KEYWORDS_REQUIRED` while leaving `torch.compile:3` in `KEYWORDS_TARGET`; run with `--manifest` and require conjunct 3 to FAIL. Scratch manifest B: move the entry correctly while the literal is still absent from the text layer; require conjunct 1 to FAIL.

Nothing may touch the working tree: fixtures, scratch sources and scratch manifests all live under `mktemp -d`, and the source copy must keep the basename `main.tex` — the freshness record is looked up by source filename, so a renamed copy silently escalates to the L2 arbiter and disarms the staleness control.

Then append a short "negative controls" section to the script's header block recording, per class, the command shape proven to make it fail. That is the file's own record that no assertion in it is a tautology.

INVARIANCE PROOF, without depending on a file another run left in `/tmp`. This task changes no assertion, so its census must be identical to the previous commit's. Derive the baseline in scratch instead of reading a stale path: `git show HEAD:scripts/verify-phase3-regressions.sh` into `$SC/prev.sh` (HEAD is Task 2's commit), run both versions, write each FAIL slice to its own temp file, and `diff` the two files. Use two real files — never `diff <(…) <(…)` process substitution, which is not portable to a non-bash verify shell — and never a fixed `/tmp` name a cleaner may have removed between tasks.
  </action>
  <verify>
    <automated>SC="$(mktemp -d)" && shellcheck -f gcc scripts/verify-phase3-regressions.sh && git show HEAD:scripts/verify-phase3-regressions.sh > "$SC/prev.sh" && /bin/bash scripts/verify-phase3-regressions.sh > "$SC/now.txt"; test $? -eq 1 && /bin/bash "$SC/prev.sh" > "$SC/prev.txt"; grep -E 'FAIL' "$SC/prev.txt" > "$SC/prev.fail"; grep -E 'FAIL' "$SC/now.txt" > "$SC/now.fail"; diff "$SC/prev.fail" "$SC/now.fail" && git status --porcelain docs/ scripts/verify-resume.sh scripts/verify-phase2-regressions.sh | grep -vc '^$' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - each of the six controls (2 retention fixtures, 2 EXP-08 halves, 2 promotion conjuncts) is recorded in the task evidence with its exit code and the P3 ID it made FAIL — the Class E `CUDA` needle is NOT controlled here because it does not exist yet; 03-05 Task 3 appends it and owns its control
    - every one of those runs exited 1 (a regression), never 0 and never 2
    - the `--gate-text` runs printed the "does NOT certify the committed artifact" disclaimer
    - the default-input census is byte-identical to the census produced by the script version at `HEAD` (Task 2's commit), re-derived in a `mktemp -d` scratch dir via `git show` — controls changed no assertion's verdict on the real artifact
    - the comparison used two ordinary temp files under `mktemp -d`, not `diff <(…) <(…)` process substitution, and read no fixed `/tmp` path written by an earlier task
    - `git status --porcelain` lists no change under `docs/`, and `scripts/verify-resume.sh` plus `scripts/verify-phase2-regressions.sh` are unmodified
    - the script header carries a "negative controls" section naming the command shape per class
    - `shellcheck -f gcc` prints zero findings
  </acceptance_criteria>
  <done>Every P3 assertion class has been observed failing. The net is a gate, not a tautology. Commit: `test(03-01): record negative controls for every phase-3 assertion class`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/AshutoshTiwari.pdf` → `pdftotext` → assertion streams | derived, untrusted-shape text crosses into the assertion logic; a mis-normalized stream silently changes every verdict |
| `docs/verify/manifest.txt` → `assert_promoted` | configuration read as DATA to cross-check; must stay inert (line-wise read, never sourced, never eval'd) |
| `--gate-text` / `--pdf` / `--tex` / `--manifest` overrides | caller-supplied inputs that can make a run certify nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | new P3 assertions | mitigate | Task 3 proves every class failable; Task 2's red-on-arrival run is the control for Class A/B; no assertion ships unobserved-failing |
| T-03-02 | Tampering | `assert_promoted` conjunct 2 | mitigate | whole-pipe-field match (`tr` to newlines + whole-line fixed), never substring — a substring test would let `A100` pass on an `A100-40GB` field |
| T-03-03 | Repudiation | `assert_whole_line_once` T-1 | mitigate | `${4:-...}` default expansion; a failing 3-arg call otherwise kills the run and reports exit 1 as if the document were wrong |
| T-03-04 | Repudiation | vacuous absence assertions | mitigate | retirement needles use the RENDERED EN DASH form plus a separate source-side ASCII assertion; `STS` rejected as a needle because it matches inside `TESTS` |
| T-03-05 | Spoofing | fixture-driven runs | mitigate | `--gate-text` retains the P2 net's stdout disclaimer; controls run only under `mktemp -d`; the flag never enters a Makefile recipe |
| T-03-06 | Information Disclosure | manifest read | accept | manifest is public repo configuration with no credential; read line-wise, never sourced or eval'd, so metacharacters stay inert |
| T-03-07 | Denial of Service | scratch workspace | mitigate | `mktemp -d` only, never caller-named; inline single-quoted trap removes it after a non-empty + is-a-directory check; nothing written under `docs/` |
| T-03-SC | Tampering | package installs | mitigate | none — this phase installs zero npm/PyPI/crates/brew packages; no legitimacy checkpoint required |
</threat_model>

<verification>
Run after every task: `shellcheck -f gcc scripts/verify-phase3-regressions.sh`, then `/bin/bash scripts/verify-phase3-regressions.sh` (bash 3.2 is the portability bar), then `bash scripts/verify-resume.sh` and `bash scripts/verify-phase2-regressions.sh` — both must stay green, since this plan touches neither.

Never gate on `make verify` (GNU Make 3.81 collapses exit 1 and 2 into its own 2). Never `make clean` (deletes the `.fdb_latexmk` record `G0.3` reads). This plan runs no build.
</verification>

<success_criteria>
- `scripts/verify-phase3-regressions.sh` exists, mode 755, shellcheck-clean, runs under `/bin/bash` 3.2.57
- Seven assertion groups covering EXP-02a/b, D-04, EXP-03 (+promotion pairing), EXP-04 (retirement + replacement + the D-05 `achiev` attribution needle), EXP-07 and EXP-08
- Exits 1 on the committed artifact with a fully-owned FAIL census recorded in the SUMMARY
- Retention and EXP-08 absence groups PASS; every class observed failing — Class A/B/`achiev` by the arrival run, retention/absence/promotion by crafted controls
- The Class E `CUDA` branch-conditional needle is specified and its ownership (03-05 Task 3) recorded in a Group 5 comment, so the D-08 fallback rule is not left to prose
- `bash scripts/verify-resume.sh` exit 0 and `bash scripts/verify-phase2-regressions.sh` 27 anchored `^RESULT P2\..*PASS` lines throughout
- No file under `docs/` modified; `scripts/verify-resume.sh` and `scripts/verify-phase2-regressions.sh` byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-01-SUMMARY.md` when done. Record: the full P3 ID → owner → status census (this is the contract 03-04/03-05/03-06 shrink to zero), the six negative-control command shapes with their exit codes, and any needle whose measured count differed from this plan's `<locked_literals>` table.
</output>
