---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - docs/verify/manifest.txt
  - .planning/STATE.md
autonomous: true
requirements: [EXP-02, EXP-07]

must_haves:
  truths:
    - "The Phase-2 regression net is reachable from make — `make verify-regressions` runs it, and `make verify` runs it first"
    - "All five G6.13 WARN lines read `(owner: Phase 3)` instead of `(owner: unassigned)`"
    - "The net, its load-bearing `C++ graph algorithms` anchor, and the new make target are named on a canonical surface outside the phase directory"
    - "Phase 6 inherits a greppable obligation for `index.html:196`, which duplicates both figures this phase retires"
  artifacts:
    - path: "Makefile"
      provides: "verify-regressions target + .PHONY entry + header index line + chain into verify"
      contains: "verify-regressions"
    - path: "docs/verify/manifest.txt"
      provides: "G6.13 WARN ownership claimed by Phase 3"
      contains: "G6.13:3"
    - path: ".planning/STATE.md"
      provides: "IG-02 documentation bullet + index.html:196 Phase-6 handoff"
      contains: "verify-phase2-regressions.sh"
  key_links:
    - from: "Makefile"
      to: "scripts/verify-phase2-regressions.sh"
      via: "REGRESS variable invoked by the verify-regressions recipe and chained first in verify"
      pattern: "REGRESS"
    - from: "docs/verify/manifest.txt"
      to: "scripts/verify-resume.sh warn_owner"
      via: "WARN_OWNERS numeric token rendered as 'Phase 3'"
      pattern: "G6.13:3"
---

<objective>
Close the three milestone-audit wiring gaps the audit assigned to Phase 3 before any content is touched: IG-01 (the 27-assertion Phase-2 net has **zero invokers** — no make target, no CI), IG-02 (it is documented only inside a phase-local validation file), and IG-03 (the `G6.13`/WR-01 WARN is `unassigned` and Phase 3 owes the claim).

Purpose: the net protects exactly the content ROADMAP points Phase 3 at — the locked Groupon/NetSpeed descriptor literals including the load-bearing `C++ graph algorithms` anchor, which is the designated re-compression lever this phase may pull. A protection nothing runs is not a protection.

Output: a reachable `verify-regressions` target, a claimed WARN, and two durable records on `.planning/STATE.md`.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the Phase-2 net into make and claim the G6.13 WARN</name>
  <files>Makefile, docs/verify/manifest.txt</files>
  <read_first>
    - Makefile (whole file, 214 lines — note the header target index at :4-15, the variable block at :28 where `VERIFY` is declared, the single-line `.PHONY` at :53, the `make help` grep/format at :57-59, and the verification section at :160-187 including the long comment block explaining why no target may waive the freshness proof)
    - docs/verify/manifest.txt (whole file — the `WARN_OWNERS` line at :60 and its sentinel-format comment at :56-59; the header at :1-6 authorizing threshold movement)
    - scripts/verify-phase2-regressions.sh :1-60 (its own contract: reads whatever artifact is on disk, never rebuilds, exits 2 with a "run make build" remedy if the PDF is missing)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §4.1 and §4.2 (the four coordinated Makefile edit sites and the two one-line manifest edits)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Harness Wiring (the recipe text, the rejected alternatives, and the Make 3.81 behaviours that constrain the design)
  </read_first>
  <action>
FOUR coordinated Makefile edits, matching the existing three-target shape at :180-187 (a `## `-suffixed doc comment, a single `@bash $(VAR)` recipe line):

1. Variable block at :28 — add `REGRESS := scripts/verify-phase2-regressions.sh` beside the existing `VERIFY := scripts/verify-resume.sh`.
2. `.PHONY` at :53 — append `verify-regressions` to the single-line list.
3. Verification section — add a `verify-regressions` target with NO `$(PDF)` prerequisite and a one-line `@bash $(REGRESS)` recipe. The no-prerequisite design is the MAJORITY pattern here, not an exception: `verify-selftest` and `verify-baseline` carry no prerequisite either, only `verify` does. Carry a comment explaining why: the net's own contract is to read whatever artifact is on disk and never rebuild, so a prerequisite would turn it into build-then-verify — the same wart the Makefile's existing comment block documents on `verify`. Then chain `@bash $(REGRESS)` as the FIRST recipe line of the existing `verify` target, above `@bash $(VERIFY)`, with a comment recording why first: the net is content-only and sub-second, so a content regression is reported before the slower page/geometry harness, and a red harness cannot short-circuit it (Make 3.81 aborts a recipe at its first failing line).
4. Header target index at :4-15 — add a `make verify-regressions` line in the existing column style. That block is the file's own documented target index and is part of IG-02's canonical-surface obligation.

HARD PROHIBITION: `--gate-text` must never appear in any recipe, for the same reason `--skip-freshness` never appears — the script's own help says a run carrying it certifies nothing. Do not use `verify: verify-regressions` as a prerequisite (same short-circuit, worse readability), do not chain the net after `$(VERIFY)` (a red harness would hide it), and never prefix with `-` or `|| true`.

COSMETIC AWARENESS, do not "fix": `make help` formats with `%-16s` and `verify-regressions` is 18 characters, so its help line shifts right by two. Widening the format string would reflow every other line.

ONE manifest edit at :60 — change `G6.13:unassigned` to `G6.13:3`. The two live `:3` entries on that same line (`G5.4:3`, `G6.6:3`) are the format proof; `warn_owner` renders a numeric token as `Phase 3`, so all five `G6.13` lines then read `(owner: Phase 3)`. Do NOT add `ROLE_BIND_WINDOW` to the manifest: it is a deliberate local constant at `scripts/verify-resume.sh:1936`, and `01-02` recorded that the commit which PROMOTES `G6.13` to a BLOCKER is the commit that adds the key. Phase 3 claims the WARN; it does not promote the gate. Touch no other manifest key — `PROBE_LINES`, `DIGIT_BULLET_FLOOR`, `LINE_PT`, `PAGES` and `CEILING_PT` are all out of bounds here, and `KEYWORDS_*` promotion belongs to the commits that land the keywords (plans 03-04 and 03-05).

`docs/verify/baseline-frozen.txt` is immutable this phase. Do not set `ALLOW_FROZEN_UPDATE`. Do not run `make verify-baseline`.
  </action>
  <verify>
    <automated>make verify-regressions | grep -c '^RESULT P2\..*PASS' | grep -qx 27 && make -n verify | grep -q 'verify-phase2-regressions.sh' && bash scripts/verify-resume.sh | grep -c 'G6.13.*owner: Phase 3' | grep -qx 5 && grep -c 'G6.13:unassigned' docs/verify/manifest.txt | grep -qx 0 && bash scripts/verify-resume.sh >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-regressions` exits 0 and prints 27 `RESULT P2.<n> PASS` lines
    - `make -n verify` shows `bash scripts/verify-phase2-regressions.sh` BEFORE `bash scripts/verify-resume.sh` in the recipe
    - `grep -c 'verify-regressions' Makefile` returns 4 or more (header index, `.PHONY`, target, doc comment)
    - `grep -c -- '--gate-text\|--skip-freshness' Makefile` returns 0
    - `make help` lists `verify-regressions` with its doc comment
    - `bash scripts/verify-resume.sh` exits 0 and all five `G6.13` WARN lines read `(owner: Phase 3)`
    - `grep -c 'G6.13:unassigned' docs/verify/manifest.txt` returns 0; `grep -c 'G6.13:3' docs/verify/manifest.txt` returns 1
    - `grep -c 'ROLE_BIND_WINDOW' docs/verify/manifest.txt` returns 0
    - `git diff --stat docs/verify/manifest.txt` shows exactly one changed line
    - `git status --porcelain docs/verify/baseline-frozen.txt docs/main.tex docs/AshutoshTiwari.pdf` is empty — no content or freeze touched
    - `make verify-selftest` exits 0 with ten G7/G8 PASS lines and no SKIP
  </acceptance_criteria>
  <done>The Phase-2 net is reachable from make and runs first in `verify`; Phase 3 owns the G6.13 WARN. Commit: `build(03-02): expose the content regression net as a make target and claim the role-block warning`.</done>
</task>

<task type="auto">
  <name>Task 2: Record the net on a canonical surface and hand the site duplicate to Phase 6</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (whole file — append to `### Decisions` in the existing `[Phase NN]` marker style used by lines 44-60, and to `### Blockers/Concerns` / `## Deferred Items` as appropriate)
    - .planning/v1.0-MILESTONE-AUDIT.md :132-165 and :230 (IG-01/IG-02/IG-03 wording and the audit's prescribed closure)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Runtime State Inventory (the `index.html:194-200` finding and its exact quoted text) and §Open Questions OQ-2
    - index.html :190-200 (confirm line 196 still carries both retired figures before writing the handoff)
    - scripts/verify-phase2-regressions.sh :12-18 (the two invariants and the `C++ graph algorithms` anchor rationale)
  </read_first>
  <action>
Append two `[Phase 3]`-marked bullets to `.planning/STATE.md` `### Decisions`, in the established marker + rationale style (statement, then an em-dash "why" clause), and one entry recording the site duplicate.

BULLET 1 — IG-02 closure. Name all four things on this canonical surface: the script path `scripts/verify-phase2-regressions.sh`; what it protects (career-break absence and the locked D-05/D-06 descriptor literals, including the load-bearing `C++ graph algorithms` anchor that ROADMAP Phase 5 criterion 4 rests on — bare `C++` was already ×2 from page 2, so the multi-word form is the only non-vacuous anchor); the new invocation surfaces `make verify-regressions` and the chained first line of `make verify`; and the standing rule that the gating invocation remains `bash scripts/verify-resume.sh && bash scripts/verify-phase2-regressions.sh` because GNU Make 3.81 collapses recipe exit 1 and 2 into its own 2. State that `G6.13`'s WARN is now owned by Phase 3 as a WARN only — the gate is not promoted and `ROLE_BIND_WINDOW` stays a local constant.

BULLET 2 — the site duplicate, OQ-2's recommendation "Phase 3 records, Phase 6 fixes". Name `index.html:196` verbatim as carrying BOTH figures this phase retires (`3–8K images/sec on 32 GPUs` and `10–50× faster image loading`), note that it is published to GitHub Pages by `.github/workflows/jekyll.yml` on every push to `master`, and note that ROADMAP criterion 3 is PDF-scoped so this is deliberately NOT Phase-3 scope. State the consequence plainly: until SITE-01 lands, the live public site contradicts the resume on exactly the two claims the honesty requirement removed. Place it next to the already-known `index.html:207-225` career-break entry (SITE-02) so Phase 6 inherits one concrete, greppable obligation covering both.

Do not rewrite or "clean" any existing STATE.md entry, and do not remove the retired figures from `.planning/` prose anywhere — ROADMAP:69, REQUIREMENTS:21, PROJECT.md:64 and the research corpus retain them BY DESIGN as the record of what was retired and why.

Leave the `[Phase 5]` CUDA-graphs blocker line alone in this task; plan 03-05's D-08 checkpoint supersedes it with a measured outcome.
  </action>
  <verify>
    <automated>grep -Fc 'verify-phase2-regressions.sh' .planning/STATE.md | grep -qvx 0 && grep -Fc 'C++ graph algorithms' .planning/STATE.md | grep -qvx 0 && grep -Fc 'make verify-regressions' .planning/STATE.md | grep -qvx 0 && grep -Fc 'index.html:196' .planning/STATE.md | grep -qvx 0 && grep -Fc 'index.html:207-225' .planning/STATE.md | grep -qvx 0 && grep -Fc '3–8K images/sec on 32 GPUs' index.html | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` `### Decisions` contains a `[Phase 3]`-marked bullet naming all four of: `scripts/verify-phase2-regressions.sh`, `C++ graph algorithms`, `make verify-regressions`, and the `bash scripts/verify-resume.sh` gating rule
    - that bullet states G6.13 is claimed as a WARN only and that `ROLE_BIND_WINDOW` remains a local constant
    - a second `[Phase 3]` record names `index.html:196` with both retired figures and both are verified still present in `index.html` at write time
    - that record names `.github/workflows/jekyll.yml`, states ROADMAP criterion 3 is PDF-scoped, and sits alongside the `index.html:207-225` career-break obligation
    - `git diff .planning/STATE.md` shows additions only — no existing line rewritten or deleted
    - `grep -c '10–50' .planning/ROADMAP.md .planning/REQUIREMENTS.md` still returns non-zero for both — the planning record was not "cleaned"
    - no file under `docs/` and no `index.html` line was modified by this task
  </acceptance_criteria>
  <done>IG-02 is closed on a canonical surface and Phase 6 has a concrete site-sync obligation. Commit: `docs(03-02): document the regression net surfaces and hand the site duplicate to phase 6`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Makefile` recipe → gate scripts | a recipe flag can silently disarm a gate; `--gate-text` and `--skip-freshness` are the two "certifies nothing" waivers |
| `docs/verify/manifest.txt` → `scripts/verify-resume.sh` | threshold and ownership configuration; lowering a threshold to match a regression deletes the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | new `verify-regressions` recipe | mitigate | recipe is a single `@bash $(REGRESS)` line with no waiver flag; asserted by `grep -c -- '--gate-text\|--skip-freshness' Makefile` returning 0 |
| T-03-09 | Tampering | manifest threshold edit | mitigate | exactly one line changed (`git diff --stat` asserts it); `PROBE_LINES`, `DIGIT_BULLET_FLOOR`, `LINE_PT`, `PAGES`, `CEILING_PT` explicitly out of bounds |
| T-03-10 | Tampering | honesty freeze | mitigate | `baseline-frozen.txt` untouched, asserted by an empty `git status --porcelain`; `ALLOW_FROZEN_UPDATE` never set; `make verify-baseline` never run |
| T-03-11 | Repudiation | ownership claim without effect | mitigate | asserted behaviourally — all five `G6.13` lines must READ `(owner: Phase 3)`, not merely that the manifest string changed |
| T-03-12 | Repudiation | silently losing the site contradiction | mitigate | Task 2 verifies both figures are still present in `index.html` at the moment the handoff is written, so the record cannot be stale on arrival |
| T-03-SC | Tampering | package installs | mitigate | none — zero packages installed this phase |
</threat_model>

<verification>
`make verify-regressions` exits 0 with 27 PASS. `make -n verify` shows the net first. `bash scripts/verify-resume.sh` exits 0 with five `(owner: Phase 3)` G6.13 lines. `make verify-selftest` exits 0 with ten G7/G8 PASS and no SKIP — this plan changes no content, so the 7.0pt `G7.2` margin is untouched.

Record `make verify` exit 0 as ROADMAP-criterion-5 corroboration only; the oracle stays `bash scripts/verify-resume.sh`. Never `make clean`.
</verification>

<success_criteria>
- `make verify-regressions` reaches the Phase-2 net; `make verify` runs it first; the header index, `.PHONY` and doc comment all name it
- No Makefile recipe carries `--gate-text` or `--skip-freshness`
- All five `G6.13` WARN lines read `(owner: Phase 3)`; `G6.13:unassigned` is gone; `ROLE_BIND_WINDOW` was not added
- `docs/verify/manifest.txt` diff is exactly one line; `baseline-frozen.txt`, `docs/main.tex` and the built artifacts are untouched
- `.planning/STATE.md` carries the IG-02 documentation bullet and the `index.html:196` Phase-6 handoff
- `bash scripts/verify-resume.sh` exit 0 and `make verify-selftest` exit 0 throughout
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-02-SUMMARY.md` when done. Record the verbatim `make -n verify` recipe lines, the five `G6.13` RESULT lines showing the new owner, and the two STATE.md bullets as written.
</output>
