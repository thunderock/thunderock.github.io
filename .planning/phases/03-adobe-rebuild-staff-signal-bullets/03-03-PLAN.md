---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md
autonomous: true
requirements: [EXP-02, EXP-04, EXP-07, EXP-08]

must_haves:
  truths:
    - "A retain/revise/retire decision exists for every one of the 11 current Adobe entries plus the governance add, recorded BEFORE any new text is written — provable by commit ordering"
    - "Every figure the rebuild will ship traces to a named CODEBASE-EVIDENCE.md entry, and every figure being retired is named as retired"
    - "A budget ledger fixes the per-item rendered-line spend, the +4-line hard ceiling, the reserve rule, and the contingency ladder in measured pt"
    - "D-12: Both EXP-08 draft families exist in a plan document — one cost/efficiency family and one mentoring/leadership line — each ⚠-flagged UNVERIFIED, and none of them is anywhere in docs/main.tex; and per D-13: every provisional magnitude is evidence-implied, with no invented dollar amount, headcount or percentage"
  artifacts:
    - path: ".planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md"
      provides: "criterion-1 disposition table, figure→evidence traceability, budget ledger, EXP-08 drafts"
      contains: "retain-and-revise"
      min_lines: 90
  key_links:
    - from: "03-DECISION-RECORD.md"
      to: ".planning/research/CODEBASE-EVIDENCE.md"
      via: "every shipped figure cites a line-numbered entry"
      pattern: "CODEBASE-EVIDENCE.md:"
    - from: "03-DECISION-RECORD.md"
      to: "docs/main.tex"
      via: "per-entry source anchors :151-:192 with keywords-carried column"
      pattern: "main.tex:"
---

<objective>
Write the phase's decision record: the criterion-1 retain/revise/retire disposition for **every** current Adobe bullet, the figure→evidence traceability table criterion 3 requires, the measured budget ledger every content plan spends against, and the EXP-08 drafts.

Purpose: ROADMAP criterion 1 demands the disposition be recorded *before* new text is written, so no true claim (Falcon-40B, Llama 2 70B, KEDA-on-SQS-depth) disappears implicitly. `PITFALLS.md:689-696` names the signature failure of this phase: rebuilding from `CODEBASE-EVIDENCE.md` alone makes everything that file does not restate look like it was never there. This record is the mechanism that prevents it. CONTEXT `<specifics>` is explicit that it is a **plan artifact, not a `main.tex` artifact**.

Output: one new document, three commits, all landing before wave 2 touches `docs/main.tex`.

**INTRA-WAVE ORDERING (binding).** This plan is wave 1 with `depends_on: []` because Tasks 1 and 2 need nothing from a sibling. Task 3 is different: its verification reads `scripts/verify-phase3-regressions.sh`, which sibling **03-01 Task 2** creates in this same wave. So the ordering inside wave 1 is `03-01 Task 2` → `03-03 Task 3`. Task 3's gate is written to FAIL loudly (`test -x` first, no `2>/dev/null`) when that file is absent rather than silently exiting 0 on a missing-file error — this phase's own rule is that an assertion never observed failing is not a gate, and a hidden error that still exits 0 is the worst version of that. If the net is not on disk yet, Task 3 waits for 03-01 Task 2; it does not weaken the check. Tasks 1 and 2 may proceed at any time.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md
@.planning/research/CODEBASE-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criterion-1 disposition table — every current Adobe entry, with the keywords it carries</name>
  <files>.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md</files>
  <read_first>
    - docs/main.tex :146-167 (the Adobe block as it stands — all 11 entries, one row each)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Current Content Inventory (the A1-A12 enumeration with source anchors, gate lines, rendered cost, digit-bearing flag and keywords carried) and §Keyword Mechanics "fragile-keyword map"
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md D-01 through D-06
    - .planning/research/CODEBASE-EVIDENCE.md (the evidence each revised entry will draw on)
  </read_first>
  <action>
Create the document with a table row for each of the 11 current Adobe entries (A1-A11) plus the governance add (A12), carrying these columns: entry ID, `docs/main.tex` source line, shape (`\resumeItem` lead / `\resumeTopic` / nested `\resumeItem`), today's gate lines, today's rendered line count, digit-bearing yes/no (the `G5.4` flag), **keywords carried**, and the D-03 disposition.

Dispositions are already fixed by D-03 and must total **5× retain-and-revise on the topics, plus the lead revise, plus the nested items, plus 1 add — and zero retire**. Record explicitly that Falcon-40B (A3), Llama 2 70B (A3) and KEDA-on-SQS-depth (A5) survive the rewrite, and that `Source → Transform → Sink` (A5) is retained.

The keywords-carried column is the point of the table, not decoration. Mark the five single-occurrence BLOCKER carriers with a warning glyph and state the consequence in prose beneath the table: `Rust` ×1 at A9 (`main.tex:163`), `SQS` ×1 at A5 (`:156`), `Flash Attention` ×1 at A8 (`:161`), `FSDP` ×2-from-one-string at A7 (`:160`), and `distributed` ×2 with BOTH carriers inside A7/A8 — where capital-D `Distributed Inference Frameworks` does not count because `G6.5` is case-sensitive. A careless reword of any one of those lines turns the phase gate red. Add the Pitfall-7 ordering rule: D-02's governance group lands the second `Rust` copy, so it must land **before or with** any A9 reword.

Record today's Adobe totals as measured: 11 entries, 18 rendered lines (gate 11-28), 4 digit-bearing bullet lines. Record the page-1 digit-bearing baseline of **8** (Adobe 4 + Swiggy 2 + Flipkart 1 + Groupon 1; NetSpeed 0) against `DIGIT_BULLET_FLOOR=7`, and that ROADMAP criterion 4 requires the count never drop below 8 — so `G5.4`'s *value in the message* is what gets asserted, not the absence of a FAIL, because `G5.4` is warn-tier and non-gating. Note Pitfall 5: the regex matches only lines beginning with a bullet glyph, so a figure that lands on a continuation line is invisible to it — the live proof being Swiggy's `(4Bn rows, 10K QPS)` on gate line 43, which is absent from the match set.

State the frozen surfaces this phase must not touch, with reasons: the five titles and dates (arg 1/2 of `\resumeSubheading`), the five employers and subtitles (arg 3/4), `docs/verify/baseline-frozen.txt` in whole, the `\setlist` and `\addtolength` declarations behind the `G4.1` geometry hash, `\pdfgentounicode=1` and `\input{glyphtounicode}`, and `\newpage`.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md && grep -c 'main.tex:15[1-9]\|main.tex:16[0-5]' .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md | grep -qvx 0 && grep -Fc 'retain-and-revise' .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md | grep -qvx 0 && for K in Falcon-40B 'Llama 2 70B' 'SQS queue depth' 'Flash Attention' FSDP Rust distributed; do grep -Fq "$K" .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - the table has one row per current Adobe entry — 11 rows for A1-A11 plus one for the A12 governance add
    - every row names its `docs/main.tex` source line, and the line numbers span :151 through :165
    - zero rows carry a `retire` disposition; the record says so explicitly
    - Falcon-40B, `Llama 2 70B`, KEDA-on-SQS-depth and `Source → Transform → Sink` are each named as surviving
    - the keywords-carried column exists and the five single-occurrence carriers (`Rust`, `SQS`, `Flash Attention`, `FSDP`, plus `distributed`'s two-in-one-place concentration) are flagged with their source lines
    - the Pitfall-7 ordering rule (governance lands the second `Rust` before or with the A9 reword) is stated
    - the digit-bearing baseline of 8 and the "assert the value in the G5.4 message" rule are both recorded
    - the frozen-surface list names all seven surfaces with reasons
    - `git log --oneline -1 -- docs/main.tex` shows a commit that predates this one — no content edit has happened yet
  </acceptance_criteria>
  <done>Criterion 1 is discharged: every current Adobe bullet has a recorded disposition and a keyword inventory, written before any new text. Commit: `docs(03-03): record the adobe bullet disposition and keyword inventory`.</done>
</task>

<task type="auto">
  <name>Task 2: Figure→evidence traceability, the budget ledger, and the Swiggy/Flipkart disposition</name>
  <files>.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md</files>
  <read_first>
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (as Task 1 left it)
    - .planning/research/CODEBASE-EVIDENCE.md (whole file — every shipped figure must cite a line here; note §orion C/D for the migrations, §genie A/B for the Ray benchmark and the fleet, §colligo C for the catalog, §asml for the H100 wave, §ff-data-ingestion for the EMR figure)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-RESEARCH.md §Page-1 Budget Arithmetic (the measured +1..+5 sweep, the free-tail table, and what the locked content costs) and §Pitfall 2, 4, 5
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md D-05, D-06, D-09, D-10, D-11
    - docs/main.tex :169-195 (the Swiggy and Flipkart blocks the disposition covers)
  </read_first>
  <action>
Add three sections.

SECTION A — figure→evidence traceability (criterion 3). One row per figure the rebuild ships: the exact rendered literal, its LaTeX source form, the owning topic, and the named `CODEBASE-EVIDENCE.md` line it traces to. Cover at minimum: `1.09M` + `8×A100` + `16 fractional-GPU actors` (the Ray Data benchmark), `24×A100-40GB` + `9 model queues`, `64 H100s`, `202.6M-row`, `24.8M`, `~700M` + `35–38M`, `40-node`, `27,135`, `9.7k-line`. Record the two attribution-safety rules verbatim as constraints on wording, not as suggestions: the Ray Data engine and its benchmark document are Joel C.'s while the AIDE module is his, so the phrasing is *his module benchmarked on* the Ray Data engine and never "achieved"; and the Content Gate work is framed as fault-tolerant distributed systems in Rust and **never** as inference optimization.

Add a retirement row naming both figures being removed — `10–50` and `3–8K images/sec on 32 GPUs` — with the reason (unprovenanced) and the assertion form: absence must be asserted against the RENDERED EN DASH form, because the source writes `10--50` while the text layer holds `10–50`, making an ASCII-form assertion vacuous.

Record the two LaTeX encoding rules that are BLOCKER-grade: `~700M` must be written `\textasciitilde{}700M` because `$\sim$` extracts as U+223C which is not in `NONASCII_ALLOW` and fails `G6.4`; and a bare `~` is a non-breaking space that silently extracts as a space, making the tilde vanish with no warning. Note `$\times$` → U+00D7 and `--` → U+2013 are both allowed.

SECTION B — the budget ledger. Record the measured baseline `G3.2` p1 headroom of **+50.5pt (+4.39 lines)** at `LINE_PT=11.5`, the measured cost of 11.4-11.5pt per rendered line, and the hard ceiling of **+4 rendered lines** (measured: +4 stays 2 pages at +4.7pt; +5 produces a 3-page PDF with `G1.1` FAIL and `G2.1` FAIL). Then a spend table in wave order: the governance topic at +2 rendered lines (topic-only — the `\resumeTopic`-plus-nested-item form measures 4 rendered lines, the entire budget, so it is forbidden); D-04 mechanisms and D-06 figures at a **0pt target** written into the ≈484 characters of measured free tail room; the D-05 Ray swap at +1; the D-07 bridge at a 0pt target; Swiggy and Flipkart at 0 by D-11. Expected end state +3 lines with `G3.2` p1 ≈ +16.1pt, leaving one rendered line (11.5pt) as the EXP-08 approval fund.

Record the free-tail table per entry (last-rendered-line length and free characters at a conservative ~120-character cap): lead ~44, DIF topic ~80, vLLM item ~18, offline item ~0 at capacity, KEDA/SQS item ~104, FMTF topic ~69, FSDP item ~19, Flash Attention item ~0 at capacity, B&D ~23, Enrichment ~49, DQF ~78.

Record the contingency ladder with numeric thresholds, and label it the authority every content plan spends against: at `G3.2` p1 ≥ +16.1pt the reserve is intact; between +4.7pt and +16.1pt the reserve is consumed and the EXP-08 checkpoint context must say so; below +4.7pt or at 3 pages, back the edit out. Record the two hard rails: never a fifth line, and `G3.2` p1 must never RISE above +50.5pt because net-freeing a rendered line makes the +5 probe stop overflowing and reddens `G7.2` at `--selftest` (reproduced: freeing one line took slack to 61.4pt and the probe rendered 2 pages). State that `PROBE_LINES` is raise-only and is not raised this phase, and that `DIGIT_BULLET_FLOOR` is never lowered.

Record the sanctioned lever and its precondition: the Groupon and NetSpeed folded descriptors may be reworded SHORTER only if the Adobe rebuild measurably runs out of room, every one of their P2-protected literals survives, and neither forbidden shortened form appears. If it is pulled to fund an EXP-08 approval, the reword and the addition go in the SAME commit so the net line delta is zero and the `G7.2` margin is untouched.

SECTION C — Swiggy/Flipkart disposition (D-09/D-10), one row per bullet with its source anchor. Swiggy `:172` Online Inference Platform (carries the `18M+` adoption count on its FIRST rendered line, which is why `G5.4` can see it), `:174` routing item, `:175` Akka item (sole `fault` carrier), `:177` Feature Store (sole page-1 `Spark` carrier; its `4Bn rows, 10K QPS` sits on a continuation line), `:178` Forecasting, `:179` DAQ. Flipkart `:186` Search Intent Models (keeps "millions every day" — note it extracts only from the squeezed stream because it wraps), `:187` Tail Query Classifier, `:188` ML Workflow Automation (D-10's closure story), `:190` Airflow framework item, `:192` Large-Scale Data Pipelines.

State the binding constraint for both blocks: **no new claims requiring new evidence.** `CODEBASE-EVIDENCE.md` covers Adobe repos only, so every Swiggy and Flipkart edit is a rewording of claim material already in those lines. The Flipkart closure needle `manual deploys` is licensed by the existing "first workflow at Flipkart to automate training and auto-deployment" claim — automating a deploy entails the deploy was manual before — and nothing beyond that entailment may be added. Record line-neutrality as a RENDERED-line property with the measured counts: Adobe 18, Swiggy **11**, Flipkart **9** gate-rendered lines today. Note that RESEARCH's "Swiggy 8" figure is superseded by this measurement, and that absolute gate line numbers shift when Adobe grows — so the counts must be re-derived from region boundaries (subtitle anchor to the next blank line), never from fixed line numbers.
  </action>
  <verify>
    <automated>D=.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md; grep -c 'CODEBASE-EVIDENCE.md:' $D | grep -qvx 0 && for L in '1.09M' '8×A100' '24×A100-40GB' '64 H100s' '202.6M' '24.8M' '35–38M' '40-node' '27,135' '10–50' '3–8K images/sec on 32 GPUs' 'textasciitilde' '+50.5pt' '+4.7pt' '+16.1pt' 'PROBE_LINES' 'DIGIT_BULLET_FLOOR' 'manual deploys'; do grep -Fq "$L" $D || { echo "MISSING $L"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - every shipped figure row cites a `CODEBASE-EVIDENCE.md:` line number; no figure appears without a citation
    - the Ray-attribution rule ("his module benchmarked on the Ray Data engine", never "achieved") and the Content-Gate framing rule (never inference optimization) are both recorded as wording constraints
    - the retirement row names `10–50` and `3–8K images/sec on 32 GPUs` and states the EN-DASH assertion rule
    - `\textasciitilde{}700M` is recorded as mandatory with the U+223C / `G6.4` reason, and the bare-`~` silent-space hazard is noted
    - the budget ledger records +50.5pt baseline, 11.4-11.5pt per line, the +4 ceiling with the +5 three-page evidence, and a per-item spend table summing to an expected +3
    - the free-tail table lists all eleven Adobe entries with their free-character estimates
    - the contingency ladder gives the three numeric thresholds (+16.1pt, +4.7pt, 3 pages) and both hard rails, including that `G3.2` p1 must never rise above +50.5pt
    - the sanctioned Groupon/NetSpeed lever is recorded with its precondition and the same-commit rule
    - Section C has one row per Swiggy and Flipkart bullet with source anchors spanning :172-:192
    - the no-new-claims constraint is stated, the `manual deploys` entailment is justified, and line-neutrality is recorded as rendered counts Adobe 18 / Swiggy 11 / Flipkart 9 with the re-derive-from-boundaries instruction
  </acceptance_criteria>
  <done>Criterion 3's traceability, the spend authority for waves 2-4, and the Swiggy/Flipkart disposition are recorded. Commit: `docs(03-03): trace every shipped figure to evidence and fix the page-1 budget ledger`.</done>
</task>

<task type="auto">
  <name>Task 3: Draft both EXP-08 bullet families, ⚠-flagged, in the plan document only</name>
  <files>.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md</files>
  <read_first>
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (as Task 2 left it)
    - .planning/research/CODEBASE-EVIDENCE.md §ff-data-engineering (the STS AssumeRole storm fix, +457 ln, thousands of STS calls/sec and throttling, shared negative cache with 60s cooldown and exactly-one half-open retry), §asml-img-data-dags (the 6-week silent outage root-cause plus install/import smoke-test CI, and the preemptible-fleet idempotent autorecovery), §orion and §genie (the framework-adoption material for the mentoring family)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md D-12, D-13, D-14, D-15
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-01-PLAN.md `<locked_literals>` Class D (the three needles the drafts must carry and the `STS` needle that is forbidden)
    - scripts/verify-phase3-regressions.sh (must already exist — 03-01 Task 2 creates it in this same wave; this task's verification reads it and FAILS rather than passing vacuously if it is absent, so wait for 03-01 Task 2 before running the gate)
    - docs/main.tex :158 and :178 (the existing "adopted across the org" and "Founding member" framings the mentoring draft must not duplicate)
  </read_first>
  <action>
Add a section holding both draft families. Every draft is a candidate only and lives here — never in `docs/main.tex`, not even as a `%`-commented line: a commented draft is invisible to `pdftotext` and to every gate, so nothing would catch it graduating from comment to live text, and the `⚠ UNVERIFIED` marker itself would be unassertable.

COST/EFFICIENCY FAMILY — draft one bullet per candidate so the user has a real choice: (a) the STS assume-role storm fix, (b) the 6-week silent outage root-cause plus smoke-test CI, (c) preemptible-fleet idempotent autorecovery. Each draft must carry its locked absence needle so the post-checkpoint assertion can flip: candidate (a) contains `AssumeRole`, candidate (b) contains `6-week`. Record explicitly that `STS` is NOT usable as an assertion needle because a fixed-string search for it matches inside `TESTS` — the wording may still say STS, but `AssumeRole` is the needle.

MENTORING/LEADERSHIP FAMILY — one draft line, carrying the needle `mentored`. It must not merely restate what the document already says: `main.tex:158` already claims a training framework "adopted across the org" and `:178` already claims "Founding member" of the forecasting platform. The draft has to add the people dimension those lines do not carry.

D-13 governs every figure in every draft: **evidence-implied magnitudes only** — "thousands of throttled calls/sec eliminated", "a 6-week silent-outage class closed". Never a dollar amount, never a headcount, never a percentage that no commit supports. Prefix each draft with `⚠ UNVERIFIED` and, beneath it, state in one line exactly what the user is being asked to confirm and which `CODEBASE-EVIDENCE.md` line the magnitude leans on.

Record the ship rule and the budget consequence: absent-by-default per D-14; the phase can close green with all drafts rejected, and rejection is a valid outcome rather than a failure. State each draft's estimated rendered cost as 1 or 2 lines against whatever headroom survives the rebuild, and state that the real cost will be re-measured at the conditional wave rather than trusted from this estimate. Note that if the rebuild ends at the +4-line ceiling there is no room for even one more line, so an approval then requires the sanctioned Groupon/NetSpeed reword-shorter in the same commit.

Record the owning role for each draft — which Adobe topic or block would carry it if approved — so the checkpoint can ask a concrete question rather than an abstract one.

ORDERING NOTE for this task's gate: the EXP-08 corroboration below runs `scripts/verify-phase3-regressions.sh`, which 03-01 Task 2 creates in this same wave. The check is deliberately written as `test -x scripts/verify-phase3-regressions.sh && …` with NO `2>/dev/null` — a missing file must abort the gate, not be swallowed into a vacuous exit 0. If the net is not there yet, wait for 03-01 Task 2. The substantive per-needle `grep -Fc … docs/main.tex = 0` checks stand on their own and do not depend on it.
  </action>
  <verify>
    <automated>D=.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md; SC="$(mktemp -d)"; grep -Fc 'UNVERIFIED' $D | grep -qvx 0 && for N in AssumeRole 6-week mentored; do grep -Fq "$N" $D || exit 1; done && for N in AssumeRole 6-week mentored; do test "$(LC_ALL=C grep -Fc -- "$N" docs/main.tex)" = 0 || exit 1; done && test -x scripts/verify-phase3-regressions.sh && bash scripts/verify-phase3-regressions.sh | tee "$SC/p3.txt" | grep -E 'EXP-08' | grep -c FAIL | grep -qx 0 && grep -cE 'PASS.*EXP-08' "$SC/p3.txt" | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - at least three cost/efficiency drafts and one mentoring draft exist, each prefixed `⚠ UNVERIFIED`
    - the drafts carry the needles `AssumeRole`, `6-week` and `mentored`, and the record states why `STS` is not a needle
    - no draft contains a dollar amount, a headcount, or a percentage; every magnitude cites the `CODEBASE-EVIDENCE.md` line it leans on
    - the mentoring draft adds a people dimension absent from `main.tex:158` and `:192`, and the record says how it differs
    - `LC_ALL=C grep -Fc 'AssumeRole' docs/main.tex` and the same for `6-week` and `mentored` all return 0 — the drafts are nowhere in the source
    - `test -x scripts/verify-phase3-regressions.sh` succeeded — 03-01 Task 2 had already landed the net, so the EXP-08 corroboration below was a real observation and not a hidden missing-file error
    - the EXP-08 assertions in `bash scripts/verify-phase3-regressions.sh` are all PASS (absent-by-default still holds), and at least one `EXP-08` PASS line was actually observed — a zero-line EXP-08 slice is a defect, not a pass
    - the ship rule records that rejection is a green outcome, and each draft names the Adobe topic or block that would carry it
    - the budget consequence at the +4-line ceiling is stated, including the same-commit reword rule
  </acceptance_criteria>
  <done>Both EXP-08 families are drafted with ⚠ UNVERIFIED flags, evidence-implied magnitudes, and locked assertion needles — none of them in `docs/main.tex`. Commit: `docs(03-03): draft the cost and leadership candidate bullets pending sign-off`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CODEBASE-EVIDENCE.md` → record → `docs/main.tex` | the only sanctioned path a claim may take onto the resume; a figure entering without a citation is an unprovenanced claim |
| draft document → `docs/main.tex` | crossing requires recorded per-bullet approval (D-14); the crossing must be gate-visible, never a LaTeX comment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Repudiation | claim silently dropped in the rewrite | mitigate | criterion-1 table with a disposition per entry, written and committed before any `main.tex` edit; commit ordering is the proof |
| T-03-14 | Repudiation | unprovenanced figure ships | mitigate | traceability table requires a `CODEBASE-EVIDENCE.md:` line per figure; a row without a citation is a defect |
| T-03-15 | Tampering | unapproved draft graduates to live text | mitigate | drafts live only here; `%`-commented drafts explicitly forbidden; Class D absence assertions cover both `$TEX` and the text layer |
| T-03-16 | Repudiation | invented magnitude presented as evidenced | mitigate | D-13 evidence-implied magnitudes only; every draft figure cites its evidence line; no dollar amounts, headcounts or percentages |
| T-03-17 | Tampering | attribution inflation (Ray engine, Content Gate) | mitigate | both binding rules recorded as wording constraints in Section A, enforced downstream by the governance framing clause in the P3 net |
| T-03-SC | Tampering | package installs | mitigate | none — zero packages installed this phase |
</threat_model>

<verification>
This plan writes no code and touches no file under `docs/`. After each task: `git status --porcelain docs/` must be empty, and `bash scripts/verify-resume.sh` plus `bash scripts/verify-phase2-regressions.sh` must stay green (they are unaffected).

Task 3 additionally runs `bash scripts/verify-phase3-regressions.sh` and requires its EXP-08 assertions to be PASS — the drafts must not have leaked into the source. That net is created by sibling 03-01 Task 2 in the same wave, so Task 3 runs after it; the check begins with `test -x` and carries no `2>/dev/null`, so an absent net fails the gate instead of passing vacuously.
</verification>

<success_criteria>
- `03-DECISION-RECORD.md` exists with the criterion-1 disposition table (11 current entries + the add, zero retires), the figure→evidence traceability table, the budget ledger with the contingency ladder, the Swiggy/Flipkart disposition, and both EXP-08 draft families
- Every shipped figure cites a `CODEBASE-EVIDENCE.md` line; both retired figures are named as retired
- Every draft is `⚠ UNVERIFIED`, carries its locked needle, and is absent from `docs/main.tex`
- Task 3's EXP-08 corroboration ran against an existing net (`test -x` succeeded, no error suppressed), after sibling 03-01 Task 2 landed it
- The whole plan lands before any commit touching `docs/main.tex`
- `git status --porcelain docs/` empty throughout
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-03-SUMMARY.md` when done. Record the disposition totals (retain/revise/retire counts), the figure→evidence row count, the budget ledger's expected end state in pt, and the draft needle list — waves 2-4 spend against these figures.
</output>
