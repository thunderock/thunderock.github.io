---
phase: 03-adobe-rebuild-staff-signal-bullets
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - docs/main.tex
  - docs/verify/manifest.txt
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: true
requirements: [EXP-02, EXP-04]

must_haves:
  truths:
    - "The Adobe block OPENS on fault-tolerance / distributed-inference language rather than job-description voice"
    - "A Rust training-data governance topic exists, framed as fault-tolerant distributed systems and never as inference optimization"
    - "All six D-04 fault-tolerance mechanism families are named inline where they occurred"
    - "D-06's scale figures are spread by owning topic, and A100 plus H100 are promoted to KEYWORDS_REQUIRED in the same commit that lands them"
    - "The document is still 2 pages, all 13 required keywords still pass, and the digit-bearing bullet count is 8 or higher"
  artifacts:
    - path: "docs/main.tex"
      provides: "rebuilt Adobe lead bullet, new governance topic, revised retained topics carrying mechanisms and figures"
      contains: "Training-Data Governance"
    - path: "docs/verify/manifest.txt"
      provides: "A100 and H100 promoted from KEYWORDS_TARGET to KEYWORDS_REQUIRED"
      contains: "A100"
  key_links:
    - from: "docs/main.tex"
      to: "docs/AshutoshTiwari.pdf"
      via: "make build; artifacts committed with the source so G0.3 holds on a fresh checkout"
      pattern: "AshutoshTiwari"
    - from: "docs/verify/manifest.txt"
      to: "docs/main.tex"
      via: "keyword promotion paired with the text that lands the keyword, in one commit"
      pattern: "KEYWORDS_REQUIRED"
---

<objective>
Rebuild the page-1 Adobe block's content: rewrite the lead bullet into fault-tolerance / distributed-inference voice (D-01), add the Rust training-data governance topic (D-02), and revise the five retained topics so D-04's mechanisms and D-06's figures land in the topics that own them (D-03/D-04/D-06).

Purpose: this is the phase's core spend. ROADMAP criterion 2's second clause requires the block to *open* on that language, not merely contain it. The measured ceiling is +4 rendered lines and this plan spends +2 of them on the governance topic, targeting **zero** additional lines for everything else by writing into the ≈484 characters of free tail room that cost 0pt.

Output: the Adobe block rebuilt except for the D-05 swap and the D-07 bridge (plan 03-05), with the P3 net's 03-04-owned FAILs driven to zero.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-CONTEXT.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md
@.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md
@.planning/research/CODEBASE-EVIDENCE.md
</context>

<measurement_loop>
Run after EVERY edit batch, in this order. Do not predict — Phase 2's central lesson is that budget arithmetic is measured.

`make build` (never `make clean` — it deletes the `.fdb_latexmk` record `G0.3` reads), then `bash scripts/verify-resume.sh` (the ORACLE; never `make verify`, which collapses exit 1 and 2 into Make 3.81's own 2), then `bash scripts/verify-phase2-regressions.sh`, then `bash scripts/verify-phase3-regressions.sh`.

Read the harness output in this order: **`G1.1` first** (page count — the PRIMARY fit gate; `G3.1` stays green on a 3-page document because LaTeX breaks the page rather than overrunning the ceiling, so checking `G3.1` for fit is checking the wrong thing, and `G2.3`/`G2.4` also stayed green on the measured 3-page variant) → `G2.1` (page-2 boundary) → **`G3.2` p1 headroom, recorded in pt AND lines** → `G5.4`'s VALUE (must read 8 or higher) → `G6.5` ×13 all PASS → `G6.6` (which targets landed) → `G6.4` (glyph census).

Baseline entering this plan: `G3.2` p1 `+50.5pt (+4.39 lines)`, `G5.4` 8, `G6.5` ×13 PASS, `G6.4` PASS. Hard rails: never a fifth rendered line (measured: +5 is a 3-page PDF); `G3.2` p1 must never RISE above +50.5pt, because net-freeing a line makes the +5 probe stop overflowing and reddens `G7.2`. `PROBE_LINES` and `DIGIT_BULLET_FLOOR` are not edited.
</measurement_loop>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the lead bullet into fault-tolerance / distributed-inference voice, line-neutral</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - docs/main.tex :146-167 (the whole Adobe block; :151 is the lead `\resumeItem` being rewritten, and :76-78 is the `\resumeItem` macro definition)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (the A1 disposition row and its keywords-carried entry; the free-tail table)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §2.1 (the measured extraction contract: the subtitle at gate line 10 is followed immediately by the lead bullet at 11 with no blank line — this adjacency is what the P3 lead clause anchors on)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-01-PLAN.md `<locked_literals>` (the lead anchor set and the 03-04-owned needles)
    - .planning/research/CODEBASE-EVIDENCE.md §orion (the fault-tolerance and distributed-GPU-inference material the new voice draws on)
  </read_first>
  <action>
Rewrite `docs/main.tex:151` — today "Leading parts of the org's MLOps platform for building, deploying, and monitoring GPU-accelerated data pipelines and model inference on Kubernetes, spanning the full ML lifecycle from data ingestion to inference." Keep the `\resumeItem` shape; the whole Adobe block keeps its `\resumeTopic` group structure per D-01 (no flattening, no restructure into dense groups).

The rewritten line MUST contain at least one of `fault-tolerant`, `fault-tolerance` or `distributed inference` and it must land on the bullet's FIRST rendered line, because the P3 clause extracts exactly the first non-empty line after the Adobe subtitle and matches the anchor set against that one line. Prefer including the lowercase word `distributed` in the new text: `distributed` is a BLOCKER keyword with only two carriers today, both inside the two Foundation-Model-Training sub-items, and case-sensitive `G6.5` does not count the title-case `Distributed Inference Frameworks` topic name — so a third lowercase carrier de-risks Pitfall 7's concentration.

Preserve the keywords A1 carries today: `inference` (×2 on this entry) and `Kubernetes`. Line-neutral: the entry renders as 2 lines today and must render as 2 after; its second rendered line is 76 characters, giving roughly 44 free characters at zero pt cost, which is where D-06's `40-node` daily EMR ingestion figure may land if it reads naturally in the lifecycle clause.

Do not touch `:148` or `:149` — the title, dates, employer and subtitle cells are frozen (`G5.1`, `G5.2`, `G5.5` each assert exactly one whole-line occurrence, and the P3 lead clause anchors on the `:149` subtitle).

Voice target: staff signal, not job description. "Leading parts of the org's MLOps platform" is the job-description phrasing being replaced; criterion 2 asks what the block OPENS on. The qualitative half of that judgment is confirmed by the human at the EXP-08 checkpoint (plan 03-07); the mechanical half is the anchor assertion.

Then `make build` and run the full measurement loop. Commit the source and the regenerated artifacts TOGETHER: `.pdf`, `.log` and `.fdb_latexmk` ride in the source commit, plus `.aux`, `.fls`, `.out` if they changed. A source-only commit makes a fresh checkout of that commit refuse at `G0.3` exit 2.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h04a.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h04a.txt && grep -Eq 'RESULT G5\.4 .*: (8|9|1[0-9])' /tmp/h04a.txt && test "$(grep -c 'RESULT G6.5   PASS' /tmp/h04a.txt)" = 13 && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh | grep -E 'EXP-02a' | grep -qc PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-phase3-regressions.sh` shows the EXP-02a lead-bullet assertion PASS
    - the extracted first non-empty line after the Adobe subtitle contains one of `fault-tolerant`, `fault-tolerance`, `distributed inference` (case-insensitive)
    - `bash scripts/verify-resume.sh` exits 0; `G1.1` reports 2 pages; `G2.1` PASS; all 13 `G6.5` lines PASS
    - the `G5.4` message reads 8 or higher
    - `G6.4` PASS — no new undeclared codepoint
    - the lead entry still renders as exactly 2 gate lines, and `G3.2` p1 is unchanged at `+50.5pt (+4.39 lines)`
    - `git diff HEAD~1 --stat docs/main.tex` shows only line 151 changed; `docs/main.tex:148` and `:149` are byte-identical to the previous commit
    - `git status --porcelain docs/verify/baseline-frozen.txt` is empty
    - the commit contains `docs/main.tex` together with `docs/AshutoshTiwari.pdf`, `.log` and `.fdb_latexmk`
    - `bash scripts/verify-phase2-regressions.sh` prints 27 PASS
  </acceptance_criteria>
  <done>The Adobe block opens on fault-tolerance / distributed-inference language at zero line cost. Commit: `feat(03-04): open the adobe block on fault tolerance and distributed inference`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Rust training-data governance topic — topic-only, +2 rendered lines</name>
  <files>docs/main.tex, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - docs/main.tex :152-166 (the insertion zone; :163, :164 and :165 are the three live topic-only examples to copy for shape and density, and :109-111 is the `\resumeTopic` macro definition)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §2.2 (why the analog is the TOPIC-ONLY shape and not the nested-list shape, with `:163`'s 138+97=235-character two-line density as the length target) and §2.3
    - .planning/research/CODEBASE-EVIDENCE.md :29-32 (Content Gate: +27,135 lines solely his, claim/receipt idempotent registration, immutable S3 evidence layouts, manifest-after-data publication, canonical sample identity, token fingerprinting, 9,722-line integration test) and :12 (the binding framing rule)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (the A12 row, the traceability rows for `27,135` and `9.7k-line`, and the budget ledger's +2-line allocation)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-01-PLAN.md `<locked_literals>` (the six governance needles plus the Rust-count clause and the forbidden framing tokens)
  </read_first>
  <action>
Insert ONE new `\resumeTopic` line inside the Adobe `\resumeItemListStart` block, anywhere in the `:152-165` range — ordering is the agent's discretion, so place it where it reads best at rendered width. It must be **topic-only with NO nested `\resumeItemListStart`**: the nested-item form measures 4 rendered lines, which is the entire page-1 budget, and combined with plan 03-05's +1 line the document becomes 3 pages. The three live topic-only examples prove a topic carries a full multi-clause claim in 2 rendered lines.

The topic NAME must contain the literal `Training-Data Governance` — the P3 net anchors its framing clause on that string, so a synonym breaks the assertion. The claim sentence must contain: the figure `27,135`, the phrase `claim/receipt`, the phrase `immutable S3 evidence layouts`, the token `9.7k-line`, the word `lineage`, and the word `Rust`.

Landing `Rust` here is load-bearing beyond the topic itself: `Rust` is a BLOCKER keyword with exactly one carrier today at `:163`, so this topic must land the second copy BEFORE or WITH any `:163` reword (Task 3 does that reword). The P3 net asserts `Rust` at 2 or more occurrences.

FRAMING IS BINDING, not stylistic. `CODEBASE-EVIDENCE.md:12` scopes this work as fault-tolerant distributed systems in Rust and explicitly NOT as inference optimization. The P3 net slices this topic's rendered region and asserts it contains none of `throughput`, `latency`, `speedup`, `faster`, `images/sec`. Write the claim around idempotent claim/receipt registration, immutable evidence layouts, manifest-after-data publication and the integration-test scale — not around performance.

Target 2 rendered lines at roughly `:163`'s density. Write the figure `27,135` onto the topic's FIRST rendered line: `G5.4`'s regex matches only lines beginning with a bullet glyph, so a digit on a continuation line is invisible to it and the digit-bearing count would not rise.

Then `make build` and run the full measurement loop. Expect `G3.2` p1 to fall from +50.5pt to approximately +27.6pt (+2.40 lines) and `G5.4` to read 9. If the topic renders as 3 lines, shorten it — do not accept the third line, because the budget ledger reserves the remaining room for plan 03-05's D-05 swap and the EXP-08 fund. Commit source and artifacts together.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h04b.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h04b.txt && grep -q 'RESULT G2.1   PASS' /tmp/h04b.txt && test "$(grep -c 'RESULT G6.5   PASS' /tmp/h04b.txt)" = 13 && grep -Eq 'RESULT G5\.4 .*: (9|1[0-9])' /tmp/h04b.txt && grep -q 'RESULT G6.4   PASS' /tmp/h04b.txt && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh | grep -E 'EXP-02b' | grep -vc FAIL</automated>
  </verify>
  <acceptance_criteria>
    - every EXP-02b governance assertion in `bash scripts/verify-phase3-regressions.sh` is PASS, including the `Rust` ≥2 clause and the forbidden-framing slice clause
    - the new topic renders as exactly 2 gate lines and has no `\resumeItemListStart` of its own
    - `27,135` appears on the topic's FIRST rendered line, and the `G5.4` message reads 9 or higher
    - `bash scripts/verify-resume.sh` exits 0; `G1.1` reports 2 pages; `G2.1` PASS; 13 `G6.5` PASS; `G6.4` PASS
    - `G3.2` p1 headroom is recorded and is at least `+16.1pt` — enough for plan 03-05's +1 line plus the reserve
    - `G3.2` p1 did not RISE above +50.5pt
    - `grep -c 'Rust' docs/main.tex` returns 2 or more
    - the governance topic's rendered region contains none of `throughput`, `latency`, `speedup`, `faster`, `images/sec`
    - `bash scripts/verify-phase2-regressions.sh` prints 27 PASS
    - the commit contains `docs/main.tex` with `.pdf`, `.log` and `.fdb_latexmk`
  </acceptance_criteria>
  <done>The Rust governance topic ships in 2 rendered lines, correctly framed, carrying the second `Rust` copy. Commit: `feat(03-04): add the rust training-data governance and lineage topic`.</done>
</task>

<task type="auto">
  <name>Task 3: Revise the five retained topics — mechanisms and figures into free tails, promote A100 and H100</name>
  <files>docs/main.tex, docs/verify/manifest.txt, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk</files>
  <read_first>
    - docs/main.tex :152-165 (the five retained topics and their nested items; note the source idioms to preserve verbatim — `Llama~2~70B`, `$\times$`, `Source $\to$ Transform $\to$ Sink`, and the `SQS queue depth` tail)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-DECISION-RECORD.md (the A2-A11 disposition rows with their keywords-carried column, the figure→evidence table, the free-tail table, and the contingency ladder — this is the spend authority)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-PATTERNS.md §2.3 (the nested-group shape and the source-idiom preservation table) and §4.2 (the manifest promotion edit, and Trap A: the `:phase` suffix must be stripped)
    - .planning/research/CODEBASE-EVIDENCE.md §orion A/C/D, §genie B, §colligo C, §asml, §ff-data-ingestion (the mechanism and figure provenance)
    - docs/verify/manifest.txt :51-54 (the promotion rule and both keyword lines)
    - .planning/phases/03-adobe-rebuild-staff-signal-bullets/03-01-PLAN.md `<locked_literals>` (the seven D-04 mechanism needles and the D-06 figure needles owned by this plan)
  </read_first>
  <action>
Revise the five retained topics and their nested items so they carry D-04's mechanisms and D-06's figures, writing into ALREADY-WRAPPED bullets' last rendered lines wherever possible. That room costs **0pt** — verified by appending roughly 145 characters across three Adobe tails with `G3.2` p1 unchanged at +50.5pt.

D-04 requires the mechanisms named INLINE WHERE THEY OCCURRED — no aggregate fault-tolerance bullet. The seven needles the P3 net asserts are `exponential backoff`, `full jitter`, `concurrency-safe S3 checkpoint`, `lease/TTL heartbeat`, `DLQ`, `circuit breaker`, `idempotent re-run`. Place each in the topic whose subsystem actually produced it, using the evidence file as the arbiter: the SQS producer's backoff-with-full-jitter and the DLQ/delivery-retry work belong with the pipeline and queue-modeling story; the concurrency-safe S3 checkpoint cache belongs with the enrichment inference path; lease/TTL heartbeats and the consecutive-failure circuit breaker belong with the enrichment control-plane services; idempotent re-runs belong with the migration and preemptible-fleet reliability story.

D-06 spreads figures by OWNING topic — the needles this plan lands are `24×A100-40GB`, `9 model queues`, `64 H100s`, `202.6M-row`, `24.8M`, `~700M`, `35–38M`, and `40-node` if Task 1 did not already place it. Source forms are mandatory, not cosmetic: `24$\times$A100-40GB` and `8$\times$A100` for the multiplication sign, `35--38M` for the en-dash range, and `\textasciitilde{}700M` for the approximate catalog size. Writing `$\sim$700M` extracts as U+223C, which is not in `NONASCII_ALLOW`, and fails `G6.4` as a BLOCKER; writing a bare `~` produces a non-breaking space that silently extracts as a space, making the tilde vanish with no warning.

PRESERVE, byte-for-byte in effect: `Falcon-40B` and `Llama~2~70B` (A3), `KEDA`-autoscaled on `SQS queue depth` and `Source $\to$ Transform $\to$ Sink` (A5), `FSDP/FSDP2` and the lowercase `distributed` (A7), `Flash Attention` and its lowercase `distributed` (A8), `Rust` and `KEDA` and `Kubernetes` (A9). `SQS`, `Flash Attention` and `FSDP` each have exactly one carrier line; `distributed` has two, both in A7/A8. Any careless reword turns `G6.5` red.

Order within this task: reword A9 (Build & Deployment) only AFTER Task 2's governance topic has landed the second `Rust` copy, so `Rust` is never momentarily ×0.

MANIFEST PROMOTION, in the SAME commit as the text that lands the keywords: append `A100` and `H100` to `KEYWORDS_REQUIRED` at `:53` **with the `:phase` suffix stripped**, and delete `A100:3` and `H100:3` from `KEYWORDS_TARGET` at `:54`. Moving an entry verbatim with its suffix makes `G6.5` grep for the literal string `A100:3` and FAIL. Do not promote `CUDA graphs` on any branch — the exact-match form is Phase 5's, and D-07's hyphenated phrasing does not land it anyway. Do not touch `torch.compile` or `Triton` here; plan 03-05 owns those. Change no other manifest key.

BUDGET DISCIPLINE. Target 0 additional rendered lines for this whole task. Work in small batches and rebuild after each — a tail append that overflows costs +1 line and `G3.2` catches it immediately. If a locked item genuinely cannot fit at 0pt, consult the contingency ladder: spending a fourth line is permitted only while `G1.1` still reports 2 pages and `G3.2` p1 stays at or above +4.7pt, and it must be recorded because it consumes the EXP-08 reserve. Never a fifth line. Never shorten locked content to free room, and never let `G3.2` p1 rise above +50.5pt.

Then `make build`, run the full measurement loop, and commit source plus manifest plus artifacts together.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh > /tmp/h04c.txt && grep -q 'RESULT G1.1   PASS  page tree reports 2 page' /tmp/h04c.txt && test "$(grep -c 'RESULT G6.5   PASS' /tmp/h04c.txt)" = 15 && grep -q 'RESULT G6.4   PASS' /tmp/h04c.txt && grep -Eq 'RESULT G5\.4 .*: (9|1[0-9])' /tmp/h04c.txt && bash scripts/verify-phase2-regressions.sh >/dev/null && bash scripts/verify-phase3-regressions.sh > /tmp/p04c.txt; grep -E 'FAIL' /tmp/p04c.txt | grep -vcE '/03-0[56] ' | grep -qx 0 && make verify-selftest >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-phase3-regressions.sh` has ZERO remaining FAILs owned by `/03-04` — every D-04 mechanism needle and every 03-04-owned D-06 figure needle is PASS
    - the only remaining P3 FAILs are owned by `/03-05` and `/03-06`
    - `G6.5` now prints **15** PASS lines (13 original plus `A100` and `H100`), and the `assert_promoted` clauses for `A100` and `H100` in the P3 net are PASS
    - `grep -c 'A100:3\|H100:3' docs/verify/manifest.txt` returns 0; `grep -c 'CUDA graphs' docs/verify/manifest.txt` returns 1 (still a target, unpromoted)
    - `bash scripts/verify-resume.sh` exits 0; `G1.1` 2 pages; `G2.1` PASS; `G6.4` PASS; `G5.4` reads 9 or higher
    - `G3.2` p1 headroom recorded; it is at or above `+4.7pt`, did not rise above +50.5pt, and if it fell below `+16.1pt` the SUMMARY records that the EXP-08 reserve is consumed
    - `grep -c 'sim\$700M\|\$\\sim\$700M' docs/main.tex` returns 0 — the approximate figure uses `\textasciitilde{}`
    - `docs/main.tex` still contains `Falcon-40B`, `Llama~2~70B`, `SQS queue depth`, `Source $\to$ Transform $\to$ Sink`, `FSDP/FSDP2`, `Flash Attention`, and 2 or more `Rust`
    - `bash scripts/verify-phase2-regressions.sh` prints 27 PASS
    - `make verify-selftest` exits 0 with ten G7/G8 PASS and no SKIP — the `G7.2` probe is still armed
    - `git diff --stat docs/verify/manifest.txt` shows exactly two changed lines, and the commit contains `docs/main.tex`, `docs/verify/manifest.txt` and the regenerated artifacts together
  </acceptance_criteria>
  <done>The retained topics carry D-04's mechanisms and D-06's figures, and A100/H100 are promoted in the same commit that landed them. Commit: `feat(03-04): name fault-tolerance mechanisms and scale figures in their owning topics`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/main.tex` → rendered PDF → text layer | content crosses into the artifact every gate reads; a wrap point or glyph choice can silently void a keyword or a figure |
| `docs/verify/manifest.txt` → `scripts/verify-resume.sh` | keyword tiers decide what is a BLOCKER; a mis-formed promotion turns a green gate red or an absent keyword invisible |
| evidence record → resume claim | only figures traced in `03-DECISION-RECORD.md` may ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | frozen title/date/employer/subtitle cells | mitigate | `:148`/`:149` asserted byte-identical against the previous commit; `G5.1`/`G5.2`/`G5.5` exact-once whole-line clauses stay PASS; `baseline-frozen.txt` untouched |
| T-03-19 | Repudiation | single-occurrence BLOCKER keyword silently lost | mitigate | `G6.5` asserted at 15 PASS lines after promotion; the P3 retention group covers `SQS queue depth`, `Flash Attention`, `FSDP`, `Rust`; governance lands the second `Rust` before the A9 reword |
| T-03-20 | Tampering | keyword promoted with the `:phase` suffix intact | mitigate | suffix-stripped promotion asserted by `grep -c 'A100:3\|H100:3'` returning 0 plus the P3 `assert_promoted` conjunction |
| T-03-21 | Spoofing | stale artifact certified as fresh | mitigate | artifacts ride in the source commit so `G0.3` holds on a fresh checkout; `make clean` never run |
| T-03-22 | Tampering | silent 3-page overflow | mitigate | `G1.1` read FIRST after every build; `G3.1`, `G2.3` and `G2.4` are known to stay green on a 3-page document and are not used as fit evidence |
| T-03-23 | Tampering | budget freed rather than spent | mitigate | `G3.2` p1 asserted never to rise above +50.5pt; `make verify-selftest` run at task 3 to prove `G7.2` still armed; `PROBE_LINES` not raised |
| T-03-24 | Repudiation | Content Gate framed as inference optimization | mitigate | P3 governance slice clause asserts the topic region carries none of `throughput`, `latency`, `speedup`, `faster`, `images/sec` |
| T-03-25 | Tampering | undeclared glyph voids the text layer | mitigate | `G6.4` asserted PASS after every build; `\textasciitilde{}` mandated over `$\sim$` (U+223C is not in `NONASCII_ALLOW`) |
| T-03-SC | Tampering | package installs | mitigate | none — zero packages installed this phase |
</threat_model>

<verification>
After each task: the full measurement loop above, with `G1.1` read first. After the final task additionally `make verify-selftest` (exit 0, ten G7/G8 PASS, no SKIP) to prove the wave does not end net-freed.

Gate on `bash scripts/verify-resume.sh`, never on `make verify`. Never run `make clean`. Record `G3.2` p1 in pt and lines in every task's evidence — the next plan spends against it.
</verification>

<success_criteria>
- The Adobe block opens on fault-tolerance / distributed-inference language, asserted by the P3 lead clause
- A topic-only `Training-Data Governance` group ships in 2 rendered lines with all six governance needles, `Rust` ≥2, and no performance framing
- All seven D-04 mechanism needles and all 03-04-owned D-06 figure needles are PASS
- `A100` and `H100` are in `KEYWORDS_REQUIRED` with suffixes stripped and out of `KEYWORDS_TARGET`; `G6.5` prints 15 PASS
- `G1.1` 2 pages, `G2.1` PASS, `G6.4` PASS, `G5.4` ≥9, `G3.2` p1 recorded and ≥ +4.7pt without ever rising above +50.5pt
- `bash scripts/verify-phase2-regressions.sh` 27 PASS; remaining P3 FAILs owned only by 03-05 and 03-06; `make verify-selftest` exit 0
- Frozen cells and `baseline-frozen.txt` untouched; artifacts committed with their source
</success_criteria>

<output>
Create `.planning/phases/03-adobe-rebuild-staff-signal-bullets/03-04-SUMMARY.md` when done. Record per task: the `G3.2` p1 figure in pt and lines, the `G5.4` value, the rendered-line count of the Adobe region, the P3 FAIL census delta, and — if a fourth line was spent — an explicit statement that the EXP-08 reserve is consumed and an approval will require the sanctioned Groupon/NetSpeed reword in the same commit.
</output>
