---
phase: 01-verification-harness
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/verify-resume.sh
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03]

must_haves:
  truths:
    - "The harness exits non-zero when the built PDF is not exactly 2 pages, asserted from the PDF page tree rather than from build output or fill measurement"
    - "The harness exits non-zero unless page 2 starts at PUBLICATIONS, asserted positively — the absence of company names on page 2 is reported as INFO only, never gated"
    - "The harness exits non-zero if any of the 5 employment date strings, 5 printed titles or 5 employer names differs by a single byte from docs/verify/baseline-frozen.txt"
    - "The harness exits non-zero if the geometry or setlist declarations drift, detected by a line-number-independent normalized hash"
    - "The harness exits non-zero when a required keyword is absent, a ligature or private-use or replacement codepoint appears, or the LinkedIn handle and portfolio URL fail to extract as literal text"
    - "Run against today's committed PDF the harness exits 1 and names exactly three BLOCKER defects — the missing contact literals, the en-dash handle, and the Education parse order"
    - "Every later-phase assertion prints as WARN with its owning phase on every run and never affects the exit code"
  artifacts:
    - path: "scripts/verify-resume.sh"
      provides: "G1 page count, G2 page-1 boundary, G3 fill/headroom, G4 geometry freeze, G5 honesty freeze, G6 ATS text-layer integrity"
      min_lines: 320
  key_links:
    - from: "scripts/verify-resume.sh"
      to: "pdfinfo page tree"
      via: "Pages field parse"
      pattern: "Pages:"
    - from: "scripts/verify-resume.sh"
      to: "per-page text extraction"
      via: "pdftotext -f 2 -l 2 first non-empty line"
      pattern: "pdftotext -f 2 -l 2"
    - from: "scripts/verify-resume.sh"
      to: "docs/verify/baseline-frozen.txt"
      via: "whole-line count must equal exactly 1 per frozen string"
      pattern: "grep -Fxc"
    - from: "scripts/verify-resume.sh"
      to: "docs/verify/manifest.txt EDU_ keys"
      via: "Education-section-scoped ordering and window check"
      pattern: "EDU_INSTITUTION"
---

<objective>
Implement every content assertion: the structural page gates that make a silent overflow impossible to ship, the two freezes that make an employment date or a margin change loud, and the ATS text-layer gates that turn the three live defects into named failures.

Purpose: this plan is where the harness becomes red-on-arrival. At its end `bash scripts/verify-resume.sh` must exit 1 against today's committed PDF and name the missing contact literals, the corrupted en-dash handle, and the Education parse order — while every other assertion passes. That combination is what makes Phase 2's own success criterion 5 reachable by fixing EXP-06 plus the Education reorder, with no change to this harness.
Output: `scripts/verify-resume.sh` extended with G1.1–G1.2, G2.1–G2.4, G3.1–G3.4, G4.1–G4.5, G5.1–G5.5, G6.1–G6.15, across four tasks (see the last note on why four).
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-verification-harness/01-01-SUMMARY.md
@.planning/phases/01-verification-harness/01-VALIDATION.md

Source of truth — read the cited ranges, not the whole file:
- `.planning/phases/01-verification-harness/01-RESEARCH.md:176-200` — R6, the severity-tier reconciliation and the table of which defects may be BLOCKERs
- `.planning/phases/01-verification-harness/01-RESEARCH.md:216-299` — the canonical assertion spec, groups G1 through G7, with today's measured result per assertion
- `.planning/phases/01-verification-harness/01-RESEARCH.md:312-411` — tooling per assertion class, all commands executed against these artifacts
- `.planning/phases/01-verification-harness/01-RESEARCH.md:543-570` — expected-failure bootstrapping, the exact three failure messages, exit-code vocabulary
- `.planning/phases/01-verification-harness/01-RESEARCH.md:651-662` — anti-patterns, each one a measured false gate
- `.planning/phases/01-verification-harness/01-RESEARCH.md:742-850` — Code Examples: geometry hash, frozen-string matcher, codepoint census, headroom parser
- `.planning/phases/01-verification-harness/01-RESEARCH.md:1063-1065` — Open Question 4 resolution for the invisible-text scan
</context>

<interfaces>
<!-- Contracts established by Plan 01. Use them; do not redefine them. -->

Already available in scripts/verify-resume.sh:
  result <id> <status> <message>        # FAIL increments BLOCKERS
  warn   <id> <owner> <message>         # never touches BLOCKERS; numeric owner prints as
                                        # "Phase N", the sentinels none/unassigned/any print verbatim
  info   <id> <message>
  die2   <message>                      # exit 2; emits NO RESULT line of its own
  manifest_get <KEY> / manifest_list <KEY>
  $PDF $TEX $MANIFEST $FROZEN $WORK
  $WORK/raw.txt   # pdftotext default mode, verbatim — has form feeds glued to page-opening tokens
  $WORK/gate.txt  # form-feed-normalized — the ONLY valid input for whole-line (grep -Fx) matchers

  --skip-freshness  # turns G0.3 into a loud SKIP so a run whose --tex is a mutated copy can
                    # reach the content assertions. Required by every geometry-drift check in
                    # this plan, because G0.3 otherwise exits 2 before G4 runs. Never used by
                    # a Makefile target; a run carrying it is not a verification pass.

Manifest keys this plan reads:
  PAGES CEILING_PT LINE_PT P1_BOTTOM_WS_PT P1_TOP_PT PAGE_SIZE
  PAGE2_OPENS_WITH EXPERIENCE_HEADING LAST_EXPERIENCE_EMPLOYER
  SECTIONS CONTACT_LITERALS FORBIDDEN_LITERAL
  PROHIBITED_WORDS PROHIBITED_PHRASES NONASCII_ALLOW
  KEYWORDS_REQUIRED KEYWORDS_TARGET WARN_OWNERS
  EDU_HEADING EDU_INSTITUTION EDU_CITY EDU_DATE EDU_BIND_WINDOW
  DIGIT_BULLET_FLOOR SKILLS_MAX_LINES GEOMETRY_KEYS
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Structural page gates — G1 page count, G2 page-1 boundary, G3 fill and headroom</name>
  <files>scripts/verify-resume.sh</files>
  <read_first>
    - `scripts/verify-resume.sh` (current state from Plan 01 — the emitters, `$WORK` layout and manifest helpers to build on)
    - `docs/verify/manifest.txt` (the real key names and values)
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:216-244` — G1, G2 and G3 assertion tables, including the two blockquotes explaining why G2.4 is INFO and why G3.1 can never replace G1.1
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:314-347` — Class 1 page-count method comparison, Class 2 page-boundary command, Class 3 bbox parsing with the determinism caveat
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:828-850` — the headroom parser code example with its verified output values
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:651-656` — the three anti-patterns that apply here
  </read_first>
  <action>
Add the G1, G2 and G3 assertion blocks to `scripts/verify-resume.sh`, emitting RESULT lines in stable ID order.

G1.1 is a BLOCKER and the primary fit gate: parse the `Pages:` field from `pdfinfo` on the resolved PDF with awk and compare against manifest `PAGES` (2). Do not substitute a fill or headroom measurement for this and do not read the LaTeX log. Measured four times against this document: a 3-page overflow build exits 0 with zero overfull and zero underfull warnings, because there is no overfull-page warning in TeX and `\raggedbottom` at `docs/main.tex:46` suppresses underfull-vbox complaints. The page count is the only reliable signal.

Do NOT try to prove G1.1 discriminates by pointing `--pdf` at `docs/transcript.pdf`. Measured with `pdfinfo`: that file is itself 2 pages, so the gate PASSES on it and the "proof" would prove nothing; it also has no sibling `transcript.fdb_latexmk`, so G0.3 would take its absent-record path first. The discrimination proof for this gate is Plan 03's G7.3, which builds a genuine +5-line overflow probe in a scratch directory and asserts a `G1.1` FAIL against it. That is the only place a real multi-page negative control exists, and duplicating it here would need a scratch `latexmk` build this task deliberately does not do.

G1.2 is INFO: count form feeds in `$WORK/raw.txt` and cross-check against the page count. `pdftotext` emits one form feed per page including the last, so the raw count equals the page count — do not add one. Report agreement or divergence; never gate.

G2.1 is a BLOCKER and must be a positive assertion: extract page 2 alone with `pdftotext -f 2 -l 2`, take the first non-empty line, and require it to match manifest `PAGE2_OPENS_WITH` anchored and case-insensitively. `\scshape` round-trips to real uppercase so case-insensitive anchored matching is correct. Verified today: the first non-empty page-2 line is byte-exactly `PUBLICATIONS` with no leading whitespace. Note this uses per-page extraction, so it reads clean and does not need `gate.txt`.

G2.2 is a BLOCKER: page 1 contains manifest `EXPERIENCE_HEADING`. G2.3 is a BLOCKER: page 1 contains manifest `LAST_EXPERIENCE_EMPLOYER` as a whole line. Both read page-1-only extraction, form-feed-normalized before whole-line matching.

G2.4 is INFO only and must never gate. Count occurrences of the employer names on page 2 and report. The reason is measured and reproduced independently: a +4-line probe produced a 3-page document whose page 2 began with a role title rather than an employer; a +5-line probe's page 2 opened with a sentence fragment; a third recorded probe opened with a different fragment matching no company name at all. The spill fragment is a function of the injection point and cannot be enumerated, so a negative test returns a false OK. Put that reasoning in a comment so a later reader does not "strengthen" G2.4 into a gate.

G3 runs `pdftotext -bbox` into `$WORK/bbox.xml` and parses it with an inlined `python3 - <<'PY'` block using only stdlib `xml.etree`. Namespace-strip each tag before comparing. Per page compute the minimum word yMin as the top extent, the maximum word yMax as the bottom extent, and the count of words positioned outside the page box on any of the four edges.

G3.1 is a BLOCKER: every page's bottom extent must be less than or equal to manifest `CEILING_PT` (764.4). G3.2 is INFO: report per-page headroom in points and in lines using manifest `LINE_PT` (11.5). G3.3 is INFO: report page-1 bottom whitespace against manifest `P1_BOTTOM_WS_PT` (27.7) — this is the same measurement as G3.1 viewed from the physical page edge rather than the text-block ceiling, kept only for traceability. G3.4 is a BLOCKER: zero words outside the page box, which is the observable proxy for off-page text stuffing.

Record in a comment that G3.1 alone can never replace G1.1: on the 3-page artifact, page-1 headroom reads positive because once content spills, page 1 is no longer the binding page. Expected values against today's PDF are page 1 bottom 764.3 with headroom about +0.1pt and page 2 bottom 710.9 with headroom about +53.5pt, 1218 words total, zero outside the box.

Pass the manifest-derived ceiling and line height into the python block as argv, not by string-interpolating them into the heredoc body.
  </action>
  <verify>
    <automated>bash scripts/verify-resume.sh >/tmp/v2a.out 2>&1; grep -q 'RESULT G1.1  *PASS' /tmp/v2a.out && grep -q 'RESULT G2.1  *PASS' /tmp/v2a.out && grep -q 'RESULT G2.2  *PASS' /tmp/v2a.out && grep -q 'RESULT G2.3  *PASS' /tmp/v2a.out && grep -q 'RESULT G3.1  *PASS' /tmp/v2a.out && grep -q 'RESULT G3.4  *PASS' /tmp/v2a.out && grep -q 'RESULT G3.2  *INFO' /tmp/v2a.out && grep -q 'RESULT G2.4  *INFO' /tmp/v2a.out</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-resume.sh | grep -c 'RESULT G1\.'` returns 2 and `grep 'RESULT G1.1'` shows PASS with the value 2
    - `bash scripts/verify-resume.sh | grep 'RESULT G2.1'` shows PASS and names `PUBLICATIONS`
    - `bash scripts/verify-resume.sh | grep -cE 'RESULT G2\.(2|3)  *PASS'` returns 2
    - `bash scripts/verify-resume.sh | grep 'RESULT G2.4'` shows INFO, never FAIL, and the script contains a comment explaining the false-OK measurement
    - `bash scripts/verify-resume.sh | grep 'RESULT G3.1'` shows PASS and reports a page-1 bottom extent of 764.3
    - `bash scripts/verify-resume.sh | grep 'RESULT G3.2'` shows INFO and reports headroom in both points and lines, containing both `pt` and `lines`
    - `bash scripts/verify-resume.sh | grep 'RESULT G3.4'` shows PASS and reports 0 words outside the page box
    - The page-count gate reads the page tree, not the log: `grep -c 'Pages:' scripts/verify-resume.sh` is at least 1 and `grep -ci 'overfull\|underfull' scripts/verify-resume.sh` returns 0 outside comments
    - The gate is parameterized, not hardcoded to 2: `LC_ALL=C grep -Fc 'PAGES' scripts/verify-resume.sh` is at least 1, so the threshold comes from the manifest. Discrimination against a real multi-page PDF is proven by Plan 03's G7.3 overflow probe, NOT here — `docs/transcript.pdf` measures 2 pages, so it would pass this gate
    - `grep -c 'pdftotext -f 2 -l 2' scripts/verify-resume.sh` is at least 1
  </acceptance_criteria>
  <done>G1, G2 and G3 emit in stable order; the page-count threshold is read from the manifest and its discrimination proof is deferred to Plan 03's overflow probe; G2.4 and G3.2/G3.3 are INFO and cannot affect the exit code.</done>
</task>

<task type="auto">
  <name>Task 2: The two freezes — G4 geometry and spacing, G5 honesty invariants</name>
  <files>scripts/verify-resume.sh</files>
  <read_first>
    - `scripts/verify-resume.sh` (current state after Task 1)
    - `docs/verify/baseline-frozen.txt` (the 15 strings and the geometry hash to compare against)
    - `docs/verify/manifest.txt` (`GEOMETRY_KEYS`, `PAGE_SIZE`, `P1_TOP_PT`, `DIGIT_BULLET_FLOOR`, `WARN_OWNERS`)
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:245-270` — G4 and G5 assertion tables plus the three blockquote notes on matcher choice, extracted-versus-source forms, and the deliberate omission of the career-break title
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:150-168` — R4: why geometry drift is observed from the LaTeX source, pattern-extracted, normalized and hashed, and why a per-line grep truncates the two-line setlist declaration
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:371-377` — Class 6 byte-exactness with the verified counts
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:753-768` — the geometry-hash code example, verified to reproduce the frozen value
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:796-806` — the frozen-string matcher example. Take its structure and its exact-count comparison, but DROP the `|| true` on line 802: no `set -e` is in effect, so the guard is unnecessary, and this task's criteria assert that token is absent from the script
    - `.planning/phases/01-verification-harness/01-01-PLAN.md` `<interfaces>` block — the `--skip-freshness` semantics every geometry-drift check in this task depends on
    - `docs/main.tex` lines 15, 19, 46, 60, 66-72 (the `glyphtounicode` input, the `pdfgentounicode` switch, `\raggedbottom`, the `\addtolength` block and the three `\setlist` declarations)
  </read_first>
  <action>
Add the G4 and G5 blocks.

G4.1 is a BLOCKER and the hard margin gate. Compute a normalized hash of the geometry declarations from the resolved TEX and compare against the `[geometry-sha256]` value in the frozen file. Build the hash by concatenating two sorted streams and piping to `shasum -a 256`: first, a grep that extracts each `\addtolength` declaration whose target is one of manifest `GEOMETRY_KEYS`, matched only within the braces so trailing content cannot leak in; second, a small inlined python block that regex-extracts every `\setlist` declaration with a brace-balanced pattern, collapses each match's internal whitespace to single spaces, and sorts. The two-stream approach is required because the `\setlist[itemize]` declaration spans two source lines and a per-line grep captures only half of it. Sorting plus whitespace normalization is what makes the hash line-number independent, which matters because line numbers shift in every later phase. Against today's source this must reproduce `8b13a46bf5716cb9bb9042a4e1bb60a9d6919fd5f347a34ecdc6122d7f465a56`; a `\textheight` change from `1.2in` to `1.3in` in a temp copy must produce a different hash. Prefer `shasum -a 256` over a BSD-only hash tool so the check also works on Linux.

Observing that drift end-to-end requires Plan 01's `--skip-freshness` flag alongside `--tex`. A mutated copy no longer matches the `.fdb_latexmk` md5 record, so G0.3 emits its FAIL and exits 2 before any G4 assertion runs; the flag turns G0.3 into a loud SKIP and lets the run reach G4.1. Every drift check in this task's criteria and in Plan 03's G8.2 control therefore passes both flags. Do not add a second bypass, do not reorder G0.3 behind G4, and do not call the hash function outside the assertion path.

Do not attempt to observe `\textheight` from the PDF. `pdfinfo` reports only paper size and cannot see the text-block height, which is why the source declaration is the observation point.

G4.2 is a BLOCKER: `pdfinfo` `Page size` matches manifest `PAGE_SIZE` (`612 x 792 pts (letter)`). This detects a paper-size change only, which is why G4.1 carries the real margin gate.

G4.3 is a WARN whose owner is the sentinel `none`, purely corroborating: the page-1 top content extent from the G3 bbox parse is within one point of manifest `P1_TOP_PT` (27.3). Read that owner from `WARN_OWNERS` like every other WARN — Plan 01's manifest carries a `G4.3:none` entry precisely so this assertion does not print an empty owner. G4.4 is a BLOCKER: the resolved TEX contains both `\input{glyphtounicode}` and `\pdfgentounicode=1` — these are the ATS switch that makes the text layer extractable at all, and their removal would invalidate every text-layer assertion in G5 and G6. G4.5 is INFO: the TEX contains `\raggedbottom`, reported because its presence is the reason G1.1 must exist.

G5 reads the frozen file with `while IFS= read -r`, skipping blank lines, `#` comment lines and `[section]` headers, and tracking which section it is inside so each string reports under the right assertion ID — dates under G5.1, titles under G5.2, employers under G5.5. For each string run `LC_ALL=C grep -Fxc` against `$WORK/gate.txt` and require the count to be exactly 1. Not at least 1 — exactly 1. Emit FAIL naming the observed count and the string when it is anything else.

Adapt the research snippet at `01-RESEARCH.md:796-806` in one respect: drop its `|| true` on the `grep -Fxc` command substitution. The guard is only needed under `set -e`, which Plan 01 deliberately does not set, so `n=$(LC_ALL=C grep -Fxc "$s" "$WORK/gate.txt")` already yields `0` without aborting when the string is absent. Keeping `|| true` would put an error-swallowing idiom in the one place the honesty freeze must be loudest, which is why this task's acceptance criteria assert the token is absent from the whole script. Take the snippet's structure and its exact-count comparison verbatim; take nothing else.

Three matcher properties are load-bearing and each was measured. `-x` (whole line) resolves a real substring collision: `Software Development Engineer` is a substring of `Software Development Engineer (Search Relevance)`, so a substring count returns 2 for the bare form and would report PASS even if Groupon's title were deleted; whole-line counts return exactly 1 for both. `-F` (fixed string) prevents the parentheses and ampersands inside titles from being interpreted as regex. `LC_ALL=C` gives byte semantics so the UTF-8 EN DASH inside the frozen date strings matches byte-exactly. The input must be `gate.txt`, not `raw.txt`.

Skip the `[geometry-sha256]` section's value when iterating strings — it is consumed by G4.1, not by the whole-line matcher.

G5.3 is a BLOCKER: zero occurrences of `Staff` and zero slash-hedged titles (a slash immediately adjacent to a seniority word, covering both `/Staff` and `Senior/`) anywhere in the extracted text. G5.4 is a WARN owned by Phase 3: count page-1 extracted lines that begin a bullet and contain a digit, and compare against manifest `DIGIT_BULLET_FLOOR` (7). Keep it WARN because the metric is crude — a rewording that moves a number onto a wrapped continuation line trips it spuriously, and it exists to make compression that silently eats metrics visible rather than to gate.

Read the WARN owner for each WARN-tier assertion from manifest `WARN_OWNERS` rather than hardcoding the phase number in the script, so a promotion is a manifest edit.
  </action>
  <verify>
    <automated>bash scripts/verify-resume.sh >/tmp/v2b.out 2>&1; grep -q 'RESULT G4.1  *PASS' /tmp/v2b.out && grep -q 'RESULT G4.2  *PASS' /tmp/v2b.out && grep -q 'RESULT G4.4  *PASS' /tmp/v2b.out && test "$(grep -cE 'RESULT G5\.(1|2|5)  *PASS' /tmp/v2b.out)" -eq 15 && grep -q 'RESULT G5.3  *PASS' /tmp/v2b.out && grep -q 'RESULT G5.4  *WARN' /tmp/v2b.out</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-resume.sh | grep 'RESULT G4.1'` shows PASS and names the hash `8b13a46bf5716cb9bb9042a4e1bb60a9d6919fd5f347a34ecdc6122d7f465a56`
    - Geometry drift fires, using the documented override so G0.3 does not refuse first: `T=$(mktemp -d); sed 's/{\\textheight}{1.2in}/{\\textheight}{1.3in}/' docs/main.tex > "$T/main.tex"; bash scripts/verify-resume.sh --tex "$T/main.tex" --skip-freshness 2>&1 | grep 'RESULT G4.1'` shows FAIL
    - The override is genuinely load-bearing here, not decorative: the same command WITHOUT `--skip-freshness` exits 2 and emits no `G4.1` line at all
    - `bash scripts/verify-resume.sh | grep -cE 'RESULT G5\.(1|2|5)  *PASS'` returns exactly 15
    - Honesty freeze fires on a one-byte change: `T=$(mktemp -d); sed 's/Sep. 2017/Sep. 2018/' docs/verify/baseline-frozen.txt > "$T/frozen.txt"; bash scripts/verify-resume.sh --frozen "$T/frozen.txt" 2>&1 | grep 'RESULT G5.1'` shows a FAIL naming count 0
    - The matcher is exact-count, not at-least: `grep -cE 'Fxc|grep -Fx' scripts/verify-resume.sh` is at least 1, and `grep -nE '\-ge 1|>= *1' scripts/verify-resume.sh` shows no match applied to a frozen-string count
    - The research snippet's error-swallowing guard was dropped, not carried over: `LC_ALL=C grep -Fc '|| true' scripts/verify-resume.sh` returns 0
    - `bash scripts/verify-resume.sh | grep 'RESULT G4.2'` shows PASS naming `612 x 792`
    - `bash scripts/verify-resume.sh | grep -cE 'RESULT G4\.4  *PASS'` returns 1
    - `bash scripts/verify-resume.sh | grep 'RESULT G4.3'` shows WARN and contains `owner: none` — the sentinel read from `WARN_OWNERS`, never an empty owner
    - `bash scripts/verify-resume.sh | grep 'RESULT G5.4'` shows WARN and contains the text `owner: Phase 3`
    - `bash scripts/verify-resume.sh | grep 'RESULT G5.3'` shows PASS
    - `grep -c 'shasum -a 256' scripts/verify-resume.sh` is at least 1
  </acceptance_criteria>
  <done>The geometry hash reproduces the frozen value and detects a `\textheight` change when observed through `--skip-freshness`; all 15 frozen strings verify as unique whole lines and a one-byte mutation flips the assertion to FAIL; the digit-bullet metric prints as WARN with its owner read from the manifest, and G4.3 prints its `none` sentinel rather than an empty owner.</done>
</task>

<task type="auto">
  <name>Task 3: ATS glyph, keyword and literal gates — G6.1 through G6.11, including two of the three live defects</name>
  <files>scripts/verify-resume.sh</files>
  <read_first>
    - `scripts/verify-resume.sh` (current state after Task 2)
    - `docs/verify/manifest.txt` (`NONASCII_ALLOW`, `KEYWORDS_REQUIRED`, `KEYWORDS_TARGET`, `PROHIBITED_WORDS`, `PROHIBITED_PHRASES`, `SECTIONS`, `CONTACT_LITERALS`, `FORBIDDEN_LITERAL`, `WARN_OWNERS`)
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:271-290` — the G6 assertion table with today's measured result for every one of the 15 assertions
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:349-369` — Class 4 keyword match modes with the measured `Go`/`Goal` trap, Class 5 the four-class codepoint gate with the full census
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:543-565` — the exact expected FAIL messages for G6.9 and G6.10, each naming the fix owner
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:808-826` — the codepoint census code example
    - `.planning/phases/01-verification-harness/01-01-PLAN.md` note 3 (finding P1 — the form-feed normalization) and its `<interfaces>` block (the pinned `--census` semantics this task's glyph gate must satisfy, since Plan 03's G8.4 control drives G6.1–G6.4 through that mode)
    - `docs/main.tex` lines 130-140 (the masked contact anchors)
  </read_first>
  <action>
Add the first G6 block: the glyph gate, the keyword tiers, the prohibited terms, the heading extraction and the two contact-literal defects.

G6.1 through G6.4 are the glyph gate, implemented as one inlined `python3 - <<'PY'` block reading `$WORK/gate.txt` and taking the manifest `NONASCII_ALLOW` list as argv. Build a codepoint census of every character above U+007F, then apply four rules. G6.1 BLOCKER: zero ligature codepoints in U+FB00 through U+FB06. G6.2 BLOCKER: zero private-use codepoints in U+E000 through U+F8FF. G6.3 BLOCKER: zero U+FFFD replacement characters. G6.4 WARN owned by any phase adding punctuation: every non-ASCII codepoint present is in the allowlist, reporting each unlisted codepoint with its Unicode name, its count, and the remedy of adding it to `NONASCII_ALLOW` if intentional. The four-class design supersedes a five-character ligature check because private-use and replacement characters are the classes that actually appear when the `ToUnicode` mapping breaks, and neither is caught by looking for the five ligature glyphs. Today's census is exactly EN DASH times 40, right single quote times 6, bullet times 6, rightwards arrow times 3, increment times 2, multiplication sign times 1, em dash times 1, and the two double quotes once each, with zero ligatures, zero private-use and zero replacement characters.

Factor this block so its input file is a parameter rather than hardcoded to `$WORK/gate.txt`. Plan 03's G8.4 control invokes the same gate through `--census FIXTURE` against a crafted text file, per the mode contract Plan 01 pins; a hardcoded path would force Plan 03 to duplicate the census logic.

G6.5 is a BLOCKER: every keyword in manifest `KEYWORDS_REQUIRED` is present, counted case-sensitively with a fixed-string occurrence count. Case sensitivity is required, not optional: ATS matchers are substring-literal, and a case-insensitive count reports `PyTorch` as more present than it indexably is because the Skills line writes a differently-cased variant. G6.6 is a WARN owned per manifest `KEYWORDS_TARGET`: for each `keyword:phase` pair report the occurrence count and the owning phase. These two lists together are the promotion mechanism; do not add any other deferral construct.

G6.7 is a BLOCKER: zero occurrences of each entry in manifest `PROHIBITED_WORDS` matched with word boundaries, and zero occurrences of each entry in `PROHIBITED_PHRASES` matched as a fixed string. Use the exact invocation research measured — `grep -Fowc` for the word-bounded count and `grep -Fc` for the phrase count — so the acceptance criterion can check for a literal rather than guess at a regex. The word-boundary flag is mandatory and measured: a naive substring count for `Go` returns 1 today because it matches `Goal` in the source text, while a word-boundary count returns 0. Without the flag this assertion fails on today's document for a false reason.

G6.8 is a BLOCKER: every heading in manifest `SECTIONS` extracts as a whole line from `$WORK/gate.txt`. Read this one carefully — it MUST use the form-feed-normalized text. Against the raw extraction a whole-line search for the page-2 opening heading returns 0, because the page-1 form feed is glued to that heading's first character; against the normalized text all seven headings return 1. This was measured this session and corrects the research table's claim of seven passes, which only holds after normalization.

G6.9 is a BLOCKER and one of the three live defects: every literal in manifest `CONTACT_LITERALS` is present in the extracted text. Emit one FAIL line per missing literal, each naming the observed count and an actionable remedy. Today the email is present once, and the handle, the GitHub URL and the portfolio URL are all absent, because `docs/main.tex:134-136` masks them behind anchor text rather than making the anchor text the literal URL — 26 URI annotations exist inside compressed object streams and are invisible to text extraction. Each FAIL message must name `Owner: Phase 2 / EXP-06`.

G6.10 is a BLOCKER and the second live defect: zero occurrences of manifest `FORBIDDEN_LITERAL`, the EN DASH form of the handle. Today it occurs once, because LaTeX converts the source double hyphen to an en dash. The remedy in the message is to break the ligature in the anchor text at `docs/main.tex:134`, owner Phase 2 / EXP-06.

G6.11 is INFO and must never gate: run `pdftotext -raw` into `$WORK/rawmode.txt` and report its divergence from the default extraction as a word-gluing count or a short sample. Use exactly that filename — no hyphen — so the scratch-file names stay a single consistent token family alongside `raw.txt` and `gate.txt` and the acceptance criterion can grep for a literal. Gating on raw mode is provably unfixable in LaTeX, so report only.
  </action>
  <verify>
    <automated>bash scripts/verify-resume.sh >/tmp/v2c.out 2>&1; ec=$?; test $ec -eq 1 &amp;&amp; test "$(grep -cE 'RESULT G6\.(9|10) +FAIL' /tmp/v2c.out)" -eq 4 &amp;&amp; test "$(grep -cE 'RESULT (G0|G1|G2|G3|G4|G5)\.[0-9]+ +FAIL' /tmp/v2c.out)" -eq 0 &amp;&amp; test "$(grep -cE 'RESULT G6\.(1|2|3|5|7|8) +FAIL' /tmp/v2c.out)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-resume.sh; test $? -eq 1` succeeds — the two contact defects alone already turn the harness red
    - `bash scripts/verify-resume.sh 2>&1 | grep -c 'RESULT G6.9  *FAIL'` returns 3 (handle, github.com/thunderock, thunderock.github.io — the email passes)
    - `bash scripts/verify-resume.sh 2>&1 | grep -c 'RESULT G6.10  *FAIL'` returns 1
    - No BLOCKER outside G6.9 and G6.10 fails yet: `bash scripts/verify-resume.sh 2>&1 | grep -E 'RESULT G[0-9.]+ +FAIL' | grep -cvE 'G6\.(9|10)'` returns 0
    - Every FAIL message names an owner: `bash scripts/verify-resume.sh 2>&1 | grep -E 'RESULT G[0-9.]+ +FAIL' | grep -cv 'Phase 2'` returns 0
    - All seven headings pass on normalized text: `bash scripts/verify-resume.sh 2>&1 | grep -c 'RESULT G6.8  *PASS'` returns 7, and `bash scripts/verify-resume.sh 2>&1 | grep -c 'RESULT G6.8  *FAIL'` returns 0
    - The `Go` word-boundary trap is handled with the measured invocation: `bash scripts/verify-resume.sh 2>&1 | grep 'RESULT G6.7'` shows PASS, and `LC_ALL=C grep -Fc 'grep -Fowc' scripts/verify-resume.sh` is at least 1
    - Glyph gate passes with an exact census: `bash scripts/verify-resume.sh 2>&1 | grep -cE 'RESULT G6\.(1|2|3) +PASS'` returns 3 and `grep 'RESULT G6.4'` shows no unlisted codepoint
    - The glyph gate takes its input file as a parameter, so `--census` can reuse it: `grep -cE 'RESULT G6\.(1|2|3)' scripts/verify-resume.sh` counts occurrences inside a single reusable block, and `grep -c 'gate.txt' scripts/verify-resume.sh` shows the path passed in rather than embedded in the python heredoc body
    - `bash scripts/verify-resume.sh 2>&1 | grep -c 'RESULT G6.5  *PASS'` equals the number of pipe-separated entries in the manifest `KEYWORDS_REQUIRED` value
    - Deferred keywords print as WARN with owners, never FAIL: `bash scripts/verify-resume.sh 2>&1 | grep -c 'RESULT G6.6  *WARN'` returns 7 and each line contains `owner: Phase`
    - The raw-mode scratch file uses the hyphen-free name: `LC_ALL=C grep -Fc 'rawmode.txt' scripts/verify-resume.sh` is at least 1 and `LC_ALL=C grep -Fc 'raw-mode.txt' scripts/verify-resume.sh` returns 0
    - `bash scripts/verify-resume.sh 2>&1 | grep 'RESULT G6.11'` shows INFO and never FAIL
    - No suppression construct was introduced — checked as four separate literal greps so no single brittle alternation can mask one: `LC_ALL=C grep -Fic 'expected_fail' scripts/verify-resume.sh` returns 0, `LC_ALL=C grep -Fic 'allowlist' scripts/verify-resume.sh` returns 0, `LC_ALL=C grep -Fic 'known_fail' scripts/verify-resume.sh` returns 0, and `LC_ALL=C grep -Fc '|| true' scripts/verify-resume.sh` returns 0
    - Repo stays clean: `bash scripts/verify-resume.sh >/dev/null 2>&1; test -z "$(git status --porcelain)"`
  </acceptance_criteria>
  <done>The glyph gate, keyword tiers, prohibited terms and heading extraction all pass on today's artifacts; G6.9 and G6.10 fail with owners named; the census block is parameterized so Plan 03's `--census` control can drive it; the harness already exits 1.</done>
</task>

<task type="auto">
  <name>Task 4: Education binding, adjacency metrics and the invisible-text scan — G6.12 through G6.15</name>
  <files>scripts/verify-resume.sh</files>
  <read_first>
    - `scripts/verify-resume.sh` (current state after Task 3)
    - `docs/verify/manifest.txt` (the `EDU_` keys, `SECTIONS`, `SKILLS_MAX_LINES`, `WARN_OWNERS`)
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:286-290` — the G6.12 through G6.15 rows with today's measured results
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:543-565` — the exact expected FAIL message for G6.12, naming the fix owner
    - `.planning/phases/01-verification-harness/01-RESEARCH.md:1063-1065` — Open Question 4: assert near-white colour usage, not definition
    - `.planning/phases/01-verification-harness/01-01-PLAN.md` note 4 (finding P2 — the duplicate institution line) and note 5 (why Phase 2, not Phase 4, owns the Education fix)
    - `docs/main.tex` line 19 (the unused `accentlight` definition) and lines 236-246 (the Education `\resumeSubheading` whose argument order is the defect)
  </read_first>
  <action>
Add the remaining G6 assertions and the run's closing summary.

G6.12 is a BLOCKER and the third live defect. Scope it to the Education section: locate the manifest `EDU_HEADING` line in `$WORK/gate.txt`, then find the next heading from manifest `SECTIONS` after it, and take only the line range between them. Within that region assert two things — first, that the `EDU_INSTITUTION` line appears BEFORE the `EDU_CITY` line, and second, that the `EDU_DATE` line is within manifest `EDU_BIND_WINDOW` non-empty lines of the institution line. Report FAIL when either clause fails, naming which clause and the observed line offsets, with the remedy of reordering the `\resumeSubheading` arguments at `docs/main.tex:240-242` and `Owner: Phase 2 (EXP-01 already edits Education; re-asserted by Phase 4 / PG2-03)`.

The section scoping is mandatory and is a correction to the research spec, measured this session: `Indiana University, Bloomington` appears twice in today's extraction, once on page 1 inside the Career Break block and once in the Education section. A whole-document ordering comparison would compare the page-1 occurrence against the Education city line, conclude the institution comes first, and return a false PASS — silently un-asserting one of the three defects ROADMAP Phase 1 criterion 5 requires the harness to name. Today's correct scoped observation is the city line first, then the institution line, then the date line, so the ordering clause is what fails.

G6.13 is a WARN owned per `WARN_OWNERS` as unassigned: for each employer, report whether the employer, title and date-range lines fall within a small line window. Today they split into three non-adjacent blocks. Keep it WARN — no requirement owns the fix, so a BLOCKER here would make `make verify` permanently red and break every later phase's criterion 5.

G6.14 is a WARN owned by Phase 5: count the rendered lines of the Skills section, scoped the same way as the Education region, and compare against manifest `SKILLS_MAX_LINES`. Today it measures 4 non-empty lines against a target of 3. Factor the heading-to-next-heading region extraction from G6.12 into a reusable helper and call it here rather than writing the scoping twice.

The helper needs an explicit terminal-section case, and G6.14 is the assertion that exercises it. `TECHNICAL SKILLS` is the LAST entry in manifest `SECTIONS`, so no following heading exists to bound its region: measured today, that heading sits at line 170 of `gate.txt` and the file ends at line 176. A helper that returns an empty range when it finds no next heading makes G6.14 count 0 lines, and 0 is under `SKILLS_MAX_LINES`, so the assertion would report a silent false OK in place of today's real deviation of 4. Specify the fallback explicitly: when the located heading is the last one present in the document, the region runs from the line after the heading through EOF. Compute the region end as the smallest line number among all OTHER `SECTIONS` headings occurring after the located heading, falling back to EOF when that set is empty — do not assume the manifest's key order matches document order, and do not bound on "the next line that looks like a heading", which would stop at a body line in all caps.

G6.15 is a BLOCKER: scan the resolved TEX for invisible-text constructs — a white text or colour command, a phantom, and negative-argument horizontal or vertical skips. For near-white colour definitions, assert USAGE rather than definition: flag a near-white colour only when its name appears inside a colour-applying call site. This distinction matters today — `accentlight` at `docs/main.tex:19` is 92.3 percent gray and defined but never used, so a naive definition-based rule would fire a false BLOCKER on day one. Report the unused definition separately as INFO, noting Phase 6 must use it or delete it.

After every assertion group, emit the human summary block. When `BLOCKERS` is non-zero the summary must list the failing assertion IDs so the three named defects are visible without reading the full RESULT stream.
  </action>
  <verify>
    <automated>bash scripts/verify-resume.sh >/tmp/v2d.out 2>&1; ec=$?; test $ec -eq 1 &amp;&amp; test "$(grep -cE 'RESULT G6\.(9|10|12) +FAIL' /tmp/v2d.out)" -eq 5 &amp;&amp; test "$(grep -cE 'RESULT G[0-9.]+ +FAIL' /tmp/v2d.out)" -eq 5 &amp;&amp; test "$(grep -cE 'RESULT G6\.(13|14) +WARN' /tmp/v2d.out)" -ge 2 &amp;&amp; grep -qE 'RESULT G6\.15 +PASS' /tmp/v2d.out</automated>
  </verify>
  <acceptance_criteria>
    - Inverted acceptance holds at the script level: `bash scripts/verify-resume.sh; test $? -eq 1` succeeds
    - Exactly the three live defects are BLOCKER failures: `bash scripts/verify-resume.sh 2>&1 | grep -E 'RESULT G[0-9.]+ +FAIL' | grep -cvE 'G6\.(9|10|12)'` returns 0, and the total FAIL count is 5
    - `bash scripts/verify-resume.sh 2>&1 | grep -c 'RESULT G6.12  *FAIL'` returns 1 and the message names `docs/main.tex:240-242` and `Phase 2`
    - Every FAIL message names an owner: `bash scripts/verify-resume.sh 2>&1 | grep -E 'RESULT G[0-9.]+ +FAIL' | grep -cv 'Phase 2'` returns 0
    - Education scoping is real, not whole-document: `LC_ALL=C grep -Fc 'EDU_HEADING' scripts/verify-resume.sh` is at least 1, and the script contains a comment recording that the institution string occurs twice in the extraction — `grep -cE '^[[:space:]]*#.*twice' scripts/verify-resume.sh` is at least 1
    - The region helper is shared, not duplicated: the same heading-to-next-heading extraction serves G6.12 and G6.14, so `LC_ALL=C grep -Fc 'SKILLS_MAX_LINES' scripts/verify-resume.sh` is at least 1 and the scoping logic appears once
    - `bash scripts/verify-resume.sh 2>&1 | grep -cE 'RESULT G6\.(13|14) +WARN'` is at least 2 and neither ID ever appears with FAIL
    - G6.13's unassigned owner prints, rather than an empty owner: `bash scripts/verify-resume.sh 2>&1 | grep 'RESULT G6.13'` contains `owner: unassigned`
    - `bash scripts/verify-resume.sh 2>&1 | grep 'RESULT G6.14'` contains `owner: Phase 5` and also reports the measured Skills line count `4` against the target `3`. Assert the number, not only the owner string: `TECHNICAL SKILLS` is the last heading in `SECTIONS`, so a region helper missing its EOF fallback yields an empty region and a vacuous count of 0, which is under the target and would pass silently while the owner string still printed correctly
    - `bash scripts/verify-resume.sh 2>&1 | grep 'RESULT G6.15'` shows PASS (the unused `accentlight` definition must not trip it) and an INFO line mentions the unused definition
    - The colour rule asserts usage, not definition: `LC_ALL=C grep -Fc 'accentlight' scripts/verify-resume.sh` returns 0 — the check is generic over near-white colours and does not hardcode today's one instance
    - Repo stays clean: `bash scripts/verify-resume.sh >/dev/null 2>&1; test -z "$(git status --porcelain)"`
  </acceptance_criteria>
  <done>The harness exits 1 against today's committed PDF, naming exactly the three Phase-2-owned defects with owners in every failure message, while all other BLOCKERs pass and all later-phase assertions print as WARN with their owning phase read from the manifest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest values → grep/python arguments | every threshold, keyword, heading and literal this plan asserts on originates in `docs/verify/manifest.txt` and is passed to external commands |
| extracted PDF text → shell and python | `$WORK/gate.txt`, per-page extractions and the bbox XML are untrusted text with respect to word-splitting, globbing and regex metacharacters |
| LaTeX source → grep patterns | G4.1 and G6.15 pattern-match `docs/main.tex` content |

Inherits Plan 01's boundaries; no new external surface, no network, no credentials.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Elevation | manifest values reaching grep, python and shasum in G4–G6 | mitigate | Values arrive through `manifest_get`/`manifest_list` (no eval, no source) and are interpolated only inside quoted arguments; keyword and literal matching uses fixed-string mode so a value containing regex metacharacters is inert; the python census and headroom blocks receive values via argv, never by interpolating into the heredoc body. |
| T-01-02 | Tampering | extracted text used as grep input in G5 and G6 | mitigate | Extracted text is only ever a grep *input file*, never a pattern and never a command fragment; no `eval` of extraction output anywhere. |
| T-01-07 | Tampering | assertion weakening under pressure to reach a green run | mitigate | Task acceptance criteria assert the matcher form itself as literal-string greps rather than fragile alternations — `grep -Fx` present, no at-least-one comparison on a frozen count, `grep -Fowc` present for the word-bounded prohibited count, and four separate zero-count checks for `expected_fail`, `allowlist`, `known_fail` and `\|\| true`. A weakened matcher therefore fails the task rather than passing quietly. This is the review control for the phase's dominant failure mode. |
| T-01-10 | Elevation of Privilege | `--skip-freshness` in the drift criteria | mitigate | The flag is required to observe G4.1 drift, so it appears in this plan's criteria — but only alongside an explicitly mutated `mktemp -d` copy, never against `docs/`, and G0.3 prints `SKIP` on every such run. Plan 03 asserts the flag is absent from the `Makefile`, so no user-facing target inherits it. |
| T-01-04 | Spoofing | verifying a stale artifact | mitigate | Inherited from Plan 01's G0.3; this plan adds no path that bypasses it. |
| T-01-06 | Repudiation | honesty freeze | mitigate | G5 compares against the hand-authored frozen file and requires an exact count of 1 per string; nothing in this plan writes to that file. |
| T-01-SC | Tampering | package-manager installs | accept — not applicable | Zero npm/PyPI/crates packages installed. Only stdlib python and preinstalled poppler are used. |
</threat_model>

<verification>
1. `bash scripts/verify-resume.sh; echo "exit=$?"` — expect exit 1
2. `bash scripts/verify-resume.sh 2>&1 | grep -E 'RESULT G[0-9.]+ +FAIL'` — expect exactly the G6.9 (times 3), G6.10 and G6.12 lines, each naming Phase 2
3. `bash scripts/verify-resume.sh 2>&1 | grep -cE 'RESULT G[0-9.]+ +(PASS|WARN|INFO)'` — expect the remaining assertions to report, with no assertion silently absent
4. Negative controls (each on a `mktemp -d` copy, never touching `docs/`): mutate one frozen date by one byte and expect a G5.1 FAIL; change `\textheight` from `1.2in` to `1.3in` and run with `--tex` plus `--skip-freshness`, expecting a G4.1 FAIL. Do NOT use `docs/transcript.pdf` as a page-count control — it measures 2 pages, so G1.1 passes on it; the page-gate control is Plan 03's G7.3 overflow probe
5. `test -z "$(git status --porcelain)"` after every run above
</verification>

<success_criteria>
- `bash scripts/verify-resume.sh` exits 1 against today's committed PDF
- The only BLOCKER failures are G6.9 (three missing contact literals), G6.10 (the en-dash handle) and G6.12 (Education parse order), each message naming Phase 2 as owner
- G1.1 reads the page tree and takes its threshold from the manifest; its discrimination proof is Plan 03's overflow probe, not a committed 2-page PDF
- G2.1 asserts positively that page 2 opens at `PUBLICATIONS`; G2.4 and G3.2/G3.3 are INFO and cannot change the exit code
- All 15 frozen strings verify as unique whole lines, and a one-byte mutation of any of them produces a FAIL
- The geometry hash reproduces the frozen value and a `\textheight` change breaks it when observed through `--skip-freshness`
- All seven section headings pass against the form-feed-normalized text
- Every WARN line prints its owning phase or sentinel, read from the manifest, and no allowlist or suppression construct exists anywhere in the script — including no `|| true`
- The Education binding is scoped to the Education section and asserts institution-before-city ordering, not only the line window
</success_criteria>

<notes>
- The three BLOCKER failures at this plan's end are the deliverable, not a regression. `01-RESEARCH.md:685-698` documents the two failure modes to resist: making the harness green on arrival by weakening an assertion, and making it so strict that Phase 2 cannot turn it green. If a fourth BLOCKER appears, the harness is wrong — investigate the assertion before touching `docs/`.
- BLOCKER is defined as: passes today, or is one of the three defects Phase 2 fixes. Everything else is WARN with a printed owner. Do not promote any WARN in this phase.
- Findings P1 (form-feed gluing) and P2 (duplicate institution line) are corrections to `01-RESEARCH.md`'s G6.8 and G6.12 rows, measured this session. Both are false-PASS traps rather than cosmetic details; Plan 01's notes 3 and 4 carry the measurements.
- **`docs/transcript.pdf` is NOT a usable page-count control.** Measured with `pdfinfo`: it is 2 pages, so G1.1 passes on it and any "proof" built on it would prove the opposite of what it claims; it also lacks a sibling `transcript.fdb_latexmk`, so G0.3 would take its absent-record path before G1 ran. The only real multi-page negative control is Plan 03's G7.3 probe, which builds a genuine 3-page artifact in a scratch directory. Do not reintroduce a transcript-based check anywhere.
- **Geometry drift needs `--skip-freshness`.** G0.3 refuses at exit 2 before G4 runs whenever `--tex` points at a mutated copy, so every drift observation in this plan and in Plan 03's G8.2 pairs `--tex` with that flag. It is the single documented override, defined in Plan 01; do not invent a second one and do not reorder the gates.
- **This plan carries four tasks, one more than the 2-3 guideline.** The deviation is deliberate: all four write the same file, so they cannot be split into separate plans without a `files_modified` conflict, and G6 alone spans 15 assertions. Splitting the Education/adjacency/invisible-text group into Task 4 keeps each task at a reviewable size and gives the run an extra commit boundary; total plan scope is unchanged.
</notes>

<output>
Create `.planning/phases/01-verification-harness/01-02-SUMMARY.md` when done. Record the full RESULT stream from a run against the committed PDF, the exit code, and any assertion whose measured value differed from the research table.
</output>
