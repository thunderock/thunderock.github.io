---
phase: 02-page-1-budget-text-layer-defects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/main.tex
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: true
requirements: [EXP-01, EXP-05, EXP-06]

must_haves:
  truths:
    - "A text-only parser reading the PDF finds the LinkedIn handle as 'ashutosh--tiwari' (two ASCII hyphens) and never as 'ashutosh–tiwari' (U+2013)"
    - "A text-only parser finds the portfolio URL and the GitHub URL as literal strings on the contact line, all five contact items still clickable"
    - "Work Experience runs Adobe -> Swiggy with no career-break row anywhere in the text layer"
    - "Groupon and NetSpeed each read as one compact block whose descriptor still names C++, Network-on-Chip and all four D-06 module names (Virtual Channel Arbitration, Polarity-based Arbitration, Multi-Cast Filtering, Structural Latency Breakdown), and whose Groupon half still names Cyclops, NSVD, Neo4j, customer segmentation and the customer-service platform live in every Groupon country"
    - "Every employment date, title and employer is still byte-identical to the Phase-1 honesty freeze"
    - "The measured page-1 headroom recovered vs the 0.1pt baseline is recorded as a number, not asserted against a predicted figure"
  artifacts:
    - path: "docs/main.tex"
      provides: "contact line whose display text is five bare URLs and whose extracted handle carries two ASCII hyphens; the ligature-break technique (empty group, mbox or nolinkurl) is the executor's choice, arbitered by Task 1's acceptance criteria, not pinned here. Also: no career-break entry; Groupon/NetSpeed folded to header + one descriptor each"
      contains: "https://linkedin.com/in/ashutosh--tiwari"
    - path: "docs/AshutoshTiwari.pdf"
      provides: "regenerated 2-page artifact whose text layer carries all four CONTACT_LITERALS and zero FORBIDDEN_LITERAL"
    - path: "docs/AshutoshTiwari.fdb_latexmk"
      provides: "latexmk md5 record tying the artifact to the edited source, so G0.3's freshness proof passes on a fresh checkout"
  key_links:
    - from: "docs/main.tex contact line"
      to: "docs/verify/manifest.txt CONTACT_LITERALS"
      via: "pdftotext default-mode extraction line 2, substring matched by G6.9"
      pattern: "linkedin\\.com/in/ashutosh--tiwari"
    - from: "docs/main.tex Groupon/NetSpeed \\resumeSubheading rows"
      to: "docs/verify/baseline-frozen.txt [dates]/[titles]/[employers]"
      via: "one-cell-per-extracted-line tabular* behaviour, whole-line matched by G5.1/G5.2/G5.5 at exactly 1"
      pattern: "Sep\\. 201[56] – (Sep\\. 2017|Aug\\. 2016)"
---

<objective>
Fix the three live contact-line defects and free the page-1 budget: rewrite the header
to display bare URLs with a broken `--` ligature (EXP-06), delete the career-break entry
(EXP-01), and fold Groupon + NetSpeed to a compact header plus one italic descriptor each
(EXP-05).

Purpose: turns four of the five live BLOCKER FAILs green and produces the page-1 slack
that Phase 3's Adobe rebuild spends. Every later phase is blocked on this budget existing.
Output: edited `docs/main.tex`, a regenerated tracked artifact set, and a **measured**
page-1 headroom figure recorded in the plan summary.

**End state of this plan is deliberately not all-green:** `bash scripts/verify-resume.sh`
must exit 1 with exactly **one** BLOCKER remaining — `G6.12`, the Education parse order,
which Plan 02 owns together with its manifest anchor. `make verify-selftest` may also go
red at `G7.2`; that is the predicted `PROBE_LINES` trap and Plan 02 owns it. Do not fix
either here, and do not touch `docs/verify/` in this plan.
</objective>

<execution_context>
@/Users/ashutosh/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ashutosh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-1-budget-text-layer-defects/02-CONTEXT.md
@.planning/phases/02-page-1-budget-text-layer-defects/02-PATTERNS.md
@docs/main.tex
@docs/verify/manifest.txt
@docs/verify/baseline-frozen.txt
</context>

<superseded_literals>
Bind these; a later verification pass must not "correct" them backwards.

1. **ROADMAP.md Phase 2 criterion 3** writes the handle as `ashutosh-tiwari` (one hyphen).
   That is a transcription error. The correct handle is `ashutosh--tiwari` (two ASCII
   hyphens), user-confirmed against a live LinkedIn probe and frozen in
   `docs/verify/manifest.txt` `CONTACT_LITERALS` — see STATE.md `[Phase 1 / A1]`.
2. **ROADMAP.md Phase 2 criterion 5** writes "≥12 lines recovered". Superseded by D-08:
   assert the **measured** post-change value. Planner's measurement (see
   `<measured_width_budget>`) puts the realistic recovery near the career-break's
   **4.25 lines**, not 12 — at the locked D-05/D-06 content length each descriptor wraps to
   two rendered lines, so the fold itself saves **0 rendered lines per role** plus the
   itemize `topsep`/`itemsep` overhead. Record what you measure. Never raise or lower a
   manifest threshold to match it, and never shorten locked content to improve it.
</superseded_literals>

<measured_width_budget>
Read-only measurements taken from the committed PDF by the planner. **`02-PATTERNS.md` §4 is
the single source for every figure in this table** — if the two ever disagree, §4 wins.
These are the arithmetic you must reason with; re-measure after every build rather than
trusting them.

| Surface | Measured today | Post-change draft | Consequence |
|---|---|---|---|
| Contact line (extraction line 2) | **113 chars** | **125 chars** for D-01's five bare URLs | the longest live rendered line anywhere in the document measures **144 chars**, so 125 should stay on one line — **prove it, do not assume it** |
| Descriptor one-line capacity (level-2 bullet, `leftmargin=1.4em` inside `0.97\textwidth`) | **137-141 chars** (longest live level-2 bullets: 141, 141, 140, 138, 138) | D-05 draft **167**, D-06 draft **164** | both drafts exceed capacity, so **each descriptor renders on two lines** — see Task 3 |
| Groupon block (gate lines 70-77) | 4 rendered lines: 2 header rows + `\resumeTopic` wrapping to 2 | 2 header rows + a 2-line descriptor = **4**; it would be 3 only if the descriptor fit one line | at 167/154 chars the descriptor wraps to 2, so the fold's rendered-line saving is **0** and only the itemize `topsep`/`itemsep` overhead is recovered |
| NetSpeed block (gate lines 79-86) | 4 rendered lines, same shape | same | same |
| Career-break block (gate lines 30-34) | 4 rendered lines, `CAREER_BREAK_PT=48.9` ÷ `LINE_PT=11.5` = **4.25 lines** | deleted | the reliable part of the budget |

**Why the fold cannot free 5-6 lines.** `\resumeSubheading`'s
`@{\extracolsep{\fill}}` is what makes `pdftotext` emit each cell as its own text
line, and that is exactly what `G5.1`/`G5.2`/`G5.5` whole-line-match at count 1. Any
"compact" shape that glues the frozen title, date or employer onto one extracted line
fails the honesty freeze at count 0. So the two-row header stays, and the only
compressible surface is the bullet block -> descriptor — which at the locked D-05/D-06
content length is worth the list overhead, not a rendered line. This is a measurement
correction to D-08's prediction, **not** a scope reduction: every element of D-04, D-05,
D-06 and D-07 ships in full, and the two-line descriptor is the sanctioned outcome
(Task 3).
</measured_width_budget>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the contact line to bare-URL display text with a broken ligature</name>

  <read_first>
    - `docs/main.tex:127-140` — the `center` block as it stands; note `\urlstyle{same}` is
      already set at `:44`, and the four `\href` **targets** are already correct.
    - `.planning/phases/02-page-1-budget-text-layer-defects/02-CONTEXT.md` §Contact line —
      D-01 (exact five display items), D-02 (display text only; target unchanged),
      D-03 (the gate contract).
    - `.planning/phases/02-page-1-budget-text-layer-defects/02-PATTERNS.md` §2 header
      centre block + §3 ligature mechanics table — the `-{}-` remedy and the rule that
      `--` inside frozen **date** strings must not be touched.
    - `docs/verify/manifest.txt` — `CONTACT_LITERALS`, `FORBIDDEN_LITERAL`.
  </read_first>

  <files>docs/main.tex</files>

  <action>
    Replace the display (anchor) text of the four `\href` calls at `docs/main.tex:133-136`
    so the visible text is the bare URL, per D-01. Leave the phone number and every
    `\href` **target** byte-identical — the targets are already correct (D-02).

    Final display texts, in order:
    `findashutoshtiwari@gmail.com` · `linkedin.com/in/ashutosh-{}-tiwari` ·
    `thunderock.github.io` · `github.com/thunderock`

    Keep the surrounding `\underline{...}` and the `$|$` separators as they are.

    Ligature break — concrete starting technique (per D-02 and PATTERNS §3): write the
    handle's display text as `ashutosh-{}-tiwari`. The empty group stops LaTeX folding
    `--` into U+2013 while rendering two visible hyphens. `\mbox` or `\nolinkurl` are
    acceptable alternatives if the harness proves them; the acceptance criteria below are
    the arbiter, not the technique.

    Do **not** touch the source `--` in any date string (`:148,:177,:191,:204,:211`) —
    those extract as U+2013 and that is the frozen byte `G5.1` matches.

    If the rewritten line wraps to two rendered lines (125 chars against a **144**-char
    longest-live-rendered-line measurement, so unlikely): tighten the separators or drop the
    contact line alone to `\footnotesize` — both are invisible to the `G4.1` geometry hash,
    which covers only `main.tex:38-42` and `:69-72`. **Dropping or shortening one of the five
    items is forbidden** (D-01). Do not add or edit any `\setlist` or `\addtolength`.

    Build and verify: `make build`, then `bash scripts/verify-resume.sh`.
  </action>

  <verify>
    <automated>make build &amp;&amp; bash scripts/verify-resume.sh; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    Let `G=/tmp/02-01-gate.txt` be `pdftotext docs/AshutoshTiwari.pdf - | tr '\f' '\n'`.

    1. `LC_ALL=C grep -cE '^RESULT G6\.9 +PASS' &lt;(bash scripts/verify-resume.sh)` == **4**
       and `... G6\.9 +FAIL` == **0**.
    2. `LC_ALL=C grep -cE '^RESULT G6\.10 +PASS'` == **1**; `... G6\.10 +FAIL` == **0**.
    3. Each of these returns ≥1 against `$G`, via `LC_ALL=C grep -Fc`:
       `findashutoshtiwari@gmail.com`, `ashutosh--tiwari`, `github.com/thunderock`,
       `thunderock.github.io`.
    4. `LC_ALL=C grep -Fc 'ashutosh–tiwari' $G` == **0** (U+2013 form).
    5. Single-line proof: exactly one line of `$G` contains both
       `findashutoshtiwari@gmail.com` **and** `github.com/thunderock` —
       `LC_ALL=C grep -Fc findashutoshtiwari@gmail.com $G` == 1 and that same line number
       also matches `github.com/thunderock`.
    6. Targets untouched — match the **exact brace-closed `\href` form**, never the bare-URL
       substring. `https://github.com/thunderock` is a *prefix* of eight page-2 project links
       (`docs/main.tex:228,285,290,295,301,306,311,313`), so
       `LC_ALL=C grep -Fc 'https://github.com/thunderock' docs/main.tex` measures **9** today
       and an `== 1` assertion on that substring is unsatisfiable. The closing brace is what
       discriminates the contact-line target from `href{https://github.com/thunderock/...}`,
       whose trailing path pushes the `}` past the match:
       `LC_ALL=C grep -Fc 'href{https://linkedin.com/in/ashutosh--tiwari}' docs/main.tex` == **1**,
       `... 'href{https://github.com/thunderock}' ...` == **1**,
       `... 'href{https://thunderock.github.io/}' ...` == **1**,
       `... 'href{mailto:findashutoshtiwari@gmail.com}' ...` == **1**.
       All four measure **1** in this brace-closed form today; the form is also what keeps the
       criterion honest if a later phase adds a `linkedin.com/in/...` or
       `thunderock.github.io/...` path link.
    7. `RESULT G4.1 ... PASS` present (geometry hash unchanged) and
       `git diff --unified=0 docs/main.tex | LC_ALL=C grep -cE '^\+.*(\\setlist|\\addtolength)'` == **0**.
    8. `RESULT G3.4 ... PASS` present with `0 words outside the page box` (no horizontal overflow).
    9. `RESULT G6.4 ... PASS`, `RESULT G6.7 ... PASS`, `RESULT G1.1 ... PASS`,
       `RESULT G2.1 ... PASS` all present.
    10. Honesty freeze intact, asserted **per ID**. The harness emits **one RESULT line per
        frozen string**, not one per gate ID, so the live counts are 5 dates + 5 titles +
        5 employers: `LC_ALL=C grep -cE '^RESULT G5\.1 +PASS'` == **5**,
        `... G5\.2 +PASS` == **5**, `... G5\.5 +PASS` == **5**
        (equivalently `... G5\.(1|2|5) +PASS` == **15**), and
        `... G5\.(1|2|5) +FAIL` == **0**.
    11. `bash scripts/verify-resume.sh; echo $?` == **1**, and the `!! FAIL:` summary line
        names exactly `G6.12` — no other ID.
    12. `docs/verify/manifest.txt` and `docs/verify/baseline-frozen.txt` are unmodified:
        `git diff --name-only docs/verify/ | grep -c .` == **0**.
  </acceptance_criteria>

  <done>The contact line displays five bare URLs, all still clickable; G6.9 PASSes four times, G6.10 PASSes, and G6.12 is the only remaining BLOCKER.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the career-break entry (EXP-01 / D-10)</name>

  <read_first>
    - `docs/main.tex:166-176` — the career-break `\resumeSubheading` and its
      `\resumeItemListStart`/`End` pair, plus the blank lines around it.
    - `.planning/phases/02-page-1-budget-text-layer-defects/02-CONTEXT.md` §Career-break
      removal — D-10 (delete outright, no replacement row, no summary line).
    - `.planning/phases/02-page-1-budget-text-layer-defects/02-PATTERNS.md` §5.4 note that
      `Indiana University, Bloomington` currently extracts **twice** (page-1 career break
      + Education), and §5.6 (`CAREER_BREAK_PT` has zero references in the harness).
    - `docs/verify/baseline-frozen.txt` — confirm for yourself that
      `Career Break for Master's Degree` and `MASTER OF SCIENCE IN COMPUTATIONAL DATA
      SCIENCE` are **not** frozen strings, so the deletion trips no honesty gate.
  </read_first>

  <files>docs/main.tex</files>

  <action>
    Delete the whole career-break block at `docs/main.tex:169-174` — the
    `\resumeSubheading` with its four arguments and the enclosing
    `\resumeItemListStart` / `\resumeItem{...}` / `\resumeItemListEnd`. Per D-10 add
    nothing back: no replacement row, no summary line, no bridging sentence. Experience
    then runs `ADOBE FIREFLY` -> `SWIGGY` directly.

    Leave the surrounding blank-line spacing consistent with the other role boundaries
    (see the Swiggy -> Flipkart gap at `:189-190`) so no stray `\vspace` is introduced.

    Note two consequences and assert them rather than assuming: `Indiana University,
    Bloomington` drops from two extracted occurrences to one, and the U+2019 in
    `Master's` loses one occurrence. Neither is gated — `G6.4` is a set-membership
    census, not a count — but `G6.12`'s region scoping stays load-bearing for Plan 02 and
    must not be "simplified" on the strength of the duplicate disappearing.

    Build and verify: `make build`, then `bash scripts/verify-resume.sh`.
  </action>

  <verify>
    <automated>make build &amp;&amp; bash scripts/verify-resume.sh; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    Let `G=/tmp/02-01-gate.txt` be the re-extracted `pdftotext ... | tr '\f' '\n'` output.

    1. `LC_ALL=C grep -Fc 'Career Break' $G` == **0** and
       `LC_ALL=C grep -Fc 'Career Break' docs/main.tex` == **0**.
    2. `LC_ALL=C grep -Fc 'MASTER OF SCIENCE IN COMPUTATIONAL DATA SCIENCE' $G` == **0**.
    3. `LC_ALL=C grep -Fxc 'Indiana University, Bloomington' $G` == **1** (was 2).
    4. `LC_ALL=C grep -Fxc 'Aug. 2021 – May 2023' $G` == **1** — the master's dates are
       still visible via Education, which is EXP-01's second clause.
    5. Honesty freeze intact, **per ID** (one RESULT line per frozen string, not per ID):
       `LC_ALL=C grep -cE '^RESULT G5\.1 +PASS'` == **5**, `... G5\.2 +PASS` == **5**,
       `... G5\.5 +PASS` == **5** — **15** PASS lines in total — and
       `... G5\.(1|2|5) +FAIL` == **0**. Each of those messages reads
       `frozen string appears exactly once as a whole extracted line: <string>`; the
       invariant is exactly that — **each frozen string appears exactly once as a whole
       extracted line**. (There is no `x5` notation in any live G5 message; that collapsed
       form exists only in `01-02-SUMMARY.md`'s table.)
    6. Adobe is immediately followed by Swiggy with no employer between them: the gate line
       number of `SWIGGY` minus the gate line number of `ADOBE FIREFLY` is smaller than it
       was before this task, and `LC_ALL=C grep -nFx 'Indiana University, Bloomington' $G`
       reports a line number **greater** than the `PUBLICATIONS` line number (i.e. its only
       occurrence is now on page 2).
    7. `RESULT G1.1 ... PASS` (still 2 pages), `RESULT G2.1 ... PASS` (page 2 still opens at
       `PUBLICATIONS`), `RESULT G2.3 ... PASS`, `RESULT G3.1 ... PASS`, `RESULT G4.1 ... PASS`.
    8. `bash scripts/verify-resume.sh; echo $?` == **1** with `G6.12` the only ID in the
       `!! FAIL:` summary.
    9. The `RESULT G3.2 ... INFO` line's page-1 headroom is strictly greater than the
       baseline `+0.1pt`; record the exact pt and lines figures in the task commit body.
    10. `git diff --name-only docs/verify/ | grep -c .` == **0**.
  </acceptance_criteria>

  <done>No career-break row exists in source or text layer, the master's dates still extract from Education, all 15 frozen strings still match at exactly 1, and page-1 headroom has measurably grown.</done>
</task>

<task type="auto">
  <name>Task 3: Fold Groupon and NetSpeed to header + one italic descriptor, and measure the freed budget</name>

  <read_first>
    - `docs/main.tex:203-215` — the two blocks to fold, and `:190-201` (Flipkart) as the
      untouched analog.
    - `docs/main.tex:82-95` — `\resumeSubheading` (4-arg, proven extraction) and
      `\resumeSubSubheading` (defined, **zero** usages, extraction unproven in this
      document).
    - `docs/main.tex:69-72` and `:116-121` — the `\setlist` levels and list scaffolding, so
      you can reason about which spacing an `\item` at level 1 vs level 2 costs.
    - `.planning/phases/02-page-1-budget-text-layer-defects/02-CONTEXT.md` §Earlier-roles
      fold — D-04 (compact header rows + ONE italic descriptor each, inline, no new
      section heading), D-05 (Groupon content), D-06 (NetSpeed content incl. all four
      module names), D-07 (frozen strings + company display names and hyperlinks
      preserved), D-08 (measured budget).
    - `.planning/phases/02-page-1-budget-text-layer-defects/02-PATTERNS.md` §2 (the
      extraction contract that forbids gluing frozen strings onto one line), §4 (width
      budget), §5.5 (the probe anchor moves to Flipkart's `\resumeItemListEnd` if you drop
      both item lists — it must still resolve), §5.6 (`G2.3` needs the NetSpeed employer
      as its own whole page-1 line).
    - `<measured_width_budget>` above.
  </read_first>

  <files>docs/main.tex</files>

  <action>
    Fold both blocks. Keep each role's `\resumeSubheading` **unchanged in shape and
    arguments** — title, date, employer (with its `\href` and colour) and domain line all
    stay byte-identical, because that macro's `@{\extracolsep{\fill}}` is what makes each
    frozen string extract as its own whole line for `G5`. Replace only the body: swap each
    role's single `\resumeTopic` bullet for **ONE italic descriptor** (D-04). "One
    descriptor" means one logical item — it may legitimately *render* on two lines; see the
    ladder below.

    Descriptor carrier — **`\resumeItem{\textit{...}}` wrapped in a minimal
    `\resumeItemListStart` / `\resumeItemListEnd` pair is the expected carrier**, because it
    is the proven-extraction shape *and* it wraps: `\resumeItem` renders through
    `\small{\justifying #1}`, so a descriptor longer than the measured 137-141-char one-line
    capacity flows onto a second rendered line instead of overflowing.

    `\resumeSubSubheading{&lt;descriptor&gt;}{}` is admissible **only** if the descriptor
    measurably fits one rendered line. Its cells are a
    `{@{}l@{\extracolsep{\fill}}r@{}}` tabular and an `l`/`r` column **cannot line-break**:
    a descriptor wider than the text block produces an overfull `\hbox` and words printed
    past the page edge, which surfaces as `RESULT G3.4 FAIL` with a non-zero
    `words outside the page box` count (and an overfull-hbox line in
    `docs/AshutoshTiwari.log`). Treat either signal as the **trigger to fall back to the
    proven `\resumeItem` shape**. Do **not** shorten the descriptor to make
    `\resumeSubSubheading` fit — the content is locked (see the ladder below). Note that at
    the drafted lengths the measurement already predicts `\resumeSubSubheading` will
    overflow, so expect `\resumeItem` to be the shape you ship.

    Whichever you use, measure it rather than trusting it: the descriptor must extract as its
    own text line(s), must not disturb the employer line `G2.3` reads, and must not raise
    `G3.4`. Record which carrier you chose, the measured page-1 headroom for each carrier you
    tried, and why.

    Descriptor text — D-05/D-06 content, user-approved, carried verbatim. Char counts are the
    planner's; measured one-line capacity is **137-141 chars** (`02-PATTERNS.md` §4):

    Groupon:
      (a) 167 — `Customer segmentation and deal-recommendation ML (NSVD unsupervised collaborative filtering on Neo4j); Cyclops customer-service platform live in every Groupon country.`
      (b) 154 — drop `unsupervised `

    NetSpeed:
      (a) 164 — `C++ graph algorithms for Network-on-Chip interconnects: Virtual Channel Arbitration, Polarity-based Arbitration, Multi-Cast Filtering, Structural Latency Breakdown.`
      (b) 150 — drop ` interconnects`

    **The ladder stops at (b). There is no rung that drops locked content.** Both (a) and (b)
    exceed the 137-141 capacity, so **two rendered lines per descriptor is the sanctioned,
    expected outcome** — not a fallback, not a failure, and not something to engineer away.
    It is the direct consequence of D-04 + D-05 + D-06 at measured width, and it is the
    outcome this task ships.

    **Explicitly forbidden** — both of these were previously drafted here as a "last resort"
    rung and are now deleted as scope reductions:
      - merging `Virtual Channel Arbitration, Polarity-based Arbitration` into
        `Virtual Channel and Polarity-based Arbitration`; it dissolves two of D-06's four
        locked module names;
      - rewriting `Cyclops customer-service platform live in every Groupon country` to
        `Cyclops service platform in ...`; it drops D-05's `customer-service` and `live`
        elements.
    Criterion 3 asserts every locked literal and criterion 12 asserts the merged form is
    absent, so neither can pass unnoticed.

    Consequently the fold's measured saving is the itemize `topsep`/`itemsep` overhead plus
    whatever a two-line descriptor recovers against today's two-line `\resumeTopic` —
    plausibly near **zero rendered lines per role**. **Record that number honestly.**
    Removing a module name, the segmentation claim, `Cyclops`, `NSVD`, `Neo4j`, `C++` or the
    `customer-service`/`live` wording to win a line is scope reduction and is forbidden —
    D-08 already designates these descriptors as Phase 3's first re-compression lever, so
    the decision is not yours to pre-empt.

    No new `\section`. No `\setlist`, no `\addtolength`, no new package.

    Then measure and record, for the plan summary:
      - rendered lines for each folded role, before and after;
      - the `RESULT G3.2 ... INFO` page-1 headroom in pt and in lines;
      - the lines-recovered figure vs the 0.1pt baseline — this is criterion 5's
        **measured** value that supersedes ROADMAP's "≥12";
      - the `make verify-selftest` `G7.2` verdict, verbatim, PASS or FAIL, handed to
        Plan 02 Task 3. If it FAILs, capture the `page-1 headroom is now Npt` figure from
        its message. **Do not edit `PROBE_LINES` here** and do not touch `docs/verify/`.
  </action>

  <verify>
    <automated>make build &amp;&amp; bash scripts/verify-resume.sh; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    Let `G=/tmp/02-01-gate.txt` be the re-extracted `pdftotext ... | tr '\f' '\n'` output.

    1. Frozen strings intact — the whole point, asserted **per ID** (one RESULT line per
       frozen string): `LC_ALL=C grep -cE '^RESULT G5\.1 +PASS'` == **5**,
       `... G5\.2 +PASS` == **5**, `... G5\.5 +PASS` == **5** — **15** PASS lines total —
       and `... G5\.(1|2|5) +FAIL` == **0**. Each message reads
       `frozen string appears exactly once as a whole extracted line: <string>`, which is
       the invariant: **each frozen string appears exactly once as a whole extracted line**.
       Independently: `LC_ALL=C grep -Fxc 'Software Development Engineer' $G` == **1**,
       `... -Fxc 'Software Engineer' $G` == **1**,
       `... -Fxc 'Sep. 2016 – Sep. 2017' $G` == **1**,
       `... -Fxc 'Sep. 2015 – Aug. 2016' $G` == **1**,
       `... -Fxc 'GROUPON' $G` == **1**,
       `... -Fxc 'NETSPEED SYSTEMS (Acquired by Intel)' $G` == **1**.
    2. `RESULT G2.3 ... PASS` present (NetSpeed employer is still a whole line on page 1).
    3. **Every locked D-05/D-06 literal present, asserted wrap-proof.** Because each
       descriptor is expected to render on two lines, `pdftotext` will break it mid-phrase, so
       build a squeezed stream as well:
       `N=/tmp/02-01-gate-1line.txt` from
       `pdftotext docs/AshutoshTiwari.pdf - | tr '\f' '\n' | tr -s ' \t\n' ' ' > $N`
       (the whole text layer as one whitespace-normalized line). Then
       `LC_ALL=C grep -Fc '&lt;literal&gt;' $N` ≥ **1** for each of:
       - **D-06, all four module names, verbatim:** `Virtual Channel Arbitration`,
         `Polarity-based Arbitration`, `Multi-Cast Filtering`,
         `Structural Latency Breakdown`
       - **D-06 evidence:** `Network-on-Chip`, plus the **descriptor-unique** anchor
         `LC_ALL=C grep -Fc 'C++ graph algorithms' $N` ≥ **1**. A bare `C++` presence check
         is **vacuous** and must not stand alone: `C++` already extracts **twice** today,
         both from page 2 and both outside this phase's scope — gate line 152
         (`Continuous Dominant Set Repair | C++, Graph Algorithms, ...`) and gate line 172
         (the Skills line) — so `grep -Fc 'C++' $N` ≥ 1 would PASS even if the NetSpeed
         descriptor lost its C++ evidence entirely. `C++ graph algorithms` measures **0**
         today, so it can only come from the descriptor this task writes. ROADMAP Phase 5
         criterion 4 ("the `C++` token is anchored to NetSpeed NoC modules ... verifiable by
         a reader from the document itself") depends on this NetSpeed anchor surviving.
       - **D-05 minimum literal set:** `NSVD`, `Neo4j`, `Cyclops`,
         `customer-service platform`, `live in every Groupon country` — plus
         `LC_ALL=C grep -Fic 'customer segmentation' $N` ≥ **1** (case-insensitive, because
         the phrase is sentence-initial in draft (a))

       And, so the normalization is not doing the work for the short tokens, each of
       `Cyclops`, `NSVD`, `Neo4j`, `C++`, `Network-on-Chip` also returns ≥ **1** from
       `LC_ALL=C grep -Fc ... $G` (raw lines).

       If a multi-word literal is missing because LaTeX **hyphenated** a word at the wrap
       point (`Arbitra-` / `tion`), that is a real finding, not a criterion artefact — a
       text-only parser cannot read the name either. Fix the wrap (`\mbox` the module name,
       or reorder the list); **do not** shorten or drop the content.
    4. Exactly one descriptor construct per role:
       `LC_ALL=C grep -c '\\resumeTopic' docs/main.tex` is **2 less** than before this task,
       and no `\resumeTopic` or `\resumeItem` remains between the `GROUPON` `\href` line
       and `\resumeSubHeadingListEnd` other than the two descriptors.
    5. Structure gates: `RESULT G1.1 ... PASS` (2 pages), `RESULT G2.1 ... PASS`
       (`PUBLICATIONS`), `RESULT G2.2 ... PASS`, `RESULT G3.1 ... PASS`,
       `RESULT G3.4 ... PASS` with `0 words outside the page box`, `RESULT G4.1 ... PASS`,
       `RESULT G6.4 ... PASS`, `RESULT G6.7 ... PASS`, `RESULT G6.8 ... PASS`.
    6. No new heading: `LC_ALL=C grep -cE '^RESULT G6\.8 +PASS'` == **7** and `... G6\.8 +FAIL` == **0**.
    7. Geometry untouched: `git diff --unified=0 docs/main.tex | LC_ALL=C grep -cE '^[+-].*(\\setlist|\\addtolength|\\usepackage)'` == **0**.
    8. `bash scripts/verify-resume.sh; echo $?` == **1**, the `!! FAIL:` summary names
       exactly `G6.12`, and `LC_ALL=C grep -cE '^RESULT G[0-9.]+ +FAIL'` == **1**
       (down from 5 on arrival).
    9. `make verify-selftest 2>&1 | LC_ALL=C grep -cE '^RESULT G7\.1 +PASS'` == **1** —
       the probe anchor still resolves after both item lists moved or vanished.
    10. The `RESULT G7.2` verdict (PASS or FAIL) is captured verbatim in the summary. If
        FAIL, its `page-1 headroom is now ...pt` figure is recorded. `PROBE_LINES` is
        unchanged: `git diff --name-only docs/verify/ | grep -c .` == **0**.
    11. The summary records, as numbers: rendered lines per folded role before/after, the
        `G3.2` page-1 headroom in pt and lines, and the lines-recovered figure — with an
        explicit statement that this measured figure supersedes ROADMAP criterion 5's "≥12".
    12. **No locked content dropped, and the deleted ladder rungs provably not taken.** Every
        literal in criterion 3 is present, **and** all three of these are **0**:
        `LC_ALL=C grep -Fc 'Virtual Channel and Polarity-based Arbitration' $N`,
        `LC_ALL=C grep -Fc 'Virtual Channel and Polarity-based Arbitration' docs/main.tex`,
        `LC_ALL=C grep -Fc 'Cyclops service platform' $N`.
        Two rendered lines per descriptor is an accepted outcome, not a defect: if either
        wraps, the summary records the measured per-role rendered-line delta and states
        explicitly that the locked D-05/D-06 content was kept in preference to the line.
    13. **D-07 hyperlinks preserved** (mirroring Task 1's contact-URL check):
        `LC_ALL=C grep -Fc 'linkedin.com/company/groupon' docs/main.tex` == **1** and
        `LC_ALL=C grep -Fc 'linkedin.com/company/netspeed-systems' docs/main.tex` == **1** —
        the fold replaces the body, never the employer cell's `\href` target.
  </acceptance_criteria>

  <done>Groupon and NetSpeed each render as a two-row header plus one italic descriptor carrying their locked content; all 15 frozen strings still match at exactly 1; G6.12 is the sole remaining BLOCKER; and the freed page-1 budget is recorded as a measured number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> `docs/main.tex` | the only mutable input this plan has; no runtime input, no network, no credentials |
| `docs/main.tex` -> tracked build artifacts | `make build` regenerates the PDF and the `.fdb_latexmk` freshness record a reader trusts |
| repo -> future reader / ATS parser | the PDF text layer is the published claim surface; the honesty freeze is what makes it non-repudiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Repudiation | the 5 frozen dates / 5 titles / 5 employers in `docs/main.tex` | mitigate | every task asserts `G5.1`, `G5.2` and `G5.5` PASS **5 times each** — the harness emits one RESULT line per frozen string, each reading `frozen string appears exactly once as a whole extracted line: <string>` — for **15** PASS lines and zero FAIL, plus direct `grep -Fxc == 1` on the two folded roles' strings; the fold keeps `\resumeSubheading` so no frozen string is glued onto a shared extracted line |
| T-02-02 | Tampering | `docs/verify/manifest.txt`, `docs/verify/baseline-frozen.txt` | mitigate | this plan declares `docs/verify/` outside `files_modified` and every task asserts `git diff --name-only docs/verify/` is empty; `ALLOW_FROZEN_UPDATE` is never invoked — the `G4.1` hash covers only `main.tex:38-42` and `:69-72`, which no task touches |
| T-02-03 | Tampering | page geometry / `\setlist` as a fit lever | mitigate | tasks 1 and 3 assert the diff adds no `\setlist`, `\addtolength` or `\usepackage`, and `RESULT G4.1 PASS` is asserted after every build |
| T-02-04 | Tampering | tracked build artifacts vs source | mitigate | `make build` (never `make clean`) so `docs/AshutoshTiwari.fdb_latexmk` keeps the md5 `G0.3` reads; artifacts are committed with their source change |
| T-02-05 | Information disclosure | contact line | accept | D-01 changes display text only; the phone and email were already in the published PDF and the `\href` targets are unchanged. No new personal data is introduced |
| T-02-06 | Denial of service | horizontal overflow off the page box | mitigate | tasks 1 and 3 assert `RESULT G3.4 PASS` with `0 words outside the page box`, which is what catches a too-wide contact line or descriptor cell |
| T-02-SC | Tampering | supply chain | accept | not applicable: zero package-manager installs and zero new LaTeX packages in this plan, asserted by the `\usepackage` clause in criteria 7 of task 3. No `npm`/`pip`/`cargo` command appears in any task |
</threat_model>

<verification>
Run from a clean tree after Task 3's commit:

1. `make build && bash scripts/verify-resume.sh; echo $?` -> **1**, with
   `LC_ALL=C grep -cE '^RESULT G[0-9.]+ +FAIL'` == **1** and the `!! FAIL:` line naming
   only `G6.12`. (Five BLOCKERs on arrival -> one.)
2. `bash scripts/verify-resume.sh | LC_ALL=C grep -cE '^RESULT G6\.9 +PASS'` -> **4**;
   `... G6\.10 +PASS` -> **1**.
3. `bash scripts/verify-resume.sh | LC_ALL=C grep -cE '^RESULT G5\.(1|2|5) +PASS'` -> **15**
   — 5 dates + 5 titles + 5 employers, one RESULT line each (per ID: `G5.1` -> **5**,
   `G5.2` -> **5**, `G5.5` -> **5**) — with `... G5\.(1|2|5) +FAIL` -> **0** and every
   message reading `frozen string appears exactly once as a whole extracted line`;
   `RESULT G4.1 ... PASS` present.
4. `pdftotext docs/AshutoshTiwari.pdf - | tr '\f' '\n' | LC_ALL=C grep -Fc 'ashutosh–tiwari'`
   -> **0**; the same pipeline with `-Fc 'ashutosh--tiwari'` -> ≥**1**.
5. `pdftotext ... | LC_ALL=C grep -Fc 'Career Break'` -> **0**.
6. `git diff --name-only docs/verify/ | grep -c .` -> **0**, and `git status --porcelain`
   shows **no NEW entry** versus the snapshot taken before this plan started — G7.4's own
   framing: the assertion is that this plan changed nothing outside `files_modified`, not
   that the tree happened to be clean. (An untracked `.omc/` is present today and is not
   this plan's to commit or remove.)
7. The plan summary carries the measured page-1 headroom (pt and lines), the per-role
   rendered-line delta, and the verbatim `G7.2` verdict for Plan 02.
</verification>

<success_criteria>
- `bash scripts/verify-resume.sh` exits 1 with exactly one BLOCKER, `G6.12`
- All four `CONTACT_LITERALS` extract; `FORBIDDEN_LITERAL` extracts zero times
- No career-break row in source or text layer; `Aug. 2021 – May 2023` still extracts once
- Groupon and NetSpeed render as header + one descriptor each (one **or two** rendered lines,
  as measured — two is sanctioned), carrying `Cyclops`, `NSVD`, `Neo4j`, `customer
  segmentation`, `customer-service platform`, `live in every Groupon country`, `C++`,
  `Network-on-Chip` and all four D-06 module names verbatim; the merged
  `Virtual Channel and Polarity-based Arbitration` form appears nowhere
- The two employer `\href` targets (`linkedin.com/company/groupon`,
  `linkedin.com/company/netspeed-systems`) are each still present exactly once (D-07)
- All 15 frozen strings still match at exactly 1; `G4.1` hash unchanged; `docs/verify/` untouched
- Page-1 headroom recovered vs baseline is recorded as a measured number
</success_criteria>

<output>
Create `.planning/phases/02-page-1-budget-text-layer-defects/02-01-SUMMARY.md` when done.

Commit one per task, repo convention `<type>(02-01): <imperative lowercase>`. Source and
artifact ride together in each task's commit (the repo tracks `.pdf`, `.log`,
`.fdb_latexmk`, `.aux`, `.fls`, `.out`); never run `make clean` before committing.
</output>
