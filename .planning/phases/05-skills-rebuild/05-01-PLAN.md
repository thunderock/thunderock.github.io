---
phase: 05-skills-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/main.tex
  - docs/verify/manifest.txt
  - scripts/verify-phase3-regressions.sh
  - scripts/verify-phase5-regressions.sh
  - Makefile
  - docs/AshutoshTiwari.pdf
  - docs/AshutoshTiwari.log
  - docs/AshutoshTiwari.fdb_latexmk
  - docs/AshutoshTiwari.aux
  - docs/AshutoshTiwari.fls
  - docs/AshutoshTiwari.out
autonomous: true
requirements: [PG2-04, PG2-05, PG2-06]
estimate:
  tokens: 72000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Skills renders in ≤3 non-empty lines (G6.14 message reports ≤3) and the pdftotext text layer contains all 17 exact-match forms (PG2-04, criterion 1)"
    - "A visibly-labeled `Concepts:` group (D-03) carries tensor parallelism, pipeline parallelism, Mixture of Experts (MoE) parallelism, KV-cache management, continuous batching — reading as a vocabulary list, not as claimed accomplishments (PG2-05, D-05)"
    - "The `C++` token is anchored to the NetSpeed NoC bullet (page 1) and the graph_ml C++/Cython kernels (page 2), both surviving in the document and reader-verifiable (PG2-06, D-04)"
    - "H100, ONNX, CUDA graphs and Iceberg are promoted KEYWORDS_TARGET → KEYWORDS_REQUIRED and machine-gated by assert_promoted, each in the same commit that lands its keyword"
  artifacts:
    - docs/main.tex (the rebuilt \section{Technical Skills} 3-tier block)
    - docs/verify/manifest.txt (KEYWORDS_REQUIRED gains ONNX, CUDA graphs, Iceberg, H100; KEYWORDS_TARGET emptied)
    - scripts/verify-phase3-regressions.sh (P3.44/P3.65 flipped in place)
    - scripts/verify-phase5-regressions.sh (new Phase-5 Skills regression net)
    - Makefile (REGRESS5 var chained into verify: and verify-regressions:)
    - docs/AshutoshTiwari.pdf (rebuilt, riding in the source commit)
  key_links:
    - "Skills text layer → G6.5 raw ($GATE) keyword presence: a promoted multi-word keyword (ONNX Runtime, CUDA graphs, Flash Attention) that lands at a pdftotext wrap point BLOCKER-FAILs G6.5 — each carrying tier MUST stay on one rendered line"
    - "H100 in Skills → same-commit P3.44/P3.65 flip + manifest promotion (whole-document $GATE/$SQUEEZED streams; skipping the flip reddens the P3 net mid-phase)"
    - "manifest KEYWORDS_REQUIRED → assert_promoted three-conjunct gate (P3.44 for H100; new P5.x for ONNX/CUDA graphs/Iceberg) — G6.5/G6.6 are convention, not a gate"
    - "SECTIONS ordering (TECHNICAL SKILLS last) → G6.14 terminal-section EOF fallback (do not disturb)"
    - "scripts/verify-phase5-regressions.sh → Makefile REGRESS5 → verify: and verify-regressions:"
  prohibitions:
    - "speculative decoding appears zero times in the extracted text layer of the entire document"
    - "Go (word-boundary) appears zero times in the extracted text layer of the entire document"
    - "page 1 (docs/main.tex, the Work Experience block, byte-frozen at 798c0fb) is not edited"
---

<objective>
Rebuild the `\section{Technical Skills}` block of `docs/main.tex` (page 2, resolved by CONTENT) into the D-01 three-tier keyword taxonomy so it earns maximum ATS keyword coverage, rendering in ≤3 lines (down from 4), landing the 4 strictly-new exact forms (`ONNX Runtime`, `H100`, `CUDA graphs`, `Iceberg`), and discharging — in the SAME commit each keyword lands — the manifest promotions and the `P3.44`/`P3.65` H100 needle-flips the harness has been carrying as Phase-5 debts. Then add the standing Phase-5 regression net that guards this final state and wire it into `make`.

Purpose: PG2-04 (keyword-tier rebuild + 17 exact forms), PG2-05 (concept-vocabulary group + zero prohibited terms), PG2-06 (C++ anchored via NetSpeed + graph_ml). This plan is the end-to-end tracer for the phase — one content edit wired through every layer (LaTeX source → PDF text layer → manifest ledger → P3 net → new P5 net → Makefile), built and verified green before any expansion.
Output: the rebuilt Skills block, the promoted manifest, the flipped P3 net, the new `scripts/verify-phase5-regressions.sh`, and the `REGRESS5` Makefile wiring — all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skills-rebuild/05-CONTEXT.md
@.planning/phases/05-skills-rebuild/05-RESEARCH.md
@.planning/phases/05-skills-rebuild/05-PATTERNS.md
@.planning/research/CODEBASE-EVIDENCE.md
@docs/main.tex
@docs/verify/manifest.txt
@scripts/verify-resume.sh
@scripts/verify-phase3-regressions.sh
@scripts/verify-phase4-regressions.sh
@Makefile
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rebuild Skills into 3 tiers + same-commit manifest promotions + P3.44/P3.65 H100 flips (end-to-end, green)</name>
  <files>docs/main.tex, docs/verify/manifest.txt, scripts/verify-phase3-regressions.sh, docs/AshutoshTiwari.pdf, docs/AshutoshTiwari.log, docs/AshutoshTiwari.fdb_latexmk, docs/AshutoshTiwari.aux, docs/AshutoshTiwari.fls, docs/AshutoshTiwari.out</files>
  <read_first>
    - docs/main.tex — resolve the `\section{Technical Skills}` itemize block by CONTENT (never a stored line number); read the current shape (two content lines inside `\begin{itemize}[leftmargin=0.15in, label={}] \small{\item{...}}`), and read the page-1 NetSpeed C++ usage + page-2 graph_ml `C++/Cython` line to confirm the D-04 anchor survives untouched.
    - docs/verify/manifest.txt — the `KEYWORDS_REQUIRED` / `KEYWORDS_TARGET` lines and `SKILLS_MAX_LINES=3`, `SECTIONS`, `WARN_OWNERS`, `PROHIBITED_WORDS`, `PROHIBITED_PHRASES`.
    - scripts/verify-phase3-regressions.sh — resolve `P3.44` (the H100 whitespace-squeezed `assert_absent`) and `P3.65` (the H100 raw-stream `assert_absent`) by CONTENT; read the neighbouring promotion triple `assert_promoted` calls (torch.compile / A100 / Triton) as the exact shape to match, and the `emit()` id auto-numbering note.
    - scripts/verify-resume.sh — G6.5/G6.6 keyword gate (raw `$GATE` grep) and G6.14 Skills-line-count gate (terminal-section EOF fallback) to know exactly what the acceptance greps read.
    - .planning/STATE.md — the `[Phase 04 / close + code review]` two-stream rule, the `[Post-Phase-3 / Page-1 Finalization + Gate Reconciliation]` + `### Blockers/Concerns [Phase 5]` H100-flip debt, and the `[Phase 3 / 03-08]` promotion-triple / resolve-by-content / never-reproduce-a-marker rules.
    - .planning/phases/05-skills-rebuild/05-CONTEXT.md — D-01 (3-tier draft, not frozen), D-02 (purge list), D-03 (`Concepts:` label), D-04 (C++ implicit anchor).
  </read_first>
  <precondition>Page 1 (the `docs/main.tex` Work Experience block) is byte-frozen at 798c0fb; assert `git diff 798c0fb -- docs/main.tex` shows no change to page-1 lines before and after this task — this edit touches only the page-2 `\section{Technical Skills}` block, the manifest, and the P3 net.</precondition>
  <reversibility rating="reversible">The Skills rewrite, manifest promotions and P3 needle-flips are all reversible LaTeX/harness edits (D-01/D-02/D-03/D-04 all rated reversible); a rejected keyword at the Plan 05-02 checkpoint is simply backed out.</reversibility>
  <action>
Rewrite the `\section{Technical Skills}` itemize block into the D-01 three functional tiers, reusing the existing `\begin{itemize}[leftmargin=0.15in, label={}] \small{\item{...}}` shape with three `\\`-separated tier lines, each carrying `\textbf{Label}{: tokens}`:
- Tier 1 `Languages & Inference:` — Rust, C++, Python, vLLM, ONNX Runtime, Triton, torch.compile, CUDA graphs, A100, H100 (per D-01).
- Tier 2 `Training & Distributed:` — PyTorch Lightning, FSDP, Flash Attention, Ray, Kubernetes, KEDA, Spark, Iceberg (per D-01).
- Tier 3 `Concepts:` — tensor parallelism, pipeline parallelism, Mixture of Experts (MoE) parallelism, KV-cache management, continuous batching (per D-03 label + D-05 vocabulary framing).

Purge from Skills every research-identity token and generalist/grad tool per D-02 (Graph Neural Networks, Contrastive Learning, Recommendations, Search Relevance, Fairness-Aware Modeling, Large Language Models, Tensorflow, Sklearn, Numpy, Pandas, Matplotlib, HDFS, Kafka, Flink, AWS Sagemaker, DynamoDB, Faiss, Django, Scala, PyTorch Geometric). Fix the casing defect: the current source `Pytorch Lightning`/`Pytorch` (lowercase-t) becomes `PyTorch Lightning` (exact casing). `C++` stays listed in Tier 1 (D-04 implicit co-occurrence anchor) — do NOT add an explicit parenthetical and do NOT touch the page-1 NetSpeed C++ bullet or the page-2 graph_ml `C++/Cython` phrasing. Bare `PyTorch` in Skills is Claude's discretion (redundant by substring; drop if space is tight).

Ordering constraint (Claude's discretion within D-01): keep multi-word forms (`ONNX Runtime`, `CUDA graphs`, `Flash Attention`, `PyTorch Lightning`) away from a tier's tail so they cannot straddle a pdftotext wrap point — a wrapped promoted multi-word keyword BLOCKER-FAILs G6.5 (raw `$GATE` grep).

MEASURE, never predict: edit → `make build` → `pdftotext -layout docs/AshutoshTiwari.pdf -` (or read G6.14's reported count) → confirm the Skills region renders ≤3 non-empty lines. The `Concepts:` tier (~129 chars vs ~132 measured capacity) is the sole wrap-to-4 risk; if it wraps, keep the label minimal (exactly `Concepts:`, per D-03) and rebalance within CONTEXT latitude — never predict a fit from character counts (Phase 3 measured a 134-char candidate wrap where 133 fit). Never `make clean` (deletes the `G0.3` freshness md5); the rebuilt `.pdf`/`.log`/`.fdb_latexmk`/`.aux`/`.fls`/`.out` ride in this source commit.

In the SAME commit, promote all four target keywords in `docs/verify/manifest.txt` by moving each field from `KEYWORDS_TARGET` to `KEYWORDS_REQUIRED` with its `:phase` suffix STRIPPED: `ONNX` (the required key is the substring `ONNX`, not `ONNX Runtime`), `CUDA graphs`, `Iceberg`, `H100`. After promotion `KEYWORDS_TARGET` is empty. `Triton` is already promoted (03-05) — additive, do not re-promote; leave `SKILLS_MAX_LINES=3` and `SECTIONS` ordering unchanged (TECHNICAL SKILLS must stay the last SECTIONS entry for the G6.14 terminal-section fallback).

In the SAME commit, flip the two H100 needles in `scripts/verify-phase3-regressions.sh` IN PLACE at their existing call positions (ids are auto-numbered by call order — flipping in place preserves `P3.44`/`P3.65`; do not renumber): change `P3.44` from the H100 `assert_absent` (4-arg) to `assert_promoted "<keep an EXP-03b-style promotion label>" 'H100'` (2-arg, matching the neighbouring triple), and change `P3.65` from the H100 `assert_absent` (4-arg) to `assert_present "<keep an EXP-04b-style raw-stream label>" 'H100' "$GATE"` (3-arg). Update the superseding comment blocks next to each so they read as the completed flip rather than the owed debt. Extend the existing promotion-triple coverage note only as needed; the three genuinely-new promotions (ONNX/CUDA graphs/Iceberg) get their `assert_promoted` in the new Phase-5 net (Task 2, option A), NOT here.

Confirm the two prohibited terms (manifest `PROHIBITED_PHRASES` / `PROHIBITED_WORDS`) remain absent from the whole document — neither is in the current Skills block and the purge introduces neither.
  </action>
  <verify>
    <automated>make build && bash scripts/verify-resume.sh; echo "verify-resume exit=$?"</automated>
    <automated>bash scripts/verify-phase3-regressions.sh; echo "p3 exit=$?"; bash scripts/verify-phase3-regressions.sh | grep -cE '^RESULT P3\.[0-9]+ +PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-resume.sh` exits 0 (no BLOCKER FAIL).
    - G6.14's message reports the Skills region renders ≤3 non-empty lines (read the G6.14 RESULT/warn line; "within the target of 3").
    - G6.5 reports PASS for every required keyword, including `ONNX`, `CUDA graphs`, `Iceberg`, `H100`, `Flash Attention`, `A100`, `Triton`, `torch.compile` (case-sensitive, on the raw `$GATE` stream) — no `G6.5 FAIL` naming a multi-word keyword.
    - `bash scripts/verify-phase3-regressions.sh` exits 0 with exactly 72 anchored `^RESULT P3\.[0-9]+ +PASS` and 0 anchored `^RESULT P3\.[0-9]+ +FAIL`.
    - The flipped assertions read as intended: the `P3.44` call is `assert_promoted ... 'H100'` and the `P3.65` call is `assert_present ... 'H100' "$GATE"`, both at their original call positions (ids still 44 and 65).
    - `docs/verify/manifest.txt` `KEYWORDS_REQUIRED` contains whole pipe-fields `ONNX`, `CUDA graphs`, `Iceberg`, `H100` and `KEYWORDS_TARGET` is empty (no `:5`/`:3` suffixes survive in REQUIRED): `grep -c 'KEYWORDS_TARGET=$' docs/verify/manifest.txt` is 1 OR the KEYWORDS_TARGET value is blank.
    - The 17 exact forms all extract: `pdftotext -layout docs/AshutoshTiwari.pdf - | tr -s ' \t\n' ' '` contains `ONNX Runtime`, `CUDA graphs`, `Flash Attention`, `PyTorch Lightning`, and the whole-document raw extraction contains `Iceberg`, `H100`, `A100`, `Triton`, `torch.compile`, `Rust`, `vLLM`, `Ray`, `FSDP`, `Kubernetes`, `KEDA`, `Spark`, `C++`.
    - The `Concepts:` label and all 5 concept phrases extract from the Skills region (squeezed stream contains `tensor parallelism`, `pipeline parallelism`, `Mixture of Experts (MoE)`, `KV-cache management`, `continuous batching`).
    - Prohibited terms stay zero whole-document: `pdftotext -layout docs/AshutoshTiwari.pdf - | tr -s ' \t\n' ' ' | grep -c 'speculative decoding'` is 0, and `pdftotext -layout docs/AshutoshTiwari.pdf - | grep -owc 'Go'` is 0.
    - `git diff 798c0fb -- docs/main.tex` shows no change to the page-1 Work Experience block (only the page-2 Skills block differs).
    - The graph_ml `C++/Cython random-walk kernels` phrasing (page 2) and the NetSpeed C++ bullet (page 1) are byte-unchanged.
  </acceptance_criteria>
  <done>Skills renders as 3 tiers in ≤3 lines with all 17 exact forms present; the four target keywords are promoted; P3.44/P3.65 are flipped in place; `bash scripts/verify-resume.sh` and `bash scripts/verify-phase3-regressions.sh` both exit 0; page 1 and the C++ anchors are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/verify-phase5-regressions.sh (green on arrival) + wire REGRESS5 into the Makefile</name>
  <files>scripts/verify-phase5-regressions.sh, Makefile</files>
  <read_first>
    - scripts/verify-phase4-regressions.sh — the exact scaffold to copy: header/shell conventions (bash 3.2.57, `set -uo pipefail`, `LC_ALL=C`, `_`-prefixed temporaries), the `emit()` id auto-numbering, `gcount` sentinel + `assert_present`/`assert_absent`/`assert_count` helpers, `display_of`, the `--pdf`/`--tex`/`--gate-text` arg parsing + usage, the `mktemp -d` scratch + EXIT trap, the two-stream setup ($GATE via `tr '\f' '\n'`, $SQUEEZED via `tr -s ' \t\n' ' '`), the `--gate-text` non-certification notice, and the `>> PASS/FAIL` footer + `die2` exit-2 path.
    - scripts/verify-phase3-regressions.sh — the `assert_promoted` helper (2-arg, three-conjunct: present in `$GATE` ∧ whole pipe-field of KEYWORDS_REQUIRED ∧ no leftover KEYWORDS_TARGET field), the `MANIFEST` default + `--manifest PATH` arg + the `[ -f "$MANIFEST" ]` precondition + the `KEYWORDS_REQUIRED`/`KEYWORDS_TARGET` extraction into `kw-required.txt`/`kw-target.txt`, and the `MANIFEST` arm of `display_of` — copy these into the new net (option A).
    - .planning/phases/05-skills-rebuild/05-PATTERNS.md — the per-helper line references and the assertion-class list.
    - .planning/STATE.md `[Phase 04 / close + code review]` — the two-stream standing rule (multi-word → `$SQUEEZED`, single-token → `$GATE`).
    - Makefile — the `REGRESS`/`REGRESS3`/`REGRESS4` var block, the `verify:` recipe, the `verify-regressions:` recipe, and the gating-invocation comment.
  </read_first>
  <action>
Create `scripts/verify-phase5-regressions.sh` by copying the whole `verify-phase4-regressions.sh` scaffold and retitling P4→P5 (`P5_SEQ`, `RESULT P5.<n>`, the `emit` prefix, usage text, footer summary). Graft the `assert_promoted` helper and its `--manifest` plumbing from the phase-3 net (option A: this net OWNS the ONNX/CUDA graphs/Iceberg promotions), including the `MANIFEST` default of `docs/verify/manifest.txt`, the `--manifest PATH` arg case, the manifest-exists precondition, the `KEYWORDS_REQUIRED`/`KEYWORDS_TARGET` extraction into scratch files, and the `MANIFEST` display arm. Keep bash-3.2.57 discipline: no associative arrays, no `mapfile`, no `local`, no case-converting expansion; feed every `while read` loop from a redirect or here-doc, never a pipe (a subshell would lose `emit` increments).

Author the assertion classes so the net is GREEN on arrival (it guards the final state Task 1 landed), each class also proven FAILABLE via a crafted `--gate-text` fixture:
- Multi-word literals on `$SQUEEZED` (`assert_present`): `CUDA graphs`, `Flash Attention`, `ONNX Runtime`, `PyTorch Lightning`, and the concept phrases `tensor parallelism`, `pipeline parallelism`, `Mixture of Experts (MoE)`, `KV-cache management`, `continuous batching`.
- Single-token needles on `$GATE` (`assert_present`): `A100`, `H100`, `Triton`, `Rust`, `KEDA`, `Iceberg`, `vLLM`, `Spark`, `Ray`, `FSDP`, `Kubernetes`.
- Honesty absence: the prohibited phrase (manifest `PROHIBITED_PHRASES`) on `$SQUEEZED` via `assert_absent`; and the prohibited word (manifest `PROHIBITED_WORDS`) via a WORD-BOUNDARY check (mirror the harness G6.7 `-w` approach) — do NOT use the substring `gcount -Fc` form for it, which would match inside longer words. Assert both zero.
- Promotions via `assert_promoted`: `'ONNX'`, `'CUDA graphs'`, `'Iceberg'` (H100's promotion is machine-gated by the P3.44 flip from Task 1 and is NOT duplicated here).

Add a `>> Groups run: ...` human summary naming the classes, and keep `die2` → exit 2 (cannot-verify) distinct from FAIL → exit 1.

Wire the net into the Makefile mirroring `REGRESS4`: add `REGRESS5   := scripts/verify-phase5-regressions.sh` to the var block; chain `@bash $(REGRESS5)` in the `verify:` recipe (after the `REGRESS4` line, before `@bash $(VERIFY)`) and append `@bash $(REGRESS5)` to the `verify-regressions:` recipe; update the gating-invocation comment string to include `&& bash $(REGRESS5)`. Never prefix a recipe line with `-` or `|| true`.

Prove each assertion class failable: run the net with `--gate-text` over crafted fixtures (one with a multi-word literal deleted, one with a single token deleted, one with a prohibited term INSERTED, and one with `--manifest` pointed at a scratch copy that leaves a keyword in `KEYWORDS_TARGET`) and confirm each flips the relevant `P5.x` to FAIL (exit 1) while printing the non-certification notice.
  </action>
  <verify>
    <automated>bash scripts/verify-phase5-regressions.sh; echo "p5 exit=$?"; bash scripts/verify-phase5-regressions.sh | grep -cE '^RESULT P5\.[0-9]+ +PASS'</automated>
    <automated>make verify-regressions; echo "verify-regressions exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-phase5-regressions.sh` exits 0 with N anchored `^RESULT P5\.[0-9]+ +PASS` (N = the authored assertion count) and 0 anchored `^RESULT P5\.[0-9]+ +FAIL`.
    - Every assertion class is proven failable: a `--gate-text` fixture with a multi-word literal deleted flips that `P5.x` to FAIL (exit 1); a fixture with a single token deleted flips its `P5.x` to FAIL; a fixture with a prohibited term INSERTED flips the honesty `P5.x` to FAIL; a `--manifest` scratch copy that leaves a keyword in `KEYWORDS_TARGET` flips the matching `assert_promoted` `P5.x` to FAIL — each run prints the non-certification notice.
    - The net asserts `assert_promoted 'ONNX'`, `assert_promoted 'CUDA graphs'`, `assert_promoted 'Iceberg'` (option A) and does NOT duplicate an H100 promotion (H100 is gated by the P3.44 flip).
    - Multi-word literals (`CUDA graphs`, `Flash Attention`, `ONNX Runtime`, `PyTorch Lightning`, the concept phrases) are asserted on `$SQUEEZED`; single tokens (`A100`, `H100`, `Triton`, `Rust`, `KEDA`, `Iceberg`, `vLLM`, `Spark`, `Ray`, `FSDP`, `Kubernetes`) on `$GATE`.
    - The prohibited word is asserted absent via a word-boundary check (not substring `-Fc`).
    - `Makefile` has `REGRESS5 := scripts/verify-phase5-regressions.sh`; `@bash $(REGRESS5)` is chained in both `verify:` and `verify-regressions:`; `make verify-regressions` exits 0 running all four nets including the new one.
    - `bash -n scripts/verify-phase5-regressions.sh` parses clean and the P5 prefix collides with none of G-series/P2./P3./P4.
  </acceptance_criteria>
  <done>`scripts/verify-phase5-regressions.sh` exists, is green on arrival, carries the ONNX/CUDA graphs/Iceberg `assert_promoted` gate, has every class proven `--gate-text`-failable, and is wired into `make verify` and `make verify-regressions` via `REGRESS5`.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (mandatory)

- **`scripts/verify-phase5-regressions.sh`** — the new standing Phase-5 Skills content regression net (created Task 2; modeled on `verify-phase4-regressions.sh` + `assert_promoted`/`--manifest` from the phase-3 net).
- **New `P5.x` assertion ids** — the anchored `^RESULT P5\.[0-9]+` assertion series (multi-word on `$SQUEEZED`, single-token on `$GATE`, honesty-absence, and the three `assert_promoted` promotion gates for `ONNX`/`CUDA graphs`/`Iceberg`).
- **`Makefile` `REGRESS5` variable** — `REGRESS5 := scripts/verify-phase5-regressions.sh`, chained into the `verify:` and `verify-regressions:` recipes.
- **Promoted manifest keywords** — `ONNX`, `CUDA graphs`, `Iceberg`, `H100` moved from `KEYWORDS_TARGET` to `KEYWORDS_REQUIRED` (`:phase` suffix stripped), each in the commit that lands its keyword in Skills.
- **Flipped P3 needles** — `P3.44` (`assert_promoted 'H100'`) and `P3.65` (`assert_present 'H100' "$GATE"`), flipped in place preserving their ids.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions (spec-less edge-coverage — 5 unresolved probe rows, authored explicitly, never silently dropped)

The deterministic edge probe produced 5 rows, all `unresolved`/`unclassified`; the probe is software-feature-shaped and maps poorly to a static LaTeX résumé. Each is authored here as an explicit flagged assumption with how it is handled/verified (no auto-backstop). Row count (5) == flagged-assumption count (5).

1. **PG2-04 (unclassified — "review manually"):** ASSUMED the Skills rebuild has no algorithmic input/empty/ordering edge — it is a static content edit. The only real "edge" is the ≤3-line typeset fit, which is MEASURED at build via G6.14 (never predicted). Verified by: G6.14 message ≤3 after `make build`.
2. **PG2-05 (adjacency — "when two things are exactly equal or just touch, do they merge/collide/separate?"):** mapped to the wrap boundary — ASSUMED that when the `Concepts:` line's content sits at ~the measured ~132-char typeset capacity it stays on ONE rendered line rather than wrapping to a 4th. This is the sole wrap-to-4 risk (RESEARCH A1). Verified by: MEASURE at build; if it wraps, apply the D-03 minimal-label lever / rebalance within CONTEXT latitude.
3. **PG2-05 (empty — "result for empty/single/null input?"):** mapped to an empty Skills region — ASSUMED the Skills region never extracts empty (which would make G6.14 count 0 and false-OK under a max of 3 via the terminal-section EOF fallback). Verified by: not disturbing `SECTIONS` ordering (TECHNICAL SKILLS stays last) + the P5 net asserting each tier's tokens present, so an empty tier cannot pass silently.
4. **PG2-05 (ordering — "when elements compare equal, is output order specified/stable?"):** mapped to token order within a tier — ASSUMED order is Claude's discretion (D-01 draft not frozen) EXCEPT that a multi-word form (`ONNX Runtime`, `CUDA graphs`, `Flash Attention`, `PyTorch Lightning`) must not sit at a tier's tail where it could straddle a pdftotext wrap (→ G6.5 raw-stream BLOCKER). Verified by: G6.5 PASS for every multi-word required keyword after build.
5. **PG2-06 (unclassified — "review manually"):** ASSUMED the C++ anchor has no algorithmic edge — the "edge" is whether a reader can trace `C++` to its two evidence sites. Handled by keeping both existing usages intact: the page-1 NetSpeed NoC bullet and the page-2 graph_ml `C++/Cython random-walk kernels` (do NOT remove that phrasing; the Phase-4 net already guards `C++/Cython` on `$SQUEEZED`). Verified by: those two usages surviving byte-unchanged.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author claim → PDF text layer | The LaTeX source encodes claims the author must be able to defend; anything shipped becomes an assertion to a reader. |
| PDF text layer → external ATS / recruiter | The extracted text layer is read by an ATS scanner and a human reviewer; an unverifiable or prohibited claim damages résumé integrity. |
| content edit → harness assertion logic | A same-commit promotion/flip omission causes a whole-document ($GATE/$SQUEEZED) regression in a sibling net. |

This is a static résumé PDF artifact with **no runtime, no network, no user input, and no package installs** — there is no injection, auth, or web attack surface to model. The honest threat class is **integrity/honesty of claims**, not classic software STRIDE.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering (claim integrity) | Skills keyword tiers (`docs/main.tex`) | high | mitigate | A keyword shipped that the user cannot defend at built/operated/evaluated tier is a fabrication risk → the D-07 blocking per-item approval checkpoint (Plan 05-02): one verdict per stretch item, no blanket "looks good", no auto-advance, backed by the internal D-06 evidence-tier record. |
| T-05-02 | Information disclosure / honesty | whole-document text layer | high | mitigate | Prohibited terms or unverifiable accomplishment claims leaking into the extracted text → harness G6.7 prohibited-word/-phrase gates + the new P5 net word-boundary/`$SQUEEZED` absence assertions (belt-and-suspenders) + D-05 concepts-as-vocabulary framing (no parallelism/MoE claimed as built). |
| T-05-03 | Tampering (silent regression) | harness assertion logic (P3/P5 nets, manifest) | medium | mitigate | A same-commit promotion/flip skipped reddens the whole-document P3 net → the `assert_promoted` three-conjunct gate (P3.44 for H100; new P5.x for ONNX/CUDA graphs/Iceberg) + in-place P3.44/P3.65 flips + each P5 assertion proven failable with `--gate-text`. |

No supply-chain (`T-*-SC`) row: this phase installs zero npm/pip/cargo packages; the toolchain (`pdftotext`, `pdflatex`/`latexmk`, `bash`) is pre-existing. Both `high` threats are dispositioned `mitigate` with concrete, in-plan mitigations, satisfying the ASVS-L1 / block-on-high gate.
</threat_model>

<verification>
- `make build && bash scripts/verify-resume.sh` exits 0 (the phase oracle): G6.14 ≤3 lines, G6.5 PASS for all 17 forms including the 4 newly promoted.
- `bash scripts/verify-phase3-regressions.sh` exits 0 with 72 anchored PASS (P3.44/P3.65 flipped).
- `bash scripts/verify-phase5-regressions.sh` exits 0 with all P5.x PASS; each class `--gate-text`-failable.
- `bash scripts/verify-phase2-regressions.sh` and `bash scripts/verify-phase4-regressions.sh` still exit 0 (no page-1/page-2 collateral regression).
- `make verify-selftest` exits 0 (ten G7/G8 PASS; the anti-rot probe still overflows).
- Prohibited terms zero whole-document; page 1 byte-unchanged from 798c0fb; C++/Cython anchor intact.
</verification>

<success_criteria>
- Skills renders in ≤3 non-empty lines and the text layer contains all 17 exact-match forms (PG2-04, criterion 1).
- A visibly-labeled `Concepts:` group carries the 5 concept phrases as vocabulary (PG2-05, criterion 2).
- `speculative decoding` and `Go` appear zero times in the extracted text of the whole document (PG2-05, criterion 3).
- `C++` is anchored to NetSpeed NoC + graph_ml C++/Cython, both surviving in the document (PG2-06, criterion 4).
- The four target keywords are promoted and machine-gated; the new P5 net is green and wired into `make`.
- (Criterion 5 — per-item approval — is delivered by Plan 05-02.)
</success_criteria>

<output>
Create `.planning/phases/05-skills-rebuild/05-01-SUMMARY.md` when done, recording: the MEASURED final Skills rendered-line count and G3.2 page-2 headroom, the exact tier wording shipped, which target keywords promoted and the flip of P3.44/P3.65, the authored P5.x assertion count and each class's `--gate-text` failability proof, and any departure from the D-01 draft (with the measured reason). Resolve every line reference by CONTENT. Do NOT reproduce any machine-read marker literal in prose. Requirements PG2-04/05/06 are ticked by the phase-closing plan (05-02), not here.
</output>
