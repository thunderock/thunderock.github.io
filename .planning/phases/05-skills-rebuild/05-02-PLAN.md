---
phase: 05-skills-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - docs/main.tex
  - docs/verify/manifest.txt
  - scripts/verify-phase3-regressions.sh
  - scripts/verify-phase5-regressions.sh
  - docs/AshutoshTiwari.pdf
autonomous: false
requirements: [PG2-04, PG2-05, PG2-06]
estimate:
  tokens: 40000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every stretch item carries a recorded evidence tier (built / operated / evaluated) plus the user's per-item verdict — not one blanket approval on a finished list (PG2-04/PG2-05, criterion 5; D-06/D-07)"
    - "The checkpoint is presented against a green document (full suite green first) and cannot auto-advance; a single blanket 'looks good' is rejected as the documented failure mode"
    - "Each rejection is applied as a green outcome (the rejected keyword is removed, its promotion/flip reversed, the document rebuilt green) before any requirement is ticked"
    - "Iceberg's evaluated-tier defense under a tools-tier placement is explicitly confirmed at the checkpoint (D-06)"
  artifacts:
    - ".planning/STATE.md (a greppable [Phase 5 / …] verdict record — one verdict per stretch item)"
    - ".planning/REQUIREMENTS.md (PG2-04, PG2-05, PG2-06 ticked once the phase is green and verdicts recorded)"
  key_links:
    - "checkpoint verdicts → REQUIREMENTS.md tick (PG2-04/05/06 close only after criterion 5 is satisfied)"
    - "a rejected stretch keyword → same-commit removal from Skills + manifest un-promotion + (for H100) P3.44/P3.65 re-flip + rebuild green"
  prohibitions:
    - "no stretch keyword ships that the user did not individually approve at the checkpoint"
    - "the checkpoint is not auto-advanced and a blanket approval is not accepted"
---

<objective>
Run the mandatory per-item approval checkpoint (ROADMAP Phase-5 criterion 5; D-07) against the green document Plan 05-01 produced: present each STRETCH skill with its proposed evidence tier for an INDIVIDUAL verdict against the rendered PDF, record one verdict per item behind a greppable STATE.md marker, apply any rejection as a green backout, and then close the phase by ticking PG2-04/PG2-05/PG2-06.

Purpose: criterion 5 makes each stretch claim interview-defensible on the user's own word — the only authority for a stretch claim crossing into the shipped résumé.
Output: the recorded per-item verdicts, any rejection backouts applied green, and the three ticked requirements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-skills-rebuild/05-CONTEXT.md
@.planning/phases/05-skills-rebuild/05-RESEARCH.md
@.planning/research/CODEBASE-EVIDENCE.md
@.planning/phases/05-skills-rebuild/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Per-item Skills approval checkpoint (blocking, green-first, no auto-advance)</name>
  <read_first>
    - .planning/phases/05-skills-rebuild/05-01-SUMMARY.md — the exact shipped tier wording and the measured final render, so the packet quotes what actually shipped.
    - .planning/research/CODEBASE-EVIDENCE.md — the evidence line each stretch item's tier leans on (ONNX Runtime CUDAExecutionProvider; torch.compile max-autotune + CUDA-graph capture; Ray Data + PyTorch Lightning benchmark; 64-H100 wave; Iceberg tables in the FCU0 export; and the note that the 5 concept items have NO commit evidence — vocabulary only).
    - .planning/phases/05-skills-rebuild/05-CONTEXT.md — D-05 (concepts conversant/evaluated), D-06 (evidence tiers + Iceberg flag), D-07 (per-item blocking checkpoint, blanket approval is the failure mode).
  </read_first>
  <precondition>The full suite is green FIRST (green-first per the EXP-08 model): `bash scripts/verify-resume.sh`, all four regression nets, and `make verify-selftest` each exit 0, and `docs/AshutoshTiwari.pdf` is the rebuilt Plan-05-01 artifact. Open the rendered PDF so the user judges each item against the real page.</precondition>
  <reversibility rating="reversible">Rejection is a valid green outcome — a rejected stretch keyword is simply removed (Task 2), exactly as the Phase-3 EXP-08 rejections were.</reversibility>
  <decision>Which stretch skills ship in the rebuilt Skills block — each judged individually for interview-defensibility against the rendered PDF, with its proposed evidence tier shown.</decision>
  <context>
Criterion 5 requires a recorded evidence tier PLUS a per-item verdict for every stretch item. A single blanket "looks good" on the finished list is the DOCUMENTED FAILURE MODE and must be refused — collect one verdict (confirm / reject / revise) per item. This gate cannot auto-advance even if an auto-advance setting exists (silence is not approval). Present recommendation-first (each item's proposed tier is the recommendation; the user confirms or rejects).

Stretch set requiring a per-item verdict (core built items Rust/C++/Python and already-shipped page-1 operated items torch.compile/A100/Triton/FSDP/Flash Attention/Ray/Kubernetes/KEDA/Spark/vLLM are already owner-approved and DO NOT need a turn):
- **H100** — proposed tier **operated**; evidence: the 64-H100 parallel wave.
- **CUDA graphs** — proposed tier **operated**; evidence: `torch.compile(mode="max-autotune")` with CUDA-graph capture (D-08 resolved cudagraphs-confirmed against a checksummed wheel).
- **ONNX Runtime** — proposed tier **operated**; evidence: the ONNX Runtime CUDAExecutionProvider fingerprint embedding generator.
- **PyTorch Lightning** — proposed tier **operated**; evidence: the Ray Data + PyTorch Lightning benchmark.
- **Iceberg** — proposed tier **evaluated/conversant** (FLAG explicitly per D-06: this is the one operated-LOOKING tools-tier placement resting on an evaluated-only defense — the user has not run it in production, knows it conceptually: table format, schema evolution, time-travel). Confirm the evaluated-tier framing is acceptable.
- **The 5 Concepts** (tensor parallelism, pipeline parallelism, Mixture of Experts (MoE) parallelism, KV-cache management, continuous batching) — proposed tier **evaluated/conversant**, NO commit evidence, vocabulary only (D-05; never claimed as built/operated). Confirm each reads as vocabulary.
  </context>
  <options>
    <option id="confirm-item">
      <name>Confirm the item at its proposed tier</name>
      <pros>The keyword stays in Skills; its evidence tier is recorded as the interview defense.</pros>
      <cons>The user must be able to defend it at the stated tier in an interview.</cons>
    </option>
    <option id="reject-item">
      <name>Reject the item</name>
      <pros>A green, honest outcome — the keyword is removed from Skills (Task 2), like the Phase-3 EXP-08 rejections.</pros>
      <cons>One fewer ATS exact-match form; if it was a promoted keyword its promotion/flip is reversed in the same backout commit.</cons>
    </option>
    <option id="revise-item">
      <name>Revise the item's tier or wording</name>
      <pros>Keeps the keyword with an honest tier the user can defend.</pros>
      <cons>May require a same-commit edit + rebuild (Task 2) before ticking.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The full suite is confirmed green BEFORE the checkpoint is presented (all scripts exit 0).
    - Each of the stretch items (H100, CUDA graphs, ONNX Runtime, PyTorch Lightning, Iceberg, and the 5 Concepts) receives an individual recorded verdict — not one blanket approval.
    - Iceberg's evaluated-tier-under-tools-placement is explicitly raised and answered.
    - The gate is presented for real (no auto-advance); a blanket "looks good" is not accepted as satisfying criterion 5.
  </acceptance_criteria>
  <resume-signal>Provide one verdict per stretch item (e.g. "H100 confirm, Iceberg confirm-evaluated, continuous batching confirm, …" or any rejections/revisions). A single blanket approval will be refused.</resume-signal>
  <done>Every stretch item has an individual verdict on record from the user, judged against the rendered PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Record verdicts, apply any rejection as a green backout, and tick PG2-04/05/06</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, docs/main.tex, docs/verify/manifest.txt, scripts/verify-phase3-regressions.sh, scripts/verify-phase5-regressions.sh, docs/AshutoshTiwari.pdf</files>
  <read_first>
    - .planning/STATE.md — the `[Phase 3 / EXP-08]` verdict record as the shape to mirror (one verdict per item, rejections as decisions), and the standing rule to NEVER reproduce a machine-read marker literal inside prose the same grep scans.
    - .planning/REQUIREMENTS.md — the PG2-04/PG2-05/PG2-06 checkboxes and the requirement-status table.
    - docs/main.tex + docs/verify/manifest.txt + scripts/verify-phase3-regressions.sh + scripts/verify-phase5-regressions.sh — needed only on a rejection branch to back an item out.
  </read_first>
  <action>
Record the per-item verdicts from Task 1 into `.planning/STATE.md` under a greppable `[Phase 5 / …]` marker in the `### Decisions` section — one verdict per stretch item, rejections written as affirmative decisions (a rejected keyword ships nowhere), following the `[Phase 3 / EXP-08]` shape. Describe the marker; do NOT reproduce any machine-read marker literal or criterion token verbatim inside the prose the same grep would scan (keep exactly one occurrence of any greppable marker).

Branch on the verdicts:
- **All confirmed (default, expected):** no document change. Verify the suite is still green, then tick.
- **Any rejection / revision:** in ONE backout commit per affected item, remove that keyword from the Skills block (`docs/main.tex`, resolved by CONTENT); if it was a promoted keyword (`ONNX`/`CUDA graphs`/`Iceberg`/`H100`), reverse its manifest promotion (move it back to `KEYWORDS_TARGET` with its owning `:phase` suffix restored) and remove its `assert_promoted` from the Phase-5 net; for `H100` specifically, also re-flip `P3.44` back to `assert_absent 'H100' "$SQUEEZED"` and `P3.65` back to `assert_absent 'H100' "$GATE"` IN PLACE (preserving ids). Rebuild (`make build`) and confirm the full suite is green again before ticking. A rejected Concept is simply removed from the `Concepts:` line and its P5 assertion dropped; MEASURE the line count stays ≤3 after removal.

Once the document is green and the verdicts are recorded, tick `PG2-04`, `PG2-05`, `PG2-06` in `.planning/REQUIREMENTS.md` (checkboxes + status table → the phase-closing plan owns the tick, per the standing "last plan ticks" precedent) and run `requirements mark-complete` for the three IDs.
  </action>
  <verify>
    <automated>bash scripts/verify-resume.sh && bash scripts/verify-phase3-regressions.sh && bash scripts/verify-phase5-regressions.sh; echo "suite exit=$?"</automated>
    <automated>grep -c '\[Phase 5' .planning/STATE.md; grep -cE '^\s*- \[x\] \*\*PG2-0[456]\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - A `[Phase 5 / …]` verdict record exists in `.planning/STATE.md` with one verdict per stretch item; no machine-read marker literal is reproduced inside scanned prose (marker uniqueness preserved).
    - If any item was rejected/revised: it is removed from Skills, its promotion/flip is reversed in the same backout commit, Skills still renders ≤3 lines, and `bash scripts/verify-resume.sh` + the P3 + P5 nets all exit 0.
    - If all confirmed: `bash scripts/verify-resume.sh`, `bash scripts/verify-phase3-regressions.sh` (72 PASS) and `bash scripts/verify-phase5-regressions.sh` still exit 0 with no document change.
    - `PG2-04`, `PG2-05`, `PG2-06` are ticked `[x]` in `.planning/REQUIREMENTS.md` (checkboxes and the status table both read complete).
    - `speculative decoding` and `Go` are still zero whole-document; page 1 is still byte-frozen at 798c0fb.
  </acceptance_criteria>
  <done>Per-item verdicts are recorded, any rejection is applied as a green backout, the full suite is green, and PG2-04/05/06 are ticked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stretch claim → shipped résumé | The per-item approval is the ONLY authorized crossing for a stretch keyword into the shipped document; it protects claim integrity. |
| user verdict → STATE.md record | One recorded verdict per item is the audit trail that the claim was individually accepted, not blanket-approved. |

Same static-artifact posture as Plan 05-01: no runtime, network, input, or package-install surface. The only integrity threat is fabrication — a claim shipping without an individual, defensible verdict.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering (claim integrity) | shipped stretch keywords | high | mitigate | A stretch claim shipping without an individual verdict is a fabrication risk → the blocking per-item checkpoint (Task 1): one verdict per item, no blanket approval, no auto-advance; rejections applied as green backouts (Task 2) before any requirement ticks. |
| T-05-05 | Repudiation (missing audit trail) | STATE.md verdict record | medium | mitigate | A missing/blanket record leaves which claims were accepted unauditable → one greppable per-item verdict recorded in STATE.md, mirroring the Phase-3 EXP-08 record; requirement ticks gated on it. |

No supply-chain row: zero package installs. The `high` threat is dispositioned `mitigate` with the blocking checkpoint, satisfying the ASVS-L1 / block-on-high gate.
</threat_model>

<verification>
- The full suite (`bash scripts/verify-resume.sh` + P2/P3/P4/P5 nets + `make verify-selftest`) is green both BEFORE the checkpoint (green-first) and AFTER any backout.
- One `[Phase 5 / …]` verdict per stretch item recorded in STATE.md; marker uniqueness preserved.
- PG2-04/05/06 ticked; prohibited terms still zero; page 1 still byte-frozen.
</verification>

<success_criteria>
- Every stretch item carries a recorded evidence tier plus the user's per-item verdict (criterion 5).
- The checkpoint was presented against a green document, could not auto-advance, and no blanket approval was accepted.
- Any rejection was applied as a green outcome before the phase closed.
- PG2-04, PG2-05, PG2-06 are complete.
</success_criteria>

<output>
Create `.planning/phases/05-skills-rebuild/05-02-SUMMARY.md` when done, recording: the per-item verdicts (one per stretch item), any backout applied and its measured effect on the Skills line count / page-2 headroom, the final full-suite green state, and the requirement ticks. Do NOT reproduce any machine-read marker literal in prose.
</output>
